# Living Town 主仓与 Studio 协调复核 — 2026-08-20 14:31 CST

> 覆盖窗口：自上一份已发布报告（2026-08-14 17:08 CST）至 2026-08-20 14:31 CST。
> 本轮先 `git fetch --prune origin`，再以 GitHub live refs、PR synthetic merge、Actions 日志、
> committed code、各 worktree 与 Codex session heartbeat 交叉核验。只修改
> `codex/main-repo-review`；没有修改 `master`、`integration/batons` 或产品候选。

## 一、结论先行

**总判决：MATERIAL PROGRESS / REQUEST CHANGES；PR #6 继续 Draft / DO NOT MERGE；仍不授权
anchor finalize。**

候选自上一报告的 `e4749c8` 只增加两个产品提交：

- P1-aa `c30d55e` 为聊天同步入口补了 warehouse/self/plane 检查，并让点击、Tab 与程序化 focus
  按 active plane 过滤；
- P1-ab `96c1d9d` 将最近回执搜索限制为 1024 条，并把 cargo tx 检查缩到回执邻域。

这两刀都对准了上一报告的真实问题，且新的 GitHub synthetic-merge run 中全部 focused/integration
scenes 通过，hosted visual canary 也通过。不能把它们写成无进展。

但是独立读当前代码后，两个完成态都仍不成立：

1. `_on_player_say()` 只做同步 preflight；异步 `AIBackend.chat` callback 捕获旧 `ag`，不重新确认
   player/target plane、距离、observatory context 或 load/session generation。玩家可在镇上发起聊天后进仓、
   换层或换档，旧 callback 仍写 thinking、UI 与 NPC memory。
2. 同步 preflight 只要求 same plane，没有明确 reachability/distance；玩家仍可对同平面远处 NPC 聊天。
3. P1-ab 只消除了两段全表扫描中的一部分。成功回执仍走
   `_historical_manifest_from_receipt → _manifest_authority_error → _manifest_arrival_receipt_error`，后者
   每次遍历完整 `event_log`。所以 redraw 成本仍随历史长度线性增长；新测试只断言常量等于 1024，
   没有测操作数或 d60/d360/d1000 的增长率。
4. P1-v 证据卡把工资误写进 cargo txid 的事实错误尚未修；readiness verifier 也仍由候选树自填批准字段。

**第一刀不是重烘，也不是新玩法：做一个 P1-ac fresh-review closure，把 chat 权限变成同步与异步共用的
单一 authority，并让观测室回执查询真正 O(1)/有界且有增长率负牙。随后更正证据卡、刷新 PR #6 当前
handoff，再做独立 fresh review。**

## 二、冻结快照

| 对象 | 冻结值 | 判定 |
|---|---|---|
| `origin/master` | `38ba4a7035b9274ddbf141a9dec8539905cdccbe` | 未动 |
| `origin/integration/batons` | `d46cbb132595185c3420bb4eb8fd7f28512baa85` | 未动；没有 exact-tip checks/push runs |
| candidate | `codex/p1a-takeover@96c1d9dedf6f4fbe2022d3bf50d28c334c3436b1` | 相对 integration `49 ahead / 0 behind` |
| candidate game tree | `b70958948da73500a911c98b26a4f17a73d7db9e` | P1-aa + P1-ab |
| candidate full diff | 101 files，`+11783/-868` | 完整 P1-a…P1-ab 候选，不应整体视为一个小补丁 |
| 本窗口 diff | 2 commits / 6 files / `+125/-10` | `e4749c8..96c1d9d` |
| PR #6 | Draft / OPEN / UNSTABLE / mergeable | title/body 仍主要停在 P1-v/P1-p，current handoff stale |
| PR #6 synthetic merge | `4527ad5cedca148c240e787b18a788f5371baa68` | parents 精确为 `d46cbb1 + 96c1d9d`；game tree 同 head |
| PR #6 run | `32337702681` | core FAILURE；visual_canary SUCCESS |
| PR #5 | Draft / OPEN / CLEAN | review head `9dff325`，上次 CI SUCCESS |
| protection | rulesets `[]`；master/integration protection 404 | 红 PR 仍无服务端硬阻断 |
| worktrees | 164 total / 28 dirty / 3 detached | Godot process=0；数量与 dirty 风险仍高 |

候选主 worktree 不是 clean：已提交 head 之外还有 modified `tools/visual_gate.sh` 与 untracked
`tools/visual-runner.Dockerfile`。`dev-branch-01` 明确将其标为其他 session/unknown-owner，本轮没有接管或提交。

Narrative Lab `codex/narrative-lab@cca5166` 也仍是 mixed dirty：3 个 tracked 文件修改，`world/` 与两套
world graph 测试/工具等为 untracked。它的机器状态报告 strict 绿，但因为归属混杂，不能把未提交 world
语料当成可交付 corpus。

## 三、GitHub 与测试证据

当前 run `32337702681` 精确绑定 PR head `96c1d9d`，checkout 的 synthetic merge parents 与当前 base/head
吻合。它不是 branch push receipt，更不是 integration exact-tip receipt。

Core CI 的终态分类没有出现第五类红：

- complement ledger：`eabcb07d8ffe != b70958948da7`；
- S0 protected golden 漂移；
- DetGate hard/determinism/data fingerprint `16/16`，golden `0/16`；
- ModelPath 仍为 5 个 anchor mismatch。

同一 run 中 P1-t/a/b/c/d/g/u/v、space、save/load、save migration、state projection、goals、story 等
场景通过。Hosted `visual_canary` 与同 runtime negative tooth 也通过。故当前红不是随机 crash 或 P1-aa/ab
focused regression；它仍是故意 fail-closed 的受保护锚。

反过来，这些 green 也没有覆盖本报告的反例：P1-v test 立即执行 callback，不模拟延迟后换层/换档；P1-ab
test 只检查 `OBSERVATORY_RECEIPT_SCAN_LIMIT == 1024`，没有测成功投影仍全扫 arrival history。

## 四、对抗性 findings

### P0 — P1-aa 没有关闭异步聊天 authority

当前 `Main.gd` 的同步路径在调用 backend 前检查：观测室拒绝、target 非 player、target 与 active plane 相同。
但 callback 直接使用捕获的 `ag`：

1. 无条件 `ag["thinking"]=false`；
2. 无条件输出气泡/日志；
3. 直接向捕获对象的 memory 写入玩家问答；
4. 不检查 player 是否已经进入 `port_warehouse/1f`、target/player 是否换层、agent 是否仍属于当前 Sim、
   是否 load 了另一局，或该请求是否被后一个请求替代。

这不是理论上的未来 async 风险；`AIBackend.chat` 本来就是 callback API。只要 backend 回答不是同步完成，
“发问后立刻过 portal/读档”就是可达路径。P1-aa 卡片宣称“观测室聊天与选择权限收口”因此过度。

修复应使用一份 immutable request context（target id、player/target address、Sim/load generation、request id），
callback 前重新 resolve agent 并复用同一 chat authority。失败时只清理属于该 request 的 presentation 状态，
不能写 memory/气泡/日志。负牙至少覆盖：发问后进仓、target 换 floor、load/session replacement、旧 reply 晚于
新 reply；同平面邻近正臂必须保留。

### P1 — P1-aa 仍允许 same-plane 远距聊天

`_agent_on_active_plane()` 只比较 `space/floor`。Tab 会枚举当前平面的全部 NPC，`_focus_agent()` 也只做 plane
检查；`_on_player_say()` 没有距离或 `_socially_reachable` 等价条件。因此“same plane”被当成了“可以当面
聊天”。如果设计希望电话/无线电，这是一个未建模 channel，UI 与记忆也没有说明；如果是普通打招呼，则是
权限缺口。

不要在三个入口各加距离 if。先决定 chat channel：当面聊天要求 same plane + bounded distance/LOS；远程聊天
必须是显式设备/关系/费用通道。当前项目没有后者，故 P1-ac 应先采用当面规则并加远距负牙。

### P0 — P1-ab 的“历史有界化”仍是 O(E)

当前调用链可直接证明：

```text
warehouse_observatory_projection
  -> _latest_cargo_unload_receipt        # 最多扫 1024
  -> _cargo_unload_receipt_at            # tx 邻域最多 5
  -> _historical_manifest_from_receipt
  -> _manifest_authority_error
  -> _manifest_arrival_receipt_error     # for raw_event in source_events：全 event_log
```

所以有合法 recent receipt 时，每次 redraw 仍至少一次 `O(E)`。上一报告要求 append/load 派生 index 或真正有界
proof，并要求 d60/d360/d1000 性能牙；P1-ab 两者都没有做。常量存在只证明搜索窗口有上限，不证明整个
projection 的复杂度有界。

应维护可重建的 arrival/tx receipt index：event append 时更新，load 后从账本一次性重建并核 exact count/
adjacency；View query 只读派生结果。派生 cache 不进 digest，但 ledger/cache 不一致必须 fail closed。测试要用
增长 event history 的操作计数或稳定基准证明 query 不随 E 线性增长，而不是只断言常量。

### P1 — 局部 tx 窗口削弱了 projection 自身的 exact-set 证明

P1-ab 只在 receipt `index-2..index+2` 收集同 txid。现有 mutation 把 duplicate stock 紧贴在 receipt 后，
因此会红；若先追加至少三条无关事件，再追加同 txid duplicate，projection 邻域看不到它。全局 hard invariant
与 save migration 仍会在完整审计时抓住，所以正常已验证 state 不会自然产生该形状；但
`warehouse_observatory_projection` 自身不能再宣称“同 txid exact-set”。

最佳修法是由同一个 ledger index保存 txid 的完整 count/contiguous span，而不是重新全扫，也不是假设坏行
必紧贴 receipt。补 far-separated duplicate 负牙，并在文档中区分“projection proof”与“全局 invariant proof”。

### P1 — 已知 evidence / approval 债务未关闭

- `analysis/p1v/east-ocean-cargo-observatory.md` 54–55 行仍声称 wage 与 cargo pay/stock/world 共用 txid；源码
  中工资是之后的 best-effort 独立事务。该事实错误已在上一报告指出，本窗口仍未改。
- `analysis/p1w/verify-anchor-finalize-readiness.ps1` 仍只相信 candidate tree 内的
  `authorizing_completed` 字段，没有绑定远端 review ref、review commit 与报告 blob/hash。
- PR #6 title 只到 P1-v，正文开头仍写 P1-p exact head，并不断 append 旧 handoff；最新 P1-aa/ab、current
  SHA 与当前阻断没有可读的顶部摘要，且正文有编码损坏。

三项都不能靠重烘解决。工资卡立即修正；PR body 压缩成 current-head summary；approval 由 review/finalize
workflow 从只读 review ref 取证，不允许产品候选自签。

### P1 — Hosted visual 仍是观察门，且本地 runner WIP 所有权不清

本轮 visual_canary 成功且有负牙，说明当前平台实际可渲，不应再说 GHA 完全没有 framebuffer 证据。但 workflow
仍允许 positive candidate fail/skip 后 observation job 绿色，故 job success 不能当 required visual gate。

本地 dirty 的 `visual-runner.Dockerfile` 试图 pin Ubuntu snapshot/Godot/Pillow，`visual_gate.sh` 增加显式 tolerance。
方向可能有价值，但它们不在 remote committed head、没有已确认 owner/consumer/CI contract，且当前 dev 明确
不认领。保持不动，先由原 owner 或 coordinator 完成 provenance、build、negative tooth 与 write-set handoff。

### P2 — 产品与区域愿景仍未新增纵向体验

这两个提交均为 authority/performance 修复，没有新玩家动作、内容或持久后果。它们是必要收口，但不应宣传成
town completeness、多镇、旅行或 page swap 进展。区域层仍没有 TownDomain/TransitState/town-scoped stock/
knowledge/hot-cold authority。

连续基础设施投入已超过一个 Beat。P1-ac 与 fresh review 完成后，下一批必须转向一个 30–90 秒可展示的
East Ocean 人—货—地点—后果小闭环；此时再消费 narrative packet，而不是再加一层 gate/ontology。

## 五、Session / Git 所有权与协调

| Session | 当前角色 | 状态与所有权 | 本轮动作 |
|---|---|---|---|
| review | Producer + REFUTE + Git coordinator | `codex/main-repo-review` / PR #5，clean | 发布 fresh review；不碰产品树 |
| dev-branch-01 | Implementation + playable integration | `June/26th-codex-takeover` / `codex/p1a-takeover` / PR #6 | 已完成 P1-ab；收到 P1-ac corrective baton |
| narrative | Narrative Architect | Narrative Lab mixed dirty；无可提交权 | 已返回只读 packet；Producer 判 `DEFER`，不进入 runtime |
| local llm evaluate | 独立 serving/eval lab | 无 Living Town 产品 write-set | 只作为 H0 read-only screening 研究，不进入当前 critical path |

本轮没有创建新 session：当前 Producer、REFUTE、Implementation、Narrative 四个角色已覆盖问题；再开
Architect/QA 只会重复本报告与现有 focused ownership。若 P1-ac 产生 derived-index 设计分歧，才值得临时创建
read-only Technical Architect；若实现完成，再按需创建独立 QA/Evidence 复核，而不是现在提前并行。

已向 `dev-branch-01` 发送 exact tip、四个反例、write-set 禁区、验收与停止线；已向 `narrative` 发送一份
只读 packet brief，禁止在 mixed-dirty Lab 写文件或调用 asset generation。Narrative 随后返回了
`East Ocean Beat 01`，但它只是现有观测室的验收清单：没有 Tao/居民的可见人物后果，也没有超出现有
cargo receipt 的新持久后果。Producer 已明确 `DEFER`，并要求 dev 不要为满足模板而新增
`player_observed_port_receipt` 标志。下一版须在 P1-ac fresh review 后，以既有 NPC 行动/反应和既有状态变化
形成真正的 playable Beat。当前预期 handoff 只剩 dev 的 P1-ac commit/tests/perf evidence/PR rewrite。

## 六、分阶段计划

### Phase 0 — P1-ac corrective batch（0.5–1.5 天）

1. 统一 chat authority：player context、non-self、same plane、当面 reachability。
2. request generation + callback revalidation；portal/load/target move/late reply 全部 fail closed。
3. arrival/tx derived index 在 append 更新、load 重建；View query 真正有界。
4. 增加 history-size growth、far duplicate tx、delayed callback 四组 teeth。
5. 修 P1-v wage 卡；重写 PR #6 顶部 current-head handoff；不碰 unknown-owner visual runner WIP。

退出条件：focused 与 current-base hosted scenes 绿；query cost 不随完整历史线性增长；异步跨 context exact no-op；
core 仍只允许旧四锚红。

### Phase 1 — Fresh review 与 approval authority（0.5 天）

1. review 只审 P1-ac diff、runtime call graph、负牙与长史证据。
2. readiness verifier 从远端 `codex/main-repo-review` commit/report blob 取证，不能信 candidate 自填字段。
3. 若仍有 P0/P1，不授权；若全清，发布 exact `AUTHORIZE_ANCHOR_FINALIZE(game_tree=...)`。

### Phase 2 — 专用 anchor finalize（0.5–1 天）

同一 clean product tree 一次性重烘 complement、S0/Det golden、ModelPath；不夹产品/fixture/阈值改变。跑
current-base PR CI 与 durable receipts。PR required core 必须全绿，visual observation 与 required 语义继续分开。

### Phase 3 — 第一个 post-port playable Beat（1–2 天）

从 Narrative Integration Packet 只选一条：一个玩家动作、一个可见人物反馈、一项真实内容消费与一个可持久
后果。Dev 明确 ACCEPT/REJECT/DEFER；先做 30–90 秒可复算 frame/trace/playtest artifact。若 packet 无现有
state consumer，拒绝而不是再造 ontology。

### Phase 4 — Delivery 与下一 Era seam

配置 required ruleset；合入后取得 integration exact push receipt。之后才进入 same-good/different-route、
TownDomain/TransitState 与 state-driven page swap spike。

## 七、下个窗口只盯这些

1. callback 是否重新 resolve/revalidate，而不是只给 `KEY_C`/入口补 if；
2. same-plane 远距 chat 是否有明确 channel 决策与负牙；
3. projection 是否还调用全表 arrival scan，性能测试是否只断言常量；
4. far-separated duplicate tx 是否 fail closed；
5. wage evidence、PR current summary 与 review-ref approval 是否关闭；
6. unknown-owner visual runner 文件是否被误混入产品 commit；
7. 是否在 fresh review 前重烘四锚；若发生继续拒绝；
8. narrative packet 是否保持 NON_CANON/只读，并由 dev 做 ACCEPT/REJECT/DEFER。

当前候选不是要推倒重来。港口已从纯 View 标签推进到有 manifest、worker、nav、portal、可进入观测空间与
玩家反馈的窄纵切。正因为它已接近证据迁移点，最后的 authority 与复杂度证明必须真实关闭，不能用
“focused green + 常量名叫 LIMIT”替代。

## 八、15:51 CST material-transition addendum

`codex/p1a-takeover` 已从 `96c1d9d` 前进到 `1e0b235`（`fix(p1ac): close chat and cargo
projection review blockers`），改动为 6 files、`+232/-43`。提交边界包含 `Main.gd`、`Sim.gd`、P1-v
focused test、两份既有分析卡和一份 P1-ac closure card；未把工作树中归属未决的
`tools/visual_gate.sh` / `tools/visual-runner.Dockerfile` 混入。候选工作树现在只剩这两项 unknown-owner
visual WIP，产品 Main/Sim write-set 已 clean，ownership 边界成立。

PR #6 已同步到 exact head `1e0b235`，仍为 Draft / UNSTABLE；run `32345416925` 的
`visual_canary` 已 PASS。主 `ci` 随后终态 FAIL，但失败集合严格收敛为既知四类受保护证据锚：complement
ledger freshness/identity、S0 golden 36、DetGate golden 0/16、ModelPath 5 anchors；硬不变量、同 seed
确定性、数据指纹、P1 focused scenes、save migration 与 state projection 均通过。因此这不是 runtime
crash 或 nondeterminism，也不是可直接“修 CI”的许可；它只满足进入独立 fresh review 的前提。本 addendum
只记录转场，不把 self-reported closure
升级为 fresh-review 结论，也不授权 anchor finalize/rebake。下一动作是从远端 committed diff 独立核验
callback revalidation、chat reachability、derived-index 复杂度与
far-duplicate transaction teeth。未向 dev/narrative 重复发令，未创建新 session，未改产品分支或 PR #6。

## 九、18:41 CST new-branch transition

远端新增 `codex/map-tile-lab-review@ab5cce5`（`feat(labs): add map-to-planet resource pool
(review)`）。它只有一个相对 master 的提交，但相对 `integration/batons@d46cbb1` 为 `468 behind / 1 ahead`；
差分为 65 个纯新增文件、39,994 行，全部位于 `game/scenes/labs/**` 与 `game/scripts/labs/**`。当前无 PR、
无既有 worktree owner，也没有生产 `Main/Sim/WorldView/data` write-set 重叠。README 自称 lab-only、opt-in、
不接生产 save/HUD/Sim authority；这些先视为待核 claim，不视为 10 个 RP receipts 已独立证明。

交付分类：`active isolated experiment / NOT a merge candidate`。理由不是规模本身，而是它基于过时 master、
尚未对 current integration/candidate contracts 做兼容验证，且 planet/region/campaign 层已经显著超出当前
East Ocean playable spine 的消费能力。禁止把“路径隔离 + focused tests”解释成可迁移或产品价值成立。

因该审计目标有清晰消费者、严格只读边界且 4 万行超过主协调器轻量抽查的经济范围，创建独立
REFUTE/QA task `01a01ee9-66e2-7c10-9e12-c421c74cc3ed`（worktree
`C:/Users/yp/.codex/worktrees/f284/26th`，starting ref `origin/codex/map-tile-lab-review`）。唯一目标是按
player action → visible feedback → real consumer → persistent consequence 矩阵核验最小可复用组件、
确定性/save/性能、证据判别力与 10-batch 重复成本；严格禁止写文件、提交、PR、cherry-pick 或重烘。
完成条件是返回逐组件 ACCEPT/REWORK/DEFER/REJECT 证据，worktree 保持 clean；暂不创建 recurring loop，
首轮证明其有持续消费者后再决定。

已向 dev-branch-01 发出一次 material-transition ownership notice：不得消费/改写该 lab 分支，当前仍只负责
P1-ac/fresh-review handoff；无需回复。Narrative write-set 与该分支不重叠且没有可消费结论，未重复发令。

## 十、20:09 CST map-to-planet audit correction and disposition

REFUTE/QA 首版把 Windows checkout 的 CRLF-normalized bytes 用 `Get-FileHash` 与 receipt 比较，错误地判定
RP-0005/6/7 source hash stale。Producer 用 `git cat-file blob <ref>:<path>` 的 canonical blob byte stream
独立复算，RP-0005 的 `SiteVisitJournalModel.gd=92a196...` 与
`site_visit_journal_test.gd=f1081d...` 均精确匹配；要求原 session 重审后，它确认 RP-0005 为 3/3、
RP-0006 为 5/5、RP-0007 为 5/5 全匹配，并正式撤回 P0。这个纠错本身是 evidence gate 的必要结果：
Windows working-tree hash 不能替代 Git blob identity。

修订后的证据缺口更窄也更准确：receipt 记录 branch/date/runtime/claimed results 与选定 source hashes，
但没有 commit/source_commit/tree/HEAD 字段。因此它们可证明“被列出的 source blobs 被 pin”，不能证明
`ab5cce5` exact tree 确实执行并通过，也不能证明 production integration/save/replay。静态 non-interference
成立：integration/candidate 的生产 scene/script/tool/CI 对 lab paths 零 consumer；这只证明隔离，不证明价值。

最终 disposition：**keep isolated research branch; no Draft PR; no candidate split now**。

- `REWORK later`：RP-0001 ScaleAddress。未来消费者必须是明确的 Sim save owner，并补 schema-2
  save/load/replay 与 malformed-address atomic rejection。
- `REWORK later`：RP-0002 expedition/debrief framing。未来消费者必须是 canonical CargoManifest/port
  transaction，且由真实玩家动作写 canonical event digest、idempotent settlement 与 save/load。
- `DEFER`：RP-0003–0005 route/site/journal，等待 owned travel/site/SpaceGraph seam。
- `REJECT for current consumption`：RP-0006–0010 network/campaign/covenant/recon/obligation；它们领先当前
  consumer，并形成耦合的 parallel authority stack。

产品矩阵仍不闭合：当前 lab 是 action → visible debrief → **无 production content consumer** → **无 canonical
persistent consequence**。因此不进入 PR #6、不成为 post-port Beat 的依赖，也不启动 recurring loop。
只读 task `01a01ee9-66e2-7c10-9e12-c421c74cc3ed` 已完成、worktree clean；保留为可追溯审计资料，
不删除 remote branch/worktree，不代表认可其产品方向。

## 十一、2026-08-21 06:00 CST stagnation intervention

截至本次介入，`master@38ba4a7`、`integration/batons@d46cbb1`、候选
`codex/p1a-takeover@1e0b235` 与本 review ref 均未前移。PR #6 仍 Draft / UNSTABLE；run
`32345416925` 的 core 红集合仍只有 complement、Harness golden、DetGate golden、ModelPath 四类受保护锚，
`visual_canary` 仍绿。候选 worktree 仍只脏在未提交的 `tools/visual_gate.sh` 与
`tools/visual-runner.Dockerfile`；这两项从 8 月 14 日起没有可核 owner handoff，本轮继续保护、不接管、不提交。

真正卡点不是缺少实现任务，而是依赖顺序被压扁成了重复 heartbeat：

1. P1-ac 自述关闭了 chat callback 与 cargo index 问题，但还没有独立 exact-tree fresh review；
2. 没有外部 review verdict，就不能授权四锚迁移；
3. dev 把 unknown-owner visual WIP 与产品候选共用一个 worktree，因而把“不能碰两项 WIP”误读成“没有任何
   可执行工作”；
4. narrative 的上一份 Beat 只包装 observatory receipt，没有可见人物后果，已正确 DEFER，但之后没有得到
   更窄的可消费重试合同。

为解除依赖，Producer 建立以下单向流水线：

- 新建独立只读 QA/Evidence + REFUTE task `01a0212b-ff4c-7d53-96a6-26a55d7fe38f`，worktree
  `C:/Users/yp/.codex/worktrees/cd55/26th`，从 exact `codex/p1a-takeover@1e0b235` 起步。它只核 P1-ac
  chat authority、异步重验、portal/load/late-reply 负牙、cargo derived index、far duplicate、5,000-event
  bounded-read tooth、P1-v wage 事实与 hosted failure provenance；禁止写文件、commit、PR、rebake。
  唯一终态为 `AUTHORIZE_ANCHOR_FINALIZE`、`REQUEST_CHANGES` 或 `BLOCKED_EVIDENCE`，并必须保持 worktree
  clean。
- `dev-branch-01` 收到新的 ready batch：不再重复“等待 fresh review”，而是在不写文件的前提下准备 clean
  worktree finalize 执行包。只有外部 QA 对 exact game tree 授权后，才可从 `1e0b235` 建
  `codex/p1a-anchor-finalize`；未来独占 write set 仅为 complement ledger、S0/Det golden、ModelPath anchor 与
  一份 `analysis/p1ad/` provenance receipt。若 QA 返回 REQUEST_CHANGES，整包作废，先只修最小 corrective
  write set。现有 dirty takeover worktree 与 unknown-owner visual 文件均不得触碰。
- `narrative` 收到一个只读 compose-ahead batch：在 task response 中给出 2–3 个 30–90 秒 East Ocean Beat
  选项，只能复用现有 player/Tao/CargoManifest/town_stock/coin/relationship/memory/warehouse authority；每项必须
  有玩家动作、可见 NPC 反应、真实 consumer、现有 save-persistent 后果、确定性触发与负对照。禁止写 mixed
  Narrative Lab、造 `observed` flag、扩 ontology、生成资产。dev 是唯一 ACCEPT/REJECT/DEFER 消费者。

滚动依赖图现在为：`P1-ac independent verdict -> (corrective patch | controlled anchor finalize) -> hosted PR green
-> owner merge decision -> integration exact push receipt -> one post-port playable Beat`。只保留这一条 ready lane
和一个 following Beat；不再开泛化 architecture/ontology/lab 任务，也不把 map-to-planet research branch 拉回当前
critical path。

## 十二、2026-08-21 06:28 CST independent fresh-review verdict

只读 QA/Evidence + REFUTE task `01a0212b-ff4c-7d53-96a6-26a55d7fe38f` 已在 clean worktree 对
`codex/p1a-takeover@1e0b2350f6474e2fa873dbb927b04a84d6b8dc1a` 完成独立复核；
`HEAD:game=5068fb03f978d0394484d350ddc5e972ed5bc08a`，base `d46cbb1` 为祖先。监督运行 receipt
`C:/Users/yp/AppData/Local/Temp/living-town-godot-runs/20260820T215944111Z_6a720b2b1c4b4c27a985f2daf91627d8/receipt.json`
在 exact source 上 PASS，且没有 native crash、logic failure 或清理失败。最终 verdict 为
**REQUEST_CHANGES**；因此 anchor finalize 继续冻结，之前的 advisory finalize packet 不得执行。

阻断项已从泛化等待收敛为六个可测缺口：

1. chat callback 没有捕获并比较 target position，目标在 reach 内移动时旧 reply 仍可应用；
2. callback revalidation 失败时仍写 `target["thinking"]=false`，违反失败路径零 UI/thinking/memory mutation；
3. request context 没有独立绑定明确的 Sim/session identity；
4. focused test 缺 target-move 与 A-then-B/late-A 两个负牙，far duplicate 也只测了相邻重复；
5. 5,000-event counter 没覆盖 index validity 与 tx-row dereference，不能证明总 query work 与事件总量无关；
6. readiness verifier 只信 candidate-tree `review_gate.authorizing_completed`，未绑定外部 review ref、report
   blob/hash、game tree 与 verdict，仍允许自我批准。

已向 `dev-branch-01` 发出唯一 ready corrective baton。严格 write set 为
`game/scripts/Main.gd`、`game/scripts/p1v_warehouse_observatory_test.gd`、
`analysis/p1ac/fresh-review-closure.md`、PR #6 body，以及为修 provenance 所需的
`analysis/p1w/verify-anchor-finalize-readiness.ps1`、`analysis/p1w/readiness-evidence.json`。
禁止修改 Sim/data/anchors/thresholds，禁止 rebake。完成标准是补齐上述负牙、外部 review 绑定、focused/verifier
PASS、hosted failure 再分类，随后停止并请求第二次独立 fresh review。只有通过第二次 review 后，才重新讨论
受控 anchor finalize。

Narrative 的三个 NON_CANON compose-ahead 选项（卸货批次选择、Tao 记住人情、工资影响下一次采购）继续
DEFER：当前产品代码尚未证明它们有现成 player action、真实 consumer 与 canonical persistent authority。
本轮不把建议稿转成实现任务，避免在 P1-ac corrective critical path 上并发扩 scope。unknown-owner visual WIP
仍保持原状；map-to-planet lab 仍为隔离研究分支，不进入此修复链。

## 十三、2026-08-21 06:36 CST corrective delivery and second fresh review

全仓 remote-first drift check 发现唯一新的交付转场：`origin/codex/p1a-takeover` 已由
`1e0b2350f6474e2fa873dbb927b04a84d6b8dc1a` 正常 fast-forward 到
`dc7b3d60e3f86f0d47f14dd7d8aeb084fde2a866`（`fix(p1ac): harden async chat and review provenance`）。
实际 diff 仅含已授权的五个文件：`Main.gd`、P1-v focused test、P1-ac closure、readiness verifier 与 evidence
JSON；未修改 Sim/data/protected anchors/visual WIP。PR #6 已同步 exact head，仍明确为
Draft / REQUEST_CHANGES / UNSTABLE；hosted run `32423850259` 正在运行。

实现 owner 报告 focused/integration scenes 通过后已停止写入。Producer 没有把该自报升级为授权，而是把同一
独立 QA/Evidence + REFUTE task `01a0212b-ff4c-7d53-96a6-26a55d7fe38f` 重新派到 exact `dc7b3d6`，继续
严格只读。复审矩阵固定为 target-position/Sim-session/request-generation binding、失败路径零 mutation、
target-move 与 A→B/late-A 负牙、>5,000 rows far duplicate、与总事件量无关的完整 query-work 证据、外部
review ref/report blob/hash 对 exact game tree/verdict 的机器绑定及 tamper/mismatch negatives，以及 hosted
failure 的精确分类。唯一终态仍是 `AUTHORIZE_ANCHOR_FINALIZE`、`REQUEST_CHANGES` 或
`BLOCKED_EVIDENCE`；在 verdict 前继续禁止 anchor rebake/finalize。

同次全仓扫描未见 master、integration/batons、其他远端 refs、tags 或 open PR 的新 material transition。
`codex/main-repo-review@8d24ce4` hosted CI 已绿；map-to-planet lab 保持隔离，Narrative 保持 DEFER/NO_WRITE。

## 十四、2026-08-21 06:48 CST second fresh-review REQUEST_CHANGES

独立 QA/Evidence + REFUTE 对 exact `dc7b3d60e3f86f0d47f14dd7d8aeb084fde2a866`
（`HEAD:game=4967e22191d080133702f8ab91fa1ae3e72a646e`）返回第二次 **REQUEST_CHANGES**。
clean detached focused receipt
`C:/Users/yp/AppData/Local/Temp/living-town-godot-runs/20260820T223911348Z_5b76a99e93d44ee79d6b1de4bc53568b/receipt.json`
PASS，且 target-move、A→B/late-A、far duplicate 与 5,000-noise assertions 确实执行；但测试同时证明了
P0 反例：target move 等 callback revalidation 失败会调用 `_clear_chat_thinking_if_owned`，写
`thinking=false` 并删除 token。要求是失败 callback literal zero mutation，因此当前测试期待值本身错误。

另有两个 P1：projection read counter 仍漏计 index-validity 与 tx-row dereference，不能证明总 query work 与事件
量无关；readiness verifier 只校验任意外部文件的 supplied SHA/JSON fields，没有 `git rev-parse` 外部 review
ref 并从该 ref 的 report blob 取 bytes，因此 self-created out-of-repo JSON 仍可满足检查。hosted run
`32423850259` 在 verdict cutoff 时 core 仍运行，只有 `visual_canary` 已 PASS，禁止沿用上一 run 推断最终失败
集合。

Producer 已立即修正 PR #6 body，撤回“失败前无 mutation”及“external provenance 已绑定”的过度声明；PR
继续 Draft / REQUEST_CHANGES / UNSTABLE。`dev-branch-01` 收到唯一 corrective batch 2，严格 write set 为
`Main.gd`、`Sim.gd`、P1-v focused test、P1-ac closure、readiness verifier/evidence JSON 与 PR body。验收要求为
失败 callback 零 mutation、完整 query/index/tx-row budget、Git external ref/report blob/hash 与
missing/tamper/mismatch/same-candidate negatives，以及 hosted exact-family classification。完成后只能 normal
fast-forward 当前 `codex/p1a-takeover` 并停止请求第三次独立 review；继续禁止 anchors、force、merge、rebase、
finalize 与 Narrative Beat 并发。

## 十五、2026-08-21 07:02 CST Human Acceptance presentation gate

Creative Owner 新增终态门禁：human acceptance 只允许在 polished、exact-SHA、可复现的试玩 presentation
package 完成后发生；在此之前统一标记 `HUMAN_ACCEPTANCE_NOT_READY`。代码/CI 绿、单帧截图、debug 录屏、
文字自述或孤立 receipt 都不能替代 Player/Content/Evidence 层完成。

必需 package 包含两类主交付物：

1. sufficiently rich 的真实游戏全流程 MP4：覆盖冷启动/控制、探索与决策、可见反馈、真实 consumer、通过
   save/load 证明的 persistent consequence、至少一个负例/恢复路径及连贯收束；最低 1280x720、稳定
   24/30 fps、H.264 yuv420p + AAC + faststart。录制真实 UI 与真实 SFX/BGM，必须多帧运行并检查
   `signal 11|FATAL|out of bounds`，不得用 mockup/concept slide 冒充 gameplay。
2. 与同一 SHA/version 绑定的 screenshot-rich player guide：安装/启动、controls、core systems、推荐
   walkthrough/攻略、选择点及预期反馈、save/load/persistence、常见失败与恢复、troubleshooting、known
   limitations、acceptance checklist；截图必须来自 exact runnable build 并有说明。

旁白采用 Edge TTS 柔和温暖中文女声（默认 `zh-CN-XiaoxiaoNeural` 或经复核等价声音，约 -10% 至 -15%
语速）。按 bounded segments 写稿，字幕时间由每段生成音频的实测时长派生并以清晰中文 ASS 烧录；游戏声床
单独响度归一，旁白出现时 sidechain duck，验证无 clipping、旁白可懂且 game audio 非静音。package 必须另附
commit/tree、build/recording commands、voice/rate、字幕源/时间轴、media hashes、ffprobe、loudness/non-silence、
sampled-frame visual QA、crash scan 与 screenshot provenance。editorial、visual/UX/accessibility、QA 的 P0/P1
必须先清零。

该门禁不会扩大当前 P1 corrective write set。只有候选 exact SHA 稳定、focused/integration/hosted 与独立 QA
闭合、player-visible flow 有真实 owner/consumer 后，Producer 才创建隔离的 Asset/Projection 或 QA/Evidence
task 负责录屏与玩法文档。当前只允许最多一份 NONCANON advisory storyboard/narration/walkthrough outline；HEAD
变化后必须重核或丢弃。已分别通知 dev 与 narrative：dev 当前继续 corrective batch 2，不加录屏 hooks、不写
guide；narrative 不生成资产、不把未证 mechanics 写成已实现。presentation readiness 也不授权 canon、merge、
release；human decision 必须带日期并绑定 candidate SHA 与 package hash。

## 十六、2026-08-21 07:06 CST corrective 2 delivery and third fresh review

全仓 drift check 发现 `origin/codex/p1a-takeover` 已由 `dc7b3d6` 正常 fast-forward 到
`b3721c44a236b521d5a70c45405df4d6d1a58258`（tree
`578076b470e969446c8e69108162d8dc2d7b9ca3`）。corrective-2 实际 diff 严格为已授权六文件：
`Main.gd`、`Sim.gd`、P1-v focused test、P1-ac closure、readiness verifier/evidence JSON；protected
anchors 与 unknown-owner visual WIP 未改。PR #6 body/head 已同步，仍 Draft / REQUEST_CHANGES / UNSTABLE。

新 hosted run `32426143022` 正在运行，`visual_canary` 已 PASS。上一 exact run `32423850259` core
已 FAIL；implementer 声称只含四类 protected anchors，但 Producer 不继承该自报，已要求第三次 independent
review 逐日志分类。独立 QA/Evidence + REFUTE task `01a0212b-ff4c-7d53-96a6-26a55d7fe38f` 已重新派到
exact `b3721c44`，严格只读，复核 failed-callback literal zero mutation、lifecycle cleanup discrimination、
target-plane/session negatives、far duplicate、完整 query/index/tx dereference budget 与 mutation tooth、真实外部
Git ref/report blob/hash binding 及 detached portability，并在新 hosted run 终态后给出唯一三态 verdict。

在第三次 verdict 前继续禁止 anchors/finalize/merge/release，也不启动录屏或玩法文档 task；状态保持
`HUMAN_ACCEPTANCE_NOT_READY`。Narrative 只产出了一份带 `[O1]`–`[O6]` observed-step slots 的
NONCANON advisory storyboard，没有 repo 写入、资产生成或已实现 claim；它只能在 future stable SHA 到来后由
Asset/Projection specialist 重核，不能作为当前 evidence。

## 十七、2026-08-21 07:18 CST third fresh-review REQUEST_CHANGES

第三次独立 QA/Evidence + REFUTE 对 exact
`b3721c44a236b521d5a70c45405df4d6d1a58258`（game tree
`c580e4e15ee0bce65337502abac8511cdbb6ebea`）返回 **REQUEST_CHANGES**。五个 supervised focused
scenes 均在 clean detached exact source PASS，source-stable/cleanup true 且无 native/logic failure；protected
anchors 未改。failed callback literal no-op、target move/plane、A→B late-A、far duplicate 与 production
ledger/index/tx accounting 结构均通过静态/运行复核。

剩余阻断已缩为 evidence discrimination 与 verifier portability，而非已证产品逻辑 bug：

1. 缺 lifecycle cancellation 独立测试，以及 wrong Sim-instance、session-only delayed callback 的分离负牙；
2. 5,000-event budget 只有正向阈值，缺一个 deterministic mutation/negative control 证明新增 query dereference
   会让门实际变红；
3. verifier 已能解析 external Git ref/report blob 并拒绝 filesystem/missing/candidate-ref probes，但 exact detached
   QA checkout 会先因 branch 为空被拒；需增加只读 detached exact-QA mode，同时保持 finalize/write 路径 attached
   branch 约束；
4. 尚无 distinct committed external review ref 上的 positive report blob，因此 external binding 不能完成，也不得
   用 candidate self-attestation 替代；
5. exact hosted run `32426143022` 在 verdict cutoff 时 core 仍运行，只有 visual PASS。

Producer 已把 PR #6 body 修正为第三次 REQUEST_CHANGES，并向 dev 发出 corrective baton 3。默认严格 write set
仅为 P1-v focused test、readiness verifier、P1-ac closure、readiness evidence JSON 与 PR body；`Sim.gd` 只有在
新增 mutation tooth 暴露真实遗漏时才可修改，`Main.gd` 与其他产品面禁止改。完成五 scenes、detached verifier
positive/negative controls、hosted exact classification 后，只能 fast-forward 当前 PR 并停止请求第四次 independent
review。anchors、presentation 与 human acceptance 继续冻结，状态仍为 `HUMAN_ACCEPTANCE_NOT_READY`。
