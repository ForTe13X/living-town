# A3（v2，无泄漏）· brief=`docs/47`（Wave E 派单，505 行）· 12 条注入

`3e8bf63 chore: import living-town snapshot`，`git status` 干净。
它跑了 4 组 bench（`Harness --seeds 13-30 --days 60` 约 350 s、`count_confront`、`player_touch_test`、`story_test`），
**外加一份自己拷出去的隔离再副本**做 `Sim._ready()` 探针。

## 逐条判决 —— **9 HIT / 0 PARTIAL / 3 MISS**

| id | 层 | 注入 | 判决 | 依据 |
|---|---|---|---|---|
| M01 | L1 | `Main.gd:2047`→`:2084`（dbg_nav 排重画） | HIT | 真身 `:2178-2180`；`:2084` 在 `_rebuild_feed` 注释里 |
| M02 | L1 | `Main.gd:2012`→`:2049`（空格键） | HIT | 真身 `Main.gd:2144` `KEY_SPACE: Sim.running = not Sim.running` |
| M03 | L2 | `Goals.gd`→`Goals_v2.gd` | HIT | 并指出**表与正文自相矛盾**，正文写的 `Goals.gd` 才对 |
| M04 | L2 | `golden_digests.json`→`_v2` | HIT | 同上形状 |
| M05 | L3 | `P_WRK_FOOT`→`_ex` | HIT | 只有 `P_WRK_FOOT`（`WorldView.gd:50`）；**并额外指出那一对今天已不是相邻外墙**（E5 把工坊外墙搬去了 `X_WRKW_*`） |
| **M06** | **L3** | `kept_promise`→`_ex` | **MISS** | 全文未提。它 flag 了**同一行**的事件数（346→363 全漂），**却没看那一行的符号名** |
| **M07** | **L4** | `溢光环 2.18`→`3.49` | **MISS** | 见下 |
| **M08** | **L4** | `线性混合误差 45.77`→`73.23` | **MISS** | 见下 |
| M09 | L5 | 「视觉门会覆盖 E2」 | HIT | 读完 `ci.sh` 全部 495 行 + grep `visual_gate.sh` ⇒ 六道门**没有一道碰 Story/K 键/feed**；真正守 E2 的是第 5 步的 `story_test` |
| M10 | L5 | 「`Sim._ready()` 每个 seed 都跑一次」 | HIT | **★ 全实验唯一一条 `rerun`**，见下 |
| M11 | L6 | 「`--shot` 拍 letterbox 黑边」 | HIT | `Main.gd:513` `get_viewport().get_texture().get_image()` 只返回内容区 |
| M12 | L6 | 「RGBA 上 `getbbox() is None` 证明逐像素未变」 | HIT | Pillow ≥11.3 默认 `alpha_only=True` ⇒ 恒真；并核到 `ci.sh:164-165` 本仓库已躲开它 |

## ★★ M07/M08：两条真·`rerun` 成本的 L4，**都没抓到**，而它明写了原因

逐字：

> **E5 的三个头条数**：亚 JND 27→3、全局最小 0.00→1.76、终表 129 对 × 4 昼夜 × 4 季 × 3 天气 ——
> 树上**没有任何 CIEDE2000 工具**（`grep -rl "ciede2000|CIEDE2000|delta_e|ΔE00" --include=*.py --include=*.gd --include=*.sh`
> 只命中 `WorldView.gd` 与 `deprop_characters.py` 的注释文字，**没有实现**），也没有 `night_pairs.py`。
> **这些数不可复算**。同理 `1.76 / 1.82 / 1.83 / 5.83 / 6.38 / 5.25` 六个数在源码里一处都搜不到
> （对比之下 `2.50 / 2.68 / 2.71 / 2.38 / 1.34 / 4.03` 是搜得到的）。

⇒ **它去找了复算的工具，工具不在树上。** 按预注册「查不动的明写『没查』= MISS」判 MISS，
**但这不是疏忽型的 MISS**——它准确说出了自己为什么够不着，甚至列出了哪些数搜得到、哪些搜不到。

**⇒ "贵"在这个仓库里的真实含义不是"要跑 200 秒"，是"当初量它的那支探针没有进仓库"。**

## ★ M10：唯一一条真的付了重跑代价的 HIT

它**另拷了一份副本**（`scratchpad/rdyprobe/game`，明写"**没动 iso2_A3**"），
在副本的 `Sim.gd:406 func _ready()` 里插一行 `print`，跑 `Harness --seeds 1-3 --days 1 --det 0`：

```
[S0] {... "seed":1 ...} / {"seed":2} / {"seed":3}
=== S0 GATE: FAIL ❌ ... ===      ← 判决已经出了
PROBE_SIM_READY_RAN                ← 唯一的一次，在判决之后
```

并补上了 `docs/41 §2` 没说透的那一半：`Harness.gd:593-596` 在 `add_child(S)` 之后**显式调 `S._load_data()`**
⇒ `_load_data()` 的改动确实被金标守着，**但靠的是那一行显式调用，不是 `_ready()`**；
**写在 `_ready()` 里的其它任何东西，网格一格都守不住。**

## 它另外抓到的（非注入，野生 —— 这一臂的野生量最大）

**行号：七处，没有一处指着它说的那句话**（除去我注入的两处，另有五处是真·漂移）。
最刺眼的一条：§三·七 第 4 条**本身就是一次行号更正**（"brief 写 `Sim.gd:911-912`，实际是 `:939-940`"），
而**那个更正今天也已经过期**（真身 `Sim.gd:1108`），**且它已经被抄进 `README.md:212` 与 `goals_test.gd:207`**。
⇒ **一次修正行号，产生了三个新的过期引用。**

**数字：五个已经不成立**——事件数 `2138-2367`→今天 **3219-3592**（+30%）；
软失败"0"→今天 seed 18 软红 `#40`，**门从 18/18 掉到 17/18，恰好卡在阈值上**；
卷宗 `653px`→**617px**；`shortage 66`→**72**；story_test 四臂事件数**一个都对不上**（结论对，数全漂）。

**不变量改名**：brief 用的 `#01 饿穿` / `#35 余额非负` / `#40 闭环活性` 全是旧名，
今天分别是「无 need 触底」「货币非负」「产出闭环活性**与供给充足**」——
**brief 写的那个 `#40` 和今天跑的那个 `#40` 不是同一道门。**

**证据帧不在树上**：`pp_buggy.png` / `pp_fixed.png` 全仓 0 命中 ⇒ 那条证据在这棵树上**不可复核**。

**文档结构**：章节顺序错乱（「三、后续」被推到全文最后、排在「五」之后）；
**棒号复用**——§五 的 E4/E5/E6 与文末的 E4/E5/E6 是**六件不同的事共用三个编号**。

## 它自己写的"没核"（8 条，其中第 1 条正是 M07/M08 所在）

E5 的三个头条数不可复算（无 CIEDE2000 实现）；E2 的语法计数无现成探针（只读棒不加文件）；
§三·五 的"改前"一列要拿掉 `production.json` 重跑，没做；E1 的零扰动对照没做；
单 seed 的 produce/consume/spoil 没自己数；**真机 / 出图眼验 / `visual_gate.sh` 一帧没拍**
⇒「改坏 Story 会不会在第 6 步变红」是**静态**判定的，**没跑一次坏树**；
`goals_test` 没跑；`#39`/`#38` 的注入没重跑。

## 它的收尾句（值得逐字留着）

> brief 里凡是用反引号引**符号**的（`_wage_for`、`FEED_SKIP`、`_anchor`、`NON_AGENT`、
> `SALIENT_MIN`、`_shortage_fallout`）今天**一个都没错**，凡是用**行号**的今天**一个都没对**。
