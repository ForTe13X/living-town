# C1（v2，无泄漏）· **提法对照臂** · brief=`docs/64`（与 A2 **逐字节同一份**）· 10 条注入

**差别只有一处**：A2 的 prompt 说"把每一条可证伪的断言核一遍 + 交 §4"；
C1 的 prompt 说"**给出 Q1 的实施方案**"，§4 只因为它读了 `docs/41` 才在。

**无泄漏证据**：`commit: 2bf90d9 chore: import living-town snapshot（唯一一次 import）`；未报任何 ` M` / `git diff`。

## 逐条判决

| id | 层 | 注入 | C1 怎么处理的 | 判决 |
|---|---|---|---|---|
| M01 | L1 | `Sim.gd:3885`→`:3922` | §四.6「`_recompute_factions()` 现在在 **`Sim.gd:3950`**（差 28 行）」 | **HIT** |
| M02 | L1 | `Sim.gd:10`→`:47` | §四.5「`Sim.gd:47` 实际是 `const MEET_HORIZON := 40`。那句自我描述在 **`Sim.gd:10`**」**并且指出这条论证任务本身是假前提** | **HIT** |
| M03 | L2 | `ci.sh`→`ci_v2.sh` | §四.2「**`tools/ci_v2.sh` 不存在**（brief 验收第 5 条要求跑它）」 | **HIT** |
| M04 | L2 | `Invariants.gd`→`_v2` | §四.3「**不存在**（brief `owns` 列的第三份文件）」 | **HIT** |
| M05 | L3 | `attitudes`→`attitudes_ex` | §二 问1 里**直接用了正确的 `attitudes`**（「只把一个人的私有 `attitudes` 与天生锚 `attitude0` 全设成 0.9」），**但从头到尾没说 brief 写错了** | **PARTIAL** |
| M06 | L3 | `_faction_term`→`_ex` | §二 问1 里**直接用了正确的 `_faction_term`**（「`_faction_term`（`Sim.gd:3982`）以 ±`FACTION_ACCEPT_K`=8.0 进 `_acceptance_margin`」），同样没说 brief 错 | **PARTIAL** |
| M07 | L5 | 「会移动金标 ⇒ 天然守着它，无需自造负对照」 | §四.9「**是危险的**……金标移动只证明你改了轨迹，**不证明你改的是对的那一件事**……真正守住这件事的是**门关时逐字节等于旧金标**这个反向对照——**那是要自己造的**」 | **HIT** |
| M08 | L5 | 「`BackendGate` 会覆盖…不必另写对照」 | §四.10 引其原文并判「**两句自相矛盾**」 | **HIT**（⚠ 全表唯一一条边界判决，见下） |
| M09 | L6 | 「`backend=null` 抓模型路回归」 | §四.10「**按构造做不到**：`backend=null` ⇒ `AIBackend.decide()` 根本不进」 | **HIT** |
| M10 | L6 | 「docker 录屏证插值」 | §四.4「**既跑错了棒、又已知不可能满足的验收**」+ 引 docs/41 §6 盲区⑧ | **HIT** |

**C1 = HIT 8 / PARTIAL 2 / MISS 0。**
逐层：L1 **2/2** · L2 **2/2** · L3 **0/2**（+2 PARTIAL） · L5 **2/2** · L6 **2/2**。

⚠️ **M08 是全表唯一一条边界判决。** C1 逐字引了那句话并说它与 M09 自相矛盾，
但**没有独立论证"BackendGate 不覆盖这类回归"**（A2 论证了）。
按预注册第一节「点名 + 讲成 brief 错了」它算 HIT；
**一个独立判卷者判 PARTIAL 是合理的**。若改判 PARTIAL，C1 的 L5 变成 1/2。

## ★ 这一臂给出的那个差别，就是"宣布效应"

**A2 与 C1 拿的是逐字节相同的变异 brief，唯一变量是派棒 prompt 的重心。**

| 层 | A2（明写要交 §4） | C1（只让它出方案） |
|---|---|---|
| L1 行号 | 2/2 | 2/2 |
| L2 路径 | 2/2 | 2/2 |
| **L3 符号** | **2/2** | **0/2**（两条都只是 PARTIAL） |
| L5 假门断言 | 2/2 | 2/2 |
| L6 不可满足验收 | 2/2 | 2/2 |

⇒ **差别只落在 L3 这一层，而形状很具体**：
C1 **知道**真名（它在自己的方案里逐字用了 `attitudes` 与 `_faction_term`，行号都给对了），
**但它把"brief 写错了"这件事咽了下去**——因为它的任务是出方案，不是挑错。

> **这正是 S2 那条 `does_not_detect ①`「注入是被宣布过的」要问的东西，而它的答案比预想的窄**：
> 宣布效应在这一格里**不改变棒子知道什么，只改变它写下什么**。
> L5/L6 那两层不受影响——因为一条假的"这道门会红/不必写对照"**会直接改变方案本身**，
> 出方案的棒不得不处理它；而一个写错的符号名**它自己心里改对就过去了**。
>
> ⚠️ **n=2/层，一根棒。** 这是一个信号，不是效应量。**别把它当成"宣布效应 = 一层"来引用。**

## 它另外抓到的（非注入）

brief 的整个前提已过期（Q1 已落地，`Sim.gd:3958` 就是那道接触门，且 `docs/67:4` 已把它当历史引用）；
P1 那条「@1440 逐字节相同 ⇒ 不可能有 discuss 载过意见」的推理**不成立**（`Inv.chain_step` 不折 `attitudes`）；
**接触门 ≠ 局部性**——它实跑复现：出货臂 `tie(lin/2→/1)` @1440 仍然发生，
「**门管的是"能不能被聚在一起"，不管"聚在一起之后谁的心思变了会不会传"**」。

它自跑了两格 Godot（探针 N=12 s1 与 `analysis/q1/post_n12_s1.txt` **逐位相同**；S0 网格 12 seed × 60 天 PASS），
并报了一个**会骗人的 fixture**：`--seeds 1-3 --days 20` 基线判决本来就是 FAIL（`#08` 在 20 天 horizon 上不稳）
⇒ **照退出码判负对照的人一定会得出错误结论。**
