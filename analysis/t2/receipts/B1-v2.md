# B1（v2，无泄漏）· **半剂量臂** · brief=`docs/71` · 5 条注入

**无泄漏证据**：`6371823 chore: import living-town snapshot`（单 commit 快照）；未报任何 ` M` / `git diff`。
剂量：`docs/71` 115 行注 5 条（**每 23 行一条**），对照 A2 的每 8.6 行一条。

## 逐条判决 —— **5/5 HIT，零漏报**

| id | 层 | 注入 | 回执 | 判决 | 证据类 |
|---|---|---|---|---|---|
| M01 | L2 | `Invariants.gd`→`_v2` | E1「全仓 `Invariants_v2` 的**唯一**出现处就是 docs/71:20 这一行本身」 | HIT | ls+grep |
| M02 | L3 | `rebake_history`→`_ex` | E4「真身是 `_meta.rebake_history`（`golden_digests.json:114`）……docs/41:437、57:49、58:61、61:65、67:71、75:45 **全部**写的是 `rebake_history`」 | HIT | grep |
| **M03** | **L4** | **人口 `×5.00`→`×8.00`** | E2「真身 **×5.00**……`docs/68:206`、`Invariants.gd:932` 逐字。**最可能的成因是把同一张表里 `整洁 ×7.93` 那一格读成了人口**」 | **HIT** | **xref** |
| M04 | L5 | 「`lod_verify` 跑 N=48 ⇒ 大 N 硬不变量也测过了」 | E5「`lod_verify.gd` 全文只用 `Inv.digest(S)`（:67），**从不调 `Inv.check_all`**」+ 引 `ci.sh:219-224` 那段专为纠正此误读而写的注释 | HIT | read |
| M05 | L6 | 「`--shot` 拍带气泡(emote)的截图」 | S-1 **自己把整条信号链读完了**（`Main.gd:400-405/479/496` → `Sim.gd:410` → `WorldView.gd:1044-1053/2931`），结论「接线之后不再有 tick，接线之前的事件到不了视图层」 | HIT | read |

⚠️ M05 值得单记：它**没有照抄 `docs/41` §6 盲区①**，而是逐行重建了机制，
并额外指出**本波真有一条验收静默未达成**（S3 交的是录制路径的 GIF，既没交这张 `--shot` 截图，也没写"做不到"）。

## ★ M03 是全实验唯一一条 L4 观测，而它证实了成本表

`cost_probe.py` 事先判它 `xref`（真值在 `docs/68:206` 等 8 行文本里）。
B1 的实际查法**正是 xref**：`Grep "人口 ×|×5\.00|×8\.00" docs/68` → 读表 → 对 `Invariants.gd:932`。
**它没有重跑任何网格**，就证伪了一条标着 `cost=rerun` 的注入。

## 它另外抓到的（非注入，野生 W≈6）

E3 岗位数 10→**9**（独立复核 S1 的更正）；
E6「held-out 一直在无声地红着」——**三个分句全不成立**（不是只在 R12 跑：`utility.json._gossip_news_dose` 有 O1 的记录；
不是"一直"：判据是**严格小于**，改前最小值恰好 0.500 **不红**，是 O1 那次调参当天才产生的；不是"无声"：O1 写进了数据文件，只是没点名 seed）；
E7 外审那句"至今没被回答过"是假的（复核 S2 的 §〇①）；
E8「按构造无法宣判 N≤24」把 R1 的原话（"N=12/N=16"）**放宽了一档**——CI 只有 N=12/N=16 两格；
E9「**只有**图书馆和阿丽房间是对的」——"只有"二字是 brief 加的，R2 对两栋民居**没有表态**，
**而 S3 在 docs/74 §一 把 brief 加的这两个字当成了"R2 的原话"引用**（一次转述收紧，在下一棒手里变成了对上游的引用）；
E10 R2 的"改名**应当**是 digest 中性的，**但必须实测证明**"被 brief 转成了判定。

**它明写「E1/E2/E4/E5/E6 五条本波三根棒一条都没抓」**，且 E5/E6 各自被某根棒的**正文**实质推翻过、
**却没有被写进那根棒的「brief 哪里是错的」**——与 §四·2 那条"知道但不写下来"同形。

**假警报 FA = 0。** 它另列了 **23 条"对得上"**，其中 `shelf` 11/8 层、seed 18 的 0.42/38 天、
4a 余量 0.056（逐 seed 与 S2/R1 逐位相同）三条是它自己重新跑出来的。

## 它独立复现了 v1-B1 那条软门退化

§四.8 逐字：「`Harness --seeds 18` 单跑时，判决行印的是 `=== S0 GATE: PASS ✅ ===`
（软门在 n=1 时退化成 `≥0/1`）。**"seed 18 是红的"只能从 `#40` 那一行读到，读判决行会读反。**」
⇒ 两根**互不相通**的棒在同一件事上收敛（v1-B1 与 v2-B1），我自己也独立算过（见 `receipts/v1-leaked/B1-findings.md`）。

## 它自己写的"没核"

N=60 的 8/12 没跑（≈2.6 小时）；「R1 落后 ~100 / R2 185 / R3 24」**树上没有出处**（三份回执都没记自己落后多少）；
`tools/ci.sh` 全量没跑；S2 的 62/87 分类与 20 条 L 清单**一条都没复核**；
S3 的像素/ΔE 需要 docker，没跑；`brief_mutate.py` 只确认存在，没跑 selftest。
