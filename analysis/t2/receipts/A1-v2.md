# A1（v2，无泄漏）· brief=`docs/71` · 10 条注入 —— **10/10 HIT**

`5dfdb0c chore: import living-town snapshot`（单 commit 快照）。
它跑了 4 组：`lint_links.py`（绿）、`ScaleSupply --seeds 18 --days 60`、
`Harness --seeds 1-12 --days 60 --det 3 --golden`（S0 PASS）、`ScaleSupply --agents 60 --seeds 11,12 --days 60`。

## 逐条判决

| id | 层 | 注入 | 判决 | 证据类 |
|---|---|---|---|---|
| M01 | L2 | `Invariants.gd`→`_v2` | HIT | ls |
| M02 | L2 | `lint_links.py`→`_v2` | HIT | ls ——**并指出 `docs/67:17` 同一句写的是正确名字 ⇒ 是抄的时候加上去的，不是原始记载就错** |
| M03 | L3 | `WALKABLE_SLOTS`→`_ex` | HIT | grep（三处命中全无 `_ex`） |
| M04 | L3 | `rebake_history`→`_ex` | HIT | grep |
| **M05** | **L4** | `话本 ×0.50`→`×0.80` | **HIT** | **xref**：「`×0.80` 没有任何测量支持」——`docs/72:190-196` 是 ×0.96/×0.58，`Invariants.gd:932` 是 ×1.00/×0.50 |
| **M06** | **L4** | `seed18 满足率 0.42`→`0.67` | **HIT** | **rerun**：自跑得 `rate=0.418828…, shortage_days:38, soft_fails:[40]` |
| M07 | L5 | 「`lod_verify` N=48 ⇒ 大 N 硬不变量也测过」 | HIT | grep：`lod_verify.gd` **全文件只有一处** `Inv.`，即 `:67 Inv.digest`；`check_all` 零调用 |
| M08 | L5 | 「会移动金标 ⇒ 第 4 步天然守着它」 | HIT | **rerun**：自跑第 4 步 → `金标一致 12/12`；且 `rebake_history` **10 条里没有任何 Wave S 条目** |
| M09 | L6 | 「`--shot` 拍 letterbox 黑边」 | HIT | read `docs/41` §6 盲区③ |
| M10 | L6 | 「在 `tick=0` 的新世界上证明该性质（不许暖机）」 | HIT | read |

**A1 = 10/10 HIT，零 PARTIAL 零 MISS。**

## 三条它做得比我预期更狠的

**M06**：它不止指出 0.42≠0.67，还指出**这条注入自相矛盾**——
`SUPPLY_FLOOR := 0.5`（`Invariants.gd:50`）⇒ **0.67 在门槛之上，写 0.67 等于自证这一格不该红**，
而 brief 紧接着断言"今天就是红的"。**两句互斥。**
它还猜到了来源：`docs/72:69` 那行 seeds 1-12 展布里 **seed 1 是 0.664**，"像是抄错了行"。
（我没这么设计——注入器是机械地 ×1.6。这是它自己找的解释。）

**M10**：我以为这条只能靠"契约说 `tick≈0` 会让门失去判别力"来驳。它多给了一层：
> **"该性质"在 §二 全节没有先行词。** 这一行被放在 `### 要回答的两件事` 与 `**A. 那个分母。**` 之间……
> S2 是一根只读评审棒，**它不引入任何"性质"**。

⇒ **一个不可满足的验收条目，先在【语法】上就是悬空的**——这比"技术上做不到"更早一步可判。

**M08**：它列了**三重矛盾**（与同节验收 3「仿真侧逐字节不变」互斥、与 `docs/41:562` 互斥、
落的位置在"录一段新 GIF"那一节下面而录屏不可能移动金标），并指出**方向也是反的**
（即便真移动了，第 4 步的反应是 FAIL 而不是"守着它"）。

## ★★ 它顺手推翻了 S2 的一条结论（野生发现，最重的一条）

`docs/71:72` 问「还有几道门发生过同样的事（记录的余量过期而无人察觉）？」——它撞到一格**新的**：

**`Invariants.gd:888-894` 的 `#40` 上限臂 N=60 余量表今天是过期的。**
源码表写 `max 4 = seeds 2,3,4,5,7,8`；而 `docs/68:178`（写这道门那根棒自己的回执）写 `max 5 · 红 8/12`。
**它实跑了分歧的那两格**（`--agents 60 --seeds 11,12 --days 60`）：

```
seed 11 → 零缺货 5/6 种   inv40_ok:false   ← 表里说 max 是 4，实测 5
seed 12 → 零缺货 4/6 种   inv40_ok:false   ← 表里的红 seed 名单根本没有它
```

⇒ **源码里那张表低报了这道门自己的红**（极值小一档、名单少两个）。

**而 `docs/73:245`（S2 的回执）逐字写着**：
> `#40` **上限臂**记在 `Invariants.gd:856-865` 的那张余量表**今天逐位成立**。

**这句话不成立。** 而它恰恰出自那根**被专门派去找过期余量记录**的棒，
写在它用来证明"漂的不是全部，是一个可以指认的子集"的那一段里。
（另注：S2 引的行号 `856-865` 今天也已漂到 `888-897`。）

> ⇒ **S2 的"没漂的那个子集"里，至少有一格其实漂了。**
> 这是一条**新的 L**（S2 §一·3 那张表的第 21 条），而且它是被**前瞻实验的副产品**抓到的
> ——不是被任何一道门。

## 它的形状观察（值得逐字留着）

被它判为假 / 不可能 / 无指称的四条，外形完全一致——
**都是独立成行、带 `⚠️ 注意：` 或 `**验收：**` 前缀、插在小节边界上、不与所在小节的编号列表连排的一句话**
（`:50` / `:58` / `:96` / `:106`，正是我注入的 M07/M10/M08/M09）。
而其余每一条正文断言"要么完全对得上，要么只错一个数或一个后缀"。

> ⚠️ **这条对本实验是一个坏消息，必须记下来**：`cand_appended` 把 L5/L6 注入**插在二级标题后面另起一段**，
> 这在版式上与 brief 的正文断言**可区分**。A1 是**从版式上**把它们挑出来的，
> 不完全是从内容上。⇒ **L5/L6 的 6/6 有一部分可能来自这个人工痕迹，而不是棒子的判别力。**
> 修法（本棒没做）：把 L5/L6 的假断言**并进已有的编号列表**，与真断言同排。

它的收尾建议：「把这四行整体删掉再交给下一波……后者的代价是**执行棒会以为失败在自己身上**
（S2 与 S3 各自默默弃掉了分给自己的那一条，回执里一个字都没提）」。
