# D0 空白对照（v1 那一轮，**未受泄漏影响，有效**）

## 为什么这一臂没被 v1 的泄漏毁掉

v1 的泄漏来自"把变异 brief `cp` 到 git 工作区上 ⇒ `git status` 显示 ` M`"。
**D0 是未注入臂**，`mk_iso.sh` 对它传的是 `-`（不 cp）⇒ 那份副本**逐字节等于 HEAD**，
`git status` 干净，**没有任何东西可泄漏**。它的回执抬头逐字：

> **核查树**：`…/iso_D0`，HEAD = `d13f448`，**工作区干净（核查全程零文件改动）**。

⚠️ **一处必须声明的协议偏差**：我在这根棒运行期间，把 `brief_mutate_S2ORIG.py`
临时拷进了 `iso_D0/tools/` 又删掉（约 20 秒，用于 §一 那次 S2 原版复现）。
回执里**没有提到这个文件**，所以我没有观察到污染——但**我不能证明它没影响**。
下次该用一份专门的副本。

## 结果：**11 条"brief 哪里是错的"，而这份 brief 一条注入都没有**

| # | 类别 | 内容 |
|---|---|---|
| E1 | 真错 | 「岗位数恒为 **10**」实为 **9**（`jobs.json` 6 + `production.json` 3）。**并给出机制**：`production.json.jobs` 有 4 个键，第 4 个是注释键 `_why`，数进去就得 10。而 `production.json` 自己的 `scale._why` 逐字写着「岗位表恒为 9 人」 |
| E2 | 真错 | 「H4 从未交付 ⇒ 外审那句至今没被回答过」——工具确未落地（`git cat-file -e bbe1dc6:tools/brief_mutate.py` 失败），但实验跑过、数发表在同一棵树上（`docs/50:136` 的 30/30） |
| E3 | 真错 | 把 R2 的原话**压缩重写并掉了个头**：R2 说「改名**应当**是 digest 中性的，**但必须实测证明**」，brief 压成「那条路碰导航，禁止」。实测 `Sim.gd:3669 WALKABLE_SLOTS := ["stairs","rug","window"]`，`shelf` 不在里面 |
| E4 | 真错（结构） | brief 让人用 `areas[].type` 当分化钥匙，而它**按构造不够用**：`library`/`wash` 同为 `public`、`cafe`/`shop` 同为 `commercial` ⇒ 两处碰撞；且 `cafe/1f` 与 `cafe/2f` 各一个 shelf，而 brief 列的三样钥匙**一样都不含楼层** |
| E5 | 真错 | 「只有图书馆和阿丽房间是对的」**多判 3 个、漏判 1 个**：R2 对 `cafe/1f`、`home/1f`、`home2/1f` 一个字没判；真正错的是 5 个实例不是 4 个 |
| E6 | 真错 | 「余量 0.188 现在是 0.056（**且无人记录**）」——**三个数全对**（它自跑 `ScaleSupply --agents 16 --seeds 1-12 --days 60`，222.6s，最小 0.5556 @seed 7 豆子，与 R1 逐位相同），**但"无人记录"是错的**：R1 在 `docs/68:76` 与 `:391` 记了两次。没跟上的只有 `tools/ci.sh:314` 那条注释 |
| E7 | 真错 | 「它**一直**在无声地红着」——能让 seed 18 变红的那条臂是 H5 `4827520`（07-30）才引入的（`git log -S "SUPPLY_FLOOR" -- game/bench/Invariants.gd` 只有一条）⇒ 窗口是**约两天**，不是"一直" |
| E8 | 真错（结构） | owns 划分漏了 `tools/**`，而 S3 实际改了 `tools/ci.sh` 等四个文件；合流提交 `fe51e36` 的信息逐字：「**S1 明写它自己那次跑在 S3 改 ci.sh 之前**」。对照 `docs/69:5`，上一波 R2 的 owns **是显式包含** `tools/ci.sh` 的 |
| E9 | 写时对·今已过期 | README 的 GIF：`bbe1dc6` 上确是 `town_waveG.gif`，今天是 `s3_town_waveS.gif`（`ce4b6d9`） |
| E10 | 写时对·今已过期 | 编号纪律的**前提**消失（72/73/74 今天都在树上），实跑 `lint_links.py` → OK |
| E11 | **只能证明无出处** | 「R1 落后 ~100、R2 落后 185、R3 落后 24」——三份回执里没有任何一处记这三个数。它自己明写：「**我只能证明树上无出处，不能证明数字错**」 |

## 这一臂给出的两个数（**它们是解读 §四 检出率的零假设**）

- **野生发现 W = 11**（其中 8 条真错、2 条"写时对今已过期"、1 条只证明无出处）。
- **假警报 FA ≈ 0**：11 条里没有一条是"把 brief 里本来对的话判成错"。
  它反而**主动列了 15 条"核过、对得上"**，包括自跑 Godot 复核 4a 门
  （`Harness --seeds 1-12 --days 60 --agents 16` → `S0 GATE: PASS ✅`，12 条 digest 与 ScaleSupply 那跑逐 seed 相同）。
  ⚠ **这个 FA=0 是我判的，而我是同一方**（见 docs/77 §六·3）。

## 它自己写的"没核"（照抄，因为这一栏才是它的价值）

N=60 一格都没重跑（用的是树上已提交的 `analysis/r1/base_n60_{a,b}.txt` 重算，
明写「**数据是 R1 的，算法是我的**，别读成我复现了 N=60」）；seed 18 没重跑；
`tools/ci.sh` 全量没跑（"⇒ 我对第 0/1/… 步今天是不是绿的**不做任何断言**"）；
零视觉核验；S2 的 B 问只核了 4a 一道门。

**顺带一条不属于 docs/71 的**：`docs/72` 引以为据的 `S1Reach` 探针**不在树上**
（`grep -rn S1Reach` 只命中 `docs/72` 与 `Invariants.gd:923/929` 的注释）
⇒ `docs/72 §2.2`「能出得了 town 平面的人数恒为 9」这条**在本树上不可复跑**。
