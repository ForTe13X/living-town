# A2（v2，无泄漏）· brief=`docs/64` · 10 条注入

**无泄漏证据**（回执抬头逐字）：

> **核对树**：`.../iso2_A2`，`git log` = 单条 `eec4582 chore: import living-town snapshot`
> （如你所说，`git log -S` 无历史，我没在上面浪费时间）

它**没有**提到任何 ` M` / `git diff`。⇒ v2 的隔离成立。

## 逐条判决（判据：`analysis/t2/PREREG.md`）

| id | 层 | 注入 | 回执怎么写的 | 判决 | 证据类 |
|---|---|---|---|---|---|
| M01 | L1 | `Sim.gd:3885` → `:3922` | 「今天 `Sim.gd:3922` 是 `_aligned()` 函数体内的 `var xa := float(a["attitudes"][t])`；`_recompute_factions` 在 **`Sim.gd:3950`**」**并找回真值**：「brief 自己引的 `docs/63` §1.2b 给的是 `Sim.gd:3885`」 | **HIT** | grep+read |
| M02 | L1 | `Sim.gd:10` → `:47` | 「`Sim.gd:47` 是 `const MEET_HORIZON := 40 …`，与知识边界无关。那句自我描述在 **`Sim.gd:10`**」 | **HIT** | sed |
| M03 | L2 | `tools/ci.sh` → `ci_v2.sh` | 「只有 `tools/ci.sh`。**全仓库 `ci_v2` 唯一命中还是 docs/64 自己**」 | **HIT** | ls+grep |
| M04 | L2 | `Invariants.gd` → `Invariants_v2.gd` | 「树上叫 `game/bench/Invariants.gd`。**全仓库 `Invariants_v2` 只有一处命中，就是 docs/64 这一行本身**」 | **HIT** | ls+grep |
| M05 | L3 | `attitudes` → `attitudes_ex` | 「字段叫 `attitudes`……`Grep "attitudes_ex"` 全仓 → **1 处命中，就是 docs/64:13**」 | **HIT** | grep |
| M06 | L3 | `_faction_term` → `_ex` | 「函数是 `_faction_term`（`Sim.gd:3982`）……全仓 15 处命中，全部是 `_faction_term`」 | **HIT** | grep |
| M07 | L5 | 「会移动金标 ⇒ 第 4 步天然守着它，无需自造负对照」 | 「**逻辑是反的**……**一份被重烘过的金标，按构造不可能再抓住导致它被重烘的那个改动**。所以负对照因此**更**必需」 | **HIT** | read-contract |
| M08 | L5 | 「`BackendGate` 会覆盖模型路上的这类回归」 | 「**写下时：不会。今天：会，但那是 Q1 自己做出来的**」——引 docs/41 §2「不守别的」+ `BackendGate.gd:134`、`Invariants.gd:492-498` 的 `★Q1` 注释 | **HIT** | read-contract+read |
| M09 | L6 | 「`backend=null` 下抓到模型路径回归的一次实测」 | 「**结构上不可能**：`Sim.gd:1377` 是模型路唯一入口 `if backend != null …` ⇒ `decide()` 一次都不进」 | **HIT** | read-contract+grep |
| M10 | L6 | 「用 docker 录屏证明插值生效」 | 「**与 Wave Q 毫无关系**……且**它本身早就被判死**：`docs/43:165` 逐字『不可达成』，容器 5.3-10.8 fps < 12.5 ticks/s」 | **HIT** | read-contract |

**A2 = 10/10 HIT。** 逐层：L1 2/2 · L2 2/2 · L3 2/2 · L5 2/2 · L6 2/2 · **L4 n=0（这份 brief 上没有锚点——唯一的候选 `§0.5` 被小节号过滤挡掉了，见 docs/77 §二·2）**。

**证据类：一次 `rerun` 都没有。** 全部靠 `ls` / `grep` / `sed` / 读 `docs/41`。
⇒ 与我 §〇③ 那张成本表一致：**这些注入没有一条真的需要重跑网格。**

## 它另外抓到的（**非注入**，野生）

⑦「docs/41 红线**四条**」实为**五条**（少数的那条正好是 **#5 复用优先**——而本波两根棒都要新建探针）；
⑧「N∈{12,20,60}×3 seeds **全格**」实际只有 **6 格**，且 `docs/63 §八` 自己写明了 seed 数很小；
⑨ §二标着"**P1 的原话**"的引文是**压缩重写**，把主语从"引擎枚举的候选表"挪成"**递给模型的**候选表"
——**而挪掉的正好是让结论成立与否的限定词**（`build_prompt` 实测从不渲染 `subject`）；
⑫「P1 只在 N=12 上拿到干净时序」已被 Q1 的 5N×5seed 网格证伪；
⑬ 验收 3 把 `#16`/`#20` 归因给派系候选支——P1 一个字都没提这两条，真正相关的是 `#26`。

**五条结构缺陷**：Q1 的 owns 拿不到它必须重烘的 `game/bench/golden_digests.json`（整块划给了 Q2）
⇒ **按 brief 的行照做，验收第 4 条交不出来**；Q1 没有 docs 的行却被要求写文档；
「取当时可用的下一个编号」在并行下必然撞号——**而且真撞了**（`docs/66:11` 自陈两棒都取了 65）。

**假警报 FA = 0**（我逐条看过，没有一条是把 brief 里本来对的话判成错的）。
⚠ 这个 FA 是**我**判的，而我是同一方。

## 它自己写的"没核"

没跑完整 `ci.sh`（引的是 Q1 提交的 `analysis/q1/ci_final3.txt` 末行）；**一个变异体都没跑**（只读棒）
⇒ 所有"这道门会不会红"都是**读代码 + 读已提交原始输出**，不是自己跑红的；
`git log -S` 一次都没跑（快照无历史）⇒ 凡涉及"改动之前是什么样"的判断，依据是**代码里的自陈注释**。
