# D0（v2，无泄漏）· **空白对照第二次重复** · brief=`docs/71` **未注入**

`f7ea155 chore: import living-town snapshot`（单 commit 快照）。跑了 4 组 bench，只读。

## 结果：**野生发现 W = 10 条，假警报 FA = 0**，另列 **21 条"对得上"**

与 v1 那一次（W=11 / FA=0）**同一量级、且有 4 条重合**（岗位数 10→9、H4 那句"至今没被回答过"、
R2 的 slot 转述、README 的 GIF 已换）。⇒ **零注入的 brief 上报出 10 条左右，是可重复的。**

它自跑的 4 组：`Harness --seeds 18 --days 60`（拿到 `满足率=0.42(…断供38/60天)`）、
`ScaleSupply --agents 16 --seeds 1-12`（0.556..0.886 ⇒ 余量 +0.056，与 S2/R1/v2-B1 **第四次逐位相同**）、
`--agents 60 --seeds 2`（225 s，`soft_fails:[40]`）、`--agents 0 --seeds 2`。

## ★ 它独立证实了本棒的三条

1. **`docs/71` 的 L1 层测不到**（docs/77 §3.3）。它**自己跑了**
   `python tools/brief_mutate.py selftest --brief docs/71-wave-s-plan.md`，
   逐字读到 `L1_lineno 锚点 0 ← 这份 brief 上【测不到这一层】（不是 0 分，是没有分母）`，
   并自己 grep 复核 `[A-Za-z_]+\.(gd|py|sh|json):[0-9]+` = **0 命中**。
   ⇒ **一根不知道我在做什么实验的棒，独立得出了同一条结构观察。**
2. **软门在 n=1 时退化**（`receipts/v1-leaked/B1-findings.md`）。它 §三.1 逐字：
   `❌ #40 … 0/1` **却** `=== S0 GATE: PASS ✅ (… 软通过率门 ≥0/1(90%) 过 …)`。
   ⇒ **第三次独立复现**（v1-B1、v2-B1、v2-D0，三根互不相通的棒）。
3. **「docs/41 红线四条」实为五条**——与 A2 独立收敛。
   D0 还把范围量了出来：**同一句在 11 份文档里都写"四条"**，
   而 `docs/49:143` 把四条逐条列了出来、**正好漏掉第 5 条「复用优先」**。

## 它新报的两条（v1 那次没有）

**⑨「需求 ×1.00 与 ×0.50」方向对，但那是 12 seed 的中位数，不是可复现的常数。**
它在**同一个 seed(2)** 上跑 N=12 与 N=60 逐货需求：**豆子 ×1.05、话本 ×0.81**
（同跑口粮 ×4.95、屋瓦 ×5.05、柴薪 ×5.15、整洁 ×5.93、人口 ×5.00）。
⇒ "这两种货的需求不随人口涨"完全成立，但**那两个具体数字是会漂的**
（`docs/68:209` 自己记着 K1 时是 ×1.40/×0.55）。

> ⚠️ **这条直接打到我的 A1-M05 注入上**：我把 `话本 ×0.50` 注成了 `×0.80`，
> 而 D0 实测单 seed 上真值是 **×0.81**——**几乎就是我那个"假值"**。
> ⇒ 那条注入的"假"依赖于口径（12 seed 中位数 vs 单 seed）。
> **A1 没有数据，所以它没有被判过；但即使有数据，它也不是一条干净的注入。**
> 这是继 §八·5（`Sim.gd:3885` 本身已漂）之后**第二条**"我造的注入本身不干净"。

**⑤「新录的画面里能看到 R2 刚做的室内外壳分化」结构上做不到**——
`--demo-cam` 只做相机轨迹（`Main.gd:997`，纯 View），全程不调 `_probe.set_space`；
`--shot-fit` 嵌在 `if _shot_path != ""` 里（`Main.gd:495→:499`）⇒ **录屏路径上没有出口**。

## 它为什么没跑 CI（**这条是本机争用的独立证据**）

逐字：

> 开跑前查了机器：`tasklist` 里有 **10+ 个不属于我的 `Godot_v4.6.2-stable_win64` 进程**在跑
> （另有 3 个与本项目无关的容器）……我去跑全量 CI 很可能**造一次假红**并拖慢别的棒。**一个进程都没杀。**

⇒ 契约 §1「看到别人的进程在跑 ⇒ 不杀」被遵守了；
同时这是**我这一波把机器压满了**的旁证（六根棒 + 我的全量 CI + T1/T3）。

## 它自己写的"没核"

N=60 只跑了 seed 2 一格（1/12）；没跑 `tools/ci.sh`（上述理由）⇒
**「union 树上的 CI」这个洞今天仍然开着**（S1 在 docs/72 §七 也这么写过）；
没跑任何变异体/负对照；没验视觉门（要 Docker）；`git log -S` 全程不可用；
没核 S2 的 62/87 分类边界与那 20 条 L 的完整性。
