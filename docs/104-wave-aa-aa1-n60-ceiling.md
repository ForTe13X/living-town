# 104 · Wave AA · AA1 回执——**承诺的天花板量出来了：硬不变量到 N=60 全绿，而供给侧在 N=60 上是三格里最差的**

> **★ 一句话交付**（全部数字在**我自己这棵树**上重跑，`5b8246a`；**360 局 × 60 天 = 12,000 agent-局**）：
>
> **1. 硬不变量：N ∈ {12,16,24,40,48,60} 六格 × 各 60 seed，硬红 0/60 —— 六格全绿，一颗都没有。**
> 全部 12,000 agent-局里 **`#01` 的 need 触底次数 = 0**（不是"少"，是 0）。
> **⚠ 而这正是 brief 里最需要更正的一条**：brief 写「N=40 那一格当时是硬不变量红的」——
> **"当时"这个限定词承担了整句话的重量**。今天这棵树上同一条命令是
> `Harness --agents 40 --seeds 1-12` ⇒ **`S0 GATE: PASS ✅`**，而 W2 在它那棵树上是 `FAIL ❌`（§〇①）。
>
> **2. 曲线的形状：连续余量在 N≥24 之后【走平】，不是继续掉。这是本棒最该被记住的一条。**
> `social` 全局地板的**最小值**：13.80 → 12.20 → **10.90 → 10.90 → 11.15 → 10.95**
> ——**N=24 到 N=60 这四格是一条平台，不是一条下坡。**
> 对照 X1 在**它那棵树**上量到的同一个量（12 seed）：22.35 → 11.40 → 9.85 → **0.00**
> ⇒ **X1 那条"随 N 单调变坏、到 40 触零"的曲线，在 Y1 出货之后变成了一条在 ~11 处止住的平台。**
> ⚠ **两棵树不可比**（§〇③），所以这句话的正确形式是"**今天这棵树上它是平的**"，
> 不是"Y1 把那条曲线扳平了"——后者需要在**同一棵树**上做 ablation，**本棒没做**（§六）。
>
> **3. 软 `#40`（供给）：随 N 变坏，而且【破点不是一个 N，是一条一直在走的曲线】。**
> 红数 1/60 → 3/60 → 6/60 → 6/60 → **4/60** → **10/60**（**非单调**：N=48 比 N=40 好）。
> **口粮**满足率的左尾逐格加深：**0.435 → 0.403 → 0.365**；12-seed 滑窗破门率 **0% → 4% → 29% → 33% → 29% → 69%**。
> ⚠ **而 CI 那一窗（seeds 1-12）在 40/48/60 三格【全部是过】** —— 三条 `Harness` 判决行都在 §三.1。
>
> **4. 红线 #3 该怎么读（建议，不是决定）**：**"到 60 为止在这些条件下是绿的"这句话可以写，但主语只能是【硬不变量】**，
> 而且必须带全条件（§五列了九条）。**软 `#40` 那一半在 N=60 上不绿**：10/60 个 seed 的口粮长期供不应求，
> 而 [docs/41 §0.5](41-baton-contract.md) 的能力矩阵对 N=60 写的是「**全部货物闭环 · 必须（口径改为宏观）· 供给必须跟得上人口**」
> ——**那一行今天在 N=60 上有 17% 的 seed 不成立**。⚠ **改契约是用户的决定，本棒只给数。**
>
> **★ 顺带那件（Z1 明写的盲区）：`k ∈ (1.5, 1.852)` 不是洞——金标从 k=1.1 起就拦得住，八档全破。
> 但那条覆盖【靠一个 seed】。** 八档（1.1/1.25/1.5/1.55/1.6/1.7/1.8/1.85）**每一档移动的都只有 seed 8**。
> 我把检出率量了出来：**留出段 seeds 13-30 上是 4/18 = 22%** ⇒
> **金标若另抽 12 个 seed，一个探测器都抽不到的概率 ≈ 0.778¹² ≈ 4.9%。**
> ⇒ **"金标覆盖了那一段"是真的，"那条覆盖稳"是假的。**（§四）
>
> **卡在哪、下一个该测什么，写在 §六。**

> 派单：[docs/103 §一](103-wave-aa-plan.md)（AA1）。契约 [docs/41](41-baton-contract.md) 全文（尤其 §0.5 / §2.5 / §5）。
> 直接前置 [docs/92](92-wave-x-x1-n40-hard-red.md)（X1）、[docs/96](96-wave-y-y1-obj-survival-pull.md)（Y1）、
> [docs/89](89-wave-w-w2-n40-red.md)（W2）、[docs/85](85-wave-v-v2-n24-nonmonotonic.md)（V2）、
> [docs/100](100-wave-z-z1-two-unguarded-narrowings.md)（Z1）。
> 分支 `worktree-agent-a6ef1fbbf5ee41a25`，worktree 开工时落后 **277 个 commit**
> （`38ba4a7`，`git merge-base --is-ancestor` 确认是祖先后 ff 到 **`5b8246a`**）。
> 全部数字：`backend=null` · 无 LOD · 60 天 · `--script` 地板，**逐 seed，不给均值**。
> **本棒是只读测量：收工的树上 `game/**` 一个字节没改。**

## 〇、这份 brief 哪里是错的（[docs/41 §4](41-baton-contract.md)）

### ① **「N=40 那一格当时是硬不变量红的」——"当时"两个字承担了整句话的重量，而 brief 后面几句把它丢了**

brief 逐字写着：

> 这棵树上被真正跑过的最大 N 是 40 —— 而 40 那一格当时是**硬不变量红的**

**前半句对，后半句在今天这棵树上已经翻过去了。** 同一条命令、同一个 seed 段：

| 树 | 命令 | 判决行 |
|---|---|---|
| W2 的 `a7d9823` | `Harness --seeds 1-12 --days 60 --det 1 --agents 40` | `S0 GATE: FAIL ❌ (硬不变量 seed 11/12 全绿, 软通过率门 破…)` |
| **今天 `5b8246a`** | **同上** | **`S0 GATE: PASS ✅ (硬不变量 seed 12/12 全绿, 软通过率门 过, 活性 过, 金标 N/A·未传--golden, det 1/1)`** |

而且不是"这一窗恰好过了"：**N=40 的 60 个 seed 全扫，硬红 0/60**（W2 那棵树上是 2/60，seed 8 与 56）。

⇒ **brief 用来立本波第一优先的那个前提（"承诺 60 而 40 就已经红了"），今天只剩前半句。**
**这不改变本波值不值得做**——N=48/60 确实零数据，而现在有了；
但它**确实改变了结论的形状**：本棒交的不是"红从 40 蔓延到 60"，是"**硬那一半到 60 都没红，软那一半在 60 上最差**"。

### ② **「S1 已证明 seeds 1-12 是全部 60 个里方差最低的一段」——转述把【量】【统计量】【N】三样全丢了，而丢掉之后它在我量的东西上是【假】的**

S1 的原话（[docs/72 §1.4](72-wave-s-s1-demand-reach-and-base-rate.md)）是：

> 选阈值那一组恰好是全 60 个 seed 里表现最集中的一段（展布 0.592..0.797，**宽度 0.205**；
> 13-30 是 0.419..0.887 宽 0.468、31-60 是 0.369..0.855 宽 0.486）

⇒ **它说的是①【最差货满足率】这个量、②【展布宽度】这个统计量、③在【N=12】上。**
brief 把它压成"那一格是全部 60 个里**方差**最低的一段"，**三个限定词一个都没留**。

**在本棒量的那个量（`social` 全局地板）上，把 60 个 seed 切成 5 个不重叠的 12-seed 块、逐块算标准差：**

| N | s01-12 | s13-24 | s25-36 | s37-48 | s49-60 |
|---|---|---|---|---|---|
| 12 | **6.01（全场最高）** | 4.46 | 3.71 | **2.39** | 3.82 |
| 16 | 3.42 | 3.95 | 3.75 | **3.34** | 3.91 |
| 24 | 4.23 | 3.43 | 3.65 | **3.24** | 3.30 |
| 40 | 3.38 | 3.75 | 2.43 | 3.88 | **2.35** |
| 48 | 3.02 | 3.26 | 4.37 | **2.76** | 3.32 |
| 60 | 3.59 | 2.90 | 2.66 | 2.63 | **2.27** |

⇒ **s01-12 在六格里【一次都不是】最低的那一块，在 N=12 上还是最高的那一块。**

**而 S1 自己那个量在大 N 上是什么样，我也量了**（`最差货满足率` 的展布宽度，本棒的 `ScaleSupply` 数据）：

| N | s01-12 | s13-24 | s25-36 | s37-48 | s49-60 |
|---|---|---|---|---|---|
| 40 | 0.310 | 0.375 | 0.376 | **0.249** | 0.362 |
| 48 | 0.296 | 0.404 | 0.435 | 0.288 | **0.258** |
| **60** | **0.206（最窄）** | 0.362 | 0.484 | 0.502 | 0.474 |

⇒ **S1 的形状在 N=60 上逐字复现（s01-12 最窄），在 N=40/48 上不复现。**

> **★ 而真正该被记住的不是方差，是【块的最小值】** —— 这才是"CI 那一窗会不会红"实际取决于的东西。
> `最差货满足率` 的**逐块最小值**：
>
> | N | s01-12 | s13-24 | s25-36 | s37-48 | s49-60 |
> |---|---|---|---|---|---|
> | 40 | **0.58** | 0.45 | 0.44 | 0.55 | 0.45 |
> | 48 | **0.58** | 0.43 | 0.40 | 0.57 | 0.60 |
> | 60 | **0.56** | 0.46 | 0.36 | 0.37 | 0.42 |
>
> **三格里 s01-12 的最小值都是（并列）最高的，而且是唯一一个三格都不跌破 0.5 的块。**
> ⇒ **brief 那句"别只跑 seeds 1-12"的【方向】是对的，只是理由不是方差，是"这一段从来不含左尾"。**
> 本棒的实测形态：三格的 CI 那一窗**全部是过**，而全格滑窗破门率是 **33% / 29% / 69%**（§三.2）。

### ③ **可比性：brief 给的规则是对的，而我一查就发现【四棵前置树没有一棵可比】**

brief 逐字给了 Y1 那条规则：「**可比性 ⟺ 两棵树在 `game/scripts/` 与 `game/data/` 上逐字节相同，一条 `git diff` 就能查**」。
照做了（落盘在 `analysis/aa1/cmp_*.txt`）：

| 前置 | commit | `git diff --name-only <c> HEAD -- game/scripts/ game/data/` | 可比？ |
|---|---|---|---|
| W2 | `a7d9823` | 7 个文件（含 `Sim.gd` +68 行、`production.json`、`utility.json`） | **否** |
| X1 | `0667018` | 6 个文件 | **否** |
| Y1 | `c9e6ee6` | 6 个文件（其中 `Sim.gd` 的非注释差异**恰好就是 Y1 自己那条 `_survival_pull`**） | **否** |
| Z1 | `33a27f5` | **1 个文件**：`game/data/production.json`（Z2 把 `craft_credit` 从一门铺到四门） | **否** |

⇒ **本文没有一个数字是跨树引用的。** 六格 × 60 seed 全部在 `5b8246a` 上新跑。
⚠ **Z1 那一行值得单独看一眼**：它与我只差**一个数据文件**，而那个文件是仿真输入
⇒ **"只差一个文件"和"可比"之间没有关系，那条规则是【逐字节】不是【差得少】。**

### ④ 一条**工具层**的坑：`tools/n_sweep.py` 从来没被 `C:/Users/yp/.local/bin/godot` 跑通过

`plan.json` 里照抄契约 §1 的 `GODOT=C:/Users/yp/.local/bin/godot` ⇒ 整个矩阵**当场全挂**：

```
OSError: [WinError 193] %1 不是有效的 Win32 应用程序。
```

那个路径是一个 **sh 包装**（`exec ".../Godot_v4.6.2-stable_win64_console.exe" "$@"`）：
bash 跑得动，而 `n_sweep` 走 `subprocess.call(..., shell=False)` ⇒ `CreateProcess` 不认。
**要给真的 `.exe`。** 已写进 `analysis/aa1/plan_*.json` 的 `_godot_why` 字段。
⚠ **而它差点静默过去**：我的命令是 `python … > out.txt 2>&1; echo "SWEEP_RC=$?"`，
第一版写成了 `…; echo …; tail …` ⇒ **bash 的 rc 是最后那个 `tail` 的**，
和契约点名的 `tee | tail` 是同一个病的另一个发作。后来全部改成**先写文件再读**。

## 一、量具（`tools/**` 是本棒的 owns；**复用优先，没有重写判据**）

| 文件 | 动作 | 为什么 |
|---|---|---|
| `tools/n_sweep.py` | **扩展**（+`bench` 键） | 本棒要两份记录：`x1_margin`（need 地板 / 锁段 / 硬软 / digest）与 `ScaleSupply`（逐货满足率）。bench 名写死在调度器里 ⇒ 要么重写一个调度器（**丢掉这里的逐文件断言**），要么手敲 72 条命令（那条断言就总会被跳过）。 |
| `tools/n_margin.py` | **新增** | `x1_margin` 的 jsonl 汇总器。**不是** `n_curve` 的开关：两份记录连字段都不重叠（一份有 `final.goods`，一份有 `floors`/`max_soclock_run`），硬塞进 `n_curve.verdict()` 只能靠 `if "final" in rec` 分叉，那会让**判据长出第二份实现**。 |
| `tools/n_curve.py` / `tools/n_window.py` | **一个字节没改** | 供给侧的判据、常量、滑窗形状全部走它们，**复用它们该复用的那一半**。 |

**`n_margin.py` 现读的常量**（本文件里没有任何判据字面量）：
`SOFT_RATE=0.9 ← game/bench/Harness.gd:37`、`SURVIVAL_GATE=36.0 ← game/scripts/Sim.gd:50`、
`W=12 ← tools/ci.sh:434`。

> ⚠ **它比 `n_curve` 多做的那一件事，是 W2 用一颗漏掉的红换来的**（[docs/89 §〇④](89-wave-w-w2-n40-red.md)）：
> `n_curve` 把硬不变量放在报告**最末尾第 224 行**，于是 V2 那颗 `neu N=40 seed 13` 的硬红
> **打印了却没被读到**。`n_margin` 把硬红放**第一节第一行**，
> **并且没有硬红时也显式打印 `硬红 0/60`** —— 「没有这一行」与「这一行是 0」在扫读时长得一模一样。

### 1.1 ★ 我自己的自检先红了两条，而**红的是我不是它**

`n_margin --selftest` 第一版我把两条期望值**写死**成"恰好 W 个窗口"：

```
❌ ② b 两颗相邻软红 ⇒ 恰好 12 个破门 (实得 11)
❌ ③ 一颗孤立【硬】红 ⇒ 12 个破门         (实得 8)
```

**两条都是我错、工具对**：相邻两颗红只被 **W−1 = 11** 个窗口同时吃下；
seed 8 靠左边界，含它的窗口被截断成 **8** 个（W2 在 [docs/89 §6.2 ③](89-wave-w-w2-n40-red.md) 写过同一个形状：
"两颗相邻的红 (9,10) ⇒ 恰好 9 个破门"，正是同样的截断）。
⇒ **改成现算期望值**（`n_windows_covering()` / `n_windows_covering_k()`），并把内点与边界**拆成两条**分别断言。

> 这与 W2 那条 `⑤c` 是同一件事的第三次发作（它的原话："**想出来的那一版读起来和真的一模一样**"）。
> ⇒ **`detects` 那一栏和 `does_not_detect` 一样，也必须是跑出来的。**

### 1.2 我给 `n_sweep` 那处扩展做了负对照（**"打印了却不阻止"不算自检**）

`n_sweep` 唯一那条"每个输出文件只有一段汇总"的断言，锚是 bench 打的抬头字符串。
**共用一个字符串 = 那条断言在别的 bench 上恒真**。所以抬头随 bench 走（`BENCHES` 表），并当场跑了三格：

```
A 合法(x1_margin)      -> OK 无问题
B 错 bench(scalesupply)-> ["…a.txt: 抬头 '=== ScaleSupply · ' 出现 0 次（应为 1）…"]   ← 必须报错
C 抬头两段            -> ["…a.txt: 抬头 '=== X1 margin · ' 出现 2 次（应为 1）…"]     ← 必须报错
```

⇒ **它还能拦得住**。360 局跑完之后两次汇总都印了
`✅ 逐文件断言全过（seed 集合 / 无重复 / n_agents / days / 单段抬头 / 无 MISMATCH）`。

### 1.3 两把尺子互为 oracle（**不是"又量了一遍得到别的数"**）

| 对照 | 结果 |
|---|---|
| `x1_margin` 的 `Inv.digest` vs `Harness` 的 `[S0] digest`（N=40/48/60 × seeds 1-12） | **36/36 逐位相同** |
| 同上 `hard_fails` / `soft_fails` | **36/36 / 36/36 逐条相同** |
| `x1_margin` 的 `soft_fails` vs `ScaleSupply` 的 `inv40_ok`（**两次独立的 180 局扫描**）——`#40` 红 seed 的**身份** | **三格逐个相同**（§三.3 那 20 个 seed 编号） |
| `n_curve` 判据重算 vs 引擎判决 | **不一致 0 条**（180 局） |

## 二、曲线：**连续余量在 N≥24 之后走平**（派单问题 2）

**六格，各 60 seed × 60 天，同一棵树、同一条命令，只换 `--agents`**
（`analysis/aa1/raw_margin_small/` + `analysis/aa1/raw_margin/`，汇总 `analysis/aa1/margin_report_all.txt`）：

| N | **硬 `#01`** | 软红 | **`social` 全局地板** min..max（中位） | **最长「social<36」段** min..max（中位） | `hunger` 地板 min（中位） |
|---|---|---|---|---|---|
| 12 | **0/60** | 1/60 | 13.80 .. 36.55（**26.05**） | 0 .. 173（72） | 7.44（15.56） |
| 16 | **0/60** | 3/60 | 12.20 .. 30.20（**20.72**） | 41 .. 170（110） | 6.44（13.60） |
| 24 | **0/60** | 6/60 | **10.90** .. 29.25（**19.60**） | 49 .. 200（119） | 3.24（11.26） |
| 40 | **0/60** | 6/60 | **10.90** .. 27.15（**18.70**） | 64 .. 180（126） | 3.24（10.30） |
| 48 | **0/60** | 4/60 | **11.15** .. 26.15（**18.55**） | 70 .. 179（128） | 6.20（10.60） |
| **60** | **0/60** | **10/60** | **10.95** .. 24.50（**18.07**） | 82 .. **196**（131） | 3.80（**9.96**） |

**四件必须一起读的：**

1. **★ 地板的【最小值】在 N=24 之后就不动了**：10.90 / 10.90 / 11.15 / 10.95 —— 四格之间的差
   （0.25）**小于同一格内相邻两个 seed 的典型间距**。⇒ **这是一条平台，不是一条下坡。**
2. **中位数还在掉，但斜率在收敛**：−5.33 / −1.12 / −0.90 / −0.15 / −0.48（每格）。
   **⇒ "随 N 变坏"这句话今天只对【中位】成立，对【左尾】不成立**，而红是左尾的事。
3. **最长锁段的中位单调（72→110→119→126→128→131），而最大值不单调**（173→170→200→180→179→196）。
   X1 当年的注记"**变的是尾巴，不是中心**"在今天这棵树上**反过来了**：变的是中心，尾巴钉住了。
4. **`hunger` 地板的最小值三格贴在 3.2-3.8**（N=24/40/60），而 `#01` 的门是 0
   ⇒ **余量约 3.2，不是"很大"**。⚠ 这是六格里离硬不变量最近的一个量，**比 `social` 近**。

### 2.1 与 X1 那条曲线的关系——**只能并排看，不能相减**

X1 在**它那棵树**（`0667018`，Y1 出货**之前**）上量的同一个量（12 seed）：

| | N=12 | N=16 | N=24 | N=40 |
|---|---|---|---|---|
| X1 那棵树 `social` 地板 **min** | 22.35 | 11.40 | 9.85 | **0.00** |
| **今天这棵树**（60 seed） | 13.80 | 12.20 | 10.90 | **10.90** |

⇒ **形状不同：X1 那条在 N=40 触零，今天这条在 ~11 止住。**
⚠ **三条边界必须一起说**：
①**两棵树不可比**（§〇③ 那条 `git diff` 非空）；②**seed 数不同**（12 vs 60）——
X1 的 N=12 那格 min 22.35 是 12 个 seed 的，我的 60 个 seed 里 min 13.80，
**这一格的差多半来自 seed 数而不是树**；③**本棒没有做 ablation**
（把 `obj_survival_pull` 删掉再跑同一个六格网格）⇒ **"是 Y1 那条修法把它扳平的"这句话本棒【证不了】**，
只能说"今天这棵树上它是平的"。**分离办法是现成的、便宜的，写在 §六.2 第 1 条。**

## 三、N=48 与 N=60 今天是什么样（派单问题 1）——**硬软分开报**

### 3.1 门自己的判决行（`Harness`，三格 × seeds 1-12 × 60 天）

```
N=40  === S0 GATE: PASS ✅  (硬不变量 seed 12/12 全绿, 软通过率门 ≥11/12(90%) 过, 活性 过, 金标 N/A·未传--golden, det 1/1) ===
N=48  === S0 GATE: PASS ✅  (硬不变量 seed 12/12 全绿, 软通过率门 ≥11/12(90%) 过, 活性 过, 金标 N/A·未传--golden, det 1/1) ===
N=60  === S0 GATE: PASS ✅  (硬不变量 seed 12/12 全绿, 软通过率门 ≥11/12(90%) 过, 活性 过, 金标 N/A·未传--golden, det 1/1) ===
```

⚠ **那句 `金标 N/A·未传--golden` 就是 Z1 上一波修好的那一格**，而它在这里**恰好是必需的**：
不传 `--golden` 是对的（`golden_digests.json` 烘在 N=12 上），
**而改前它会印成绿色的「金标 过」** ⇒ 本棒这三行会各带一句什么都没判的绿话。**Z1 那条修法在本棒身上第一次真的派上用场。**

### 3.2 软 `#40`：逐 seed、分臂、分货（`ScaleSupply` + `n_window`，各 60 seed）

| | **N=40** | **N=48** | **N=60** |
|---|---|---|---|
| `#40` 红 / 60 | **6** | **4** | **10** |
| 下限臂↓ / 上限臂↑ | **6 / 0** | 3 / 1 | **9 / 1** |
| 红的 seed | 23↓ 28↓ 33↓ 56↓ 57↓ 58↓ | **5↑** 15↓ 23↓ 26↓ | **5↑** 15↓ 17↓ 20↓ 24↓ 27↓ 35↓ 38↓ 56↓ 59↓ |
| **口粮 满足率** min .. max（中位） | 0.435 .. 0.958（0.718） | 0.403 .. 0.941（0.716） | **0.365** .. 0.924（**0.676**） |
| 最差货满足率 min（seed） | 0.435（28） | 0.403（26） | **0.365（27）** |
| 逐货【跌破下限】的 seed 数 | **口粮 6/60**（只有它） | **口粮 3/60**（只有它） | **口粮 9/60**（只有它） |
| 49 个 12-seed 滑窗里破门的 | **16（33%）** | **14（29%）** | **34（69%）** |
| **CI 那一窗（seeds 1-12）** | 红 0 ⇒ **过 ✅** | 红 1 ⇒ **过 ✅** | 红 1 ⇒ **过 ✅** |
| **硬不变量红 / 60** | **0** | **0** | **0** |
| 单局墙钟 s（min/中位/max，`x1_margin`） | 67 / **82** / 91 | 94 / **121** / 142 | 133 / **200** / 227 |

**三条必须一起读的：**

1. **下限臂那一族到 N=60 仍然只有一种货：`口粮`。** 三格 180 局、18 颗下限臂红，
   **18 颗的口粮都在地板之下，而且没有任何一局是"别的货跌破而口粮没跌"**
   ——W2 在 N∈{16,24,40} 上得到过同一句话（[docs/89 §2.1](89-wave-w-w2-n40-red.md)），**本棒把它推到了 48 与 60。**
2. **上限臂在 N=48/60 上活着，而且是【同一个 seed 5】。** W2 引 V2 那句「上限臂只可能在 N ≤ 32 响」已被 W2 在 N=40 上证伪；
   本棒进一步：**N=48 与 N=60 各响一次，两次都是 seed 5**（N=48 是 屋瓦/柴薪/话本/豆子，N=60 是 屋瓦/整洁/话本/豆子）。
   ⚠ 而 **N=40 这一格上限臂是 0/60** ⇒ **它不是"随 N 单调复活"，`话本+豆子` 那个结构底座
   （60/60 与 59-60/60 全年零缺货）之上，第三第四种货是谁完全由 seed 决定。**
3. **★ 红数非单调（6 → 4 → 10），而【左尾】是单调的**（0.435 → 0.403 → 0.365）。
   ⇒ **拿"红几个"当曲线会读出一个假的转折点。** 这是 V2 那条"N=24 非单调"的第二次发作，
   而本棒给的分辨办法与 X1/Y1 是同一条：**看连续量，不看红绿计数。**

### 3.3 逐 seed 表（**给展布不给均值**；全表在 `analysis/aa1/margin_report_all.txt` 与 `per_seed_compact.txt`）

**`social` 全局地板 · N=60 · seeds 1-60**

```
s01-20  17.50 15.65 22.75 18.15 10.95 16.05 14.90 20.65 18.60 24.50 21.75 17.80 20.05 12.65 17.70 11.05 17.90 17.55 18.40 15.70
s21-40  19.55 13.15 19.85 18.75 16.75 18.35 18.40 18.00 17.90 24.50 23.75 15.30 20.70 18.55 17.70 16.60 18.75 16.85 22.60 15.15
s41-60  21.60 19.75 17.85 15.85 18.70 18.65 13.65 14.60 18.45 17.30 21.60 19.85 17.25 16.75 22.65 18.85 15.65 14.55 19.05 20.20
```

**最长「social < 36」连续段 · N=60 · seeds 1-60**

```
s01-20    132 146  95 129 180 150 150 110 125  82 147 131 115 167 132 179 130 133 127 146
s21-40    117 164 116 124 138 127 196 130 130  82  87 148 117 145 132 139 124 137  96 150
s41-60    104 117 131 166 124 124 161 153 127 134 104 116 135 138 103 135 146 149 122 114
```

**硬不变量：三格 × 60 seed 的 `hard_fails` 全部为 `[]`**，
**`by_need` 全部为 `{}`** ⇒ **12,000 agent-局里 need 触底 0 次**（`starved` 恒 0，不是"小"）。
**软不变量**：见 §三.2 的红 seed 列；`#40` 之外**只有 `#26`（S3 同派系亲和）在 N=16 红 2 个、N=24 红 1 个，
而 N=40/48/60 三格 0 个** —— 又一条非单调。

## 四、Z1 的盲区：`k ∈ (1.5, 1.852)`（派单"顺带一件"）

Z1 [docs/100 §六.2](100-wave-z-z1-two-unguarded-narrowings.md) 点名："在 k=1.6 / 1.7 上跑
`Harness --seeds 1-12 --days 60 --golden`，看金标那一侧拦不拦得住。**两条判据的交叠区就是那一段，
而今天没人知道它是空的还是满的。**"

**九档，全在【仓库之外】的隔离副本里跑**（`filecmp` 逐目录核过：**每份只差 `data/utility.json` 一个文件**）：

| 臂 | k | `36k / 66.667` | **`#42`** | **金标** | 三元组移动的 seed |
|---|---|---|---|---|---|
| `kbase`（出货） | **1.0** | 0.5400 | ✅ 绿 | **过** | **无** |
| `k1_10` | 1.1 | 0.5940 | ✅ 绿 | **破** | **[8]** |
| `k1_25` | 1.25 | 0.6750 | ✅ 绿 | **破** | **[8]** |
| `k1_50` | **1.5** | 0.8100 | ✅ 绿 | **破** | **[8]** |
| `k1_55` | 1.55 | 0.8370 | ✅ 绿 | **破** | **[8]** |
| `k1_60` | 1.6 | 0.8640 | ✅ 绿 | **破** | **[8]** |
| `k1_70` | 1.7 | 0.9180 | ✅ 绿 | **破** | **[8]** |
| `k1_80` | 1.8 | 0.9720 | ✅ 绿 | **破** | **[8]** |
| `k1_85` | 1.85 | 0.9990 | ✅ 绿 | **破** | **[8]** |

**答案：那一段【不是】洞 —— 金标从 k=1.1 起就拦得住，比 Z1 问的那一段还宽。**
`#42` 在九档上全绿，与它的设计一致（比值 < 1.0）⇒ **两条判据的交叠区是【满】的，
而承重的那一条是金标，不是 `#42`。**

### 4.1 ★ 但那条覆盖**靠一个 seed**，我把它的脆性量成了一个数

**九档里每一档移动的都只有 `seed 8` 一个。** ⇒ 金标对 k 的覆盖 = "12 个 seed 里恰好抽中了一个探测器"。
**检出率是可以量的**，我量了（同一批留出段 seeds 13-30，两侧同一条命令，k=1.0 vs k=1.5）：

```
改前 === S0 GATE: PASS ✅ (硬不变量 seed 18/18 全绿, 软通过率门 过, 活性 过, 金标 N/A·未传--golden, det 1/1) ===
改后 === S0 GATE: PASS ✅ (硬不变量 seed 18/18 全绿, 软通过率门 过, 活性 过, 金标 N/A·未传--golden, det 1/1) ===
三元组移动 4/18：seed 16 · 18 · 20 · 27      ⇒ 检出率 22%
```

⇒ **金标若另抽 12 个 seed，一个探测器都抽不到的概率 ≈ `0.778¹² ≈ 4.9%`。**

> **★ 这是 Y1 那条更正的【镜像】。** Y1 说："**「金标 12/12 不动」不等于「N=12 不动」**"
> ——它是"不动是抽样"。本棒补上另一半：**"动"同样是抽样。**
> ⇒ **"金标覆盖了 k 的那一段"这句话可以写，但要带上"以约 95% 的把握"，
> 而它今天在任何文档里都是被当成确定性陈述在用的。**

⚠ **三条边界**：①这是 **9 个网格点**，不是对整个区间的证明——两点之间是否有一档金标恰好静默，**没测**；
②检出率 22% 只在 **k=1.0→1.5** 这一档上量过，别的剂量**没量**；
③`#42` 在 k≥1.852 上咬（Z1 的 M7 实测 1.86 红）⇒ **`(1.852, ∞)` 由 `#42` 守、`[1.1, 1.852)` 由金标守**，
而**两者的接缝处（k 略小于 1.852 且金标恰好静默）本棒没有构造出这样的 fixture。**

## 五、对红线 #3 的**建议**（[docs/103 §一](103-wave-aa-plan.md) 要的第 3 件；**改契约是用户的决定**）

**我不建议改「60」这个数**（与 X1 同）。我建议的是：**把 [docs/41 §0.5](41-baton-contract.md) 能力矩阵里
两行的状态列换掉，因为今天有数据了，而它们现在写的是【没有数据时的话】。**

| 行 | 现在写的 | **建议写成**（只是建议） |
|---|---|---|
| 社交关系与涌现 | `✅ 硬不变量守着` | `✅ 实测：N ∈ {12,16,24,40,48,60} 六格 × 各 60 seed × 60 天，硬红 0/60（12,000 agent-局，need 触底 0 次）。连续余量在 N≥24 之后走平（social 地板最小值 10.90/10.90/11.15/10.95）。条件见 docs/104 §五。` |
| 全部货物闭环 | `必须（口径改为宏观）` / `供给必须跟得上人口` | `⚠ 实测：N=60 上 10/60 个 seed 的【口粮】长期供不应求（满足率 0.365..0.490，断供 34-42/60 天）；69% 的 12-seed 滑窗会破软通过率门，而 CI 跑的那一窗（seeds 1-12）是过的。N=40 是 6/60、N=48 是 4/60。` |

### 5.1 ★「到 60 为止是绿的」这句话的**全部条件**（派单点名"绿 ⇒ 列全条件"）

它**只对硬不变量成立**，并且只在下面**九条**全部成立时：

1. **`backend = null`**（零模型地板）。⚠ 方向明确的影响：`#01` 自己的包络写着
   「零模型地板上 100% 是 social；**模型路上 `hunger` 反而是主项**」——而本棒量到
   `hunger` 地板最小值只有 **3.24**，是六格里离 0 最近的量。
2. **无 LOD**（`lod_aggregate` 关）。
3. **无玩家**。
4. **`days = 60`**（一档；`SUPPLY_MIN_DAYS=60`，更短的跑法上 `#40` 的满足率臂按构造禁用）。
5. **`seeds 1-60`**（一段；seed>60 没跑）。
6. **单镇**（区域/多镇没跑）。
7. **`--script` headless 地板**，不是真机（**帧时/内存本棒一个字没量**）。
8. **`utility.obj_survival_pull = 1.0`**（Y1 的出货值）。⚠ **这一条不是背景，是承重的**：
   X1 在没有它的树上于 N=40 就量到过两颗硬红。
9. **`game/data/production.json` = Z2 合流后的版本**（四门 `craft_credit`）。

⇒ **正确的措辞是"在这九条下，硬不变量到 N=60 为止是绿的"**，
**不是**"60 人镇子是绿的"。[docs/41 §2](41-baton-contract.md) 点名的五个温和配置里，本棒只换掉了 **N** 与 **seed**。

### 5.2 破点在哪（派单要的第 2 件）——**硬那一半：给不出，并写清排除了哪些**

- **硬不变量：本棒【给不出破点】，因为六格里一个都没破。**
  **排除掉的**：N ∈ {12,16,24,40,48,60} 各 60 seed × 60 天、backend=null、无 LOD、无玩家、单镇 —— 这一整块里没有。
  **没排除的**（= 破点仍可能在这里）：**N > 60**、**seed > 60**、**天数 ≠ 60**、**模型路**、**LOD**、**有玩家**、**真机**。
  ⚠ 而 §二 给了一条**便宜的方向**：`hunger` 地板（余量 ~3.2）比 `social` 地板（余量 ~11）近得多，
  **而 `#01` 的包络说模型路上主项正是 `hunger`** ⇒ **下一个该找破点的地方是模型路，不是更大的 N。**
- **软 `#40`：破点不是一个 N，是一条一直在走的曲线。** 三个数要分开报，别混：

  | | N=12 | N=16 | N=24 | N=40 | N=48 | N=60 |
  |---|---|---|---|---|---|---|
  | **`#40` 红 / 60** | 1 | 1 | 5 | 6 | 4 | **10** |
  | 其中**下限臂（口粮）** | 未测※ | 未测※ | 未测※ | **6** | **3** | **9** |
  | **软红 seed 合计 / 60**（含 `#26`） | 1 | 3 | 6 | 6 | 4 | **10** |
  | **49 个 12-seed 滑窗里破门的** | **0（0%）** | **2（4%）** | **14（29%）** | **16（33%）** | **14（29%）** | **34（69%）** |

  ※ N=12/16/24 三格本棒只跑了 `x1_margin`（它记 `#40` 红不红，**不记分臂**）；
  `ScaleSupply`（分臂 + 逐货满足率）只跑了 N=40/48/60。**这一栏明写"未测"，不用推断填空。**

  ⇒ **拐点在滑窗破门率上最清楚：N=24 是它从"个别"变成"常态"的那一格（4%→29%），
  N=60 是它翻倍的那一格（29%→69%）。**
  ⚠ **而 `#26`（S3 同派系亲和）只在 N=16 红 2 个、N=24 红 1 个，N=40/48/60 三格 0 个**
  ——**又一条非单调**，所以"软红几个"这个数在 N≤24 上和 N≥40 上**主语都不是同一条不变量**。

## 六、卡在哪，下一个该测什么

### 6.1 三个卡点

1. **★ "曲线是平的"与"Y1 把它扳平了"是两件事，本棒只证了前一件。**
   要证后一件必须在**同一棵树**上做 ablation（删 `utility.obj_survival_pull` 这个键 ⇒ 缺键即零扰动，
   Y1 已实测过这条路），把六格网格再跑一遍。**本棒没做**，而它便宜（隔离副本 + 一个 JSON 键）。
2. **红绿计数在软 `#40` 上同样开始不可读**：6 → 4 → 10 非单调，而左尾 0.435 → 0.403 → 0.365 单调。
   **本棒没有为软臂建立"对着零假设臂判"的流程**（X1/Y1 只对硬 `#01` 建过）。
3. **`hunger` 的余量（~3.2）比 `social`（~11）近，而所有人的注意力都在 social 上。**
   X1/Y1/Z1/本棒四棒全在查 social，**而它是零模型地板的性质**（`#01` 包络原话：模型路上 hunger 才是主项）。

### 6.2 下一个该测什么（**按手上的证据排序**）

1. **`obj_survival_pull` 的 ablation × 六格网格**（§6.1①）。**这一格决定 §二那条平台该记在谁头上**，
   而今天它在本文里只能写成"今天这棵树上是平的"。
2. **`hunger` 那条余量**：六格里最小值 3.24 / 3.24 / 3.80（N=24/40/60），**而模型路上它是主项**。
   ⇒ 跑 `BackendGate` 形状（`random` 后端）× N=48/60，看 `hunger` 地板往哪边动。**本棒一格没跑。**
3. **N > 60**：[docs/41 §0.5](41-baton-contract.md) 的区域目标是 **>100 居民**，而
   §二 那条平台是"到 60 为止"的观察，**不是外推的依据**。M2 明写 N>24 一格没测过，本棒把它推到 60；
   **100+ 仍然零数据。**
4. **软 `#40` 的零假设臂**（§6.1②）：抄 `obj_dist_penalty` 0.400→0.401，看软红数在同量级扰动下抖多少。
   **没有这条，"N=48 比 N=40 好"这句话读不出来是真的还是抽签。**
5. **k 的接缝**（§四 ⚠③）：`[1.1, 1.852)` 靠金标（22% 检出率）、`[1.852, ∞)` 靠 `#42`，
   **而"金标恰好静默 + `#42` 恰好不咬"的 fixture 本棒没构造出来。**

### 6.3 测了之后决定不做的（[docs/103 §四](103-wave-aa-plan.md) 要求写进回执）

- **没有建议把 N=48 或 N=60 接进 `ci.sh` 4a。** 理由是量出来的，两条：
  ①**代价**：单局中位 82s / 121s / **200s**，而 4a 今天跑的 N=16 是 20s
  ⇒ 换成 N=60 约是 **10×**（12 seed 从 ~4 min 变成 ~40 min），而 CI 现在全程约 26 min。
  ②**牙齿**：三格的 **CI 那一窗都是过的**，而全格破门率 33%/29%/69%
  ⇒ 接哪一窗决定颜色，**这正是 [docs/41 §6](41-baton-contract.md) 点名的"给出货配置预埋一条门"的形状**。
  **本棒不替用户拍板，只把两栏摆平。**
- **没有动任何判据 / 阈值 / 常量。** `SUPPLY_FLOOR` / `SURVIVAL_GATE` / `SOFT_RATE` / 软通过率门
  **一个字节没改**；`game/**` 一个字节没改。
- **没有把 `n_margin.py` 接进 `tools/ci.sh`。** 它是量具不是门（§七）。

## 七、§2.5 探测包络

⚠ **先说清楚：本棒【没有新增也没有收紧任何一道门】。** `n_margin.py` 与 `n_sweep.py` 的扩展
**都不进 `tools/ci.sh`、不做判决、退出码不影响任何一道门**。
所以 `detects` 那一栏只能诚实地说"它对自己的输入有多严"。

```
tools/n_margin.py（x1_margin 记录的汇总器；常量与窗口形状现读源码）

detects:（--selftest，12 条断言，**期望值一律现算**，不写死）
  ① 60 seed 全绿 ⇒ 0 个窗口破门。（=「一个什么都不做的改动能不能通过它」）
  ② 一颗【孤立】软红 ⇒ 0 个破门（软门在 W=12 上容 1 个）；
     ②b 两颗【相邻】(30,31) ⇒ 恰好 11 个 = W−1。⚠ 我第一版写死成 W，**跑出来是 ❌，工具对我错**（§1.1）。
  ③ 一颗【硬】红（内点 seed 30）⇒ 12 个破门 —— **软门对它不适用**；
     ③b 靠左边界的硬红（seed 8）⇒ 8 个（被截断）。⚠ 同样是我先写错、被实测打回。
  ④ 硬红必须出现在【头条】那一节里（不是第 224 行）；
     ④b 没有硬红时也显式打印 `硬红 0/60`。
  ⑤ SOFT_RATE 0.90→0.50 ⇒ 相邻两颗软红不再破门（need_red 2→7）；
     ⑤b W 12→24 ⇒ 窗口数 60−W+1 跟着变 ⇒ **脚本里没藏冻结字面量**。
  ⑥ 极值【并列全报】：两个 seed 同为最小 ⇒ 两个都点名。
  ⑦ seed 数 < W ⇒ 窗口数 = 0，**而不是把"不足一窗"读成"没破门"**。
  ⑧ 同一个 (arm,N,seed) 从两个目录进来 ⇒ 当场退出并**点名两个文件**（跨树拼接的第一道拦网）。
  ⑨ 半行 JSON（那一片还在跑 / 被中断）⇒ 点名文件与行号退出，**不静默跳过**
     —— 少一个 seed 会把「红 1/60」悄悄变成「红 1/59」。
  ⑩ **元证据**：它对 `#40` 红 seed 身份的判决与 ScaleSupply+n_curve 在**三格 180 局上逐个相同**（§1.3）
     ⇒ 它不是"又量了一遍得到别的数"。

does_not_detect:（**跑出来的 / 明写没跑的**）
  ① ★ **它不是门。** 不进 tools/ci.sh，不改任何 seed 的红绿。
  ② **它不查数据是不是同一棵树上量的。** §〇③ 那四条 `git diff` 是我**手动**跑的，不是它跑的。
     它只在 (arm,N,seed) 撞车时报警；**跨树而 seed 不重叠的拼接，它一个字都不会说**
     ——与 W2 给 `n_window` 写的那条盲区逐字相同，**本棒没有修它**。
  ③ **它不查【为什么】红。** 只报哪一条不变量红在哪个 seed；成因要去跑 Harness / x1_starve_trace。
  ④ **它不跑金标 / 确定性 / 活性**（`x1_margin` 不跑这三样）⇒ 凡是"这一格会不会红"的结论，
     本文只覆盖**不变量**那一半。§三.1 那三行判决行是**另外跑 Harness** 得来的。
  ⑤ **`max_soclock_run` 不区分"锁段长"与"锁得深"**（沿用 X1 给 `x1_margin` 写的盲区，未修）；
     `#01` 自己那条"不区分'一个人 61 tick'与'61 个人 1 tick'"的盲区**同样没修**。
  ⑥ **N > 60 / seed > 60 / days ≠ 60 / 模型路 / LOD / 有玩家 / 真机：一格没跑。**

confidence: **N=12 条断言**（其中 8 条定向变异、2 条形状断言、2 条常量现读校验），
  **外加 2 次自打脸**（②b 与 ③ 的期望值我先写死、被实测打回，见 §1.1）
  与 **1 条 180 局 × 3 格的跨量具一致性对照**。
  全部在 backend=null · 无 LOD · 60 天上量过。
```

```
tools/n_sweep.py 的 `bench` 扩展（抬头标记随 bench 走）

detects:（三格真跑，§1.2）
  ① 合法输入（x1_margin 的 1 行记录 + 1 段抬头）⇒ 无问题。
  ② **用错的 bench 去核** ⇒ 报「抬头 '=== ScaleSupply · ' 出现 0 次」并退出
     —— 这一条就是"共用一个抬头字符串 = 那条断言恒真"的负对照。
  ③ 抬头出现两段（两次运行写进同一个文件）⇒ 报错。
  ④ 360 局跑完两次汇总都印 `✅ 逐文件断言全过`（seed 集合 / 无重复 / n_agents / days / 单段抬头 / 无 MISMATCH）。

does_not_detect:
  ① **它不是门**（同上）。
  ② **它不查 bench 记录的【内容】对不对**，只查形状（seed 集合 / n_agents / days / 抬头段数）。
  ③ **它不阻止你把两棵树的输出写进同一个 outdir** —— 那一层靠 n_margin/n_curve 的 (arm,N,seed) 撞车检查，
     **而那一层同样看不见"跨树而 seed 不重叠"**（同上 ②）。
  ④ **WinError 193 那一类"godot 路径不对"它不预检**，只会在第一片就整批抛出（§〇④ 实测）。

confidence: **N=3 格定向变异 + 2 次真实矩阵**（180 局 × 2）。
```

## 八、`bash tools/ci.sh` 的实际输出

命令：`GODOT=… bash tools/ci.sh > analysis/aa1/ci_final.txt 2>&1`
（**没有用 `tee | tail`**——它会吃掉退出码），**`CI_RC = 0`**。然后**把整份文件读一遍**：
**全文 669 行**，`✅` 命中 **283** 条，`❌ / FAIL / ✗` 命中 **2 条，而两条都不是失败**（§八.1）。

```
### 0.  ✅ no tracked weights/binaries · ✅ 红线#4 负对照                          ⏱ 11s
### 1/1b/2. ✅ lint_data · ✅ audit_map · ✅ lint_links                            ⏱ 0s/0s/1s
### 2b/2c/2d. === ART GATE PASS ✅ === · === TERRAIN GATE PASS ✅ === · === ASSET GATE PASS ✅ ===
### 2e. === recalc · 注册表 7 条（上门 5 条 / 只记录 2 条）===
        === RECALC GATE: PASS ✅（5 条数字现读现算现比）===                        ⏱ 2s
### 2f. ✅ 互补性守卫 负对照（五条都会红；变强与冗余不红）
        互补性守卫：锚烘于 2026-08-02（commit 31b43e8，7 个夹具 × 34 条不变量）
        ✅ 互补性守卫（单点依赖的夹具逐条现读现比）                                ⏱ 0s
### 3.  ✅ import/parse clean                                                     ⏱ 9s
### 4.  === S0 GATE: PASS ✅  (硬不变量 seed 12/12 全绿, 软通过率门 ≥11/12(90%) 过, 活性 过, 金标 过, det 3/3) ===        ⏱ 124s
### 4a. ✅ 4a 预检：池倍率 ×16/12 ≠ 1 · days=60 ≥ SUPPLY_MIN_DAYS=60
        === S0 GATE: PASS ✅  (硬 12/12 全绿, 软门 过, 活性 过, 金标 N/A·未传--golden, det 1/1) ===                      ⏱ 143s
### 4b. === LOD-VERIFY GATE: ✅ PASS  (V2+V3abc 全绿) ===   (N=48 days=3)          ⏱ 11s
### 4c. === DetGate: PASS ✅  (硬 16/16, 两跑一致 16/16, 数据指纹一致 16/16[118341245], 金标 16/16 可比) ===              ⏱ 84s
### 4d. === BackendGate: PASS ✅  (硬 8/8, 两跑一致 8/8, 闭集封闭 8/8, 自检臂必红 4/4, 伪造落地 4/4) ===                  ⏱ 87s
### 4e. === ModelPathGate: PASS ✅ (失败 0)   ### 4f. === VoiceGate: PASS ✅ ===   ⏱ 5s / 21s
### 5.  ✅ 9 个 unit / integration 场景（goals_test 72s · story_test 199s）        ⏱ 294s
### 6.  === DAYNIGHT GATE: PASS === · === ROUNDTRIP GATE: PASS === · === POND GATE PASS ✅ ===
        ✅ 视觉门（昼夜 / 界外层重画 / 空间往返 / 岸线 / 室内外壳 / 家具语义 / 树丛点阵）   ⏱ 157s

  ⏱  全程 951s
=== CI PASS ✅ ===        CI_RC=0
```

⚠ **顺带更正 brief 里的一个数**：brief 写「**CI 现在约 26 分钟**」，本棒实测**全程 951s ≈ 15.9 分钟**
（`ci.sh` 自己现算并打印的那一行）。Z1 上一波实测是 **1755s ≈ 29 分钟**、Y1 是 **1544s**。
⇒ **这个数在同一棵树上能差近一倍，它测的是机器忙闲，不是 CI 的规模**
——`ci.sh` 那行自己也写着「机器忙闲会让它上下浮动，别把单次读成基准」。
**所以 §6.3 那条代价论证我用的是【单局墙钟的比值】，不是 CI 的绝对秒数。**

### 8.1 那两条 `❌` 是什么（**先说清楚再说 PASS**）

两条都在 **2e 可重算门**的「**只记录、不上门**」那两行里（注册表 7 条：上门 5 条 / 只记录 2 条），
`RECALC GATE` 自己是 `PASS ✅`（上门的 5 条**全部对得上**）：

```
 99| palette-gpl-de00-6p3    否   文档 6.3   现跑 8.0659   ❌
102| lint-links-md-count     否   文档 93    现跑 121      ❌
```

⚠ **第二条里的 121 有我一份**：本棒新增了本文档，把全仓 `.md` 数从 **120 推到 121**
（实测：加本文件之前 `find . -name '*.md'` 是 120）。
[docs/100 §四.3](100-wave-z-z1-two-unguarded-narrowings.md) 记的是 117 ⇒ **Z1 之后到我之前又多了 3 份**（不是我加的）。
那条条目**刻意不上门**（注册表原话：给它上门 = 造一道会因为无关改动变红的门），所以它不是红。

### 8.2 四条诚实边界

1. **本棒【没有移动任何锚】，R12 不适用。** `game/**` 一个字节没改
   （`git status` 只有 `tools/n_sweep.py` 改、`tools/n_margin.py` 新增、`docs/104-*` 新增、`analysis/aa1/**` 新增）。
   金标一致由第 4 步的 `金标 过` 与第 4c 步的 `金标 16/16 可比` 自己证。
2. **这一跑跑完之后，我又编辑了本文件**（把本节的 CI 输出贴进来）。
   ⇒ **提交的树与 CI 跑过的树，差别只有 `docs/104-*` 的正文**，
   **没有任何代码 / 数据 / 量具 / 锚的改动**（`tools/n_margin.py` 与 `tools/n_sweep.py` 在 CI 跑的时候
   **已经在树上，是同一份字节**）。贴完之后**单独重跑了 `tools/lint_links.py` 与 `tools/recalc.py --gate`**，两条见 §八.3。
3. **`godot` 的退出码在本机不可信**（X1 §〇④ 记过，本棒复核仍然成立：`C:/Users/yp/.local/bin/godot` 是个 sh 包装）。
   本文全部红绿**按判决行判**，`CI_RC=0` 只作旁证。⚠ 而 §〇④ 那个 `WinError 193` 正是同一个包装的另一面。
4. **CI 跑的是本棒的树，不是 Wave AA 三根棒的 union**（§九 末条）。

### 8.3 事后单跑的两道（因为 §八.2② 改了 `.md`）

```
python tools/lint_links.py     rc=0
  lint_links: OK — 121 markdown files, all relative links resolve and
  all docs/NN citations exist (102 numbered docs)

python tools/recalc.py --gate  rc=0
  ── 上门的 5 条：5 条对得上，0 条对不上 ──
  === RECALC GATE: PASS ✅（5 条数字现读现算现比）===
```

⚠ `lint_links` 同时打印了一段**只打印不判红**的 `docs/NN@branch` 逃生门清单（5 条不同引用 / 29 处），
其中 **`docs/52@worktree-agent-a0c76a3f96ae5dbcf` ×11 是「分支在、文档不在」** ⇒ 那条引用今天指向一个不存在的文件。
**本棒没有碰它**（那是别人的文档），但既然读到了就记在这里 —— 它与 `palette-gpl-de00-6p3` 一样，
是一条**已知的、被刻意放行的**过期项，不是本棒引入的。

## 九、没能测到什么（**"没测"明写，不用推断填空**）

- **N > 60 一格没跑。** 本文凡是"到 60 为止"的话，真实范围就是 **N ≤ 60**。
  [docs/41 §0.5](41-baton-contract.md) 的区域目标 **>100 居民** 仍然**零数据**。
- **seed > 60 没跑**；**days ≠ 60 没跑**。
- **`backend != null` 没跑、LOD 没跑、有玩家没跑、真机没跑。** ⚠ 这一条对本文结论有
  **方向明确**的影响（§五.1 第 1 条）：`#01` 的包络写着模型路上 `hunger` 才是主项，
  而本棒量到 `hunger` 地板最小值 3.24 —— **六格里离 0 最近的就是它。**
- **帧时 / 内存没量。** N=48/60 在手机上跑不跑得动，本棒一个字都说不了
  （D7 量过 N=60 真机 83-88 FPS，但那是**渲染**，不是本棒量的仿真侧）。
- **`obj_survival_pull` 的 ablation 没跑** ⇒ §二那条平台**归因不了**（§6.1①）。
- **软 `#40` 的零假设臂没跑** ⇒ "N=48 比 N=40 好（4/60 vs 6/60）"**读不出来是真的还是抽签**。
- **没有做逐决策点的因果归因**；**没有查那 18 颗下限臂红的成因**
  （W2 在它那棵树上给过 ρ(口粮, 面点师+渔夫)≈0.87，**本棒没有在今天这棵树上复核这条相关**）。
- **k 的九档只是九个网格点**，不是对区间的证明；**检出率 22% 只在 k=1.0→1.5 一档上量过**。
- **本棒没有跑 Wave AA 三根棒的 union。** 只碰 `tools/n_sweep.py`（改）、`tools/n_margin.py`（新）、
  本文档、`analysis/aa1/**`，与 AA2（`Story.gd`/`Director.gd`）、AA3（`Sim.gd`/`game/data`/`game/bench`）
  **不重叠**；但 **union 上的那一次 CI 本棒没有跑过**，合流的人要自己再跑一遍。
  ⚠ **而这一次的合流比平常危险一格**：**AA3 动的正是 `Sim.gd` 与 `game/data/**`，
  而本文六格 360 局的数全部是在【AA3 之前】那棵树上量的** ⇒ AA3 若落地，本文逐 seed 的数**全部要重跑**
  （判据这一层的结论多半仍成立，但"哪个 seed 红"一个都不能直接抄——[docs/89 §〇之前](89-wave-w-w2-n40-red.md) 那条教训）。
