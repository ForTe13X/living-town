# 87 · Wave W 计划——**先还上一波自己造的债；再把 N=40 那颗没人见过的红查清楚；顺带做故事层**

> 前置：docs/41 全部、docs/47 **R12**、**docs/84（V1：社会痕迹 + 它自己造的回归）**、
> **docs/85（V2：逐 N 曲线，以及 N=40 seeds 1-12 今天就是 FAIL）**、docs/86（V3 外景）。
> **回执必须带 §2.5 三行包络，`does_not_detect` 必须是跑出来的。**

## 〇、这一波的排序理由

1. **V1 出货了一个它自己找不到机制的回归**：盟约互助 `aid` **118 → 68（−42%）**，
   11/12 个 seed 同向。**我明知机制不明仍然合了**（CI 绿、R12 走完、回滚成本一个数）。
   ⇒ **那笔债这一波第一个还。**
2. **V2 找到了一颗没人见过的红**：`N=40 seeds 1-12` 在**出货树**上是 `S0 GATE: FAIL ❌`，
   而**此前没有任何人跑过 N=40**。
3. 用户从第一次 loop 就点名要「剧情、故事」，而 `Story.gd` 是**独立文件**，
   终于可以和仿真侧并行——**这一波做它。**

## 一、W1 · 那 42% 到底去哪了（**owns 仿真侧**）

**owns**：`game/scripts/Sim.gd`、`game/data/**`、`game/bench/**`
**不得触碰**：`game/scripts/Story.gd` / `Director.gd`（W3 的行）、`tools/**`（W2 的行）、`WorldView.gd` / `Main.gd`
**你的编号文档**：编号 **88**

V1 的原话（照抄，别转述）：
> 盟约互助 `aid` **118 → 68（−42%）**，12 个 seed 里 11 个方向一致，
> 而同一张表上其余通道全在 4-9/12 的噪声带里。归因到 `standing` 那条路。
> **机制没找到，什么都没排除。** 它把 `#29` 的前置条件从 5/12 推到 8/12 不满足。

### 要回答的

1. **机制是什么？** `standing` 变了之后，是哪一步让 `aid` 少发生了——
   候选枚举、接受判定、盟约成立条件、还是别的？**给出证据链，不要只给相关性。**
2. **这 42% 是设计上想要的吗？** 两种结局都合法：
   - **是**：那就把"手艺信誉会挤占盟约互助"**写进文档当设计**，并说明为什么它合理；
   - **不是**：那就修，或者**把 `craft_credit` 的 `standing` 关掉**（V1 说回滚是一个数）。
   ⚠️ **不许在没找到机制的情况下调参把数字调回 118。** 那是用调参冒充解释。
3. **`#29` 的前件从 5/12 掉到 8/12 不满足**——这本身要不要报警？现在没有任何门看着它。

### 验收（W1）

1. 机制的证据链（**逐步，不是相关性**）；找不到就写"找不到 + 排除了哪些"。
2. 结论（设计 / 缺陷）+ 相应处置 + 前后对照（逐 seed，**给展布不给均值**）。
3. `#41` 与九个岗位的社会痕迹**不得被顺手拆掉**；`#16`/`#20`/`#5`/放逐一族仍需干净。
4. N=12 逐字节不变，或走**完整 R12**（⚠️ U1 与 V1 已经各重烘过一次，你的基线是它们之后）。
5. §2.5 三行包络；`bash tools/ci.sh` 全绿（**读输出**）。

## 二、W2 · N=40 那颗红（**只读诊断 + `tools/`**）

**owns**：`tools/**`、你的编号文档
**不得触碰**：`game/**`
**你的编号文档**：编号 **89**

V2 实测（它的日志已入库，在 `analysis/v2/`）：
- 出货配置逐 N 的红数（每格 24 seeds）：`12:0 · 16:0 · 20:1↓ · 24:2 · 32:0 · 40:2↓ · 48:2↓ · 60:0`
- **`N=40 seeds 1-12` = `S0 GATE: FAIL ❌` (rc=1)**，硬不变量 12/12 全绿，破的是软通过率门。
- V2 明写它**没做**的：细扫 {18,22,23,25,26}（计划文件已入库，从未运行）。

### 要回答的

1. **N=40 那两个红是什么货、什么臂、什么机制？** V2 说是下限臂（↓），**从那里接着往下**。
2. **它与 N=24 那两个是同一族吗？** V2 已证明 N=24 是 `(N=24, seeds 1-12)`，**别把 N=40 也当成一个点**——
   **先按 seed 窗口拆开看。**
3. **CI 该不该加一格大 N？** V2 把 N=16 与 N=24 的基准率、牙齿、代价都量出来了，
   并提醒"**加 N=24 可能问错了，N=40 更糟**"。
   ⇒ **把 N=40 的同一组数补齐，让用户能在同一张表上比。决定仍然留给用户，不许你拍。**

### 验收（W2）

1. N=40 的逐 seed 分解（哪几颗、哪种货、哪条臂）。
2. 与 N=24 那一族的对照：同族还是异族，**给证据**。
3. N=16 / N=24 / N=40 三者的基准率 · 牙齿 · 代价同表。
4. §2.5 三行包络（若造了新量具）；`bash tools/ci.sh` 全绿（读输出）。

## 三、W3 · 故事层（**用户从第一次 loop 就点名要的**）

**owns**：`game/scripts/Story.gd`、`game/scripts/Director.gd`
**不得触碰**：`game/scripts/Sim.gd`、`game/data/**`、`game/bench/**`（W1 的行）、`tools/**`（W2 的行）、`WorldView.gd` / `Main.gd`
**你的编号文档**：编号 **90**

### 先量

1. **`Story.gd` 今天到底把什么讲成了故事？** 逐事件类型列出：
   哪些有中文叙述、哪些只打印枚举名、哪些**根本不进纪事**。
2. **V1 刚加的手艺信誉事件（`CR:<职位>`）有没有叙述？** 我的先验是"没有"——**请用数据判**。
3. **`Director.gd` 是干什么的、今天有没有在跑？** ⚠️ **看到"零调用"先跑 `git log -S`**
   ——本 session 因为跳过这一步吃过亏。

### 再动

把**最值钱的那几类事件**讲成人话。判据不是"我觉得好看"：
- **覆盖率**：进纪事的事件类型占比，改前改后。
- **可追溯**：每一句叙述都要能指回它依据的那条 `event`——
  **不许凭空生成情节**（红线 #2 的精神：模型不写世界状态；**叙述层同理不许发明事实**）。
- ⚠️ **仿真侧必须逐字节不变**——你只动表现/叙述层。**用 digest 证明**，
  照 S3/V3 的规格给**三条独立证据**（自造 A/B + 开工前的金标 + 留出 seed）。

### 验收（W3）

1. 改前的事件类型清点表（有叙述 / 只有枚举名 / 不进纪事）。
2. 改动 + 覆盖率前后对照 + **可追溯性怎么保证**（给机制，不是给保证）。
3. **digest 三条独立证据**。
4. §2.5 三行包络（若加门）；`bash tools/ci.sh` 全绿（读输出）。

## 四、共同约束（照抄，不重述理由）

- docs/41 红线四条 + §0.5；**计划不得给契约加码**。
- **`does_not_detect` 必须是跑出来的**；**"测了之后决定不做"必须写进回执**。
- **单 seed 的 PASS 不含信息**（`soft_threshold(n<=1)==0`）；**`tee | tail` 会吃掉退出码** ⇒ 写文件再读。
- **别在 CI 跑的时候改它正在读的任何文件**（U1、V3 各自陈过一次；V3 因此**作废并重跑**了一次通过的 CI）。
- **判据写好 ≠ 接进 CI**：V3 的门在 `visual_gate.sh` 里第一次跑就红了，
  而红的原因是**打印 ✅ 时的编码**，不是判据。**你造的门，请在它真正要跑的那个环境里跑一次。**
- **报极值连并列一起报**；**收紧判据前先量余量**；**加不齐先重新数**；**看到"零引用"先跑 `git log -S`**。
- **先确认你量的是哪个对象**——H1、S3、V3 三根棒的第一版度量都量错了对象。
- **编号已占好，但【不要】在正文写 `docs/` + 数字的纯文本形式**（`lint_links` 检查 (2) 认它）。
- **别把 `lint_links` 自己的失败输出粘进回执**（R2、R3、U2 都踩过）。
- **worktree 的 checkout 可能落后两百多个 commit**（上一波三根棒都是 231）：**先确认基线，是祖先才 ff。**
- 隔离副本里做负对照；越界要声明。
