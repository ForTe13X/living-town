# 81 · Wave U · U2 回执——**阶段产出出新版；而这一波要写进去的东西里，一多半是"我们以前对自己的描述是错的"**

> 派单：[docs/79 §二](79-wave-u-plan.md)（U2）。共同约束：[docs/41](41-baton-contract.md) 全文。
> **行**：`docs/README.md`、`README.md`、`README_EN.md`、`docs/56-*`、`docs/13-*`、`docs/05-*`、本文。
> **一行 `game/**` 与 `tools/**` 都没有碰**（`git show --stat` 可验）。
> 本棒**没有跑 `tools/ci.sh` 全量**——只跑了它的第 2 步（`lint_links`）。
> **⇒ 本文不对任何其它门做绿或红的断言**（这一句照抄 [docs/70 §〇](70-wave-r-r3-docs-receipt.md) 的口径，理由相同）。

---

## 〇、先自查基线——**这次落后 221 个 commit，是本 session 的新纪录**

worktree 的 checkout 停在 **`38ba4a7`**（= `master`），而 `integration/batons` 已经在 **`6cb7383`**。

```
git merge-base --is-ancestor HEAD integration/batons   → ANCESTOR: YES
git rev-list --left-right --count HEAD...integration/batons → 0  221
```

**确认是祖先之后才 `merge --ff-only`。** 在 ff 之前，本棒要读的 **68/69/70/72/73/74/76/77/78/79 十份文档
在这棵树上一份都不存在**——照着那棵树写，会交出一份停在 Wave Q 的"新版"。

> 上一波三根棒各落后 **209**，再上一波是 **199**、**185**、**~100**。
> ⇒ **[docs/41 §1](41-baton-contract.md) 那条"先自查基线"的上界还在往上抬**，
> 而它的代价是**静默的**：落后的树不会报错，它只会让你验证一棵早已过期的树。

---

## 一、交付了什么

| 文件 | 改了什么 |
|---|---|
| `docs/56-*` | **出新版**（2026-08-01 当日第二版）。§三整节换成 R→T 九棒；§二加了室内那一段；§四、§五、§六重写。**上一版正文留在 git 历史里，一个字没改写。** |
| `docs/13-*` | 新增一章 `2026-08-01 · Wave R→T`（九节），**按"被推翻的东西"组织**；含 §七「我自己错了几次」逐条。 |
| `README.md` | 换上室内录屏 + **重写那段过时的说明**；「还不到位的」里规模那条与分母那条重写；文档表补 68/72/73/76/77/78；篇数 66 → 78。 |
| `README_EN.md` | 同上（英文侧同步，含同一张图与同一段更正）。 |
| `docs/README.md` | 抬头 `53-67` → `53-79、81`；波次表补 68-79 与本文；52 号那条墓志铭**改写**（那件事已经做完了，只是没用那个号）；docs/13 行数 1700+ → 2300+（实测 2312）。 |
| `docs/05-*` | 权威计划指针 **R → U**；波次表补 R/S/T；**加了一段"CI 全绿现在是什么意思"的口径声明**，因为它改变本文档所有"已交付"字样的读法。 |
| `docs/81-*` | 本文。 |

### 1.1 `docs/56` 新版的要点（口径照旧：不含一行代码、"还不行的"那节不打圆场）

**⭐ 派单点名的那一段，我写成了整个 §三 的第①条**，因为它是这一版和以往几版性质不同的地方。
原文的落点是三句话：

1. **"全绿"的准确含义是"在这 12 个开局上没有出问题"，不是"这套规则成立"**——
   而那 12 个开局**既是当初挑及格线用的那一组，又恰好是全 60 个里波动最小的一段**（0.205 vs 0.468 / 0.486）。
2. **证据不是推算**：48 个没参与挑线的开局里 5 个跌破（10.4%），而 `--seeds 49-60` 在**未改动的树**上
   直接打印那一行 `FAIL`。
3. **另一半必须同时说**：破的只有**一条软规则**；**25 条硬规则在全部 60 个开局上一条都没破**
   （我复核过 `HARD_IDS` 的长度确实是 25）；**不是有人作弊**——当初留了 18 个开局做复核，
   只是**没有人回头再跑**。

**其余八条**（按分量排，不按新增排）：
② 根因=工位打分从不读库存，两头都开环，机制实测有效**而出货关着**；
③ **会重算并打印余量的臂全部准确、抄成死数字的全部过期**（连同那个"实测地板 101 vs 活数 81"的标本）；
④ 版权红线的门放行 `.onnx/.tflite/.dlc`——**本项目自己路线图上的三种格式**，已修，**且连修它的人第一版对中文文件名失明这件事也写了进去**；
⑤ 方法论的分母两半都有了，**而它是上界**；
⑥ **探测率是任务框架的函数**（外加"这个实验先把自己做废了一次"）；
⑦ 单开局下那个长在自己失败明细旁边的绿勾；
⑧ 镜头进屋是两半 + HUD 最痛的是最小的那一行；
⑨ 新上限臂**自认是棘轮不是判决**。

**§四「还不到位的」比上一版更长，而且往难听的方向改了两条**：
「规模还差两格」**撤回**，换成"48 个留出开局里今天 5 个红、其中一整块 12 个直接不通过；
钥匙有了但锁着"；「分母未知」**更新**成"有数了，而它是上界、且有三条方向已知的偏差"。
另新增五条：钥匙打开后 CI 里没有守护者、那条没能复现也没被解释的 `DetGate` 红、
三间没有人会进的屋子、HUD 只有 1× 可读、以及"两张图标太像"那条**里我撤回了上一版没有核实的半句**。

### 1.2 `docs/13` 记了哪几条被推翻的结论

新章共九节。**被推翻的清单如下（这就是派单要的那份）**：

**A. 我自己写进 brief 的四条**（§七逐条，全部在原文就地更正）：

| # | 我写的 | 实测 | 出处 |
|---|---|---|---|
| 1 | **seed 36 属于"产得太少"那一族** | 它红的是**上限臂**；最差货 0.504 **在门槛之上、根本没破** ⇒ **任何抬高产出的修法都会把它推得更红**（实测一个只加不减的档把 10 个里的 4 个翻成这种红） | [76 §〇①](76-wave-t-t1-stock-pull.md) |
| 2 | **"面点师+渔夫【一起】塌，ρ=0.618"** | 0.618 是 ρ(**两人之和**, 口粮满足率)；**两人之间的 Spearman ρ = 0.003**，几乎正交 ⇒ **"一起塌"不是机制** | [76 §〇②](76-wave-t-t1-stock-pull.md) |
| 3 | **"docs/58 与 docs/13:830 互相矛盾，去代码里判"** | **两条都对，因为吧台有两个**（`counter_1`@town 无 staff/amount 20；`cafe1f_counter`@cafe/1f 有 staff/amount 30）。R1 与 R2 各自独立收敛 | [68 §〇①](68-wave-r-r1-economy-ceiling.md)、[69 §四·3](69-wave-r-r2-interior-art-receipt.md) |
| 4 | **"R2 的室内分色能在录屏里看到"** | S3 实测 **45 帧 0 帧室内**；T3 在未改动的树上用加严判据复测 **0/100**，最小草占比离阈值还有 5.5× | [74 §五·2](74-wave-s-s3-furniture-role-receipt.md)、[78 §二·1](78-wave-t-t3-interior-cam-and-hud.md) |

> 札记里我另写了一句关于这四条**形状**的话，因为它比四条本身更有用：
> **第 1 条与第 3 条的共同点是"把两个东西并成一个"**（两条臂并成一族、两件家具并成一件），
> **第 2 条是"把一个量安到另一个对象上"**。三条都不是"记错了一个数"，
> 而是**先有一句读起来顺的话，再把事实塞进它**——与 Wave L→Q 那次「框架先行、事实后补」同病，只是伪装得更像技术细节。

**B. 我把别人的推论当规矩用的三条**（T2 逐条实测推翻）：
「五种注入的证伪成本差**两个数量级**」（S2 是**推**的不是量的；实测 L4 六条里四条只要一次 `grep`）、
「S2 那次 L4 假阳的机制」（**两个分句都不成立**，且那条注入**根本不该被生成** ⇒ `1/2` 应当是 `0/1`）、
「`#40` 上限臂余量表今天逐位成立」（**源码那张表低报了这道门自己的红**）。

**C. 各棒推翻自己的**（札记逐条收了）：S1 对 varA 的预测（"低 amount 落点赢不了"——**赢了一个、输了一个，
而且离得更远的那个赢了**）、T3 的第一版室内判据（夜里众数是界外背板，**漏判 4 帧**）、
S3 的第一版裁剪（**量到的差全是地板**）、R2 的 `flower_white`（**它把"缺口被补上了"和"缺口被修好了"讲反了**）。

**D. 三处"绿着却不成立"**（§八）：单 seed 软门恒过（`84bd95d`）、
`ci.sh` 4a 抬头那个每次都执行一次命令替换并**吃掉自己文字**的 `echo`（`e765af4`）、
`ScaleSupply` 自校验只有下限臂 ⇒ **11 条假 MISMATCH**。

### 1.3 README 改了什么

- **主图从 `s3_town_waveS.gif` 换成 `docs/media/t3_demo_interior.gif`**（1280×768、1× 整数倍、5 fps × 6.5 s / 33 帧、2.98 MB，实测文件 3 124 222 B）。
- **那段过时的说明重写了，而不是删掉。** 原文是「**这段录屏只有小镇平面，看不到室内**」+ 一个**只对了一半**的归因。
  新版明写四件事：① 这句话已过时、② 旧归因对了一半（`--shot-fit` 嵌在出图分支里）、
  ③ **缺的是第二半**（`demo_apply()` 没有"在哪个 Space"这一维；只修第一半 ⇒ 房间只占画面 15.2%、楼层标签读不出）、
  ④ 修完之后 **0/100 → 12/100 帧、房间占 61.8%**。
- **两条边界照写**：插曲窗口锚在 **tick** 上不是秒上（**要复现某段室内得按 tick 找**）；
  **工坊那一拍是空屋**（127 个采样点里有人 0 次）⇒ "演示里的室内会有人活动"对它是假的。
- 保留并延长了「只走整数倍缩放」那一段，**并明写 T3 没有推翻 `make_gif.sh` 抬头那张五行表**
  （0.5× 上笔画依旧 1px ⇒ 主图仍走 `divisor=1`）。
- **纯小镇平面那段没有删**，改成正文里的一条链接。
- 「还不到位的」两条重写（规模、分母）+ 新增一条（版权红线的门）；文档表补六行；篇数 66 → **78**（数字取自 `lint_links` 自己打印的那个数，不是手抄）。
- `README_EN.md` 同步了以上全部。

---

## 二、`lint_links` 的**实际输出**

**收工的树上，最后一次**：

```
$ python tools/lint_links.py
lint_links: OK — 97 markdown files, all relative links resolve and all docs/NN citations exist (78 numbered docs)
EXIT=0
```

> ⚠️ **中途它红过一次，而那次红是我自己安排的，写在这里而不是省略**：
> 我在 `docs/README.md` 的波次表里先写下了指向本文的链接、而本文还没落盘 ⇒
> ```
> lint_links: FAIL (1 dead reference(s)):
>   - docs/README.md: -> 81-wave-u-u2-docs-receipt.md
> ```
> 本文落盘后即绿。**这一条同时是这道门判别力的又一次现场证据**（它是**检查 (1)**，
> 与 R2/S2/T2 撞到的**检查 (2)** 不是同一条分支）。

**派单点名的两个禁区，我按字面守住了**：全稿**没有出现过 80 或 82 的 `docs/` 引用形式**。
`docs/README.md` 抬头需要说明"80 此刻不存在"，我把它写成了**不带前缀的纯数字**，并在括号里说明为什么要这么写
——[docs/70 §六·3](70-wave-r-r3-docs-receipt.md) 已经论证过这是两条可行写法里更好的那条
（另一条 `NN@分支` 会写下一个**不被核实**的分支名）。

---

## 三、探测包络（[docs/41 §2.5](41-baton-contract.md)）

> **本棒【没有新增也没有收紧任何一道门】**——`tools/**` 不在我的行里，我一个字节都没碰。
> 下面这份包络给**我唯一真的跑过的那道门**（`lint_links`，`ci.sh` 第 2 步）。
> 写它的理由是：**本棒对"我的改动是安全的"这个说法，全部证据只来自这一道门**
> ——那就必须把这道门的边界摊开，否则"它绿了"会被读得比它实际保证的强。

全部变异体跑在一份**隔离的最小 fixture** 上（把 `lint_links.py` 复制出仓库、喂给自造的 `docs/` 树，
**仓库一个文件都没碰**）。脚本：`scratchpad/u2/ll_nc.py`（探针输出，不入库）。

```
detects:（逐条实测变红；⚠️ 失败输出【不能原样抄进来】，理由见本节末，所以下面只描述）
  ① 检查 (2)：正文里以纯文本形式引用一个本树没有的编号 ⇒ 红，并点名文件与【行号】，
     且提示可以改写成带分支后缀的形式。（我用的正是本波那个不存在的邻号。）
  ② 检查 (1)：相对链接指向不存在的文件 ⇒ 红：`docs/probe.md: -> 99-does-not-exist.md`
  ③ 检查 (1)：`<img src>` / `![]()` 指向不存在的素材 ⇒ 红：`docs/probe.md: -> media/nope.gif`
     （⇒ **本棒换 README 主图这件事，是被这一条守着的**：图名打错会当场红。）

does_not_detect:（**逐条都是跑出来的**，不是想出来的）
  ① **带分支后缀的那条逃生门**：把 ① 那个不存在的编号加上 `@` 与一个**当场编出来的分支名** ⇒ **绿**。
     它不查分支存不存在、也不查文档在那条分支上存不存在。
     （这正是 52 号那个洞活下来的通道，[docs/73 §二·5-P2](73-wave-s-s2-denominator-and-gate-teeth.md) 的独立复现。）
  ② **链接解析得到、但指向【错的那一份】文档** ⇒ **绿**。它只查"目标文件存在"，不查"目标是不是你想引的那一份"。
  ③ **链接文字写着 `docs/41`、而 href 指向另一份文档** ⇒ **绿**。
     ⇒ **本棒新写的每一条引用，"指对了没有"没有任何门在守，只有我自己逐条核过。**
  ④ **被排除目录下的 md**（`build/` 等）里的死链 ⇒ **绿**（那个文件根本不进扫描集，文件计数从 4 掉回 3）。
  ⑤ **内容对不对，它一概不管**：`docs/41 says the sky is green and CI has 900 gates.` ⇒ **绿**。
     ⇒ **它是一道"引用能不能走通"的门，不是一道"这句话是不是真的"的门。**
     **本棒交付的是一份【全是断言】的文档，而这道门对断言的真假分辨率恰好是零。**

confidence: N=9 个变异体（3 红 + 5 绿 + 1 干净基线），全部在隔离 fixture 上跑完并核过退出码。
            **注意分母**：这只量了 `lint_links` 一道门；本棒的改动**没有被任何其它门看过**（见 §五）。
```

> ### ⚠️ 写这一节的时候我把门弄红了一次——**而这正是 R2/R3 已经记过的那个陷阱，我照样踩了**
> 我第一版把 `detects ①` 的**实际失败输出原样贴了进来**。那行输出里含着被检查的那个编号，
> 于是 `lint_links` 下一次运行**把我这份回执自己判成了 4 条死引用**（同一段里那个形状出现了四次）：
> **一条真实的负对照证据，变成了四条真实的红。**
> ⇒ **这道门认的是【字符串形状】，不是【引用意图】：把它的失败信息贴进任何 `.md`，就等于再犯一次同样的错。**
> [docs/70 §〇](70-wave-r-r3-docs-receipt.md)（"我第一版正是原样贴的，当场把红从 3 条变成 15 条"）与
> [docs/69 §四·6](69-wave-r-r2-interior-art-receipt.md)（"一行红变两行红"）**都已经把这条写下来了，而我还是踩了。**
> ⇒ **"文档里写过"与"下一个人不会再犯"是两件事。** 修法沿用 R3 那一条：**描述那条输出，不原样引用它。**

**措辞**（照 §2.5 的要求）：本棒**没有阻断任何失效模式**——它没有新增门。
这份包络的用途是**给本棒自己的绿定价**：`lint_links: OK` 只意味着"引用都走得通"，
**不意味着"这份文档说的是对的"**。后者的全部保证来自 §四那一节，而那一节是人做的。

---

## 四、这份 brief 哪里是错的（[docs/41 §4](41-baton-contract.md)）

### ① **⭐「四条自我纠错逐条在编号 76/77/78 里」——只有两条在。另外两条在 68/69 与 74。**

派单原文逐字：「本段里我自己至少错了四次（把 seed 36 的臂说反、把相关系数安错对象、把两个吧台并成一个、
说 R2 的室内分色能在录屏里看到），**逐条在编号 76/77/78 里**。」

| 错误 | 出错的 brief 住在哪 | 更正住在哪 | 在 76/77/78 里吗 |
|---|---|---|---|
| seed 36 的臂说反 | [75 §一](75-wave-t-plan.md) | [76 §〇①](76-wave-t-t1-stock-pull.md) | ✅ |
| 相关系数安错对象 | [75 §一](75-wave-t-plan.md) | [76 §〇②](76-wave-t-t1-stock-pull.md) | ✅ |
| **两个吧台并成一个** | **[67 §一②](67-wave-r-plan.md)** | **[68 §〇①](68-wave-r-r1-economy-ceiling.md)＋[69 §四·3](69-wave-r-r2-interior-art-receipt.md)** | ❌ **在 Wave R 的回执里** |
| **室内分色能在录屏里看到** | **[71 §〇](71-wave-s-plan.md)** | **[74 §五·2](74-wave-s-s3-furniture-role-receipt.md)** 首报，[78 §二·1/§六·1](78-wave-t-t3-interior-cam-and-hud.md) 加严复测 | ⚠️ **半个**（78 有，而**最先报它的是 74**） |

**⇒ 照 brief 的字面只读 76/77/78，会写丢一条半。**
而这一条的形状值得记：**派棒者把"这四条错"当成同一波的产物，实际它们横跨 R / S / T 三波**
——**因为它们不是四次疏忽，是同一个习惯在三波里各犯了一次**（见 §一·2 那段关于"形状"的话）。
**这正是 [docs/41 §4](41-baton-contract.md) 说的"brief 里的引用大多是二手的"的又一例，只是这次二手的是【出处】而不是【数字】。**

### ② 「**至少四条**是对本仓库自我叙述的修正」——**是下限，实际更多**

派单 §二 列了 S1 / S2 / T2 / T1 四条。我在写 `docs/56` 时按同一标准数下来至少还有四条：
**R1 的上限臂自认是棘轮不是判决**（改的是"这道门证明了什么"的叙述）、
**S2 点名并随后修好的版权红线门**（改的是"红线守着"这句话）、
**单 seed 那个绿勾**（改的是"看到绿勾就是过了"）、
**`ci.sh` 4a 抬头那个从来没人发现的 shell 错误**（改的是"CI 的输出就是它想说的话"）。
⇒ **"至少四条"作为下限成立，但按它去写会漏掉一半。**

### ③ 「12/100 帧」这个数属于**录制**，不属于**那张 GIF**

派单写「新录屏有室内，12/100 帧，含一次完整的室外→室内切」。
**逐字核 [docs/78 §二·4/§二·5](78-wave-t-t3-interior-cam-and-hud.md)**：`12/100` 是那次
**100 秒录制、每秒抽 1 帧**的判定结果（室内两段：42–47 s 咖啡馆 / 55–60 s 工坊）；
而交付的 GIF 是**从同一次录制里取的 6.5 秒 / 33 帧**，它**包含**其中一次切换。
⇒ 两句话都对，但**挂在 README 上的那张图不是"100 帧里的 12 帧"**。
我在 README 里按这个口径分开写了（录制 100 秒 → 取 6.5 秒），**没有把那个比例安到 GIF 头上**。

### ④ 「现有那段说明写着'这段录屏只有小镇平面，看不到室内'——那句话现在过时了，改写成正确的」——**过时的不止那一句**

那段说明里紧跟着的**归因**（"`--shot-fit` 的相机代码嵌在出图模式分支里"）**今天也只对一半**：
T3 证明缺的是第二半（`demo_apply()` 没有 Space 这一维），**而只修第一半的话房间只占画面 15.2%**。
⇒ **只把"看不到室内"那一句换掉、留着旧归因，会留下一个读起来完整、实际只对一半的解释。**
我把整段重写了，并**把旧归因作为"对了一半"保留在原地**——按本仓库口径，被推翻的话不删，标清适用范围。

### ⑤ 「本 session 它已经抓到我三次了」——**是下限；而抓到 U2 的这一次走的是【另一条分支】**

从各棒回执里能数到的命中远不止三次（[docs/70 §〇](70-wave-r-r3-docs-receipt.md) 记的基线 3 条、
[docs/69 §四·6](69-wave-r-r2-interior-art-receipt.md) 自陈"我是第四和第五次"、
[docs/77 §〇④](77-wave-t-t2-prospective-injection.md) 一次、[docs/76 §十](76-wave-t-t1-stock-pull.md) 一次）。
**不是错，是这条经验的计数一直在往上走。**
⚠️ 而**本棒被抓的那一次是检查 (1)（相对链接指向未落盘的文件），不是大家反复撞的检查 (2)**
——⇒ **这道门的两条分支都有牙，而它们抓的是不同的错。**

---

## 五、我没能测到什么（**"没测"明写，不用推断填空**）

- **没有跑 `tools/ci.sh` 全量。** 只跑了第 2 步（`python tools/lint_links.py`）。
  ⇒ **本文不对 0 / 1 / 1b / 2b / 2c / 2d / 3 / 4 / 4a / 4b / 4c / 4d / 4e / 4f / 5 / 6 中的任何一道做绿或红的断言。**
  理由与 [docs/70](70-wave-r-r3-docs-receipt.md) 相同：本棒只改 markdown，`ci.sh` 里唯一读 markdown 的就是第 2 步；
  而本波另有两根棒（U1 / U3）正在动 `game/**` 与 `tools/**`，**跑全量会与它们抢机器**
  ——[docs/41 §6](41-baton-contract.md) 明写界外层重画门按帧数计、对机器速度敏感，**抢 CPU 就可能给别人造一次假红**。
  **这是一个取舍，写在这里而不是省略。**
- **没有杀任何进程**（[docs/41 §1](41-baton-contract.md) 并行期纪律）。本棒没有启动过 Godot 或 Docker。
- **没有眼验任何画面、没有出图、没有真机。** 换 README 主图这件事，我核的是
  **文件存在 + 尺寸/帧数/字节数与 [docs/78 §十](78-wave-t-t3-interior-cam-and-hud.md) 的记载对得上**
  （实测 `3 124 222 B` ≈ 2.98 MB），**我没有把那张 GIF 逐帧看过**。
  ⇒ "它含一次完整的室外→室内切"这句话，**我引的是 T3 的实测，不是我自己的眼验**。
- **没有复核 `docs/56` §一/§二 的存量描述**（12 个居民、九个职业、十张脸、表情图标）。
  沿用上一版口径，**并把"沿用不等于复核"这句话写进了正文**。
- **没有核对 README 里每一个历史数字。** 我核的是**本波新增的**与**明显与今天冲突的**
  （文档篇数、`docs/13` 行数、`HARD_IDS` 的长度=25、`#40` 是否只有下限臂、`tools/brief_mutate.py` 在不在树上）。
  **早于 Wave R 的那些数字我没有逐个重测**——它们的出处链接都在，但"链接在"不等于我核过。
- **没有走 Codex 打磨那一环，也没有发起任何授权请求**（沿用 [docs/70 §四](70-wave-r-r3-docs-receipt.md) 记的用户意愿）。
  ⇒ **README 与阶段报告的措辞没有第二双眼睛看过。**
- **本棒引用的所有实测数字都是二手的**——它们来自九份回执，我**一个网格都没有重跑**。
  按 [docs/41 §4](41-baton-contract.md)：**这是一份转述，而转述会把观察伪造成机制。**
  我能做的只有一件事，并且做了：**凡是引用结论，一律连同它自己划的边界一起引**
  （棘轮不是判决 / 上界不是点估计 / 信号不是效应量 / 没能复现不等于没有问题）。

---

## 六、留给下一棒的

1. **`docs/56` 的 §一/§二 已经三版没有被实测复核过了**（R3 与本棒各自明写"沿用上一版"）。
   ⇒ 按 [docs/73](73-wave-s-s2-denominator-and-gate-teeth.md) 那条统一结论，**这正是会烂掉的那一族**：
   它们是**冻结的字面量**，旁边没有任何东西会重算它们。**下一个动阶段产出的棒该先跑一次清点，而不是再沿用一版。**
2. **README 的篇数、`docs/13` 的行数这两个数今天是【手抄的活数】**——我抄的是 `lint_links` 与 `wc -l` 当场打印的值，
   但**它们写进 markdown 的那一刻就冻住了**。同族的病，同一份文档里现在有两处。
   ⚠️ **能不能做成"文档里的数字旁边指向重算它的命令"，正是 U3 那一棒的行**（[docs/79 §三](79-wave-u-plan.md)）
   ——**这两处是现成的客户。**
3. **`docs/05` 的 NOW/NEXT/LATER 三张表本轮只加了两条复核，没有重写。**
   它今天靠一段"过期情况"注解活着，而那段注解本身已经是第二版了。
