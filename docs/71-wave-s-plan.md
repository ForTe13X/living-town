# 71 · Wave S 计划——**门到底有没有牙？以及那个从来没人算过的分母**

> 前置：docs/41 全部（**§0.5**、**§0.8**、**§2.5 探测包络**、**§1.5 读仓库纪律**）、docs/47 **R12**、
> **docs/68 / 69 / 70（Wave R 三根棒的回执，本波三条码全部来自它们自己写下的交接）**。
> **回执必须带 §2.5 三行包络，`does_not_detect` 必须是跑出来的。**

## 〇、这一波从哪来——三条都是上一波自己交出来的，不是我找的

1. **R1 亲手给自己的门下了这个判词**：上限臂的阈值是在"CI 跑的那两格必须绿"的约束下选的，
   ⇒ **它按构造无法宣判今天的 N≤24 有问题**。**那是一道棘轮，不是一次判决。**
   而唯一不受这个约束的一格（N=60）**今天红 8/12**。
2. **R3 查出 `52` 号从未落地**：docs/50 §四派的 **H4「给方法论装一个分母」（brief 变异测试）从未交付**，
   `tools/brief_mutate.py` 不存在。⇒ 外审那句"**24 不是质量指标，因为你不知道分母**"**至今没有被回答过**。
   本仓库通篇在报"第 N 次被自己的数据推翻"，**而 N 的分母是未知的。**
3. **R2 交接的 `shelf`**：11 个实例铺满 8/8 层，只有图书馆和阿丽房间是对的，
   商店/工坊/澡堂都不对。**它是室内的 `bench.png`。**

## 一、S1 · 需求侧的落点，以及 held-out 那颗今天就红的 seed

**owns**：`game/scripts/Sim.gd`、`game/data/**`（含 `map.json` / `interiors.json`）、`game/bench/Invariants.gd`
**不得触碰**：`game/scripts/WorldView.gd`、`game/assets/**`（S3 的行）、`tools/ci.sh`
**你的编号文档**：编号 **72**（文件名 `72-….md`，放在 `docs/` 下）

### ① N=60 红 8/12，而 R1 已经把成因分成了两半——**别把它们当成一件事**

- **豆子 12/12、话本 12/12 全年零缺货是【需求侧】的**：需求 ×1.00 与 ×0.50，而人口 ×5.00。
  **这不是 `work_pull` 造成的**，`work_pull` 之前就是这样。
  ⇒ 真正的形状问题是**满足物的落点数量不随人口涨**（岗位数恒为 10 是同一族毛病的另一个投影）。
- **屋瓦 10/12、整洁 8/12 才是 `work_pull` 推上去的**。

⚠️ **R1 明写它没做这一半是因为它需要 `map.json` / `interiors.json`，那不在它的 owns 里。现在在你的。**

### ② held-out seed 18 **今天就是红的**

`#40` 口粮满足率 **0.42**，断供 **38/60 天**。held-out 13-30 只在 R12 时跑
⇒ **它一直在无声地红着**。R1 在完成的树上复核过，不是一次抖动。

**先判定这是什么**：是 seed 18 的地图/人口分布特例，还是一个 CI 从没覆盖到的普遍区间？
**先量再动**——扫 13-30 全段，**给展布不给均值，报极值连并列一起报**。

### 验收（S1）

1. N=60 的两半**分开量、分开报**；需求侧的修法（若做）必须证明它**没有**顺手改动供给侧。
2. seed 18 的判定 + 13-30 全段的展布；若判为普遍问题，**说明为什么 CI 的两格没抓到**。
3. 任何移动金标的改动走**完整 R12**（三锚 + `rebake_history` + held-out 两侧）。
4. §2.5 三行包络；`bash tools/ci.sh` 全绿（**读输出，不是读退出码**）。

## 二、S2 · **只读对抗评审**：给方法论装上那个分母

**owns**：只写 `tools/brief_mutate.py`（新文件）与你的编号文档；**除此之外不改仓库任何一行**
**你的编号文档**：编号 **73**
**这一棒的立场是 REFUTE。** 你的任务不是确认本仓库做得好，而是**尽力证明它的门是纸的**。

### 要回答的两件事

**A. 那个分母。** 本仓库反复报"第 N 次被自己的数据推翻"，而**没人知道分母**：
派出去的 brief 里有多少条错误断言？其中多少被棒子抓住了？**抓住率是多少？**

H4 当初的设计是 **brief 变异测试**：往 brief 里注入**已知的**错误断言，看棒子抓不抓得住。
**它从未交付。现在交付它。** 最低限度：
- 从本 session 已有的 brief（docs/46-71）与回执里，**建一张"已知错误断言 → 是否被抓"的对照表**。
  这是**回溯**的，比注入便宜得多，且数据已经在树上了。
- **抓住率给区间不给点估计**（本 session 已经因为把 Wilson 下界当点估计吃过亏）。
- 若还有余力，再做**前瞻注入**（`tools/brief_mutate.py`）。**回溯那一半是必交的，前瞻是加分。**

**B. 门的牙。** 逐门回答：**这道门的阈值是"从行为里量出来的"还是"调到让今天绿"的？**
R1 已经对自己的门给了后者的诚实答案。**其余十几道门呢？**
- 特别查：`4a` 记录的余量 0.188 现在是 0.056（**掉了约 3.4 倍且无人记录**）——**还有几道门发生过同样的事？**
- 给出一张**按"牙"排序**的表，并点名**你认为是纸的那几道**。

⚠️ **只查、只报。除了 `tools/brief_mutate.py` 与你自己的文档，不许改任何文件。**
**发现的缺陷不要顺手修**——那会让评审与被评审共用一双手。

## 三、S3 · `shelf` 是室内的 `bench.png`（**纯表现层**）

**owns**：`game/scripts/WorldView.gd`、`game/assets/**`
**不得触碰**：`game/data/**`（S1 的行——**尤其是 `slot` 值，它驱动 `WALKABLE_SLOTS` 导航**）、
`game/scripts/Sim.gd`、`game/bench/**`
**你的编号文档**：编号 **74**

R2 的交接原话：`shelf` **11 个实例铺满 8/8 层**，全画成三色书架；
**只有图书馆与阿丽房间是对的**，商店/工坊/澡堂都不对。

⚠️ **R2 说"修它要动 `slot` 值 ⇒ 触碰导航"。本波不许你走那条路。**
你要做的是**纯表现层的分化**：用**已有的**数据（`areas[].type`、space id、家具在哪栋楼）
让同一个 `slot` 在不同建筑里**画成不同的东西**——正如 R2 让同一面墙按建筑类型上色。
**若你判定纯表现层做不到，照实写"做不到，以及为什么"，那也是交付。**

### 还有一件（**本波的阶段产出**）

**录一段新的**：README 现在那张 GIF 是 Wave G 的，此后落地了 emote 重画、岸线、`civic_*`、
**以及 R2 刚做的室内外壳分化**——**画面已经和它不一样了**。
`tools/record-godot.sh` 录 → `tools/make_gif.sh` 转。
⚠️ **只走整数倍缩放，且实测唯一可读的档位是 1×**（`tools/make_gif.sh` 抬头有那张五行对照表）。
**录不出来就照实说录不出来**，别交一张旧的充数。

### 验收（S3）

1. 改前的 `shelf` 逐实例清点（哪栋、哪层、语义对不对）。
2. 改动 + **前后对照截图**（同一栋、同一机位）；或"纯表现层做不到"的论证。
3. **仿真侧逐字节不变**——**用 digest 证明，别只是声称**（R2 有两条独立证据的先例，照抄那个规格）。
4. 新 GIF（或"录不出来"的说明）。
5. `bash tools/ci.sh` 全绿（读输出）。

## 四、共同约束（照抄，不重述理由）

- docs/41 红线四条 + §0.5 规模矩阵；**计划不得给契约加码**。
- **`does_not_detect` 必须是跑出来的**；**"测了之后决定不做"必须写进回执**。
- **看到"零引用/零调用"先跑 `git log -S`**；**别在 CI 跑的时候改 `ci.sh`**。
- **加不齐先重新数**；**报极值连并列一起报**；**收紧判据前先量余量**。
- **编号已在上面占好，但【不要】在正文里写 `docs/` + 数字的纯文本形式**——
  `tools/lint_links.py` 的检查 (2) 认它，而那个号此刻还不存在。**docs/67 就是这么把门弄红的。**
- **worktree 的 checkout 可能落后上百个 commit**（R1 落后 ~100、R2 落后 185、R3 落后 24）：
  **开工前先确认基线，是祖先才 ff。**
- 隔离副本里做负对照；越界要声明。
