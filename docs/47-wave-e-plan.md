# 47 · Wave E 计划——让劳动真的产出东西，让事件链讲成故事

**2026-07-28 立，接 [docs/46](46-wave-d-plan.md)（Wave D）。** 共同契约 [docs/41](41-baton-contract.md) 全文生效；
Wave C 的 R1-R8（[docs/43 §二](43-wave-c-plan.md)）与 Wave D 的 R9/R10/R11（[docs/46 §〇](46-wave-d-plan.md)）继续生效。

---

## 〇、这一波与前两波的根本不同：**它要动仿真**

Wave C 与 Wave D 的每一根棒都在 **View / tools / bench** 层——所以"金标逐字节不动"是它们的**通用**验收条件。
**Wave E 的 E1 会蓄意改变世界轨迹**，金标**必须**移动。这不是违规，是 [docs/41 §3](41-baton-contract.md) 写明的**受控动作**，
但它把验收标准整个换了一套：

> **★ R12 · 会移动 digest 的棒，"金标不动"不再是验收条件，"金标动得能解释"才是。**
> 具体：①先确认这是**蓄意**的行为变更并说清**为什么**；②**三份**提交锚都要重烘——`Harness --bake-golden`、
> `DetGate --bake-golden`，**外加 `ModelPathGate --bake-anchor`**（`game/bench/modelpath_anchor.json`，D1 在 `ci.sh`
> 第 4e 关建的，锚的是 **random(full) 后端臂的世界轨迹**）。
> **⚠️ 我原来只写了前两份——E1 实测：只烘那两份，`ci.sh` 必红（`ModelPathGate: FAIL ❌ 失败 5`）。**
> 这条本身就是"契约里的数字会过期"的又一例：D1 加门的时候没人回来更新 R12。
> ③在 `_meta.rebake_history` 补一条日期 + 原因；④**必须跑留出种子**（改前/改后都跑 `--seeds 13-30 --days 60`，两侧数字都报）
> 证明不是对基准过拟合；⑤**绝不为了让门变绿而放松硬不变量**——硬不变量变红 = 真发现，报告，不要削它。
>
> **⇒ 这一波最危险的失败不是"没做出来"，是"digest 动了而没人能解释哪一部分是故意的"。**

---

## 一、为什么是"社会产出"——先说清现状，别在推断上盖楼

**实测（以代码为准，不是设计文档）**：`game/data/jobs.json` 给每个居民一个 `{title, action, wage, shift}`，
`Sim.gd:1281-1300` 在有薪动作完成时由**镇库**发工资，`_wage_for` 让本职在班拿职位工资、其余人拿零工价。
经济闭环是 `吃饭付费 → 镇库 → 做活发薪`，由硬不变量 **#34（金钱守恒）** 与 **#35（余额非负）** 守着。

> **★ 所以现在的分工只有【行为】没有【产出】：木匠"做活"什么也没造出来，面点师"做活"没有面包被谁吃掉。**
> ~~**把全镇的工作行为全部删掉，除了钱包数字，世界逐格相同。** 劳动是**装饰性**的。~~
>
> **⚠️ 删除线那句是我写错的，E1 实测撤回（2026-07-28）。** 行号对（`Sim.gd:1281-1300`，现为 `:1309-1328`），
> 结论过头了。今天一次本职工作已经写了三样**非钱包**状态：
> ①`ag["skills"][action] += 1`（`:1313-1319`）——60 天终态实测 aria 5-7、ben 12-14、dan 10-18、hai 8-13、lin 5-16、shu 14-17 级次，**不是零**；
> ②两条记忆（"上工…挣了 N 个钱"、升级里程碑），进 voice grounding；
> ③而技能经 `_wage_for` 加薪、工资经 `poor_line` 的 `work_urgency` 加分**反过来改决策**（`_object_candidates:1504-1506`）
> ⇒ **删掉工作，agent 的行走轨迹就变了，不只是钱包数字。**
> **真正缺的是「物」**：没有东西被造出来、被别人消耗、缺了有后果。方向对，措辞会让人以为工作是个 no-op 分支——它不是。
> 这正是本波要改的那一件事——也是"商贩/环卫/建筑各有 behavior 和社会产出"这句话的落点。

**必须一起说清的边界**：钱在本项目里的设计意图是**造分化与戏剧，不造饿死**（`economy.json` 的 `_doc` 明写：
付不起照吃 `meals_free`）。**产出系统必须继承这条**——缺货可以让人不满、可以造成八卦与冲突，
**不得**成为新的饿死通道。硬不变量 #01 是硬的。

---

## 二、Wave E 第一轮（三棒并行，disjoint by file）

| 棒 | 独占文件 | 动 digest？ |
|---|---|---|
| **E1** 劳动产出与消耗闭环 | `game/scripts/Sim.gd`、`game/data/production.json`(新)、`game/bench/Invariants.gd`、`game/bench/golden_digests.json` | **是（受控）** |
| **E2** 把事件链讲成故事 | `game/scripts/Story.gd`(新)、`game/scripts/Main.gd` | 否 |
| **E3** 角色去道具 pass（原 D8） | `game/assets/art/`、`tools/`(仅新脚本) | 否 |

---

### E1 · 劳动产出与消耗闭环（**本波核心，会移动金标**）

**独占 `Sim.gd` + 新 `game/data/production.json` + `Invariants.gd` + `golden_digests.json`。不得动 `Main.gd`/`WorldView.gd`（E2/E3 在用）。**

**做什么**：让"做活/看摊"产出**具体的东西**，让别人**消耗**它，让**缺货有后果**。最小闭环建议：

- **产出**：本职在班完成一次工作 → 往一个**镇级库存**里加一件（面点师→面包、木匠→木料、商贩→货、环卫→"整洁度"）。
- **消耗**：既有动作去扣它（吃饭优先扣面包；修缮/建造扣木料；整洁度随时间衰减、环卫补）。
- **后果**：缺货**不造成饿死**（红线：`meals_free` 那条设计意图继承下来），而是造成**可观测的社会后果**
  ——不满、议论、声誉变化、或某个动作变贵/变慢。**后果必须写进 `event_log`**，否则 E2 讲不出故事、编年史也看不见。

**数据驱动 + 缺文件即零扰动**（照抄 `jobs.json`/`economy.json` 已经验证好用的那套）：
`production.json` 缺失 ⇒ 产出系统整个关闭 ⇒ **逐字节回到今天的轨迹**。这条本身要有一个断言。

**验收（R12 全套，一条都不能省）**
- **零扰动对照**：删掉 `production.json` ⇒ 金标**逐字节等于今天**。**这是最重要的一条**，它证明新机制是可 ablate 的。
- **开启后**：`Harness --bake-golden` + `DetGate --bake-golden` 都跑；`_meta.rebake_history` 补一条。
- **留出种子**：改前/改后各跑 `--seeds 13-30 --days 60`，**两侧数字都报**。硬不变量必须 18/18 全绿。
- **硬不变量 #01 不许破**：报**逐 seed 的饿穿计数**，不是"没看到饿穿"。
- **#34 金钱守恒不许破**：如果产出涉及买卖，`transfer` 仍是唯一通道。
- **新增至少一条不变量**守着新机制（例如"库存非负"、"产出/消耗账本可溯源"），并给它一个**负对照**证明它会红。
- **R9**：能证明的是"劳动现在改变世界状态"，**不是**"游戏更好玩了"。

### E2 · 把事件链讲成故事（View 侧，零仿真风险）

**独占新 `game/scripts/Story.gd` 与 `game/scripts/Main.gd`。不得动 `Sim.gd`。**

D2 的 `Goals.gd` 已经证明了这条路走得通：**对 `event_log` 的纯 fold，回放等价 30/30 seed**。
E2 是它的下一步——**目标是"还差几件"，故事是"发生了什么"**：把因果链
（邀约 → 爽约 → 积怨 → 对质 → 道歉 → 和解 / 或 → 冷战 → 泄密 → 背叛）识别出来，
**按人**讲成一段有开头结尾的短叙事，而不是按时间倒序刷屏。

**必须复用而不是重写**：`Sim._verb` 与编年史已有的中文措辞（docs/43 记过"用中文讲出来而不是打印枚举名"），
`Goals.gd` 的 fold 结构与 `chain` 见证做法。**红线同 D2**：只读派生，**绝不写世界状态**。

**验收**
- **回放等价**：`goto_tick(T)` 与 `goto_tick(T/2)` 之后从 `event_log` 重算的故事状态逐字段相同 + `chain` 见证。
  **12 调参 seed + 18 留出 seed**（D2 的做法，别自己发明）。
- **判别力**：给一个**故事必然为空**的对照（例如只喂 greet 的 fixture），证明这条断言不是平凡通过的
  ——D2 报告里明写"一个什么都不记录的 tracker 也能通过 `live == replay`"。
- 金标逐字节不动；`probe_digest_test.sh` 与改动前同值（**自己跑基线**）。
- **R10** 全帧眼验 + 多缩放档。

### E3 · 角色去道具 pass（原 D8）

**独占 `game/assets/art/` 与 `tools/` 下你新建的脚本。不得动 `game/scripts/*`。**

裁决与许可分析在 [docs/44 §一](44-art-direction.md)：现用 CC0 素材包是**奇幻 RPG 原型**（尖帽/法杖/弓箭/盔甲），
与咖啡馆-澡堂-工坊的现代小镇题材打架。**CC0 允许修改** ⇒ 这是一次**去道具 pass**（自绘衍生），
**不是**重做整套，也**不是** AI 出图（docs/43 R2：生成图只能当情绪板，绝不进精灵帧）。

**验收**
- **逐帧对照**：改前/改后精灵表并排 4× 放大，**每个居民每个朝向每一帧都要看**（R10；D3 的教训是"只看一个缩放档"）。
- **不许把轮廓改糊**：报**改动像素数**与**剪影（alpha 通道）变化率**——去掉尖帽会改剪影，这是有意的；
  但身体轮廓不该动。
- **在游戏里看**：`--shot` 一张跟随相机帧眼验（`shot1.sh` 默认会走 1.8× 特写，正好适合看人）。
- **许可**：产物是对 CC0 的修改 ⇒ 仍是 CC0/自绘。在 `game/assets/art/` 下留一份说明写清改了什么、源是什么。
- 金标逐字节不动（纯资产替换）。

---

## 三·五 ★ E1 回执——劳动第一次改变世界状态，金标**受控地**动了

**零扰动对照（最重要的一条，成立）**：删掉 `game/data/production.json` 后跑**重烘前**的金标：
S0 **金标一致 12/12**（含 12 条逐 tick 前缀链）、DetGate **金标 16/16**、
留出种子 13-30 × 60 天 **18/18 seed 的 digest / event_digest / chain / 事件数 / 饿穿数逐字段相同**。
**机制可 ablate 成立**——这正是 `jobs.json` / `economy.json` 已经验证好用的那套。

**留出种子 13-30 × 60 天，改前 / 改后**：

| | 改前 | 改后 |
|---|---|---|
| 饿穿（逐 seed） | **全 0** | **全 0** |
| 硬不变量 | 18/18 | **18/18** |
| 软失败 | 0 | **0** |
| 事件数 | 2138-2367 | 2349-2764 |
| digest / 逐 tick 链 | — | **18/18 全部移动**，首个分叉在第 1 天 |

**30 个种子 × 60 天，一次饿穿都没有。** 金标该动的三个理由（前两条是真轨迹移动，不只是多了日志）：
①缺货加价改 coin → 经 `poor_line` 的 `work_urgency` 改决策；②缺货信念与 standing 改社交接受；
③`event_log` 新增 produce/consume/spoil/shortage 四类账本事件。

**新不变量 + 正交负对照**（硬 #38 库存账本自洽、硬 #39 产出溯源到在班本职、软 #40 闭环活性）：

| 注入（20 天, seeds 13-14） | #38 | #39 | #01 |
|---|---|---|---|
| 干净树 | ✅ | ✅ | ✅ 0 |
| 绕过唯一通道直接写 `town_stock` | **❌** | ✅ | ✅ |
| 产出 `amount+1`（超申报批量） | ✅ | **❌** | ✅ |
| **缺货时阻断动作**（= 把库存做成生存门） | ✅ | ✅ | **❌ 饿穿 670010 / 785612** |

前两个正交注入各只点亮自己那条臂。**第三行是设计红线的对照**：
「缺货不得成为新的饿死通道」**有既有的硬门在守，不是口头纪律**。

**★ 批量是量出来的，不是拍的**：实测全镇「本职在班完成」只有 **41-47 次/60 天**，
而消费动作开用 **1745 次/60 天** ⇒ **劳动比消费稀约 40 倍**。1:1 会让镇子永远断粮、缺货退化成常数。
所以一次上工 = 一批（一炉/一网/一车），数值来源全写在 JSON 的 `_calibration` 里。

**两处只有做了才会发现的工程约束**：①**消耗按天入账**——逐次入账要往 `event_log` 塞约 1400 条流水，
而 `Main._rebuild_feed` 只回扫尾部 200 条 ⇒ 会把小镇纪事整块冲掉；同理 shortage 同日同货只报一次
（`MemoryStream` CAP=200，逐次记会把社交记忆整片挤出去）。
②`Invariants.gd:15` 的社交排除表必须补进四个新类型 ⇒ 否则 **#3「无永久孤立」会被"他吃过饭"喂饱**，
一个从不社交的居民也能过门。

**交接 E2**：`Main.SALIENCE` 未给新类型定级，未知类型默认 60 ≥ `SALIENT_MIN` 55
⇒ `produce/consume/spoil` 会挤进「镇上的大事」（60 天约 470/1915/463 条）。只有 `shortage`（66）是有戏的那条。
已通过 SendMessage 交给 E2。**未测**：真机、出图眼验（纯仿真层）；木匠/教书先生的货靠复用既有动作落地，
真正的"工地/学堂"要 E4 的地图配合。

## 四、Wave D 外部评审转过来的待办（**每一条都已复核，不是转述**）

全部证据在 [docs/46 §二·九](46-wave-d-plan.md)。这些**不是**新想法，是**已知的、被量过的缺陷**——
排进 Wave E 的第二轮，别让它们变成"下次再说"。

| # | 缺陷 | 归属 | 量级 |
|---|---|---|---|
| **W1** | **`Goals` 回放安全的适用范围比声称的窄。** `goals_test` 钉 `backend=null`，而 `Sim.goto_tick` 会强制 logic 重演 ⇒ 非 logic 后端下拖时间轴会**重新生成一段不同的历史**；玩家事件计入 `Goals` 却**不在重演里**；且**前向拖动不重置 `Goals`**（`Goals.sync` 只在日志**变短**时自重置），fold 会接进一段**前缀已经不同**的日志，`chain` 于是见证一个从未存在过的序列 | `Goals.gd` + `Main.gd` | **M** |
| **W2** | docs/44 §二 自己点名"夜间最危险"的 public↔workshop，**两对夜间 ΔE00 只有 1.23 / 1.35（低于 JND）**，而 §四·五 报的是**另外**两对 | `WorldView.gd` + docs/44 | **S** |
| **W3** | D6 自己造出**两对 ΔE00 = 0.00** 的房间地板（bed↔shop、parlor↔shop）——正是它声称避免了的那个失败模式 | `WorldView.gd` | **S** |
| **W4** | 那 33 对样本是**锚点对锚点**，**所有 alpha 合成的表面结构上都在样本之外**：关系线在夏草上实测 **0.52-1.29**（全部低于 JND），溢光环 2.37，路面棕罩 3.18 | `WorldView.gd` | **M** |
| **W5** | `BackendGate` 的闭集臂**在它跑的两条臂上都不可能变红**（`random` 与 `slm` 都只能返回 `cands` 里的元素）——它防的是"后端凭空造 dict"，而没有任何出货后端能做到 | `game/bench/BackendGate.gd` | **S** |
| **W6** | 三个 `_terrain_built`/`_paths_built`/`_decor_built` 缓存**从不重置**，`_grass_var` 按**第一个**世界的尺寸分配却用**当前** `w` 索引——今天地图尺寸恒定所以是潜伏的，`F8` 读档没有任何路径清它们 | `WorldView.gd` | **S** |
| **W7** | **没有人看过空间切换之后的任何一帧。** 真 bug 就活在那里，而 `--shot` 结构上看不见缓存陈旧（它只渲一帧、`auto_run=false`） | 工具链 | **M** |

> **W1 是这批里唯一可能推翻一条已发布结论的**（README 与 docs/46 都写了"回放安全 30/30 seed"）。
> 先量清适用范围，再决定是收窄措辞还是修代码——**别直接改措辞了事**。

## 三、后续（第一轮合并后再派）

- **E4 · 建筑与商贩的空间落点**：E1 的产出如果要有"店"和"工地"，需要地图与室内配合（`map.json`/`interiors.json`/`audit_map.py` 的门）。
- **E5 · 真机复验**：Wave C/D/E 累计的所有改动**从没在一台设备上一起验过**——音频、触屏、夜间光、
  57 色调色板、产出系统。**这是欠得最久的一笔。**
- **E6 · 阶段性产出**：新录屏（带演示相机 + 声音）、GIF、README 中英更新、docs/13 札记。
