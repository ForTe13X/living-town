# 43 · Wave C 路线图与分棒计划——从「研究仓库」转向「像个游戏」

⚠️ **`getbbox()` 在 RGBA 上默认只看 alpha（Pillow 11.3+），本条断言若不先 `convert("RGB")` 或传 `alpha_only=False` 就是空真的——见 docs/41 §6 的复核。**

**2026-07-26 立。** 本文是 Wave C 的**唯一权威计划**：路线图 §A 的更新在 §一，每一棒的 brief 在 §三。
派棒时**不要重抄 brief**——只给「读 docs/41（共同契约）+ 本文 §三-CN（你那一棒）」。
这条规矩本身来自 docs/41 §0 的写作动机：**抄第 N 遍就会漏第 N 条**。

---

## 〇、这一波的判据（一句话）

前 42 份文档回答的是「**这台机器对不对**」。现在有 23 条硬不变量、金标逐字节锚、
逐 tick 前缀链、观察无关门——**机器是对的**。而 2026-07-26 的现场眼验（`shot1.sh seed=3 day=5`）
拍出来的是：**一个信息面板占满右侧的调试视图**，不是游戏画面。

> **Wave C 的验收不是"CI 更绿"，是"把新录屏发给一个不认识这个项目的人，他会问'这是什么游戏'而不是'这是什么工具'"。**

---

## 一、路线图更新（docs/05 §A 的差量）

三份并行只读评审（产品面 / 视觉面 / 开放线程面）交叉核对后，**docs/05 §A 有 5 处已经过期**，
且**有两条最高杠杆的工作已经写完但没合进来**。以代码为准：

### 1.1 docs/05 §A 里已经不成立的行

| docs/05 原文 | 实测 |
|---|---|
| §A-NOW「让已仿真的戏剧变得可读……只 `_log_event` 从不 `emit_signal`」 | **已做**。五类全部 emit：betray `Sim.gd:1850`、conflict-born `:2027`、election `:2474`、pact dissolved `:3284`、pact formed `:3348` |
| §A-NEXT「`buildings.json`（现在 `buildings: []`）」 | **已填**：7 栋楼 / 12 间房（`b7780c8`） |
| §A-NEXT「灯笼节对象在 [12,7]，四次节日到场 0」 | **已移到 [31,24] 广场内**，到场 9.94 人/节日日（`3d38c0f`） |
| §A-NOW「`ZOOM_MIN 0.6` 意味着回到全镇只显示约 14%」 | **混了两个 bug**：14% 来自 `go_home()` 硬设 `zoom=1.0`；0.6 地板本身到 38.5%（docs/13:1064 已更正）。**但开局取景仍然是坏的**——`ProbeController.setup()` 从不调 `go_home()`/`fit_zoom()`，首帧约 13.9% 可见 |
| §A-NOW「把确定性钉在单一二进制之外」 | **已做**：金标 + 逐 tick 前缀链 + FNV-1a 自有哈希 + 稳定候选身份加盐，全在 `tools/ci.sh` step 4 |

### 1.2 两条已经写完、但没合进 `integration/batons` 的工作

**这是本轮成本最低、价值最高的两项，且读 docs/34-42 完全看不出来**（它们不是研究任务，是 merge 任务）：

| 分支 | 内容 | 为什么重要 |
|---|---|---|
| `claude/objective-lumiere-e6f125` | `game/bench/BackendGate.{gd,tscn}`(160行) + `Sim.gd`+108 + `tools/ci.sh`+13 + 一份 268 行文档 | **正是 docs/38 §八-2 / docs/41:41 声称"至今无门覆盖"的那道门**。docs/42 §7.3 明写：任何 prompt 修复**必须先有这道门**，否则又是一次"CI 全绿而产品已破" |
| `claude/stoic-shamir-e08cef` | `tools/audit_map.py`+54：节日对象的越界/压阻挡/压家具/**可达交互格**校验 | `festivals.json` 在 HEAD 上**零 CI 覆盖**。`471d3e3 merge(B17)` 合的是**另一个** B17（挪锚点），能抓住这次错放的那道**门从未落地** |

⚠️ **合并 lumiere 会撞文号**：它带一份 `docs/40-external-backend-invariant-gate.md`，而 HEAD 已有
`docs/40-device-n60-slm-the-shipping-intersection.md`。不是 git 冲突（文件名不同），但编号体系会静默破掉，
`lint_links.py` 抓不到。**必须改号为 [docs/45](45-external-backend-invariant-gate.md)**。

> **⚠️ 2026-08-02 · Z3 就地更正（只改引用形式，不改本节任何结论）**
> 本节原先三处把它写成 `docs/45@claude/…`（后缀是下面 §1.2 分支表里那条 lumiere 分支的全名）。
> **那个 `@分支` 后缀今天是错的，而且错两层**：
> ① 这份文档**已经并进 HEAD**（`45-external-backend-invariant-gate.md`，见下面 §1.2b「文号已改 45」）；
> ② 就算去那条分支上找也找不到——**它在那条分支上仍然叫 `docs/40-external-backend-invariant-gate.md`**
> （`git ls-tree claude/objective-lumiere-e6f125 -- docs` 实测，本节自己也是这么写的）。
> ⇒ 三处后缀去掉，改成指向 HEAD 的真链接；**分支名保留在本注记与 §1.2 的分支表里**，可追溯性不变。

### 1.2b ★ C0 回执（2026-07-26 当日）——两道门都已落地，且**上面 §1.2 有一句是错的**

两道门都合进来了（`33ca14d` / `8f8efcf` / `5561ab4`，文号已改 45），`tools/ci.sh` → **CI PASS**，
金标 12/12 + 逐 tick 前缀链 12 条 + DetGate 16/16 逐字节未动（lumiere 自称的"金标零扰动"**已被独立复验**：
两条新边界只长在 `backend != null` 分支里，金标路走 `else`）。新的 4d BackendGate：硬 8/8、两跑一致 8/8、无饿穿 8/8。

**负对照才是这一棒的产出**（一道从没红过的门，不算已知是门）：

| 负对照 | 硬不变量臂 | 确定性臂 | 饿穿臂 |
|---|---|---|---|
| 阳性（合入后） | 8/8 ✅ | 8/8 ✅ | 8/8 ✅ |
| 把 P2-3 否决短路掉（= lumiere 之前的树） | **0/8 ❌** | 8/8 ✅ | **0/8 ❌** |
| 给 `Sim._rng_at` 掺 `randi()`（破红线 #1） | 8/8 ✅ | **0/8 ❌** | 8/8 ✅ |

两个正交的注入各自只点亮**自己那条臂**——这比"门是绿的"强得多。确定性臂每个 seed 都精确报出 `首个分叉 tick_no=1`。

> **★ §1.2 那句「能抓住这次错放的那道门从未落地」是错的，撤回。**
> 按 §三-C0 的验收去做负对照——把灯笼节锚点挪回 `[12,7]`——**门是绿的（exit 0, AUDIT PASS）**。
> 原因写在 shamir 自己的 commit message 里：`[12,7]` 在 home2 屋内，经门 `[12,8]` 可达、不叠格，
> 且 `fest_` 对象按 `Sim.gd:2882` 不入导航网 ⇒ 三条规则全满足。
> **这道门抓的是「非法 / 够不着 / 叠格」，抓不到「合法但语义不对」（该在广场却在屋里）。**
> 真要拦住 B17 那次错放，得另加一条**区域归属**规则。C0 改用 8 个真正非法的构造证明了门不空转。
> 教训与 docs/41 §4 同源：**派棒者写进 brief 的那条"必红"的负对照，本身就是一个未经验证的断言。**

**两条新发现（C0 越出 brief 范围挖到的，均已复核）：**

1. **★ 熔断器一旦触发，换模型也救不回来，且零 CI 覆盖。**
   `AIBackend.gd` 里 `_slm_circuit_open` 只在声明处为 `false`、在 `:813` 被置 `true`，**全文件没有任何一处把它置回 `false`**。
   `_slm_fail_streak` 有两条清零路（`:329` 任何 worker 完成、`:800` 成功），但那只防止**再次**触发，不解除已触发的熔断。
   会话内永久回退是**故意的**（`:814` 的告警原文就写着"永久回退 logic 地板"，防 docs/34 那个原生泄漏 OOM）——
   **但 `set_model_path()`(:467) 与 `request_backend()`(:225) 也不复位它**，而设置面板里正有"SLM 模型 cycle"这一行，
   且 C3 正在把设置面板做成触屏可达。⇒ **用户熔断后去"换个模型试试"，得到的是无声的失败。**
   换一个权重文件是一个**全新的失败假设**，没有理由继承旧假设的熔断。已并入 §三-C6。
2. **被否决的 intent 不再进 S4 决策 trace。** lumiere 的新 `elif` 插在 `elif record_decisions` 之前 ⇒ 否决触发时
   `_record_decision` 不被调用。`s4_replay_test` 在 CI 里是绿的，但它跑 `backend=null` 路 ⇒ **这条没有任何门覆盖**。
   C0 只读出来、没测。**记为 Wave D 待查项**，别当成已知无害。

**另外三条（非结论，供后续避坑）**：①`claude/objective-sinoussi-2d8005` **先别删**——`breaker_test.{tscn,gd}` 只在它上面，
且**不能 cherry-pick**（它测的 `BREAKER_MAX_FAILS`/`breaker_tripped`/`_note_ai_fail` 在 HEAD 一个都不存在，
HEAD 走的是 `scale-diagnostic` 并集带来的另一套实现）。②**文号 44 现在悬空**——§三-C5 说"取下一个空闲文号"会取到 44、排在 45 前面，
可接受但要有意识。③`analysis/` 的去留：`phase-d-contract-hardening` 上的 `bench/bakeoff/phase_d_repro/` 是**更新且策展过**的那份
（晚 47 分钟，8 个同名文件逐字节相同），但两边互不为超集——**唯一没有副本的是 `gen_judge_wf.py` / `gen_combined_payload.py`**。
`.gitignore` 只挡了 `analysis/**/packet_*.jsonl`，**故意不挡整个 `analysis/`**（它在 `codex/repository-review-2026-07-12` 上是被跟踪路径）。

### 1.2c ★ C2 回执——小镇有声音了，**但"CI PASS"在这一棒上比别处弱**

`game/scripts/Audio.gd`（约 380 行）+ `project.godot` 的 `[autoload]` 一行，**没有任何音频文件入库**
（全部运行时数学合成为内存 `AudioStreamWAV` ⇒ 红线 #4 没有受力面）。内容：昼/夜环境床（交叉淡入）、脚步、UI 点击、
5 个挂在 `Sim.social_event` 上的事件音 + 1 个节日音。接线全在 `Audio.gd` 自己的 `_ready()` 里，**只读 `Sim`**。

**证据不是"RMS 非零"**（一条环境床就能过这个门）。它给了两把更硬的尺子：环境床的峰值有**解析上界**，
超过 2× 上界的 20ms 帧才算一次 onset；外加逐 cue 计数。5 段采集 5/5、9/9、22/22 窗口非零，
夜间那段 `{step:21, warm:5}` = 26，与检出的 26 次 onset **对得上**。负对照 `--no-audio`：**每一个采样点精确为 0**。
昼夜交叉淡入也不是自称——过零率 414.3/s(正午) vs 338.3/s(夜)，与两张床的基频（A2 110Hz / E2 82.4Hz）吻合。
仿真零影响是**直接量的**：同一路径 `--digest-at 520` 只切换那一行 autoload，两侧逐字节相同。

> **★ 这一棒最重要的产出是一条契约级发现，已写进 docs/41 §2：`--script` 模式不加载 autoload。**
> 而 S0 门(4)、LOD 门(4b)、DetGate(4c) **全部走 `--script`** ⇒ **autoload 里的代码在那张 12 seed × 60 天的网格上零覆盖**。
> 真正加载它的只有 step 3(`--import`) 与 step 5(6 个场景)。**这一棒的"CI PASS"证明的东西比字面上少**，
> 它自己另造了 `--digest-at` 的 autoload 逐字节 A/B 来补——这才是有效证据。

**这份 brief（§三-C2）有两处是错的，已确认：**
1. **「`tools/ci.sh` 在 `--audio-driver Dummy` 下」——`ci.sh` 里根本没有这个旗标**，它传 `--headless`（隐含哑驱动）。
   硬编码该旗标的是 `shot1.sh`/`shot_tick.sh`/`shot_scale.sh`/`chat-shoot.sh`/`observe-shoot.sh`/`probe_digest_test.sh`/
   `pan_test.sh`/`record-godot.sh` 这八个。**结论对、机制错**，同样的措辞错误已在 docs/41 §2 一并更正。
2. **「节日音挂 `Sim.social_event`」——挂不上。** 根本没有 festival 事件类型：`_update_festival()` 只发无节日语义的
   `world` spawn/despawn，`festival_active` 是日边界设的普通变量。只能挂 `day_changed` + 读 `Sim.festival_active`。

**一个由测量而非设计逼出来的修正**（值得记）：第一次调音后，40 秒/2000 tick 的采集里 `warm` 响了 51 次、
`resolve` **响了 0 次**。不是"没有和解发生"，是**和解被日常打招呼挤出了全局限流配额**——
**最稀有、最有戏的事件恰恰是被静音的那些**。现在 `resolve`/`bell`/`festive` 走单独的宽松配额。

**代价与未测**：启动一次性 +140ms（`_ready()` 里烘 9 个 PCM 缓冲；n=2、±90ms 噪声 ⇒ 读作"0.1-0.2s"）。
**没有真机数据**，外推 3-5× 是 0.4-0.7s；若真机启动画像里看得见，最小修法是把三个 `_build_*` 推离首帧
（照抄 `Main.gd` 已有的 SLM 探针延后）。`click` 与整条 `_input` 路径 headless 生不出输入事件 ⇒ **零覆盖**。
**执行者听不到声音**——上面每一条都是测量，不是审美判断。**Stride 4 必须有人真的戴上耳机听一遍。**

**Stride 4 要用的**：`tools/record-godot.sh` 现在录不到声音。改法（C2 按契约没动它）：保留 `--audio-driver Dummy`
（它仍在推进混音，headless 采集正是靠这个），在 `--` 后追加 `--audiocap /out/town_audio.wav <SECS+2>`，
去掉 `kill $GODOT_PID`（hook 自己退出），最后补偿脚本里那 3 秒 `sleep` 再混流：
`ffmpeg -i town_raw.mp4 -ss 3 -i town_audio.wav -c:v copy -c:a aac -shortest town.mp4`。**别装 PulseAudio**，容器里没有声卡也不需要。

### 1.2d ★ C1 + C3 回执——**这份计划的验收标准有三条是坏的，两条被执行者当场证伪**

两棒都 CI PASS、金标逐字节不动、`probe_digest_test.sh` 与改动前的树**数值完全相同**
（`520 / 2600875897 / 568220217582462501`——注意它们各自**跑了改动前的基线**来对比，而不是只看"门绿了"）。

**C1（`WorldView.gd` +277/−41，`project.godot` 仅 `[rendering]` 一行）**：
灰虚空 `#4d4d4d` **28.30% → 0.00%**，界内 diff bbox=`None`、界外非 `None`；
居民插值（`_render_pos` + `_process`，**剔除仍用 `_center()`**，`_rpos()` 只喂绘制）；
四季主色 before 春夏秋冬**全是 (133,166,67)、distinct 1/4** → after **distinct 4/4**，雨天 ≠ 晴天；
脚影改椭圆 + 3 圈衰减。

**C3（`Main.gd` 唯一修改 + 2 个新文件）**：昼夜量具修好（见下）；顶栏背板 `y=10` 行 ≥40% 亮度像素
**1112/1280 → 217/1280**（剩下的 217 就是白字本身）；开局取景整镇入画；触屏动作条 7 键；键位提示补 `Home`；
玩家模式进设置面板。新门 `player_touch_test.tscn` **31 项断言 + 负对照**（故意把第 4 个按钮接到 `greet`
→ `B·invite` 立刻红、可分辨动词数 7→6，跑完还原）。
> ⚠️ **上面这句在写下的那一刻就是假的：那道门当时并没有跑在 CI 里。** C3 自己**明写过**"它没有接进 `tools/ci.sh`"，
> 是**我在写这段回执时把那句话压掉了**，于是读起来像已落地。C8 在两轮之后查出 `ci.sh` 第 5 步的场景列表里根本没有它
> ⇒ **C3 的 31 条 + C8 的 13 条断言，一次都没在 CI 里跑过。** 已补进第 5 步。
> **教训与 docs/41 §4 同源：转述别人的报告时，「验证过了」和「有门守着」是两件事，压缩会把后者凭空伪造出来。**

> **★ 昼夜量具（R4-④）修好了，而且修在比 brief 更好的位置。**
> brief 说"在 `--shot` 分支里截图前施加一次"。C3 放在 `CanvasModulate` 建好处——理由成立：
> **白 modulate 同样污染普通启动的首帧和 `record-godot.sh` 的第一帧**，而 Stride 4 正要用录屏做 README GIF。
> 改前：`--warmup-tick 488`(00:48 夜) 与 `600`(12:00 昼) 主色**逐字节都是 (133,166,67)**。
> 改后夜帧 (56,78,53) = `GRASS × _daylight(8/240)` 逐字节吻合，而**正午帧改前改后 bbox=`None`**
> （tod=0.5 时乘子恰为白）——该动的动了，不该动的一个像素没动。

**这份计划里被证伪的三条验收标准（都是我写的）：**

1. **「四季两两 `getbbox() is not None`」没有牙齿**——它在**未改动的树上 6 对全过**（因为 agent 每天都在移动）。
   一个什么都没做的改动能拿满分。真正有判别力的是**主色（modal grass colour）**。
   *这正是 docs/41 §5「数字要有零假设才可解读」在视觉验收上的同一个病。*
2. **「采样 y=10 一整行，每个采样点亮度 < 40%」字面上不可能达成**——状态栏文字本身是 `#e6e9f2`（亮度 91.4%）
   且就落在那一行，任何背板都压不暗字形。改用**行均值 + ≥40% 像素计数**。
3. **「插值必须走 `tools/record-godot.sh` 验」不可达成**——容器软渲染实测只有 **5.3-10.8 帧/秒**，
   **低于 12.5 ticks/s 的仿真速率**，两个 tick 之间根本不存在帧。已作为盲区⑧写进 docs/41 §6。

**另外两处事实错误（我写的）：** ①§1.3 说"室内 63.1%"——**HEAD 上实测 0.000%**，`68fe0cb` 早已修好，
而本文 §三-C1 自己就是这么说的，**§1.3 与 §三 自相矛盾**。②"57.7%" 只对 `--shot-fit` 成立；
**默认跟随相机下是 0.00%**，且那 57.7% 拆开是 28.30% 裸露 + 25.17% 被半透 HUD 盖住。

**C3 顺手挖出三个 brief 没提的 bug（都已修）：**
①玩家模式下 `_status` 是两行文本而 `size.y` 恒为 28 ⇒ **第二行（游戏内唯一列出 7 个动词键位的地方）被裁到约 6px、完全不可读**；
②⚙ 设置面板**在加新行之前就已经装不下自己**——实测内容 454px、可用 404px ⇒ **「关闭」钮本来就在暗底板外面**
（量的时候先把自己设的 `custom_minimum_size` 归零，否则量到的是自证的地板）；
③`Sim.remove_player()` 不存在，关玩家模式只能同样走 `Sim.start_new(_seed)`。

**★ 两条跨棒实测，必须在合并后的树上眼验（C1 与 C3 并行，谁都没看过对方的结果）：**

1. **`CanvasModulate` 不染 `#4d4d4d`。** 夜帧里草地已经是 (56,78,53)，而灰虚空**仍然恰好是 (77,77,77)**
   ——它是 viewport 清屏色、不是 CanvasItem。⇒ **只设 `default_clear_color` 会得到一条对昼夜免疫的死色带。**
   真正跟着光照走的是 C1 的 `_draw_town_backdrop()`（CanvasItem）。**合并后要确认背板确实盖住了清屏色。**
2. **C3 的开局取景把 boot 帧的 `#4d4d4d` 占比从 0.0% 抬到 25.5%**（现在整镇入画）——
   **这条改动让灰虚空更显眼，不是更轻。** 两棒的收益在合并后才互相成立。

**一次事故，记在这里因为它会重演：** C1 为清一个卡住的探针跑了 `docker ps -q | head -5 | xargs docker kill`，
**杀掉 5 个容器，其中最多 4 个属于并行棒**（此后它改为只按名字杀自己的）。
⇒ **R6 应补一句：并行期只能按自己命名的容器杀，禁止按 `docker ps` 批量杀。**

### 1.2e ★ C6 回执——**"只改两个字符串"是错的，照做会种下一个比修掉的更大的 bug**

落地：`AIBackend.gd` + 新 `game/bench/ModelPathGate.{gd,tscn}`（已由主 session 接进 `tools/ci.sh` 第 4e 关）。
`_system_prompt()` 的字面示例 `如 3 或 A` → `一个大写字母`；编号字母表 `0-9/A-Z`(36) → `A-Z`(26)；
`_probe_once` 补上 `_cap_for_llm`（`Main.gd:480` 传的是未裁剪候选，超过 26 个时旧码会吐多字符标签、把探针 p50 抬高）。

> **★ 派棒者的错误：`_cap_for_llm` 在裁剪时返回的是【按 score 降序】的列表。**
> 把上限从 36 砍到 26，触发裁剪的决策点从 0.4% 涨到 **5.84%（15 倍）** ⇒
> 那 5.84% 的出货 prompt 会把**引擎自己的 argmax 顶到 A 号槽**。
> 这正是 docs/42 §9-4 判定 `bench/log_decisions.gd` **不可用于位置研究**的同一个混淆，
> 而且它会摧毁「index 0 有 74.84% 是吃饭/睡觉」这条性质——**那恰恰是本次要修的 bug 的另一半**。
> 实测样本：睡觉 从槽位 0/1 被挪到 G/H。修法：先按 score 选出 top-cap，**再还原枚举序**
> （比较器构造成全序，因此不依赖排序稳定性）。**不做这一步，C6 修掉一个 bug、种下一个更大的。**

**熔断复位的范围收得比 brief 更准。** 新 `_reset_slm_circuit()` 由 `set_model_path()` **与 `set_slm_use_gpu()`** 调用。
后者是 §1.2b 漏掉的第三个调用点，而且**是更强的一例**：docs/34 的挂死是 Adreno **GPU** 特有的，
"改用 CPU"正是那份文档自己给的自救处方——**照文档自救的用户，恰好撞上无声失败**。
`request_backend()` **故意不复位**：它不改变任何失败假设，且是设置面板里用户反复点的那一行，
在那里复位等于给 docs/34 的泄漏上限开一个"切走再切回"的万能后门。复位同时清 `_slm_fail_streak`——
否则新模型只得到 1 次重试而不是 `SLM_CIRCUIT_TRIP`=6 次。

**负对照做得很干净**：`ModelPathGate` 把期望值硬编码、不引用任何新符号，所以**能在改动前的树上原样跑**——
**改前 9✅/7❌，改后 17✅/0❌**。关键是那 7 条红里**不含**「熔断确实打开了」与「`request_backend()` 之后仍然是开的」
——这两条改前就是绿的 ⇒ **先开后关的次序被证明了，没改的那条路保持开也被证明了**。
prompt 逐字节 diff（PickCtxDump，5290 行，与 docs/42 同一数据集）：决策点对齐 0/5290 失配；
未被新上限触及的 4981 行候选逐字节相同、prompt 只差编号字符；新触发裁剪的 211 行，after 列表是 before 的**保序子序列**。
`random` 臂的 digest **确实动了**——这是预期且被量到的（5.8% 的决策点 `|C|>26`），不是假设。

**C6 明写的未测**：**全程没有跑过任何模型**——"维生动作占比 4.8% → ~20%"是 docs/42 的引用，**不是本轮复现**。
更要紧的是：**出货的这个组合从没被评测过**。docs/42 把 `noex` 与 `letters` 当**两条独立臂**测，没有任何一臂是两者叠加；
且合法率 `nat` 85.1% vs `nat_letters` 79.1%，而那条 letters 臂**还带着如今已非法的数字示例** ⇒ **组合效果两个方向都可能**。
另：`bench/log_decisions.gd:58` 与 `bench/dump_decide_prompt.gd:58` 各自留着**发数字的 `_idx_label` 私有副本**，
`tools/b21_pick_eval.py` 的 `--labels digits` 现已过期——**下一个跑那条研究流水线的人会踩到**。

---

### 1.2f ★ 合并树验证（R8）——两条跨棒担忧，一条被证伪、一条成立

`integration/batons` 已合入 C1∪C2∪C3（`85ee925`）与 C6，**全量 `tools/ci.sh` → CI PASS**
（金标 12/12 逐字节 + 逐 tick 前缀链、DetGate 16/16、4d BackendGate 8/8/8、4e ModelPathGate、6 个场景）。

**① C3 警告的"对昼夜免疫的死色带"——在合并树上不成立，已实测证伪。** 用 `--warmup-tick 488/600` + `--shot-fit` 拍同取景两帧：

| 采样点 | 正午 | 夜 | 比值 |
|---|---|---|---|
| 界外环 左(150,400) | (11,22,11) | (4,10,9) | 0.36 / 0.46 / 0.82 |
| 界外环 下(600,700) | (15,30,17) | (6,14,13) | 0.40 / 0.47 / 0.77 |
| 界内草地 | (134,173,79) | (57,82,63) | 0.425 / 0.474 / 0.797 |
| 水 | (31,161,175) | (13,76,140) | 0.42 / 0.47 / 0.80 |

**界外环的比值与界内草地一致，也与 `_daylight` 的 (0.4242, 0.4714, 0.7972) 一致** ⇒
`_draw_town_backdrop()` 是 CanvasItem，**跟着光照走**。C3 的担忧只对**裸露的清屏色**成立，
而 C1 的背板把它盖住了 ⇒ 两棒的收益在合并后**互相成立**。
（正午/夜两帧只有 **1.3%** 的像素逐字节相同——那 1.3% 是 HUD，它在独立 CanvasLayer 上，本来就不该被染。）

**② 但眼验暴露了一个新的头号视觉问题，两棒各自都看不到它：**
小镇现在是一块**亮绿色矩形，硬边**贴在暗色森林环上——读起来像「深色地板上铺了一张绿地毯」。
灰虚空杀掉了，**接缝取代了它成为最刺眼的东西**。
修法**不是**再调背板色，而是 docs/44 §四 的**第一层（2-3 格边界延续）**：
道路收口、排水沟、矮石墙、草坡或河岸，让地图不像被剪刀截断。**记为 Stride 3 的第一项。**

### 1.3 新的 NOW（Wave C）

按 (玩家可感知增量) / (工作量 × 风险) 排序。**前四条全是 View 层，碰不到红线 #1。**

1. **手机上能玩而不只是能看**——7 个玩家动词全部锁在 `--player` **启动旗标 + 物理键盘**后面
   （`Main.gd:131`、`:1284-1294`）。出货目标是 Android APK，**两样都没有**。设置面板 7 行里没有"玩家模式"。
   引擎侧早已完整且被 CI 守着（`Sim.player_act/_move/_mediate`、`player_agency_test.tscn`）。
   **在真出货平台上，Living Town 现在是一块屏保。**
2. **第一眼像个游戏**——①开局取景约 13.9%；②全镇视角 **57.7%** 的画面是 Godot 未改的默认清屏色
   `#4d4d4d`（室内 63.1%），小镇是灰色虚空里的一座孤岛；③居民**每秒瞬移 12.5 次 48 像素**
   （`_center()` 直接返回格心，无插值）；④顶栏状态条是全屏最密的文字，**唯独没有背板**。
3. **给小镇声音**——全仓库 `grep` 不到一个 `AudioStream`。一个 19.2 秒走完昼夜的镇子是全哑的。
4. **仿真了却看不见的东西**——四季与天气每天边界都在算（`Sim.gd:1076-1078`）并进效用乘子，
   而 `WorldView.gd` 里 `season|weather` **零命中**：四季渲染逐像素相同。21 个 `social_event` 发射点
   只有 10 个表情图标。
5. **模型路径的两个字符串**（docs/42 §7.3）——`如 3` 字面示例 + `0-9` 编号字母表，
   直接导致 **1.5B 只有 4.8% 的选择是吃饭或睡觉**（随机 18.5%、引擎 36.9%）→ 硬不变量 #01 在模型路径上 8/8 seed 破。
   **零训练、改两个字符串**，但**必须等 §1.2 那道门先落地**。

### 1.4 明确推到 Wave D 的（不是忘了，是排序）

- **单局形状与目标**（docs/01 §3.2 承诺的社会成就；`grep quest|achievement|tutorial` → 0 命中）。
  这是**最大的**缺口，但它是 L 且需要设计。**必须做成对 `event_log` 的只读派生**（与 `ProbeController` 同一纪律），
  否则会污染仿真。留到画面与手感立住之后。
- **真机 3B 代价实测**（docs/42 §8-10：时延/落地率/FPS/电量/Native Heap 一个都没量）。设备独占，与任何真机棒互斥。
- **黑边 24.7%**（`aspect=keep`）——`bd17bed` 试过 `expand`，真机黑屏，`3557c26` 只回滚了旗标。
  这是 Adreno + `gl_compatibility` 的驱动排查，不是布局活；且**只能用 adb screencap 验**（docs/41 §6 盲区③）。
- 教师上界 / crisis 档覆盖 / 世界级反事实（docs/42 §8）。

---

## 二、Wave C 共同规则（docs/41 的**增补**，不替代）

docs/41 全文仍然逐条生效。本节只加这一波特有的六条。

- **R1 · `game/project.godot` 按节分权。** 它是唯一被多棒共享的文件。
  **C1 只能改 `[rendering]` 段，C2 只能改 `[autoload]` 段**，谁都不许碰 `[display]`（黑边归 Wave D）。
  改完在报告里贴出你新增的**整行**。
- **R2 · AI 生成的图片不是美术资产。** 红线 #4 要求 CC0 或自绘，**AI 出图两者都不是**。
  仓库唯一的例外是 `docs/media/cover.png`，而 docs/09 已标注它需要在商用前重审条款。
  docs/12 A-art-5 早有裁决：GPT 产出**仅对话框头像、绝不进精灵帧**。
  **本波的硬规矩：生成图只能当情绪板 / 配色参考，产出物是 `.gpl` 调色板和文字，图片不入库。**
  新的游戏内美术一律走 `tools/fetch_assets.py` + 附 `LICENSE.txt` 的 CC0 源。
  已点名禁用：LimeZu Modern Interiors（非 CC0）、Bonsaiheldin Interior（CC-BY-SA 传染）、2DPIXX（CC-BY）。
- **R3 · 证据落到固定目录，供后续拼片用。** 每棒把改前/改后 PNG 写进
  `<scratchpad>/wave_c/<棒号>/`，命名 `before_<场景>.png` / `after_<场景>.png`，
  并在报告里**逐张写出 `im.size`**（docs/41 §6 盲区③：画幅问题只体现在尺寸上）。
  Stride 4 要靠这批图拼录屏与 README，**不重跑**。
- **R4 · 契约 §6 的工具链盲区补两条**（本波实测新增，已同步进 docs/41 §6）：
  **④ `--shot` 永远渲不出昼夜**——`_modulate` 建出来是白的（`Main.gd:223`），
  `_daylight` 只在 `_on_tick`/`_after_jump`/`_after_load` 里施加，而 `--shot` 把 `auto_run=false`（`:245`），
  三条路一条都不走。**证据**：`town_integrated.png`（HUD 写 `00:38 夜`）与 `shot-town-roads.png`（`12:00 昼`）
  的主草地色**逐字节都是 (133,166,67)**。
  **⑤ `--shot` 恒显示 `第N天 00:48 夜`，`--speed` 不推进时钟**（`b7780c8` 记录）。
- **R5 · 纯 View 棒的双向断言。** ①`tools/ci.sh` 金标逐字节不动；②`tools/probe_digest_test.sh` 绿；
  ③对**未受影响的区域**做 `ImageChops.difference(...).getbbox()`——**该动的地方 bbox 非 None，不该动的地方 bbox 必须是 None**。
  「看起来一样」不是证据。
- **R6 · 并发纪律。** 本波多棒同时在独立 worktree 里跑。**完整 `tools/ci.sh` 全程只跑一次**（收尾时），
  迭代期用定向检查。跑之前先清场游离的 `godot.exe`（docs/41 §1），并断言每个输出文件只有一段汇总。
  **⚠️ 并行期"清场"这条要反过来读**（C1 实测事故 + C6 的正确处置）：
  **禁止 `docker ps -q | xargs docker kill` 这类批量杀**——C1 这么做过一次，**杀掉 5 个容器、其中最多 4 个属于并行棒**。
  **只能给自己的容器 `--name` 再按名字杀。** 同理 `godot.exe`：C6 开工时看到别人的 `player_touch_test` 与 Harness 在跑，
  **正确的做法是不杀**，改用唯一输出路径 + 断言每个输出文件只有一段汇总。
  **⚠️ 这条对【进程】同样成立，而且已经被违反过第二次**：D1 跑了几次
  `Get-Process -Name "Godot*" | Stop-Process -Force`——**按名字通配杀，等于杀掉本机所有仓库的所有 Godot**
  （实测同一时刻本机还跑着另一个项目的 gut 测试）。**要杀只能按 PID，且那个 PID 必须是你自己启动并记下来的。**
- **R7 · worktree 不会自动同步到派棒基线。**（2026-07-26 实测，C0 与 C2 各独立踩到一次）
  新建的 worktree checkout 可能停在**派棒基线之前**——C2 拿到的是 `38ba4a7`，那棵树上**本文根本不存在**。
  ⇒ **每根棒起手第一件事**：`git log --oneline -3` 对照 brief 里写的基线 SHA，落后就先确认是祖先再 ff。
  这正是 docs/41 §1 记的那个坑，只是这一波它来自派棒机制本身而不是人。
- **R8 · 合并要验在合并后的树上。**（journal 第二波方法论第 4 条）各棒的 CI 是在**各自的基线**上跑的；
  Wave C 的棒基线不同（C0/C6 在 `ef92d53`，C1/C2/C3 在 `fdb212f`）。
  ⇒ **合并后必须再跑一次全量 CI**，不能拿"每根棒自己都绿过"当合并树的证据。

---

## 三、分棒（disjoint by file）

**文件所有权表**——越界前必须先在报告里说明（docs/41 §1）：

| 棒 | 独占文件 | 共享（按 R1 分权） |
|---|---|---|
| **C0** | `tools/audit_map.py`、`game/bench/BackendGate.*`、`.gitignore`、[docs/45](45-external-backend-invariant-gate.md)（当时在 `claude/objective-lumiere-e6f125` 上、且还叫 40 号） | `Sim.gd`（仅接受 lumiere 的既有 diff）、`tools/ci.sh` |
| **C1** | `game/scripts/WorldView.gd` | `project.godot` **仅 `[rendering]`** |
| **C2** | `game/scripts/Audio.gd`（新建） | `project.godot` **仅 `[autoload]`** |
| **C3** | `game/scripts/Main.gd` | — |
| **C5** | `game/assets/art/palette.gpl`（新建）、新建的美术方向文档（本波产出，落盘时取下一个空闲文号） | — |
| **C6**（Stride 2，blocked on C0） | `game/scripts/AIBackend.gd` | — |

> `Sim.gd`、`golden_digests.json`、`Invariants.gd`、`Harness.gd` **本波任何棒都不得主动改**。
> C0 是唯一例外，且它只是把一份已经写好并自称"金标零扰动"的 diff 接进来——**必须自己复验这句话**。

---

### C0 · 把两道已经写完的门接回来（**先做，解除 C6 的阻塞**）

**为什么**：见 §1.2。这不是研究任务，是 merge + 复验。**读 docs/34-42 完全看不出这两件东西已经存在**——
任何只基于文档排的优先级都会把它们低估一个数量级。

**做什么**
1. `claude/objective-lumiere-e6f125`（1 commit `6e2ba78`）合进来。**把它带的 `docs/40-external-backend-invariant-gate.md`
   改号为 [docs/45](45-external-backend-invariant-gate.md)（合入时改名为 45 号）**，并修好所有指向它的链接。
2. `claude/stoic-shamir-e08cef`（1 commit `b93d15f`）合进来（`tools/audit_map.py` 的节日门，纯工具、不碰仿真）。
3. `.gitignore` 补 `__pycache__/`、`*.pyc`。
4. **不要**提交 `analysis/`——它 723 MB，其中 `phase_d/packet_c30.jsonl` 638 MB + `packet_smoke.jsonl` 84 MB
   是 2026-07-19 的 LLM-judge 负载。给 `.gitignore` 加 `analysis/**/packet_*.jsonl`。
   `analysis/phase_d/` 其余 ~1.3 MB 与 `phase-d-contract-hardening` 分支上的 `bench/bakeoff/phase_d_repro/` 近乎重复——
   **在报告里说清哪份更新，别自己决定删**。⚠️ `analysis/` 在 `codex/repository-review-2026-07-12` 上是**被跟踪路径**。
5. `claude/objective-sinoussi-2d8005` 的内容**已经**通过 `scale-diagnostic 5707247` 的三方并集进了 HEAD
   （`SLM_CIRCUIT_TRIP`/`PROBE_TIMEOUT_MS`/`_slm_reload_pending`/`_slm_log` 都在）——
   但**先花 30 秒确认 `game/scenes/breaker_test.tscn` / `game/scripts/breaker_test.gd` 是否被漏下**，再谈删分支。

**验收**
- `GODOT=C:/Users/yp/.local/bin/godot bash tools/ci.sh` → `CI PASS`，且**金标逐字节未动**
  （lumiere 自称"金标零扰动"，`Sim.gd` 却 +108 行——**这句话必须由你复验，不能采信**）。
- 新的 BackendGate 步骤**真的在跑**：给它一个**故意失败**的负对照（例如临时把某条硬不变量的阈值调到必红），
  证明这道门不是空转。跑完还原。
- `tools/audit_map.py` 的节日门同样要负对照：把 `festivals.json` 的锚点临时挪回 `[12,7]`，门必须变红。跑完还原。
- 报告里给出：两个负对照的输出、`git log --oneline -3`、新文号的链接全绿（`tools/lint_links.py`）。

---

### C1 · 第一眼像个游戏（世界层）

**独占 `game/scripts/WorldView.gd`；`project.godot` 只许动 `[rendering]`。**

四件事，按杠杆排序：

1. **杀掉灰色虚空。** 全镇视角 **57.7%**、室内 **63.1%** 的画面是 `#4d4d4d`（Godot 未设 `default_clear_color`）。
   室内早已有解（`_draw_interior_backdrop`，`WorldView.gd:533-554`，`68fe0cb` 落地），
   **小镇分支没有对应物**——草地被钳在 `[0,w)×[0,h)`（`:771-774`）。
   加 `[rendering] default_clear_color` + 一个 `_draw_town_backdrop()`（界外的暗色林/水环 + 暗角），
   在 `_draw()` 的草地循环之前调。
2. **给居民插值。** `_center()`（`:987-989`）直接返回格心，而 `Sim.tick_interval=0.08` ⇒
   **每秒瞬移 12.5 次、每次 48 像素**。在 `WorldView` 内维护 `_render_pos: {id → Vector2}`，
   在 `_process(delta)`（**渲染时钟**）里向 `_center(ag)` lerp，`queue_redraw()`；
   agent 绘制、关系连线、交谈连线统一改用 `_render_pos`。
   ⚠️ **只读 Sim，永不写 Sim**；`_render_pos` 不得参与任何喂给绘制之外的剔除（`WorldView.gd:22` 的 LOD 红线）。
3. **让四季与天气看得见。** `Sim.weather_today` / `season_today` 每天边界都在算并进效用乘子（`Sim.gd:2305`、`:2322`），
   而 `WorldView.gd` 里这两个词**零命中**。加季节色调（草地变体选择 `:138-142` 附近）+ 一层降水叠加。
4. **软化脚下阴影。** `:1157` 的 `draw_circle(feet, T*0.22, α=.25)` 几乎和精灵一样宽，读起来像人浮在一个圆盘上。
   压成椭圆、缩到约 `0.15*T`、加 2-3 圈 alpha 衰减。

**验收**（严格按 R5 的双向断言）
- 灰虚空：`shot1.sh` 在 1280×768 **与** `--shot-fit` 1920×1152 各拍一张。
  断言 `#4d4d4d` 占比从 ~58% 掉到 <5%；**`im.size` 不变**；`ImageChops.difference` 的 bbox
  **必须完全落在地图矩形之外**——界内世界要逐像素相同。
- 插值：**静帧拍不出来**，必须走 `tools/record-godot.sh`。同时：因为 `--shot` 会 `auto_run=false`（`Main.gd:245`），
  lerp 会收敛 ⇒ 冻结 tick 的 `--shot` 前后 diff bbox **必须是 `None`**。这是这条改动的关键守门。
- 四季：`--warmup` 取四个分别落在春/夏/秋/冬的天数 + 一个雨天，两两 `getbbox() is not None`（必须不同）。
- 全程：`tools/ci.sh` 金标逐字节不动、`game/bench/lod_verify.gd` 绿、`tools/probe_digest_test.sh` 绿。
  **纯 View 改动移动了 digest = 你改到仿真了，回滚查因。**

---

### C2 · 给小镇声音

**新建 `game/scripts/Audio.gd`；`project.godot` 只许动 `[autoload]`。不得修改 `Main.gd` / `WorldView.gd` / `Sim.gd`。**

全仓库 `grep -rn "AudioStream" game --include=*.gd` → **0 命中**。一个 19.2 秒走完完整昼夜的镇子是全哑的。

**做什么**：数学合成（`AudioStreamGenerator` / 程序化 `AudioStreamWAV`），**零外部音频文件、零许可风险**。
`godot-game-pipeline` 技能里有现成的合成 SFX/BGM 配方与 headless 采集配方，**先读它再动手**（红线 #5 复用优先）。
最小可用集：环境底噪（昼/夜两层交叉淡入）、脚步、UI 点击、以及 3-5 个挂在 `Sim.social_event` 上的事件音
（问候 / 冲突 / 和解 / 节日 / 选举）。

**接线方式很关键**：`Audio.gd` 作为 autoload 在自己的 `_ready()` 里直接连 `Sim` 的信号
（`social_event`、`day_changed`）。**这样它不需要碰任何别人的文件**——这是它能与 C1/C3 并行的全部原因。

**验收**
- headless 采集（`AudioEffectRecord`，配方见技能）：断言 ≥3 个**互不相交**的 2 秒窗内 RMS 非零。
- `tools/ci.sh` 在 `--audio-driver Dummy` 下**必须仍然全绿且不出现任何新的 ERROR 行**——
  注意 `ci.sh` 的 `scan()`（`:30-36`）会抓 `push_error`/`SCRIPT ERROR`，**因为 GDScript 的退出码抓不到它们**。
  哑驱动下建生成器是最容易出这类静默错误的地方。
- 金标逐字节不动。
- ⚠️ `tools/record-godot.sh` 现在写死 `--audio-driver Dummy`——**录屏是没有声音的**。
  你**不要**改那个脚本（Stride 4 会做），但在报告里写清需要怎么改才能录到声音。

---

### C3 · 手机上能玩（玩家外壳）

**独占 `game/scripts/Main.gd`。不得修改 `Sim.gd`（引擎侧早已完整且被 CI 守着）。**

1. **玩家模式搬进设置面板。** 现在要 `--player` 启动旗标（`:131`）。设置面板（`_build_settings`，`:503-585`）
   加一行开关。⚠️ **打开它会合法地移动 digest**（它调 `Sim.add_player()`）——这是**受控动作**，
   走 docs/41 §3 的流程；但**关着的时候 digest 必须逐字节等于今天的金标**。
2. **触屏动作条。** 7 个玩家动词（greet/give/gossip/invite/confront/apologize/mediate，`:1284-1294`）
   现在只有物理键。加一排屏幕按钮，走 `_player_do`（`:1047`）**同一条路径**。
3. **顶栏状态条补背板。** `:270` 是全屏最密的文字却**唯独没有** `_mk_panel`（对比：日志 `:285`、观察 `:295`、
   滚动条 `:301` 都有）。实测device图 y=14 行草地 (133,166,67) 与浅墙 (216,189,147) 直接透上来。
   `_scrub_hint` 当初正是为这个原因补的板，**状态条被漏了**。
4. **开局取景。** `ProbeController.setup()`（`:45-52`）从不调 `go_home()`（`:114-121`）⇒ 首帧只看得见约 13.9% 的地图。
   在 `Main.gd` 的 `setup` 之后调一次。
5. **键位提示** `:321` 只列了约 25 个绑定里的 6 个，**恰好漏掉 `Home`**（那个能修好取景的键）。
6. **顺手修 R4-④：`--shot` 渲不出昼夜。** `--shot` 分支（`:244-263`）里在截图前施加一次 `_daylight`。
   这条**必须做**——它是整个项目所有视觉判断的量具，现在这把尺子是坏的。

**验收**
- **按钮路径 ≡ 按键路径**：新建一个 headless 断言，对 7 个动词逐一验证按钮触发与 `Sim.player_act(verb, target)`
  在同一 fixture 上返回**同一个字符串**。
- **画幅**：`shot1.sh` 在 1280×768 **和** 2688×1216 各拍一张，用 PIL 断言 7 个按钮**全部落在 `im.size` 之内**
  （docs/41 §6 盲区③：只看画面会漏掉画幅问题）。
- **关着的时候 digest 不动**：`tools/probe_digest_test.sh` 与 `tools/ci.sh` 金标逐字节等于今天。
- **昼夜量具修好了**：`shot_tick.sh` 在一个夜 tick 与一个正午 tick 各拍一张，**主草地色必须不同**，
  且夜那张 ≈ `(133,166,67) × _daylight(night)`。把这条做成可复跑的脚本片段留在报告里——Stride 4 要把它变成 CI 里第一条视觉断言。
- 状态条：采样 y=10 一整行，每个采样点亮度 < 40%；diff bbox **完全落在** HUD 面板矩形内，世界区域必须没动。

---

### C5 · 美术方向与调色板（由主 session 驱动，不派棒）

**产出物是 `game/assets/art/palette.gpl` + 一份新的美术方向文档（落盘时取下一个空闲文号）。图片不入库（R2）。**

要解决的是一个**已经在打架的**问题：vendored 的 CC0 精灵包是奇幻 RPG 原型
（`Mage-Red` / `Archer-Green` / `Warrior-Blue` / `Soldier-*`）——**戴尖帽拿法杖的法师**走在咖啡馆、澡堂、工坊之间；
而 `docs/media/cover.png` 承诺的是一座 3/4 透视的黄昏村庄，`docs/16:44` 明确 **SKIP** 伪等距。
**封面承诺了一个永远不会被造出来的游戏。**

同时 `docs/12` §3 的 **A-art-0（32-48 色 `palette.gpl`）至今 OPEN**——
现在每个颜色都是散在 `WorldView.gd`（`BLD_PAL:119`、`_mat_wall:1228`、`_mat_floor:1244`、`_draw_room_decor:1349-1353`）
和 `Art.gd:7-15` 里的硬编码十六进制字面量。

**怎么做**：用 Chrome 里的 GPT 出图当**情绪板**（只作设计输入），定下「温暖现代小镇」的色域与建筑语汇，
再把它落成一份 `.gpl`。**生成的图留在 scratchpad，不进仓库、不进精灵帧。**

---

### C6 · 模型路径：两个字符串 + 一个救不回来的熔断器（**C0 已落地，解除阻塞**）

**独占 `game/scripts/AIBackend.gd`。** C0 已把 BackendGate 接进 `tools/ci.sh` 第 4d 关（硬 8/8、两跑一致 8/8、无饿穿 8/8），
docs/42 §7.3 要求的前置条件**已满足**。两件事，都在同一个文件里，所以合成一棒。

**C6-a · docs/42 §7.3 的三条改法**（原文在那里，不重抄）。预期收益必须说实话：
这三条**买不到判断力**（八种编码下名次恒在 0.46-0.50 = 随机），
它们买到的是**去掉"系统性跳过吃饭睡觉"这个 bug**——1.5B 在出货 prompt 下只有 **4.8%** 的选择是吃饭或睡觉，
随机是 18.5%、引擎是 36.9%。**这是修 bug，不是加能力。**
注意第 2 条（编号字母表 `0-9/A-Z` → `A-Z`）会把可编号候选上限从 36 降到 26，`_cap_for_llm` 的 36 要跟着改——
这是**会移动模型路径行为**的改动，正是 4d 那道门存在的理由。

**C6-b · 熔断器复位**（新，见 §1.2b 发现 1）。`_slm_circuit_open` 全文件只被置 `true`（`:813`）、从不被置回 `false`。
会话内永久熔断是**故意的**（防 docs/34 的原生泄漏 OOM，`:814` 告警原文就这么写），**这一条不要动**。
要动的是：**`set_model_path()`(:467) 应当复位熔断**——换一个权重文件是一个全新的失败假设，
没有理由继承旧假设的熔断；否则用户熔断后从设置面板"换个模型试试"会得到**无声的失败**，
而 C3 正在把那个面板做成触屏可达。`request_backend()`(:225) 是否也该复位，**由你判断并给出理由**
（论据两边都有：切走再切回像是"重试"，但它并没有改变任何失败假设）。

**验收**
- `tools/ci.sh` → CI PASS，**金标逐字节不动**（这两处都在模型路径上，金标路 `backend=null` 根本不进 ⇒
  金标不动是**必要不充分**条件，docs/41 §2）。真正的证据是下面两条。
- **C6-a**：用 `game/bench/PickCtxDump.{gd,tscn}` 导出改前/改后的出货 prompt，逐字节 diff，证明改的正是那两个字符串
  且**没有连带改到别的**。`PickCtxDump` 直接调 `AIBackend` 的真函数（`_system_prompt`/`_cap_for_llm`/`build_prompt`），
  零重实现——**不要**用 `bench/log_decisions.gd`（它的 `_cap_order()` 会按 score 重排候选，对位置类问题会给出相反结论，
  docs/42 §9-4）也**不要**信仓库里那份 `bench/distill_contexts.jsonl`（是过期的老 JSON-schema prompt）。
- **C6-b**：写一个能证伪的对照——构造连续 ≥`SLM_CIRCUIT_TRIP` 次失败把熔断打开，断言
  ①熔断确实开了、②`set_model_path()` 之后它是关的、③在你**不改**的那条路上它仍然是开的。
  三条缺一条都说明你测的不是这个东西。
- 报告里写清：`_slm_circuit_open` 的每一处读点（`:747` / `:1085` / `:1126`）在复位后的行为是否都符合预期。

---

### C7 · 杀掉「深色地板上的绿地毯」（Stride 3，世界层第二轮）

**独占 `game/scripts/WorldView.gd`。不得动 `project.godot`（C1 已设好清屏色）、不得动 `Main.gd`。**

C1 把灰虚空从 28.30% 降到 0.00%，**但接缝取代它成了画面上最刺眼的东西**（§1.2f-②）：
小镇是一块**亮绿色矩形、硬边**贴在暗色森林环上。修法**不是**再调背板色，而是 docs/44 §四 的**第一层**：

> 紧贴地图边缘 2-3 格，继续画少量道路收口、排水沟、矮石墙、草坡或河岸，**不让地图像被剪刀截断**。
> 硬质边界用 `#9B968D` / `#6D6A61`，草坡用 `#5F7B34`。
> **不放门、路标或完整建筑**——那会暗示界外可以进入。

第二层（4-8 格低细节环境带，主体 `#585C64`、深轮廓 `#3F4B50`，**只保留两级明度**）可一并做，
但**第一层优先**：它才是解决"硬边"的那一层。

**验收**
- 用 `--warmup-tick 488`(夜) 与 `600`(正午) + `--shot-fit` 各拍改前/改后（**别用 `shot1.sh` 默认参数**，见 docs/41 §6-⑦）。
- **量接缝，不要只看**：沿地图矩形边界法向取一条 ~16px 的剖线，报改前/改后的**最大相邻像素亮度跃变**。
  它必须显著下降——这是这一棒唯一有判别力的数字，"看起来柔和了"不算。
- **界内必须逐像素不动**：`ImageChops.difference(...).getbbox()` 限制在地图矩形**之内**必须是 `None`
  （地图矩形的正确算法见 docs/41 §6-⑥，别按窗口尺寸算；`scratchpad/wave_c/C1/diff.py` 里已烘好）。
- 新画的界外层**必须跟着昼夜走**（它是 CanvasItem）——按 §1.2f-① 的方法取样比值，应落在 `_daylight` 的
  (0.4242, 0.4714, 0.7972) 附近。**只要有一层是免疫的，就说明你画到清屏色上去了。**
- `tools/ci.sh` 金标逐字节不动 + `tools/probe_digest_test.sh` 与改动前的树数值相同。

### C4 · 让工具链停止说谎（Stride 3/4，纯工具）

**独占 `tools/`（`ci.sh` 除新增一步外不得改动既有步骤）与 `docs/media/`。不得动 `game/` 下任何文件。**

三件事，都是为 Stride 4 的阶段性产出铺路：

1. **把昼夜断言变成 `tools/ci.sh` 的第一条视觉门。** C3 已经留下可复跑片段与
   `scratchpad/wave_c/C3/assert_daynight.py`（自带同源色停表现算期望值，不是魔法数字；三条断言含"两帧必须不同"
   以挡住"两边一起错成同一个值"的空过）。把它收进 `tools/`，接进 CI。
   ⚠️ 它需要 Docker + Xvfb，而 CI 也在 GitHub Actions 上跑——**先确认那条路上能不能跑**，
   跑不了就做成**可选步骤**（有 docker 才跑）并在报告里写清，**不要让 CI 在别人的机器上假红**。
   **必须给负对照**：把修复那一行注释掉，门必须变红。
2. **让 `tools/record-godot.sh` 录得到声音。** C2 给了完整配方（§1.2c 末尾）：保留 `--audio-driver Dummy`
   （它仍在推进混音），在 `--` 后追加 `--audiocap <wav> <SECS+2>`，去掉 `kill $GODOT_PID`，
   最后补偿脚本里那 3 秒 `sleep` 再混流。**别装 PulseAudio**，容器里没有声卡也不需要。
3. **README 主图的重编码。** 现在 `docs/media/town_chronicle.gif` 是 **680×408**，
   由 1280×768 做的 **0.53× 非整数**降采样 ⇒ 像素糊、HUD 文字全不可读。
   重编码为 **1280×768 或恰好 640×384**（整数倍），并报出改前/改后的文件大小与尺寸。
   ⚠️ 素材要用**合并后**的构建重录（现在画面已经不一样了），不要拿旧片子重新缩放。

**验收**：三项各自的负对照 / 前后数字；`tools/ci.sh` 仍然 PASS；**不碰 `game/`** ⇒ 金标按定义不动，但仍要跑一次确认。

### 1.2g ★ C7 回执——接缝 131.54 → 2.92（−97.8%），而**派棒者的判据量错了对象**

`WorldView.gd` +194/−2，新 `_draw_town_verge()`：3 格亮度斜坡（环数随缩放自适应、每级约 1px）+ 0-1 格地面延续 +
1-2.2 格散布的矮石墙与排水沟 + 2-3 格没入林中。近边色**不是硬编码的绿**，而是草地纹理的加权均值 × 季节色调 × 气候罩
——否则冬天的霜白会让接缝以另一种形式复活。

| 跨界最大相邻像素亮度跃变 | 正午 | 夜 |
|---|---|---|
| 改前 | 131.54 | 62.08 |
| **改后** | **2.92** | **1.64** |

`max = p90 = median` ⇒ **整条边界均匀塌下去**，不是被均值拖低的。1280×768 与 1920×1152 读数相同。

> **★ 我写的判据量错了对象。**「~16px 剖线的最大相邻跃变」有 **8px 落在界内**，
> 而界内像素画本身就有约 130 的跃变（实测：`left@y336` 一朵黄花）⇒ **该指标有地板，永远报不出成功**
> ——修好之后它只从 131.54 动到 129.93（−1.2%）。拆成 `cross`/`outband`/`win16` 之后才看得见真实的 −97.8%。
> **另一条「界外新层必须跟着昼夜走」在未改动的树上逐条通过**（C1 的背板本来就是 CanvasItem）⇒ 零判别力。
> C7 补了一个真有判别力的对照：band 内**昼夜免疫像素 0/22473，改前改后都是 0**。
> **这一波四条视觉判据被证伪，已把这条纪律写进 docs/41 §6。**

**两个只有数字抓得到的回归**（都在 C7 自己手里被抓住并修掉）：
1. **v1 的矮石墙变成了相框**——固定 1.10 格偏移 + 72% 覆盖率，`cross` 已经是 2.9、**眼验却是"地图周围加了一圈装饰边"**，
   正是 C1 记过的"矩形色带"同款失败。**沿边界等距且连续的任何东西都会从"延续"退化成"相框"，比原来那条硬边更糟**
   ——它把你正想藏起来的矩形又描了一遍。`outband` 同时报警（26.5 → 68.8）。修法：抖动距离与长度、覆盖率压到约 1/3。
2. **树冠排除判的是圆心不是圆**——半径够到 1.08 格 ⇒ 树仍画在 1.47 格处，造出 81.1 的尖峰，**比什么都不做更差**。

**两个 motif 被删掉，因为这张图上没有可延续的东西**：道路——`_build_paths` 只连门与广场，**没有一条到达边界**，
而"画一条通向图外的路"又与同一份规格的「不放门、路标…那会暗示界外可以进入」冲突；
水——最近的池子在界内 2 格，从不触边。硬做出来的河岸 motif **渲染成一块浮在池塘上方的半透明灰绿板、读起来像 UI 面板**，已删。
**只有石墙与林缘 motif 出货。**

**验证**：CI PASS（收尾单次）；金标文件 git 从未触及；`probe_digest_test.sh` **改前改后都跑**，
两侧同为 `520 2600875897 568220217582462501`；界内 `ImageChops` bbox = `None`（2 tick × 2 分辨率），全图 bbox 非 `None`；
额外对照：**默认跟随相机那一帧全图逐字节相同**——相机在界内时这段代码什么都不画。
第二层**故意没做**：C1 的树冠已经占了 4-8 格带，再叠规格里的 `#585C64`/`#3F4B50` 剪影有**重新抬高对比**的风险，
且需要自己的一轮前后对照。

**留给后续**：`scratchpad/wave_c/C7/seam.py` 与 `assert_verge_daylight.py` 自带地图矩形公式，**建议 C4 或 Wave D 收进 CI**。
**未测**：真机、非默认 seed / 其它地图布局（石墙与林缘的配比是照着这张图 x=2/x=61 的树带调的）、
春季以外的季节（季节耦合是**按构造推理的，没有渲染过**——冬天与雨天要在 Wave D 眼验）、手机填充率。

---

### 1.2h ★ C4 回执——本仓库有了第一条视觉门，**外加一个已经发在公开面上的缺陷**

只动 `tools/`（`game/`、`docs/media/` 零改动）。CI PASS，金标 12/12 逐字节。

**① 第一条视觉门**（`tools/visual_gate.sh` + `tools/assert_daynight.py` → `ci.sh` 第 6 关）。
这一棒真正的判断题是**可移植性**，处理得对：门在**拍图之前**做能力探测，探不到渲染环境就 `77 SKIP`、一帧都不拍。

| 环境 | 结果 |
|---|---|
| docker `gamecraft-runner:4.6.2`（mesa pin 死，tol=0） | PASS，dmax=0 |
| native Xvfb / Ubuntu 22.04（**容器内无 Pillow** ⇒ 走 stdlib PNG 读取器，tol=4） | PASS，dmax=0 |
| 裸 Linux（无 docker 无 Xvfb）≈ GitHub Actions | **77 SKIP** |
| 同上 + `LT_VISUAL=require` | **1 FAIL** |

> **一道在别人机器上因环境变红的门，比没有门更坏——它训练所有人忽略红色。** 这条判断写进了 `ci.sh` 的注释里。
> 附带一个不显眼但重要的决定：`tools/ci.sh` 此前是**纯 stdlib**，而 GHA 的 `setup-python` **不预装 Pillow**
> ⇒ 把 PIL 写进 CI 本身就是个假红雷。于是断言自带 zlib PNG 读取器，
> **与 PIL 在 C3 的 20 张证据图上 20/20 逐字节同结果**。
> **负对照**：在 `game/` 的 scratchpad 拷贝上只回滚 `Main.gd:271` 一行（`diff --strip-trailing-cr` 确认只有那一行）
> → `dmax=91 ❌` + 「夜昼同色 ❌」→ exit 1。

**② 录屏带声**（C2 配方落地）：20s 片带 aac，2 秒窗 RMS **9/9 非零**；`REC_AUDIO=0` 负对照无音轨。
**③ GIF 工具**（`tools/make_gif.sh`）：整数倍是机检红线，`divisor=3` 直接拒绝并列出合法值。

**★ 一个 brief 完全没提、而且已经发在公开面上的缺陷：每段录屏的前 5-7 秒录的是 Godot 启动闪屏。**
原脚本 `sleep 3` 之后就起 ffmpeg，而容器里引擎要 **8.3-10.4s** 才出画面。
**仓库里已经发布的 `docs/media/town_chronicle_demo.mp4` 就是这样**——t=0/0.5/1/2/3s 五帧全是
`(36,36,36)` 占 96.1% 的闪屏。改成内容探测：40×24 灰度帧众数占比，闪屏 **72.6%** 恒定 vs 游戏画面 **4.1-11.8%**，
阈值 40% 两边各有 3 倍余量，超时退回固定 sleep。
⇒ **Stride 4 不只是"补个新 GIF"，既有的几段 demo 片子起手都有 5-7 秒黑屏，值得一并重录或裁头。**

**这份 brief / 既有配方里错的四处：**
1. **★「重编码成整数倍 ⇒ HUD 文字可读」这个前提是错的。** 同素材同调色板只变缩放实测：
   `680×408 bicubic` 4.26MB 勉强可读 │ `640×384 neighbor` 3.57MB **更差**（笔画整根丢失）│
   `640×384 area` 3.51MB 仍不可读 │ **`1280×768` 3.48MB 完全可读**。
   病因是**12px 中文字形再砍一半**，不是重采样相位 ⇒ **「1280×768 或 640×384」这两个选项不等价**，只有前者达标。
2. **「复用 C3 的断言」照做会立刻假红**：C3 硬编码的 `GRASS=(133,166,67)` 是**它那棵树**的草地色，
   而 C1 的季节色调已把它改成 `(134,173,79)`。门改成**从正午帧量基色**（色停表在 `tod=0.5` 恰为纯白，脚本自己断言这一点），
   判据变成与美术无关的**比值** ⇒ C7 落地也不会红。
3. **C3 的 `int()` 是错的，只是它的数据分不开**（三个小数部分都 <0.5）。合并树把两者分开：
   `int()` 给 `(56,81,62)`，实测 `(57,82,63)`。
4. **C2 配方里的 `-ss 3` 会稳定错约 1.4 秒**：`--audiocap 24` 实际吐 25.36s，且差值不恒定 ⇒ 改用 WAV 实测时长回推 0 点。

**自我证伪**：先造了个"HUD 横带水平梯度能量"当可读性指标，它给 `neighbor` 打**最高分 +42.5%**——
**那个指标量的是锯齿不是可读性**，4× 最近邻放大裁切眼验当场推翻。已写进脚本抬头。

**未测**：GHA 上**没真跑过**（native 分支只在 Ubuntu 22.04 容器里验过）；**没人听过声音**（全是 RMS/窗口计数）；
视觉门**只守昼夜一条性质**，不守构图/接缝/文字；门给 CI 增加约 45s。

**Stride 4 的机械命令**（C7 之后需重测参数，画面变化更多会涨体积）：
```
GIF_DITHER=none bash tools/make_gif.sh <新录屏>.mp4 docs/media/town_chronicle.gif 1 6 <起点秒> 6
```

### C8 · HUD 不再压在世界上（Stride 4，玩家外壳第二轮）

**独占 `game/scripts/Main.gd`。不得动 `WorldView.gd` / `project.godot` / `tools/`。**

C1+C3+C7 之后再眼验合并帧，最刺眼的已经不在世界层，而在 HUD：

1. **编年史面板是一块硬边半透明矩形压在世界上**——它的右边界在地图上切出一条竖直硬线，
   而面板本身 alpha 太低（`_mk_panel` 的 `Color(0,0,0,0.42)`），**既读不清字、又挡着世界**。
   C3 已经证明这条路可行：顶栏改用 `Color(0.02,0.03,0.05,0.74)` 之后 ≥40% 亮度像素 1112→217。
2. **右侧观察面板占了约 23% 的屏宽**，且是**调试密度**的文本（`OBS_MAX_LINES=34`），
   中间还会 truncate 到半个字（`_mk_label` 的 `scroll_active=false`）。它是这张图上"这是工具不是游戏"的最大单一来源。
   **不要删功能**——它是本项目最独特的能力之一（任意 tick 检查居民）。要做的是**默认收起 / 可切换**，
   或分主次两档（默认只显示人设+当下+需求，展开才给关系/记忆/派系/观点/信念）。
3. 聊天输入框浮在世界中间（`:324-331`），且**没有 gate 在玩家模式上**——非玩家模式下选中任一居民就会出现。

**验收**（先读 docs/41 §6 那条新纪律：**写完判据先问"一个什么都不做的改动能不能通过它？"**）
- 每一项都要 `--warmup-tick 600 --shot-fit` 的改前/改后，并且 **diff bbox 必须完全落在 HUD 矩形内、世界区域为 `None`**。
- 面板可读性用**行均值 + ≥40% 亮度像素计数**（不要用"每点都低于阈值"，C3 已证明那字面不可能）。
- 右侧面板收起后，**世界可见面积的增量要报数字**（占屏比 before/after）。
- 金标逐字节不动 + `probe_digest_test.sh` 与改动前的树数值相同（**自己跑基线，别信文档**）。

---

### 1.2i ★ C8 回执——三条硬边全部抹平，而**我指定的验收帧恰好是问题最看不见的那一帧**

`Main.gd` +213/−25，外加一处**已声明**的越界（`player_touch_test.gd` +86，13 条新断言）。

**① 编年史面板**：硬边 `Color(0,0,0,0.42)` 矩形 → 羽化 **scrim**（`0.02,0.03,0.05,0.84`）。
左/下边缘直接跑出屏幕（那两条缝不再存在），右/上边缘用烘进 64² RGBA 纹理的 smoothstep alpha 斜坡消掉
（`TEXTURE_FILTER_LINEAR` 拉伸 ⇒ 一个节点、一次 draw call、任意分辨率都平滑）。
**② 观察台两档，一个功能都没删**：默认是**卡片**（人设/在哪/在做什么/钱+职业/5 条需求），
完整档案（关系·冲突·记忆·派系·盟约·秘密·观点·信念）**一键之遥**（顶栏「详情」按钮或 `V`）。
两档出自**同一个** `_panel_text(brief)` 的提前返回 ⇒ headless 门断言**卡片是详情的逐行前缀**，
两者不可能讲出不同的故事。占屏 294×676（**20.22%**）→ 240×340（**8.30%**）。
**③ 聊天框**改为 gate 在 `_player_mode`（原先只 gate 在"选中了某人"）。

| 面板边缘（只统计压在世界上的那一段，逐行/列中位跃变） | fit 正午 | 跟随相机 |
|---|---|---|
| 编年史右缘 x=568 | 65.78 → **0.00** | 71.04 → **0.00** |
| 编年史上缘 y=470 | 65.78 → **1.00** | 71.04 → **1.00** |
| 观察台左缘 x=978 | 65.78 → **0.00** | 57.32 → **0.00** |

用 C7 的相框判据复检（逐列中位跃变、前后**差分**以消掉像素画地板）：杀掉 −65.78/−71.04/−57.32，
**新造出来最强的一条线只有 +1.72 ~ +4.07**。编年史文字带背景均亮 **102.3/255 (40.1%) → 43.6/255 (17.1%)**。
两向断言 7 场景 × 2 分辨率：全图 bbox 非 `None`（约 41.8% 像素移动），**HUD 矩形之外 0 像素改变**，`im.size` 每帧不变。
`probe_digest_test.sh` **两棵树各跑一次**，同为 `520 2600875897 568220217582462501`。

> **★ 我指定的 `--shot-fit` 是这三个问题最看不见的那一帧。** 在 fit 下地图矩形是 `(288,120)-(992,648)`，
> 编年史面板**大部分压在界外背板上**（文字带背景 29.4/255），而在**默认跟随相机**下是 **102.3/255**；
> 观察台在 fit 下只与地图重叠 14px。**只看 fit，"读不清 / 挡着世界"几乎不成立。** C8 自己补了跟随相机帧——那才是有判别力的。
> **★ 第六条被证伪的判据**：「diff bbox 必须落在 HUD 矩形内、世界区域为 `None`」——
> 在**未改动的树**上（before vs before 复制）**0 像素在 HUD 外、平凡通过**。**它是护栏，不是判据。**
> 有牙齿的是接缝中位数与可读性计数。
> **★ 而且我只点了三条硬边里最小的那一条**：观察台**左**缘在地图内横跨 **526 行**，编年史右缘只有 177 行，量级相同；
> 编年史**上**缘（y=470，279 列，max 131.07）我一个字没提。

**C8 顺手查出的两处（一处是我的文档说了假话）：**
1. **`player_touch_test.tscn` 从来没进过 CI。** docs/43 §1.2d 把 C3 的门写成"已落地"，
   而 `tools/ci.sh` 第 5 步的场景列表里根本没有它 ⇒ **C3 的 31 条 + C8 的 13 条断言一次都没在 CI 里跑过。**
   *（已由主 session 在 `ed8cc98` 之后补进第 5 步。）*
2. **`OBS_MAX_LINES` 并不真的封顶。** 它只经 `maxi(2, OBS_MAX_LINES - L.size() - 2)` 裁**信念尾巴**，
   那个下限 2 意味着**总行数不受 34 约束**，前置各节可以自己把它顶穿。当前余量 11px ≈ 0.6 行——
   这就是 C8 第一版（面板内加表头行）会把档案**静默顶到滚动条底下**的原因，也是它改把按钮放进顶栏的原因。

**玩家失去了什么（C8 自己写的，不是我问出来的）**：卡片收起时**无法同时**看着小镇和读某人的关系/记忆/信念——
详情一键之遥，但展开时又占回 20% 屏幕 ⇒ **"边看边读"从环境式变成了顺序式**。
具体受影响的是"拖时间轴看信念列表变化"这个用法，现在需要先钉住面板。缓解：档位**粘滞并持久化**到 `user://settings.cfg [ui]`；
卡片最后一行**明写**哪些节搬走了（`关系 · 冲突 · 记忆 · 观点 · 信念 → 点右上「详情」`），
**因为静默收起会被读成"这个项目没有这些东西"**。

**被 C8 自己证伪的五个假设**（其中两次是 C7 那个"地板"问题复发）：羽化宽度不能按比例共享（一张纹理两个高度 ⇒
卡片只拿到一半斜坡，最后两行洗到草地上）；羽化不能压在正文上；**更窄不等于更小**（232 宽会溢出，240 宽反而更矮，
因为简介行不再折行）；「band 内最大相邻跃变」有地板（广场瓦片边界 y=424 本身就是一条 25.42 的轴对齐线，**两张图都有**）
⇒ 只有**差分**才无地板；`C` 聊天键**不能**一并 gate——`tools/chat-shoot.sh:20` 就是不带 `--player` 按 `c` 的，
gate 掉会让那个工具静默产出没有聊天的截图。

**未测**：真机（「详情」按钮 62×30 设计像素 ≈ 手机上 98×47）；顶栏少了 68px 宽度，**没测 NPC 60 或长选举串**；
`[ui] obs_expanded` 只在同一会话内验过读写两条路径，**没跨真实重启**；设置面板压在新 scrim 上没眼验；春季以外没渲染过。

### 1.2j ★ Stride-5 三路评审——**本波最重的四条错误是在这里才被抓到的，其中两条已经发布出去了**

三路独立评审（外部对抗 AI + 两个只读 agent，全部指令为"REFUTE"）。**收获全部是负面的，且都指向同一个结构缺陷**：
本波每一条视觉验收都是**点采样或 diff-bbox**，**没有任何一条是"有人把整帧看一遍"**。
计划里给音频写了"Stride 4 必须有人真的戴上耳机听一遍"，**却没给画面写对应的那一句**。

**① ★ 契约里那条"`--script` 不加载 autoload"是假的**（审计给出探针，我随后独立复跑证实）。
正确机制窄得多：autoload 在**主循环对象构造之后**才挂上，只有 `_init()` 看不见它们；而三个 bench 恰好整个写在 `_init()` 里。
⇒ autoload 的 `_ready()` 与至少一帧 `_process` **确实在 S0/LOD/DetGate 里跑**，`push_error` **会**被 `scan` 抓到。
**这条错误的来源是我**：C2 报了一个真实观察（`--script` 下加 `--audiocap` 不出 WAV），
我把它升格成机制断言、写进契约、还加了"实测复核"四个字，**而我从没跑过探针**。已在 docs/41 §2 逐字更正并留下探针输出。
真正的盲区是另一条：benches 用 `SimScript.new()`，**不入树 ⇒ `Sim._ready()`/`_load_data()` 在 S0 网格上从不执行**。

**② ★ 已发布的四份素材每一帧正中都有一个鼠标光标。** `record-godot.sh` 的 `x11grab` 缺 `-draw_mouse 0`，
Xvfb 根光标停在 (640,384)。**实测：发布的 PNG 在该 30×30 框内有 32 个纯黑像素，`--shot` 渲染是 0。**
C4 审过这一行、修好了开头闪屏，**但没看见光标**——因为它的就绪探测只量"众数灰阶占比"。

**③ ★ 地图西北角一片"缺贴图占位块"，把原始数据键印在草地上。** `Sim._compile_interiors()`（`Sim.gd:532`）
把室内家具塞进 `world["objects"]`，坐标是**室内局部格**且带 `space/floor`；而 `WorldView` 的镇上对象循环
**无条件画全部对象**，`slot` 取到 `cafe1f`/`home1f` 没有贴图 ⇒ 掉进占位分支，
`draw_string` 把 `table` / `床` / `桌子` / `咖啡吧台` 直接印在地图上。**外加一块金边招牌写着「测试阁楼」**
（`spaces.json` 仍在发 P1 门用的 `test_loft`）。**这三样在本波每一张已发布的截图与 GIF 里都在。**

**④ 两道"门"其实不是门。**
- `ModelPathGate._part_c`（"random 臂零扰动锚"）**零条断言**——只 `print` digest，`_fails` 从不被触碰。
  收据自己的算术就是证据：`9✅ + 8✅ + 0 = 17`。
- `BackendGate` 的**三条臂其实是两条**：`scenario==""` 时 `Invariants` 的 #1 化简为 `starved==0`，
  而 arm C 就是 `starved <= max_starve`（默认 0）⇒ **arm C ≡ arm A 的 #1，逐位相同**。
  收据的负对照表本身就露了馅（短路 P2-3 同时点亮"硬 0/8"与"饿穿 0/8"，因为它们是同一个谓词）。
  连带：**`--max-starve` 是个死旋钮**——调高它 arm C 变绿，但 `Inv.check_all` 仍然红 ⇒ 仍 `quit(1)`。
- **视觉门的可移植性论证建立在一个假事实上**：GitHub `ubuntu-latest` **同时带 docker 和 xvfb**
  ⇒ `have_native()` 为真 ⇒ `RUNNER=auto` 选 **native 而不是 SKIP**，会在**未 pin 的 mesa** 上以 `tol=4` 真跑两次渲染。
  **这正是那句"一道在别人机器上因环境变红的门比没有门更坏"要防的情形。**
- `Parse JSON failed` 白名单是**引擎级字符串**（`grep` 全仓库 `.gd` → 0 命中），却被套在整条 4e 日志上。
  更早的 `ERR_OK` 里那条裸 `Condition "!FileAccess::exists(path)" is true` 遮得更宽（**任何**缺失动态库，在**每一个**被扫的步骤）。

**⑤ 产品面的判决：没达标，而且诚实的说法是那句最难写的话。** 外部只读评审的原话：
> **"它看起来明显更好了，而且仍然没有人会玩它。"**

它同时指出：**发布的录屏 70 秒里相机从不推近、从不选人**，而 `LABEL_MIN_ZOOM=0.45` 会压掉名字/表情/气泡
⇒ **这部片子在结构上拍不到这个项目真正在做的事**；夜里**零个光源**（暖光像素 0、亮度 >190 的像素 0），
纯全局乘暗，而两个池塘因为蓝通道只掉到 0.797 ⇒ **夜里全图彩度最高的东西是两个青色矩形**；
玩家模式下"你"是一个**金色圆盘**（`Sim.gd:752` 的 `sprite: ""`），相机还跟着**被选中的 NPC** 而不是玩家。

---

### D1 · 把不是门的门变成门（Wave D 第一棒，**先做**）

**独占 `game/bench/ModelPathGate.gd`、`game/bench/BackendGate.gd`、`tools/ci.sh`、`tools/visual_gate.sh`。**
不得动 `game/scripts/*`、`golden_digests.json`。

四件事，全部来自 §1.2j-④，**每一件都必须带负对照**（一道从没红过的门不算门——这一波已经用它抓到两次）：

1. **`ModelPathGate._part_c` 现在零断言。** 要么给它**提交一份期望值**（`random` 臂逐 seed 的 digest 锚，
   写进它自己的 golden 段，比照 `DetGate` 的做法），要么**把它从"门"的措辞里删掉**并改 `ci.sh:97` 的标签。
   **不要留一个 print 冒充门。** 若选锚：注意 C6 已经证明 cap 36→26 会移动这条臂的轨迹，所以锚必须**现在**烘。
2. **`BackendGate` arm C ≡ arm A。** 要么让 arm C 量一个**真正不同**的东西，要么合并它并改掉"三条臂"的措辞。
   连带修 `--max-starve`：它现在**不可能**让门通过（`Inv.check_all` 仍红）⇒ 要么让它真的放行 `#1`，要么删掉它并删掉文档里那句"按预算放行"。
3. **视觉门的 GHA 判断基于假事实**：`ubuntu-latest` 带 docker **和** xvfb。请**自己核实**（读 `.github/workflows/ci.yml`
   + `actions/runner-images` 的镜像清单），然后决定：是显式 `RUNNER=skip`-on-CI（用 `$CI`/`$GITHUB_ACTIONS` 环境变量），
   还是接受在未 pin 的 mesa 上跑并把 `tol` 调到能容忍。**把真实理由写进脚本抬头，替换掉现在那段错的。**
4. **收窄白名单**：`Parse JSON failed` 应当只在**预期出现的那一步/那一行**放行（4e 里预期次数≈1）。
   顺带审 `ERR_OK` 里那条裸 `Condition "!FileAccess::exists(path)" is true`——它遮住**任何**缺失动态库。

**验收**：`tools/ci.sh` PASS；**四条各自的负对照输出**；金标逐字节不动；
报告里明写"这份 brief 哪里是错的"——上面每一条都是二手转述，**本波已经证明我的转述会把观察伪造成机制**。

## 四、Stride 计划与阶段性产出

| Stride | 内容 | 产出 |
|---|---|---|
| 1 | C0 / C1 / C2 / C3 并行开跑；主 session 做 C5 与本文 | 本文 + docs/41 §6 增补 |
| 2 | 合并 stride-1 的棒；C6 开跑；C4（工具链 + **第一条视觉 CI 断言**，依赖 C3 的昼夜修复） | 合并后一次全量 CI |
| 3 | analyze & fix：眼验新画面，把 stride 1-2 暴露的问题收口 | 改前/改后对照图 |
| 4 | 阶段性产出：新录屏 → **整数倍缩放**的 GIF（现在 README 主图是 680×408，从 1280×768 做的 **0.53× 非整数**降采样，像素糊、文字全不可读）、截图集、docs/13 札记、README 中英双语更新 | `docs/media/*` + README |
| 5 | 新一轮评审：多 agent + 外部对抗评审（指令"REFUTE"），analyze & fix | 评审记录进 docs/13 |

**README 已知的自相矛盾（Stride 4 必修）**：`README.md:34` 仍写「最戏剧的五类社交事件目前**不会**出现在事件日志里」，
而 14 行之后的 `:48` 写「现在背叛、盟约、选举、结怨、和解都会自己浮上来」。**48 行是对的，34 行是过期的自我低估**——
正是 docs/41 §4 存在的理由。
