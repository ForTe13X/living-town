# 103 · Wave AA 计划——**红线承诺 60，而我们只量到 40**

> 前置：docs/41 全部、docs/47 **R12**、**docs/92（X1）**、**docs/96（Y1）**、**docs/98（Y3）**、
> **docs/100（Z1）**、**docs/101（Z2）**。
> **回执必须带 §2.5 三行包络，`does_not_detect` 必须是跑出来的。**

## 〇、这一波从哪来

1. **红线 #3 写的是"单镇最多 60 居民"，而这棵树上被真正跑过的最大 N 是 40**
   ——而 40 那一格当时是**硬不变量红的**（X1 查明成因、Y1 修法出货）。
   **N=48 与 N=60 在今天这棵树上零数据**（X1 与 W2 各自写过这句）。
   ⇒ **本波第一优先：去把承诺的那个天花板量出来。**
2. **Y3 的短语锁在跨类型上抓 573/573，在【同类型反转】上抓 0/19**
   ——`promise/end:kept ↔ promise/end:broken`（"赴约了"换成"放了鸽子"）它一声不吭。
   Y3 **刻意没有顺手加 `accepted` 检查**：没有第二个独立来源，它会塌回自证。
   ⚠️ **而 Y3 自己已经证明"第二个独立来源"这条路走得通**（它拿 `Main._event_prose` 校验过词表）。
3. **商贩没有社会痕迹，而原因不是他没干活。** ⚠️ **先更正一条我差点写进 brief 的错**：
   商贩**有**岗位、有班次、有两条广告位（对内『摆摊』job 门、对外『赶集』全镇可见），
   **实测拿走全镇约 14% 的饭口（153/1045）**。他缺的是**社会痕迹**——
   因为 `craft_credit` 挂在 `produce` 管线上，而**他的产出在消费侧**（人→人的 transfer）。
   Z2 的原话：「商贩压根没有 produce 记录（**要挂在消费侧，是另一条管线**）」。

## 一、AA1 · 把承诺的天花板量出来（**只读测量 + `tools/`**）

**owns**：`tools/**`、你的编号文档
**不得触碰**：`game/**`
**你的编号文档**：编号 **104**

### 要回答的

1. **N=48 与 N=60 在今天这棵树上是什么样？** 逐 seed 跑，**给展布不给均值**，
   硬不变量与软不变量**分开报**。⚠️ **别只跑 seeds 1-12**——S1 已经证明那一格是全部 60 个里
   **方差最低**的一段，而 W2 证明"N=24 是红的"这句话的主语其实是 `(N=24, seeds 1-12)`。
2. **曲线的形状**：把 N=40 也重跑一遍（Y1 的修法之后没人在 40 上重测过），
   与 48/60 放在同一张表上。**⚠️ 跨树引用会出事**——Y1 给了可用的规则：
   **可比性 ⟺ 两棵树在 `game/scripts/` 与 `game/data/` 上逐字节相同，一条 `git diff` 就能查。**
3. **红线 #3 该怎么读？** 若 60 绿 ⇒ 承诺兑现，**照实写"到 60 为止在这些条件下是绿的"并列全条件**；
   若不绿 ⇒ **量出它在哪一个 N 上开始破，并把决定留给用户**。⚠️ **改契约是用户的决定，不是你的。**

### 顺带一件（Z1 明写的盲区）

Z1：`k ∈ (1.5, 1.852)` —— **没有人量过金标抓不抓得住这一段**。
⇒ 便宜的话就量了；贵就写清代价留给下一棒。

### 验收（AA1）

1. N ∈ {40, 48, 60} 的逐 seed 表（**硬/软分开**，给展布）。
2. 破点的位置与机制（能给多少给多少；**给不出就写"给不出 + 排除了哪些"**）。
3. 对红线 #3 的**建议**（不是决定）。
4. §2.5 三行包络；`bash tools/ci.sh` 全绿（**读输出**；现在约 26 分钟）。

## 二、AA2 · 短语锁的同类型盲区（**只动叙述层**）

**owns**：`game/scripts/Story.gd`、`game/scripts/Director.gd`
**不得触碰**：`Sim.gd` / `game/data/**` / `game/bench/**`（AA3 的行）、`tools/**`（AA1 的行）、`WorldView.gd` / `Main.gd`
**你的编号文档**：编号 **105**

Y3 跑完了**整个** 592 个变异体的成对对调空间：**跨类型 573/573，同类型 0/19。**
⚠️ **Y3 刻意没有加 `accepted` 检查**，理由写得很清楚：**没有第二个独立来源，它会塌回自证。**
**而它自己已经证明这条路走得通**——词表就是拿 `Main._event_prose`
（**另一个文件、另一位作者、对同一批事件的第二次渲染**）校验的，0 污染、17/31 逐字印证。

⇒ **本波的问题**：**同类型反转有没有第二个独立来源？**
- 有 ⇒ 用它，把那 19 个里能抓的抓住，**并跑完整个变异空间给出新的精确包络**（别只报一个总数）。
- 没有 ⇒ **照实写"没有，以及我查了哪些"**——那是合法交付，Y3 上一波就是这么交的。
⚠️ **不许用"从同一张语法表里再读一次"冒充第二个来源。** 那正是 Y3 说的自证。

### 验收（AA2）

1. 第二来源的候选清单 + 逐个判定（是/否 + 为什么）。
2. 若做成：**整个变异空间重跑**，给新的精确包络（跨类型 / 同类型分开）。
3. **仿真侧逐字节不变**：三条独立证据（自造 A/B + 开工前的金标 + 留出 seed）。
4. §2.5 三行包络；`bash tools/ci.sh` 全绿（读输出）。⚠️ **`story_test` 现在跑 40 天，你的改动会真被它判。**

## 三、AA3 · 商贩的社会痕迹在【消费侧】（**很可能走 R12**）

**owns**：`game/scripts/Sim.gd`、`game/data/**`、`game/bench/**`
**不得触碰**：`tools/**`（AA1 的行）、`Story.gd` / `Director.gd`（AA2 的行）、`WorldView.gd` / `Main.gd`
**你的编号文档**：编号 **106**

**先把前提读准**（我差点写错，已在 §〇③ 更正）：商贩**不是**没干活。
他有岗位、有班次、有两条广告位，**实测拿走全镇约 14% 的饭口**。
他缺的是社会痕迹，**因为 `craft_credit` 挂在 `produce` 管线上而他的产出在消费侧**。

### 要回答的

1. **消费侧的"被看见"是什么？** `produce` 那一侧的定义是"产出那一刻工位旁有谁"。
   消费侧对应的是什么——**买东西的那个人自己就是见证者？** 还是"排队的人"？
   ⇒ **先量**：`赶集` 发生时旁边有几个人、买的人是谁、有没有可复用的字段。
2. **值不值得做？** ⚠️ **Z2 立的规矩照抄**：**门槛 3 = 已经在树上跑着的那个岗位在最紧那一格的余量**。
   商贩的余量若够不上，**照实写"测了之后决定不做"**——Z2 否掉了四个岗位，四个不同理由。
3. **别默认要新事件类型。** V1 当初刻意不新增事件类型（搭在既有 `produce` 的 `witnesses` 上）
   ⇒ `Story.gd` 一行没动。**同样的克制在这里也适用**——新事件类型会同时撞 AA2 的行。

### 验收（AA3）

1. 消费侧"被看见"的清点（**改前**，给展布，按 Z2 那个"余量"口径而不是平均人数口径）。
2. 做/不做的判定 + 证据；若做，**负对照**（删掉它 ⇒ 摘要逐字节复原）。
3. `#41`/`#42` 不得被拆掉；`#16`/`#20`/`#5`/`#29`/放逐一族仍绿。
4. N=12 逐字节不变或走**完整 R12**（⚠️ U1/V1/Y1/Z2 已各重烘一次，**你的基线是它们之后**）。
5. §2.5 三行包络；`bash tools/ci.sh` 全绿（读输出）。

## 四、共同约束（照抄，不重述理由）

- docs/41 红线四条 + §0.5；**改契约是用户的决定**。
- **红数不是判据**（X1/Y1/Z2 三次实测：`obj_dist_penalty` 0.400→0.401 这个语义为零的扰动
  能消掉两个硬红、也能把一个岗位的余量从 2 打到 0）⇒ **对着零假设臂用连续余量判。**
- **"金标 N/N 不变" ≠ "该 N 不变"**（Y1：另外 48 个 seed 有 31% 会动）。
- **可比性 ⟺ 两棵树在 `game/scripts/` 与 `game/data/` 上逐字节相同**（Y1 更正）。
- **一个自检若"打印了却不阻止"，它就不是自检**（我自己刚在 `gate_fixture_audit` 上撞到：
  它打印 `HARD_IDS ❌ 对不上` 然后照样把不完整烘成了基线）。**你造的检查要能拦住。**
- **`does_not_detect` 必须是跑出来的**；**"测了之后决定不做"必须写进回执**。
- **先确认你量的是哪个对象**——H1/S3/V3/W3 四次量错；Y3 又补一条：**"按条数"排序本身可能就是错的口径**；
  Z2 再补一条：**"平均在场人数"与"每个 seed 至少被看见一次"不是同一个统计量，而后者才是判据用的。**
- **`tee | tail` 会吃掉退出码**；**别在 CI 跑的时候改它正在读的任何文件**。
- **编号是三位数**（`lint_links` 已修，见 `2de5e44`，且带 10/100/102 回归守卫）。
- **worktree 的 checkout 可能落后 270+ 个 commit**：**先确认基线，是祖先才 ff。**
- 隔离副本里做负对照；越界要声明。
