# 79 · Wave U 计划——**把 T1 那把已经证明有效的钥匙从"关着"变成"开着"；把数字变回可重算**

> 前置：docs/41 全部、docs/47 **R12**、**docs/76（T1 的机制与它自己划的阻塞点）**、
> docs/77（T2 的前瞻分母）、docs/78（T3 的相机与 HUD）、docs/72（S1 的基准率）、docs/73（S2 的门牙审计）。
> **回执必须带 §2.5 三行包络，`does_not_detect` 必须是跑出来的。**

## 〇、这一波从哪来

T1 把根因量到了（**打分从不读库存**：空仓与满仓给出逐字节相同的分数），
做出了 `_stock_pull_mult`，并实测它**真的**把用户点名的那件事修好了：

| 网格 | 关（出货） | 开 |
|---|---|---|
| `--seeds 49-60` | **FAIL ❌** | **PASS ✅ 12/12** |
| `--seeds 13-60` | FAIL ❌ (43/48) | **PASS ✅ (47/48)** |

**但它出货关着**，因为开启会让 `ci.sh` 的 4a（N=16）变红。
成因是一条明确的机制：**N>12 时 `work_pull_mult(16)=1.125` 抵消了阻尼那一半**
（乘积区间 `[1.0125, 1.2375]`）⇒ 机制退化成**只会加速**，于是把上限臂顶红。

**T1 选择停手而不是继续扫验收网格**——那正是 Wave T 的 brief 禁止的事。**本波接着往下，规矩不变。**

## 一、U1 · 让钥匙能开（**很可能走 R12**）

**owns**：`game/scripts/Sim.gd`、`game/data/**`、`game/bench/**`
**不得触碰**：`tools/**`（U3 的行）、`game/scripts/Main.gd` / `WorldView.gd` / `game/assets/**`、`docs/README.md` / `README.md`（U2 的行）
**你的编号文档**：编号 **80**

### T1 交出来的那条假设——**它明写自己没有用干预去测**

> 阻塞点**可能不是**我那两个数：`#40` 的上限臂在 N≥16 时，
> **6 个货位里有 2 个被 豆子/话本 永久占着**（S1 的结构性发现：它们在 town 平面没有满足物、
> 需求分母不随人口涨）⇒ **只剩一个货位的余量**。

⇒ **这是本波第一个要用干预去证伪或坐实的东西。** 若成立，那么**修法可能根本不在乘子上**，
而在"让上限臂不要把结构性从不缺货的货算进分子"——**但那是改判据，Wave T 明令禁止过。**
⇒ **先测清楚，再判断这算不算"改判据冒充修根因"。若你认为它是正当的，请给出论证，别偷偷改。**

### 硬要求（照抄 Wave T，不放松）

1. **判据不许放宽。** `SUPPLY_FLOOR` / `SUPPLY_MIN_DEMAND` / `SUPPLY_MIN_DAYS` / 软通过率门一律不动。
2. **验收网格是 `--seeds 49-60`、`13-30`、`31-60`，且 `1-12` 与 `4a(N=16)` 都不得回归。**
3. **负对照**：把机制关掉，那些红必须回来。T1 已经建好了这条路（`hi==lo==den ⇒ f≡1.0` 逐位）——**用它**。
4. ⚠️ **T1 留了一个未解**：key-on 的一次 CI 里出现过一次 `DetGate` freerider「同 seed 两跑分歧」，
   **在隔离的 key-on 副本上复现不出来（16/16）**。
   **你要开这个键，就得先追它。** 追不到就照实写"追不到"，**但不能当它不存在**。
5. 移动金标走**完整 R12**。

### 验收（U1）

1. 那条"2 个货位被永久占着"的假设：**用干预证伪或坐实**，给数据。
2. 若钥匙能开：三个网格的逐 seed 前后对照 + `4a` 不回归的证据 + `DetGate` 那条未解的结论。
3. 若钥匙**开不了**：照实写"开不了，以及卡在哪"，并说明**下一个该测的是什么**。
   ——**这是合法交付**，Wave T 的 T1 就是这么交的。
4. §2.5 三行包络；`bash tools/ci.sh` 全绿（**读输出**）。

## 二、U2 · 阶段产出（**只碰文档**）

**owns**：`docs/README.md`、`README.md`、`README_EN.md`、`docs/56-*`、`docs/13-*`、`docs/05-*`
**不得触碰**：任何 `game/**`、`tools/**`
**你的编号文档**：编号 **81**

R3 上一次刷新 `docs/56` 停在 Wave Q。此后落地了 **R / S / T 三波九根棒**，而其中至少四条
**是对本仓库自我叙述的修正**，不是新功能：

- **S1**：S0 软门的绿有相当一部分是**选种子**选出来的（没参与挑阈值的 seed 上破门率 10.4%，[4.5%, 22.2%]）；
  `--seeds 49-60` 在**未改动的树**上就是 FAIL。
- **S2**：分母 **62/82 = [0.653, 0.836]，而整个区间都是上界**；
  统一结论「**每次重算并打印余量的臂至今全部准确；打印冻结字面量的全部过期**」。
- **T2**：**探测率是任务框架的函数**——同一份 brief，被告知"可能有错"的臂 10/10，
  只被要求"产出计划"的臂 8 HIT/2 PARTIAL，差异**全部落在符号名那一层**；
  且**只存在于一份文档里的数字 0/2 被抓住**（重算路径不存在）。
- **T1**：根因是**打分从不读库存**；机制有效但**出货关着**。

⇒ **`docs/56` 新版必须把这些写进去，而且不许打圆场。**
特别是：**"CI 全绿"这句话在本仓库的含义已经被 S1 实测缩小了**，
非技术读者最容易误读的正是这一句。**请用一段平实的话解释它现在到底意味着什么。**

**另两件**：
- `docs/13` 补 R→T 段，**照旧记被推翻的结论**（本段里我自己至少错了四次，逐条在编号 76/77/78 里）。
- README 换上 T3 的室内录屏（`docs/media/t3_demo_interior.gif`），
  并**保留**现有那段"这段录屏只有小镇平面"的说明**改写成正确的**——现在它**有**室内了。
  ⚠️ **不要声称你没跑过的门是绿的**；**不要引用编号 80 或 82**（它们此刻不存在，`lint_links` 会红）。

## 三、U3 · 让数字变回可重算（**T2 那条最干净的发现的可执行一半**）

**owns**：`tools/**`、你的编号文档
**不得触碰**：`game/**`、`docs/` 下除你自己那份之外的任何文件
**你的编号文档**：编号 **82**

T2 的 L4 分层劈得干干净净：

| 数字 | 被抓住 |
|---|---|
| 树上**还有第二份拷贝**、或能重算的 | **3/3** |
| **只存在于那一份文档里**的 | **0/2** |

A3 说出了原因：**它去找 CIEDE2000 的工具想重算，而那套工具从未入库。**

⇒ **本棒把这一半做成基础设施**：
1. **把缺失的重算工具补上**（CIEDE2000 是点名的那个；**先扫一遍还有哪些数字没有重算路径**）。
2. **给"可重算"建立一条约定**：一个数写进文档时，旁边要能指向重算它的命令。
   ⚠️ **别把它写成红线**——本仓库的经验是"门比宣言值钱"。**能做成门就做成门，做不成就照实说做不成。**
3. ⚠️ **先量再动**：**到底有多少个数字没有重算路径？** 给出扫描结果，**给展布不给均值**。
   **别一上来就假设它很多**——S2 的 11 条对账里有 9 条是准确的。

### 验收（U3）

1. 扫描结果：哪些数字有重算路径、哪些没有、比例是多少。
2. 补上的工具 + **它自己的负对照**（拿一个已知的数重算，必须复现；改一个像素/参数，必须不同）。
3. 若做成了门，报 §2.5 三行包络；**做不成门要说明为什么**。
4. `bash tools/ci.sh` 全绿（读输出）。

## 四、共同约束（照抄，不重述理由）

- docs/41 红线四条 + §0.5；**计划不得给契约加码**。
- **`does_not_detect` 必须是跑出来的**；**"测了之后决定不做"必须写进回执**。
- **别在 CI 跑的时候改 `ci.sh`**；**看到"零引用"先跑 `git log -S`**。
- **报极值连并列一起报**；**收紧判据前先量余量**；**加不齐先重新数**。
- **单 seed 的 PASS 不含信息**：`soft_threshold(n<=1)==0`，软门在单 seed 上恒过。
  报告侧已改成 ⚠️ 与「N/A·单seed无判别力」，**但判据没变**——**别用单 seed 判定任何事**。
- **单 seed 标定出来的容差是冻结字面量**（S2 证明这一族全会过期，S3 当场撞上 2.08 vs 5.65）。
- **`tee | tail` 会把退出码吃掉**——T3 的第一次 CI 是 FAIL 而管道返回 0。**读输出。**
- **编号已占好，但【不要】在正文写 `docs/` + 数字的纯文本形式**（`lint_links` 检查 (2) 认它；本 session 它抓到我三次）。
- **worktree 的 checkout 可能落后两百多个 commit**（上一波三根棒都是 209）：**先确认基线，是祖先才 ff。**
- 隔离副本里做负对照；越界要声明。
