# 70 · Wave R · R3 回执——索引追到 67、阶段报告出新版、札记补 L→Q；**而 `lint_links` 在本棒树上仍然是红的**

> 派棒：[docs/67 §三](67-wave-r-plan.md)。共同约束：[docs/41](41-baton-contract.md) 全文。
> **行**：`docs/README.md`、`README.md`、`README_EN.md`、`docs/56-*`、`docs/13-*`、`docs/05-*`、本文。
> **一行 `game/**` 与 `tools/**` 都没有碰**（`git show --stat` 可验）。
> 本棒**没有跑 `tools/ci.sh` 全量**——只跑了它的第 2 步（`lint_links`）。**因此本文不对任何其它门做断言。**

---

## 〇、先说没做到的那一条：**验收 1 我做不到，而原因不在本棒**

docs/67 §三 验收 1 写的是「`docs/README.md` 覆盖到 66 且 **`lint_links` 绿**」。
**覆盖到 67 做到了；`lint_links` 绿做不到。**

在**我动手之前**，未改动的 `integration/batons`（`c551bdc`）上跑：

```
$ python tools/lint_links.py
lint_links: FAIL (3 dead reference(s)):
  - docs/67-wave-r-plan.md:22:  -> 68-*.md ... (no 68-* on this tree; write 68@<branch> if it lives on an unmerged branch)
  - docs/67-wave-r-plan.md:73:  -> 69-*.md ... (no 69-* on this tree; write 69@<branch> if it lives on an unmerged branch)
  - docs/67-wave-r-plan.md:105: -> 70-*.md ... (no 70-* on this tree; write 70@<branch> if it lives on an unmerged branch)
EXIT=1
```

> ⚠️ **这段输出被我动过一处，必须说明**：三个目标名原本都带 `docs/` 前缀（形如「前缀 + `68-*.md`」），
> 我**把那个前缀去掉了**。原因是本文自己实测出来的：
> `lint_links.py` 的检查 (2) 认的是**纯文本** `docs/NN`，
> ⇒ **把它自己的失败输出原样贴进任何 `.md`，就会让它下一次运行多报若干条死引用。**
> 我第一版正是原样贴的，**当场把红从 3 条变成 15 条**（多出来的 13 条全部来自本文）。
> **这一条是我自己踩出来的，所以写在这里而不是省略。**
> 本文其余所有提到 68/69/52 号的地方，一律**不写 `docs/` 前缀**——这不是笔误。

⇒ **基线就是红的，红源是 docs/67 自己。** 那三行正是它替 R1/R2/R3 占号的三句
「**你的编号文档**：`68-*.md`（已替你占好，别另取号）」（原文带 `docs/` 前缀）。
**"占号"这个动作本身就制造了死引用**——检查 (2) 分不出"引用一份文档"和"预告一份文档"。

**而 docs/67 §三 的原文是**：

> ⚠️ **不要引用 67 之后的任何编号**（68/69 是 R1/R2 的，**它们此刻还不存在**）。
> **本 session 已经两次因为写死未来文档名把 `lint_links` 弄红了，别成为第三次。**

**第三次已经发生了，就发生在写下这句警告的同一份文件里。**（前两次的痕迹在 `10a4aa5`：
「去掉对未创建文档的前向引用——lint_links 当场红了，而这是我今天第二次犯」。）

### 我做了什么、没做什么

- **没有改 `docs/67`**——它不在我的行里，而且 R1/R2 落地后那两条会自动解决。**闷声越界才是罪，我选择声明而不是动手。**
- **本文（70 号）落盘，直接消掉三条里的一条。** 改动后实测（目标名同样去了前缀）：

```
$ python tools/lint_links.py
lint_links: FAIL (2 dead reference(s)):
  - docs/67-wave-r-plan.md:22: -> 68-*.md ...
  - docs/67-wave-r-plan.md:73: -> 69-*.md ...
EXIT=1
```

- **剩下两条与本棒无关，且我证明了这一点**（负对照，在 scratchpad 的隔离脚本里做，
  **没有往仓库里写任何临时文件**）：用一个 driver 把 `os.listdir` 打桩，
  让 `lint_links.py` 以为 68 号与 69 号存在，**脚本本身一个字节不改**（`runpy` 跑的是仓库里那一份），再跑 ⇒

```
lint_links: OK — 75 markdown files, all relative links resolve and all docs/NN citations exist (68 numbered docs)
EXIT=0
```

> 68 = 本树真实的 **66** 篇编号文档 + 打桩的 2 篇。**66 这个数就是这么核出来的**，
> 也是 README 里那句"编号文档现有 66 篇"的出处（改动前是 65，本文落盘 +1）。

⇒ **本棒新增/改写的每一条引用都是活的**；`lint_links` 今天的红**完全由 docs/67 的两条占号构成**，
它们要等 R1/R2 落盘。**这一条我不写成"绿"，也不写成"已修复"。**

> **这道门本身值得记一笔**（§2.5 口径，**但探测包络不是我的产出，我只报我实测到的**）：
> `lint_links.py` 对 `NN@<branch>` 形式**直接放行、不去那个 branch 上核实**。
> 实测：`docs/50:87` 写着 52 号 `@worktree-agent-a0c76a3f96ae5dbcf`，
> 而 `git ls-tree worktree-agent-a0c76a3f96ae5dbcf docs/` **没有 52 号**（那个分支的 docs 停在 49）。
> ⇒ **`@branch` 是一条不被检查的逃生舱**：它抓得到"忘了写分支"，抓不到"分支上也没有"。
> **两条合起来看，这道门的形状是**：`detects` 纯文本引用指向本树不存在的编号；
> `does_not_detect` ① `@branch` 后缀的真伪、② "预告一个未来编号"与"引用一份文档"的区别
> ——**而 ② 正是本 session 三次弄红它的原因**。

---

## 一、`docs/README.md`：从 48 追到 67

**改动前**：抬头写「本树上有编号文档 00-25、28-48」，分类表实际只链到 **00-25 / 28-35 / 38-48**。
**36、37 从来就没进过索引**，49-67 全部缺失。**共 20 篇不在索引里。**

**改动后**：抬头改成 **00-25、28-51、53-67**，并补了：

- **新增一节「波次计划与回执」**——43/46/47/48/49/50/53/55/57/58/61/64/67 + 本文。
  这是一个独立的文体（一份计划派若干并行棒），此前散在别的分类里、或根本不在。
  节首明写：**计划文档是写作当时的权威计划，不是当前状态；当前状态永远看 05。**
- **新增一节「规模、经济与多镇」**——把 12/14/32/33 与新的 54/59/60/62/63/66 收在一起。
- **「测试·度量·负结果」**补进 36 / 37 / 51 / 65。
- **新增一节「给非技术读者」**，并在「先读这三篇」下面直接给出 56 的入口。
- **修掉两处索引自己的过期数**：「37 条不变量」→ **40 条（25 硬 + 14 软 + 1 诊断，实测 `_chk(` 的 id 集合 = 1..40）**；
  「995 行的过程日志」→ 1700+ 行（改动前实测 1783 行）。
- **给 52 号写了一条墓志铭**，见 §三。

## 二、`docs/56`：新的一版（2026-08-01）

**口径照旧**：不含一行代码、"还不行的"那节继续直白、每条都能追到编号文档或 commit（出处写在括号里）。
**上一版正文没有被改写**，留在 git 历史里——包括它那句"规模问题已送外部评审、等意见"。

新版 §三把 K→Q 这一段讲成八件事，每件都带出处：

| # | 内容 | 出处 |
|---|---|---|
| ① | 双尺度落地：N=60 屋瓦断供 **60/60 天 → 0-34 天**；N=12 逐字节不变；机制完全可 ablate | `02ad571` / [57](57-wave-k-plan.md) |
| ② | 大 N 下"工作输给社交"是**结构性比较**，不是某个数字调低（八个广告六个一起滑，滑动与 `amount` 无关） | `e31d4a5` / [58](58-wave-l-plan.md) |
| ③ | CI 第一次在池倍率 ≠ 1 的配置上评估判据（此前 N=12 时倍率恰为 1 ⇒ 那条路一步没走过） | `2b4565e` / `6c7c8bc` |
| ④ | 那个"N=20 新回归" **768 次运行判为不是回归**（一个人躺了六天）；随后 `SURVIVAL_GATE` 32→36 走的是剂量-响应曲线 | `b73b9a5` / `1b2f281` |
| ⑤ | 闲话终于传得开：旅人的消息 **0 → 15 人**（另两 seed 0→6、1→10）；N=60 知晓人数 2 → **53-55**，最深跳数 1 → **9-12** | `def63e7` / [60](60-multi-town-feasibility.md) / [61](61-wave-o-plan.md) |
| ⑥ | 多镇提案：外部对抗评审**不批准整体开工**；第一刀该切知识边界不是地图边界 | [62](62-multi-town-review-verdict.md) |
| ⑦ | 知识局部性：信念层**30 天逐字节相同**（评审的前提在本仓库不成立）；真正破的是**派系层**——一个人私下改主意，一夜之间 60 人里 16+ 人被重贴标签 ⇒ 加接触门，从未谋面的对 **25/25 逐字节零影响** | [63](63-epistemic-locality.md) / `7dc8cd9` / [66](66-faction-contact-gate.md) |
| ⑧ | 模型路的知识泄漏：不在字段值里，在**候选表的成员资格**里；**52.4% 的决策点**至少带一条这样的选项；判定是契约缺口不是违规 | [65](65-model-path-epistemic-read.md) |

**§四「还不到位的」不但没有打圆场，还比上一版长了**——新增五条：

- **没有引导、没有第 3 分钟叫回来的东西**，且那句三路评审的判决**是 2026-07-26 下的、此后一次没重跑**；
  **本项目至今没有任何能测 playability 的仪器**（上一版 §四漏了这条，而它是 README 里的头号阻塞）。
- **规模问题不是"快好了"**：N=60 与 N=16 的软门**各还差一个 seed**，**两格红，而它们还红着**。
- **修通闲话当场付了代价**：held-out 软失败 0 → 1（`#40` @ seed 22，柴薪 0.48 < 0.50）；
  **而改前那一格的最小值恰好就是门槛 0.500，余量本来就是零**，只是文档里记的 0.569 早已过期、没人回头复核。
- **派系接触门只修了一半**（认识的人之间读心这件事性质未变，作者自己写的）。
- **方法论的分母至今未知**（见 §三）。

## 三、`docs/13`：补 Wave L→Q，**含被推翻的结论**

新章 `2026-07-31 → 08-01 · Wave L→Q`，按"被推翻的东西"组织而不是按时间——
因为这一段里**被推翻的比被建成的多**。docs/67 §三 点名要求的四条**全部在内**，另有若干：

1. **O1 推翻三个假说**，其中两条是我写进 brief 的（§一）：
   基础分差 5.0「对 N 不敏感」（−4.8 → −4.9）；候选数量那条**证伪**（`gossip_rep` 的分里**没有逐邻居项**）；
   而**真凶 `greet` 我和 docs/60 都一个字没提**——它严格支配 `gossip`，
   **非爱八卦者在 25,567 次机会里赢 0 次**。第四条是载荷被 `W:*` 劫持（N=60 s1：R1 占 2.1%、`W:*` 占 93.2%）。
2. **我把 `#5` 说成硬不变量**（`HARD_IDS` 里没有 5），并在这个错误上搭了整套"硬不变量在最该守的地方被豁免"的说法；
   删掉豁免后**没有任何一格变色**（负对照：未改动的树、N=16 × seeds 1-12，`#5` **本来就 12/12 绿**）；
   我引的增长比值表还**混了 seed**，**把增长夸大约 3.3 倍**。**⇒ 三处错是同一个框架的三个投影，不是三次疏忽。**
3. **我把早已结案的 N=20 当成未决**（M1 的 768 次运行 `630e40d` 说四个配置上红率分辨不出来），
   **并在 Wave R 的 brief 里又派了一遍**。⇒ **结案的事没写回索引/札记，等于没结案**——这正是本棒存在的理由。
4. **P1 的推断被 Q1 证伪，而我在 brief 里原样重复了它**：`Inv.chain_step` **不折 `attitudes`**
   ⇒ 同 tick 同结果的 `discuss` 可以传走一个不同立场而完全不动 chain。
   实测 **21 个触发格里 10 个**，第三方态度在派系变动**之前**就分叉；最坏一格 **557 个 tick 里前提为真而结论为假**。
   而 P1 说"造第二个平面 = 造第二个镇"，Q1 **约 10 行**就做到了。
5. **我说"模型这条路一道门都没有"——假的**：`BackendGate` 挂的是真后端（`decide()` **4879 次**），
   **但 `build_prompt()` 0 次** ⇒ 正确说法是"**CI 守的是模型交回来什么，对模型被展示了什么零覆盖**"。
6. **我在 Q1 的 brief 里把它框成"违反 `Sim.gd:10` 的自我描述"——不对**：那一行的括号管的是 belief，
   `attitudes` 是后来的 S2 子系统、根本没被提到 ⇒ **文档缺口 + 代码缺陷，不是自相矛盾。**

另外三节：**§五**收"门跑在它守的事不会发生的配置上"这一波的两次（K1 的池 / Q1 自己第一版的 `#25`）
外加 ModelPathGate 锚**对这类改动分辨率为 0**；**§八**收两个探针自己翻的车
（P1 的嵌套 `PackedInt64Array` 是值类型 ⇒ 指纹静默全零；Q2 的名字普查没排除自己的名字，84.4% → 76.1%）；
**§九**逐条抄代价与诚实边界，不打圆场。

**§十是新加的一条自知之明**：docs/50 §四派的 **H4「给方法论装一个分母」**（brief 变异测试）**从未交付**——
本仓库**任何分支上都没有 52 号文档**，`tools/brief_mutate.py` **也不存在**（我逐条查过：
对全历史 `git log --all --diff-filter=A --name-only` 过滤 52 号文件名，结果为空）。
⇒ 外审那句 **"24 不是质量指标……你不知道分母"** 至今**没有被回答过**。
**本章列了十几条被推翻的结论，而它们的分母仍然是未知的。**

## 四、`README.md` / `README_EN.md`：自己打磨（**"经 Codex 打磨"这一环没走**）

⚠️ **明写：本波没有走 Codex 打磨那一环。** docs/67 §三 给的理由是 Codex desktop 的 computer-use 授权
被用户拒绝过、**不再重复弹窗**——我遵守了，**没有发起任何授权请求**。
⇒ 下面这些是我自己核出来并改的，**没有第二双眼睛看过**。

**改掉的过期/失真：**

- **「编号文档现有 55 篇」→ 66 篇**（`lint_links` 每次运行打印的那个数；本棒落盘后实测 66）。
- **「索引落后，它收到 48 号为止，`36/37/49/50/51/53/54/55/56/57` 十篇不在里面」**——
  **十篇这个数是错的，实际是 20 篇**（还漏了 58-67）。现改为"2026-08-01 已补齐到 67 号"。
- **「16 道关」→ 17 道关**：`tools/ci.sh` 里 **`4a` 宏观池尺度门**没被计进去（它的 `echo` 不在行首、
  上一版是按 `^echo "###` 数的）。顺带给 4a 写了一段，因为它补的正是本仓库反复中招的那类洞，
  且它**自己带四条预检**（`CI_POOL_N=12` 时**主动判红**并说明"会退化成第 4 步的复读"）。
- **docs/50 那一行把「给方法论装一个分母（brief 变异测试）」写成了已交付**——**它没有交付**，已改成 ⚠️ 并指向新条目。
- 抬头那段关于 docs/56 的引子：从"唯一一处被追上的是 §四那段"改成"新版已出，旧版正文未改写"。

**补上的（此前 README 完全没有的 K→Q）：**

- 「当前状态」的**涌现社会层**下加了一段注：**八卦级联此前止步第 1 跳、观点派系此前靠"读心"结成**
  ——两条都是"有机制、无效果"，是 Wave O/Q 才真正成立的。**这是朝"显得更完整"方向的补，所以给了数**
  （25,567 次赢 0 次；一夜 16+/60 人被重贴标签；改后 25/25 逐字节零影响）。
- 「还不到位的」新增四条：规模还差两格、闲话修通的代价、接触门只修一半、模型被展示什么零覆盖，
  外加方法论分母未知那条。
- 文档表补进 57/58/60/62/63/65/66。

> **一条我自己踩的纪律**：K2 立过"漂移朝哪边都是失真、别按方向筛"。
> 本次两个方向都有：**朝好看**（把 4a 这道门算漏了、把 66 篇写成 55 篇）与**朝难看**
> （把已经出货的八卦级联/派系当成还没有）。**两边都改了，没有只挑一边。**

## 五、`docs/05`：把「权威计划」的指针从 Wave C 追到 Wave R

`docs/05` 是本仓库**仅有的两份承诺跟随当前进度**的文档之一，而它 §A 抬头还写着
「2026-07-26 起，当前这一波的权威计划是 docs/43（Wave C）」——**落后十几波**。

- 加了一张 **C→R 的波次索引表**（每波一句话 + 计划文档链接），并明写"当前是 Wave R"。
- **2026-07-26 那段针对 Wave C 的过期声明原文保留、不改写**，只标清它的适用范围。
- 新增一段 **NOW/NEXT/LATER 三张表的过期复核**：**已交付的逐条标出**（确定性跨进程锚、小镇纪事、
  Node 端口退役、诚实分母、音频代码、11 条目标、触屏动作条断言……），
  **仍然为真的也逐条标出**（没有引导 / 没有 playability 仪器、音频没人听过、触屏没人按过、
  `event_log` 压缩未做、Theory Engine 未接进游戏循环，以及 **docs/42 要求的三次重跑至今没有发生**）。
- 按 K2 的规矩：**给过期判词标日期，比删掉它更诚实**——一条都没删。

---

## 六、这份 brief 哪里是错的（契约 §4）

1. **⭐「`docs/README.md` 抬头写着 00-25、28-48」——抬头是这么写的，但索引表本身更差。**
   实际链到的编号是 **00-25 / 28-35 / 38-48**：**36 与 37 从来没进过索引**，
   而 README.md 自己那句"36/37/…十篇不在里面"也**数少了**——真实缺口是 **20 篇**。
   ⇒ **"抬头写到 48"和"索引覆盖到 48"是两件事，brief 把它们当成了一件。**
2. **⭐「树上已经到 66」——树上已经到 67。** docs/67 自己就在树上（`c551bdc`）。
   差一号不影响工作，但它决定了索引的抬头该写什么。
3. **⭐⭐ 最要紧的一条：brief 警告我"别成为第三次"，而第三次早已发生，就在 docs/67 里。**
   基线 `c551bdc` 上 `lint_links` **本来就是红的（3 条死引用，全部来自 docs/67 §一/§二/§三 的占号句）**。
   ⇒ 验收 1「`lint_links` 绿」**在本棒的行内是不可达的**：68/69 要等 R1/R2 落盘。
   我用打桩负对照证明了**本棒引入零条死引用**（见 §〇），但**不写成绿**。
   > 更一般的一条：**"替你占好编号"这个动作本身与 `lint_links` 的检查 (2) 冲突。**
   > 想占号而不变红，只有两条路：写成 `NN@<分支名>` 那种带分支后缀的形式，
   > 或者**根本不写编号前缀、只写"你的文档编号见本节末"**。
   > **而第二条更好**，因为第一条会写下一个（如 §〇 所示）**不被核实**的分支名。
4. **「docs/13 的 K-Q 这一段」——K 已经写过了。** docs/13 现有最后一章就是
   `2026-07-31 · Wave J/K`（`39e28ee`）。真正的缺口是 **L→Q**，我按这个范围写，并在新章里
   **回头更正了 K 那一章之后才发现的两件事**（N=20 结案、`#5` 的档次）。
5. **「O1 推翻了三个假说（包括它自己的 brief 给的那个）」——是三个假说没错，但归属要更正**：
   其中**两个**是 brief 给的（5.0 常数、`#5` 豁免承重），第三个（候选数量）承自 L2 的机制叙述。
   **而 O1 还推翻了第四条没人提的**（载荷被 `W:*` 劫持）。⇒ **"三个"是下限不是全部。**
6. **brief 没提、但我核出来的一条**：`lint_links.py` 的 `docs/NN@<branch>` 形式**不去分支上核实**。
   `docs/50:87` 的 `docs/52@worktree-agent-a0c76a3f96ae5dbcf` **在那个分支上也不存在**
   ⇒ 52 号是一个**从未落地的编号**，而 H4「给方法论装一个分母」**从未交付**。
   **这是这次追索引最有价值的副产物**：它意味着本仓库反复引用的"第 N 次被推翻"，**分母至今是未知的**。

## 七、我没能测到什么（明写，不用推断填空）

- **没有跑 `tools/ci.sh` 全量。** 只跑了第 2 步 `lint_links`（单跑：`python tools/lint_links.py`）。
  ⇒ **本文不对 0/1/1b/2b/2c/2d/3/4/4a/4b/4c/4d/4e/4f/5/6 中的任何一道做绿或红的断言。**
  理由：本棒只改 markdown，`ci.sh` 里唯一读 markdown 的就是第 2 步；跑全量要 Godot 网格，
  与本棒改动零交集。**这是一个取舍，写在这里而不是省略。**
- **没有眼验任何画面、没有出图、没有真机。** 本棒零视觉改动。
- **没有走 Codex 打磨**（§四，用户拒绝过授权，未再弹窗）⇒ **README 的措辞没有第二双眼睛看过。**
- **没有核对 README 里每一个历史数字。** 我核的是**本波新增的**与**明显与今天冲突的**
  （文档篇数、索引覆盖、CI 关数、H4 是否交付）。
  **早于 Wave K 的那些数字我没有逐个重测**——它们的出处链接都在，但"链接在"不等于"我核过"。
- **`docs/56` 里 §一/§二 的存量描述（12 个居民、九个职业、十张脸、表情图标）沿用上一版口径，我没有重新实测。**
  本波改动不触及它们，但**沿用不等于复核**。
