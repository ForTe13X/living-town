# 49 · Wave G 计划——**美术是这个仓库唯一完全没有门的资产类**

> 前置：docs/41 契约（红线 + 六节纪律 + 四个结构性盲区）、docs/43 R1-R8、docs/46 R9-R11、docs/47 R12。
> 本波不新增规则编号，只**用**它们；如果执行中发现需要第 13 条，写进本文 §四并说明是被什么实测逼出来的。

## 〇、这一波的由来：**Wave F 收尾时做的三次拆门，把同一个问题指到了美术**

Wave F 末尾我拆了三道长期全绿的门（#39 名字里少查一半、#40 粒度太粗、`story_test`
在一个裁剪从不发生的 horizon 上跑），每一条都用负对照亲眼看着变红了才算数（docs/48 §四/§五/§六）。
拆完之后一个更大的洞露出来了：

**`tools/ci.sh` 里没有任何一处**为美术资产设门。

> ⚠️ **更正（G1 抓到）**：这里原本写的是"grep 实测，**0 命中**"。**实测是 1 条命中**——
> `tools/ci.sh:92` 的 `fresh-rest**art**`，一个撞进来的子串。实质结论（没有美术门）成立，数是错的。
> 更难看的是：**那一行就在我自己当时跑出来的 grep 输出里**，我读着一行结果报了个"零"。
> 与本 session 早些时候那次"CI PASS"是同一个毛病——**手上有输出而没有读它**。

⇒ **十张角色表、整个 `game/assets/art/pro/` 是这个仓库唯一一类"改了没有任何门会响"的资产。**
而它同时是玩家**唯一直接看见**的东西。

## 一、G1 · 给美术资产一道门（**本波最高杠杆**）

**owns**：`tools/art_gate.py`（新文件）、`tools/ci.sh`
**不得触碰**：`tools/coif_characters.py`、`tools/deprop_characters.py`、`game/assets/art/**`（那是 G2 的行）

### 要守的性质

**出货的 `pro/` 必须等于 `coif_characters.py` 现在能重建出来的东西。**
不是"有个校验和文件对得上"——那种门可以靠更新校验和文件通过；是**当场重新生成、逐字节比对**。
`coif_characters.py --metrics` 已经证明重建管线是可复跑的（它的"改前"基线就是这么算的，见该文件 :444 注释）。

### 验收（每一条都要有回执，且**先证明它会红**）

1. **负对照①（已知的静默回退路径，必须红）**：单独跑 `python tools/deprop_characters.py`
   —— 已知它会把 F2 的成果在多数表上悄悄覆盖掉。**跑之前先确认现在所有门都是绿的**，
   跑之后新门必须 **RED**。⚠️ 在**隔离副本**里做，不要污染工作区。
2. **负对照②（单像素，必须红）**：把某一张出货表里**改一个像素**，新门必须 RED，且**报得出是哪一张**。
3. **未改动的树必须绿**（不许假红）。
4. **门必须自证它扫过东西**：输出里要打印**实际比对了几张表、几个字节**——
   数字为 0 却打 ✅ 是本仓库反复出现的病（docs/41 §2 第三个盲区）。
5. `bash tools/ci.sh` 全量 **PASS**，且**新门确实在 CI 里被执行到**（不是加了个从不运行的函数）。

### 明确不做

**不要**把描边规则塞进这道门——那 132 个像素是 G2 的活，两棒同时改会撞。
G1 只守"出货 == 可重建"。

## 二、G2 · **F2 自己写下的描边规则，在 5/10 张表上没有成立**

**owns**：`tools/coif_characters.py`、`game/assets/art/pro/**`
**不得触碰**：`tools/ci.sh`、`tools/art_gate.py`（G1 的行）

### 规则在哪

`coif_characters.py` 模块 docstring 第 3 条白纸黑字：**"新加的像素必须自带描边"**（描边色 `OUTLINE = #040404`）。
**而文件里没有任何一条断言在查它**——`--metrics` 只有断言 A（alpha 外扩 > 0）和 B（剪影两两可分度）。

### 实测（2026-07-30，主 session 量的；**口径是 12 个可达帧**，`ROWS_USED=(0,1,3) × COLS_USED=(0,1,2,3)`，`FRAME=32`）

裸边 = **在本帧剪影外沿（四邻中有透明/出帧）却不是描边色**的新增像素。

| 表 | 可达帧新增 px | 裸边 px | |
|---|---|---|---|
| Mage-Cyan（可可·贝雷帽） | 240 | **72** | ❌ 占新增的 30% |
| Archer-Green（小薇·双马尾） | 408 | 24 | ❌ |
| Mage-Red（阿丽·侧马尾） | 276 | 12 | ❌ |
| Warrior-Blue（阿本·鸭舌帽） | 264 | 12 | ❌ |
| **Character-Base（阿梅）** | 192 | **12** | ❌ **它同时是 `WorldView.FALLBACK_SPRITE`** |
| Archer-Purple / Warrior-Red / Soldier-Blue / Soldier-Red / Soldier-Yellow | — | 0 | ✅ |
| **合计** | | **132** | |

> ⚠️ **这个数字我自己先量错过一次，写在这里当警告**：第一版扫的是**整张 768×256 的表**，
> 得到 9/10 张违规、合计 **2066** px。但整张表有 **192 帧**（768÷32 = 24 列 × 256÷32 = 8 行），
> 出货只用 **12 帧**，其余 **180 帧**永远不上屏。
>
> **（二次更正，G2 抓到）**：这段原本写的是"整张表有 **768** 帧"——**也是错的**。
> 768 是**像素宽度**，不是帧数。一句用来警告别人「量错了对象」的话，
> 自己把对象的数量算错了。2066 那个故事是对的，故事里的算术不是。
> 换成可达帧口径才是 5/10、132。**量错的不是算法，是对象。**
> 你接手时**先复现 132 这个数**；复现不出来就先停下来查口径，不要直接开始改像素。

### 验收

1. **先让判据红**：把上面的裸边检查写成 `coif_characters.py` 的**断言 C**，在**未修的树**上跑，
   必须报出 **5 张表 / 132 px**（与上表逐行相同）。这一步是判别力证明，**不能跳**。
2. 修这 5 张表 ⇒ 断言 C 归 **0**。
3. **不许拆东墙补西墙**：断言 B（剪影两两可分度）的**最小值不得低于 120**，
   且**并列在最小值上的对数要一起报**（docs/44 §一·五 的教训：报单个极值会让读者以为其余都更宽松）。
4. `Character-Base` 必须归 0——它是玩家 sprite 缺失时的回退皮，脸上没描边会直接出现在屏幕上。
5. 断言 A（alpha 外扩 > 0）继续成立。
6. `bash tools/ci.sh` 全量 PASS。
7. **眼验**（R10）：修完之后至少出一张 2× 的对照图，人能看出"多了一圈描边"而不是"看不出差别"。
   看不出差别是一个**需要写进回执**的结果，不是失败。

## 三、G3 · 建筑：第三门有行为、有社会产出的分工（**阻塞中**）

**owns**：`game/scripts/Sim.gd`、`game/data/*.json`
**状态**：**已解封**（F5 于 2026-07-30 合入 `cd55df6`，那一行腾出来了）。

### 派单时必须带上的三条现状（都是 F5/F3 刚刚改掉的，抄旧文档会踩空）

1. ~~**#39 现在逐事件校验「在班」**：新岗位的 `shift` 写错 ⇒ 硬不变量直接红（不是软失败）。~~
   **⚠️ 这条是假的，G3 当场实测证伪（2026-07-30）。** 两个变体各跑 3 seed：
   `shift:["night"]` 与拼错的 `shift:["daytime","dusk"]` **都 S0 GATE PASS、#39 全绿**，
   而瓦产量从 646-771 塌到 90-390、缺货翻两番。
   机制：#39 从 `e["tick"]` 重算相位，再与**生产者当时用的同一个 `shift` 字段**比，构造上不可能不一致。
   ⇒ **#39 守的是「代码绕过班次门」，不守「数据把班次写错」。** 完整包络见 docs/41 §2.5。
   **写新岗位班次时，没有任何门会替你兜底——自己扫产量。**
2. **`audit_map.py` rule ⑨ 现在守 `job_action ∪ production.jobs`**：
   在 `production.jobs` 里声明的新岗位**必须有 worksite 广告它**，否则地图审计红。
   ⚠️ F3 实测过更宽的"遍历所有已声明职位"变体：干净树上 4 条假红，F5 之后降到 1 条（咖啡师）。
   **若你的新岗位由 `map.json` 的普通家具承担而非 worksite，会假红——报上来，不要靠删岗位绕过。**
3. **`VoiceGate`（CI 4f）现在是活门**：新增动作若没有台词，CI 直接红。
   F5 加了三个动作就当场被抓到（`dan|劈柴` / `hai|打渔` / `shu|授课`）。
   ⇒ **你加的每一个新动作都要配台词**，走 F4 立的规矩：本地 70B endpoint 出稿、人只改标点。

### F5 留下的两条经验，直接适用（别再交一次学费）

- **新工位是【竞争者】不是替代者**：`做活` 没有岗位门，F5 照抄 `amount: 26` 的结果是
  新工位一次都没被选中（渔夫在班完成 6-8 → **0/0/0**）。**吸引力要扫，不能抄。**
- **`type` 值写错会画错东西**：F5 的 `type: "waterfront"` 落不进 `BLD_PAL` ⇒ 在滩头画了栋灰房子；
  "开阔公共地面"目前**只有 `type: "plaza"`** 一个合法值。
- **精灵槽来自 `id.split("_")[0]`**：ID 前缀命不中贴图就出占位框（F1 的四个工位就这么出货了一整波）。

用户点名的分工里，商贩与环卫已由 F1/F5 落地，**建筑还没有**。要的不是一个会播动画的工地，
是**行为 + 社会产出**：有工位、有班次、有产物进镇库、有人消耗它、缺货时有社会后果。
派的时候必须带上：#39 现在**逐事件校验在班**（班次数据写错会直接红）、
rule ⑨ 现在守 `job_action ∪ production.jobs`（新岗位没有 worksite 会红）。

## 四、G4 · 阶段性产出：录屏 GIF + 截图 + README（**经 Codex 打磨**）

**owns**：`README.md`、`README_EN.md`、`media/**`
**不得触碰**：任何 `game/**`、`tools/**`

Wave E/F 的成果还没有一次面向读者的呈现。要的是：技术难点、创新点、**含录屏 GIF**。
硬约束：README 里每一个数字都必须能在仓库里找到出处（docs/4x 的某一节），
**不许出现没有出处的漂亮数字**——本 session 已经因此更正过三处（246→208、11→88 FPS、"最小对"是单数）。

## 五、共同约束（照抄，不重述理由）

- docs/41 红线四条：确定性 + 逐字节可回放；零模型可玩；消费级硬件含手机；无权重/二进制入 git，美术 CC0 或自绘。
- **每一棒的验收里必须有一条"这道门在什么情况下会红"，并且真的跑出来给我看。**
  绿字不是证据（docs/48 §四/§五/§六 三次实例）。
- 报单个极值时**连并列一起报**。
- 收紧一道门之前**先量它的余量**，否则是把假绿换成假红。
- 判据要连**采样间隔 / 帧口径 / horizon** 一起写死——本波 §二 那个 2066 就是口径错的产物。
- 隔离副本里做负对照，不要污染工作区；worktree 不会自动同步到派单时的基线（R7）。


---

## 六、真机眼验（2026-07-30，NX789J，Wave F+G 全栈第一次一起上机）

APK 现导现装（`livingtown-waveG.apk`，120 MB），跑起来截 3 帧。**证据：**
`docs/media/device_waveG_town.png`（整帧）、`docs/media/device_waveG_pond_3x.png`（3× 局部）。

### 先说验到的两件好事

- **F5 的工位改名在真机上成立**：澡堂的浴池/柴垛、工坊的两张工作台都是真精灵，
  **没有一个占位框、草地上没有一行数据键**。F5 修的正是"眼验过、干净"却根本没干净的那件事，
  所以它必须在真机上再看一眼——看了，成立。
- 镇子跑得动：第 2 天 18:17 暮、春晴、NPC 12、事件 86，纪事面板在讲 2/11 段故事。

### 再说眼验捞出来的新东西：**两个池塘是两块纯色贴纸**

| 证据 | 数 |
|---|---|
| 真机帧上北池取样 110×70 px | **1 种颜色**，7700 px 全同 `(12,117,100)` |
| `game/assets/art/terrain/water.png` | **16×16，1 种颜色**，256 px 全是 `(4,160,180)` |
| 同目录 `grass_b` / `grass_flowers` | 各 **4** 种颜色（草地有变化，水没有） |
| 水的贴图变体 | **只有 `water.png` 一张**，没有 b 版、没有岸边过渡瓦 |

⇒ 两个 8×5 的水域渲染成**硬 90° 直角的纯色矩形，直接压在草地上，没有任何岸线**。

**一个反讽值得记下来**：`WorldView.gd:2052` 的水层**有一条更好看的回退路径**——
纹理缺失时它画 `P_WATER_DEEP` 并在约 30% 的格子上点静态涟漪高光。
**那条路永远不会跑**，因为贴图存在。**一份更差的素材让一段更好的代码变成死代码。**
（这也是我判定"贴图路"而非"回退路"的依据：6×4 格的取样里一个涟漪都没有。）

**不是这一波的回归**：`water.png` 的时间戳是 6 月 27 日，从未改过。
它只是从来没有人拿新鲜眼睛看过一帧真机画面。

**为什么现在要紧**：F5 刚刚花了一整棒论证"渔夫必须留下"——理由是老海是
`model_hard_scenarios.json` 的承重案例、是"海边小镇"这个设定本身。
**而他打渔的那片水，现在看起来像一块刷上去的油漆。**

## 七、G5 · 让水像水（下一棒，未派）

**owns**：`game/assets/art/terrain/`、`tools/slice_*.py`、`game/scripts/WorldView.gd` 的水层
**不得触碰**：`pro/**` 与 `coif_characters.py`（G2 的行）、`tools/art_gate.py`（G1 的行，但**要读它**）

### 硬要求

1. **先决定走哪条路**：把 `water.png` 做成有变化的瓦片（+ 岸线过渡），
   还是**删掉它、让 `WorldView` 那条已有的涟漪回退路复活**？
   后者可能是更小的改动，且它的代码已经写好并注释过了。**两条都要量，不要凭喜好选。**
2. **岸线**：8×5 的硬直角是当前最刺眼的一处。至少要有一圈过渡（沙/浅水/草边）。
3. **红线 #4**：素材必须 CC0 或自绘，且**不得有生成图**（docs/43 R2）。
   现有 terrain 瓦片全是 16×16 纯色/近纯色手写，照这个路子加最省事。
4. **G1 的 `art_gate` 只守 `pro/` 那 10 张角色表**，`terrain/` 不在其中。
   G1 自己指出还有 **31 张出货 png**（emote 10 / decor 8 / terrain 5 / obj 5 / building 3）
   同样无门 ⇒ **本棒顺手把 `terrain/` 纳入同一套"当场重切 + 逐字节比对"**，
   照抄 `art_gate.py` 的形状，别发明第二套。

### 验收

- **真机截图前后对照**（不是 docker 帧）：同一 seed、同一 tick、同一分辨率。
- **取样断言**：北池取样区的 distinct colour 数**必须 > 1**（现在是 1）。
  这条要先在**未改动的树**上跑一遍，看它红，再改。
- 岸线：水域边界格与草地之间**不得**是单像素硬跳变（沿用 F3 的 `cross` p90 量具，
  **注意 F3 的结论：雨天必须判 p90 不判 max**）。
- `bash tools/ci.sh` 全绿；金标**不得移动**（纯素材改动）。
- 如果选"删贴图走回退路"，必须说明为什么那不是在开倒车。
