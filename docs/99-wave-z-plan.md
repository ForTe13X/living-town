# 99 · Wave Z 计划——**给"报了没补"的那几条补上门；把手艺信誉铺开；清掉悬空引用**

> 前置：docs/41 全部、docs/47 **R12**、**docs/96（Y1）**、**docs/97（Y2）**、**docs/98（Y3）**。
> **回执必须带 §2.5 三行包络，`does_not_detect` 必须是跑出来的。**

## 〇、这一波从哪来

上一波三根棒各留下**一条自己报了但没补的**，而三条同形：**我们收窄了某个东西，却没有任何门在维护那个收窄。**

1. **Y1**：出货的 `_survival_pull` 收窄成"只作用于 social"，而实测
   **放宽到全部需求 PASS、剂量翻倍也 PASS** ⇒ **这个收窄今天没有门守。**
2. **Y3 与 Y1 各自独立发现**：`Harness` 在**没传 `--golden`** 时，判决行照样打印 **`金标 过`**
   ⇒ **一道根本没跑的门被报成"过"。**
3. **X3/Y2 的互补性**已经补上守卫了（第 2f 步），**它是这一族里唯一已经收口的**——**照抄它的形状。**

## 一、Z1 · 给那两条补门（**观察侧，多半不动金标**）

**owns**：`game/bench/**`
**不得触碰**：`game/scripts/**`、`game/data/**`（Z2 的行）、`tools/**` / `docs/` 下除你自己那份外（Z3 的行）
**你的编号文档**：编号 **100**（⚠️ **三位数**，`lint_links` 刚为此修过，见 `2de5e44`）

### ① `金标 过` 那条

`Harness.gd:359` 打印 `"过" if not golden_red else "破"`，而 `golden_red` 在**金标从未加载**时同样是 false。
⇒ 与本 session 早先修过的"单 seed 软门恒过照样打 ✅"**同族**（那次的修法是**只改报告不改判据**，退出码一字节不变）。
**照那个先例做**，并**跑一次两侧对照**（传 `--golden` 与不传）证明输出确实分开了。

### ② `_survival_pull` 的"只作用于 social"没有门

Y1 实测：去掉该项 → 红；把 social 守卫取反 → 红；**但放宽到全部需求 → PASS、剂量翻倍 → PASS。**
⇒ 造一道门守住这个收窄。**先量余量再定判据**（本 session 写过两次零余量的判据）。
⚠️ **Y1 明说补它要动 `Invariants.gd`，那正是你的行。**

### 验收（Z1）

1. 两条各自的判据 + §2.5 三行包络 + **负对照**（Y1 那四个变异体里"放宽/翻倍"两个**必须由你这道门抓住**）。
2. `金标 过` 两侧对照的实际输出。
3. **金标逐字节不变**（观察侧改动不该动它）；若动了，走完整 R12。
4. `bash tools/ci.sh` 全绿（**读输出**；现在约 26 分钟）。

## 二、Z2 · 手艺信誉铺开 + `slipped` 够不着（**很可能走 R12**）

**owns**：`game/scripts/Sim.gd`、`game/data/**`
**不得触碰**：`game/bench/**`（Z1 的行）、`game/scripts/Story.gd` / `Director.gd`、`tools/**`、`docs/` 下除你自己那份外
**你的编号文档**：编号 **101**

### ① `production.craft_credit` 只覆盖 8 个产出岗位里的 1 个

V1 当初只给环卫工加了。Y3 复核后给了一条更准的判断：
**只有 B（干活的人）被钉死成同一个居民，而 A（见证者）是 8-10 个不同居民**
⇒ 叙述层不该做任何事，**缺的是配置事实，修法在数据侧，也就是你这里。**

⚠️ **别默认"全铺开"是对的。** 先量：**每个岗位的产出被看见的次数**（V1 的普查工具在
`game/bench/v1_social_census.gd`，**读它，别重写**）。**旁观者为 0 的岗位铺开等于加一条死通道。**
⇒ **量完再决定铺几个，并说明为什么是那几个。**

### ② `slipped`（说漏嘴）结构上够不着

W3 与 Y3 两次复核：`leak` 事件里**没有秘密主人**（只在 `beliefs[...]["owner"]`）。
⚠️ **Y3 同时更正了它的重要性**：同一个 `leak` 处理器也发 `betray`，
而 `betray` + `leaked` 的 `dir: rev` 恰好键到 `confide` 弧上 ⇒ **第一道门是通的，`slipped` 是第二道门。**
⇒ **先判断它值不值得修**；值就修，不值就**照实写"测了之后决定不做"**。

### 验收（Z2）

1. 逐岗位"产出被看见"的清点（**改前**，给展布）。
2. 铺开的决定 + 理由 + 前后对照；`#41` 不得被拆掉；`#16`/`#20`/`#5`/`#29`/放逐一族仍绿。
3. `slipped` 的判定与处置。
4. N=12 逐字节不变或走**完整 R12**（⚠️ U1/V1/Y1 已各重烘一次，**你的基线是它们之后**）。
5. §2.5 三行包络；`bash tools/ci.sh` 全绿（读输出）。

## 三、Z3 · 悬空引用 + 阶段产出（**`tools/` + `docs/`**）

**owns**：`tools/**`、`docs/**`（除 Z1/Z2 各自那份外）
**不得触碰**：任何 `game/**`
**你的编号文档**：编号 **102**

### ① `@branch` 逃生门现在会说话了，而它说的话有三类

X3 给 `lint_links` 加了真解析，实测 6 条不同引用 / 27 处：
- `docs/54@worktree-agent-…` ×2 —— **已经并进 HEAD** ⇒ **那个后缀可以去掉了**。
- `docs/45@claude/objective-lumiere-e6f125` ×4 —— **不在该分支上**，但 HEAD 上有 ⇒ **分支名已经不对**。
- `docs/52@worktree-agent-a0c76a3f96ae5dbcf` ×9 —— **分支在、文档不在** ⇒ **9 处指向一个不存在的文件**。
  （S2 与 X3 各自独立查到过这一条；docs/50 §四派的 H4 从未交付。）

⇒ **前两类是可以当场清干净的；第三类不是笔误，是"派出去没回来的一棒"** ——
**别把它改成看起来干净的样子**，请**照实标注它指向的东西不存在**，并保留可追溯性。

### ② 阶段产出

`docs/56` 停在 Wave X（X2 那一版覆盖 U/V/W）。此后 X/Y 两波六根棒，其中**至少三条要写进去**：
- **硬 `#01` 在 N=40 上的成因不是口粮，是社交自己的生存门把自己锁死**（X1），而 Y1 把它修到了出货。
- **叙述审计从"只守有依据"推进到"抓得住 96.8% 的跨类型胡说"**（Y3，592 个变异体全跑）。
- **"金标 12/12 不变" ≠ "N=12 不变"**（Y1：另外 48 个 seed 有 31% 会动，0/12 是 ~1.1% 的巧合）。

⚠️ **不得声称任何你没亲自跑过的门是绿的**；**别把 `lint_links` 的失败输出粘进回执**（R2/R3/U2 都踩过）；
**整篇改写会静默删掉限制条款**（X2 自陈，且没有门会响）⇒ **改完与上一版逐条对一遍。**

### 验收（Z3）

1. 三类悬空引用各自的处置 + `lint_links` 的**实际输出**（含 `@branch` 那一段）。
2. `docs/56` 新版要点 + `docs/13` 补 X→Y 段。
3. **与上一版逐条对照**，证明没有静默删掉限制条款。

## 四、共同约束（照抄，不重述理由）

- docs/41 红线四条 + §0.5；**改契约是用户的决定**。
- **红数不是判据**（X1/Y1：一个语义无关的 0.25% 扰动同样消掉两个硬红）⇒ **对着零假设臂用连续余量判。**
- **"金标 N/N 不变" ≠ "该 N 不变"**（Y1 实测）——**别用金标的边界去挑常数。**
- **可比性 ⟺ 两棵树在 `game/scripts/` 与 `game/data/` 上逐字节相同**（Y1 更正了我过度一般化的说法），
  **一条 `git diff` 就能查**；查完仍建议自己跑基线。
- **`does_not_detect` 必须是跑出来的**；**"测了之后决定不做"必须写进回执**。
- **先确认你量的是哪个对象**——H1/S3/V3/W3 四次量错，**其中三次是读过这条纪律之后踩的**；
  Y3 又补了一条：**"按条数"排序本身就可能是错的口径**（`give` 看着最有关系分量，可落地率 0.9%）。
- **`tee | tail` 会吃掉退出码**；**别在 CI 跑的时候改它正在读的任何文件**。
- **编号是三位数了**（`lint_links` 刚修，见 `2de5e44`）；**正文里仍然不要写 `docs/` + 数字的纯文本形式指向尚不存在的号**。
- **worktree 的 checkout 可能落后 260+ 个 commit**：**先确认基线，是祖先才 ff。**
- CI 现在约 **26 分钟** ⇒ **`run_in_background` 或分步跑。**
- 隔离副本里做负对照；越界要声明。
