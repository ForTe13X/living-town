# 67 · Wave R 计划——**还两笔 L2 自己记下的账；补一道没人守的"缺货变太少"；把索引追回来**

> 前置：docs/41 全部（**§0.5 规模矩阵**、**§0.8 大设计前评审**、**§2.5 探测包络**、**§1.5 读仓库纪律**）、
> docs/47 **R12**、docs/58 §四（L2 的回执，本波两条码的来源）、docs/66（Q1 刚落的接触门）。
> **回执必须带 §2.5 三行包络，`does_not_detect` 必须是跑出来的。**

## 〇、先更正两条我自己转述错的

1. **"N=20 那个回归"已经结案，不是待办。** M1 的 768 次运行（`630e40d`）说 `#01` 的红率
   **在四个配置上分辨不出来**——那不是 L2 造的回归；M2 随后用剂量-响应曲线把 `SURVIVAL_GATE`
   32→36（`d7f4ac4`）。**我在上一轮小结里把它列成未决，是错的。**
2. **`#01` "~1.3%"** 是 M2 修之前的数，且 1.3% 本身是 docs/13 里一条 **Wilson 区间的下界**
   （1.3%–15.8%），不是点估计。**引用它当"当前残余红率"是双重失真。**
   ⇒ 本波不以它立论；谁要用这个数，**自己重测**。

## 一、R1 · 经济的两笔欠账（**很可能移动金标，走 R12**）

**owns**：`game/scripts/Sim.gd`、`game/data/production.json`、`game/data/jobs.json`、
`game/bench/Invariants.gd`、`tools/audit_map.py`
**不得触碰**：`tools/ci.sh`（R2 的行）、`game/scripts/WorldView.gd`、`game/assets/**`、
`game/data/interiors.json`（R2 的行）、`docs/README.md` / `README.md` / `docs/13` / `docs/56`（R3 的行）
**你的编号文档**：`docs/68-*.md`（**已替你占好，别另取号**）

### ① `#40` 没有上限臂——"缺货变得太少"是一个只有人读表才能发现的回归

L2 的 `work_pull` 把全年零缺货的 seed 数推成（12 个 seed 中）：

| | 口粮 | 柴薪 | 屋瓦 | 豆子 | 话本 | 整洁 |
|---|---|---|---|---|---|---|
| 出货 N=12 参照 | 0 | 0 | 1 | 5 | 5 | 5 |
| N=60 改前 | 0 | 0 | 1 | 12 | 12 | 0 |
| **N=60 改后** | 0 | **2** | **4** | 12 | 12 | **8** |

`production.json._calibration` 写的是"**周期性缺货，不是从不缺货**"，
而整洁 8/12、屋瓦 4/12 已经越过去了。**K1 的 nc3 已证 `#40` 批量 ×8 照样绿 ⇒ 它只有下限臂。**

**要做的**：
- **先量再动**：`k` 现在是被 N=16 的余量钉死的**一条直线**，L2 明写数据要的是
  "**低渐近线 + 早期更陡**"的形状。**扫出来再选形状，别直接套一个函数。**
- **给 `#40` 加上限臂**——判据由你定，但**必须先量余量再定阈值**
  （本 session 已经两次写出零余量的判据）。上限臂必须有**负对照**：
  故意把产量批量放大，门必须变红；**不红就是这道门没牙。**
- ⚠️ **豆子/话本在改前就已经是 12/12 全年零缺货**——它们**不是** `work_pull` 造成的，
  别把它们算进"过冲"的账上。**要不要连它们一起收，是另一个决定，请单独说明。**

### ② `看摊` 是第九个工作动作，而且没有岗位门

`jobs.json.extra_advertises` 往 `counter_1` 注入 `看摊`（`amount: 20`，与烤点并列全场最低），
**它没有 `job` 键**。docs/58 说 `counter_1` 也没有 `staff` 标志，
**但 docs/13:830 写着"阿丽的吧台标 `staff:true`"——这两条互相矛盾，请你去代码里判，别采信任何一边。**

⇒ 后果：任何人都能枚举到"看摊"；且 `work_pull` 只加成**岗位门控且在班**的广告 ⇒ 看摊拿不到加成。
同时它影响 `audit_map.py` rule ⑨ 的语义（F3 定的是 `job_action ∪ production.jobs`，这条广告两边都不在）。

⚠️ **`看摊` 是 `#01` 的历史关键**：docs/13:802 明写"**吧台 advertises social → 独自看摊也能解社交、
孤守店里绝不社交饿穿**"。**给它加门有可能把阿丽推回饿穿。**
⇒ **改之前先把这条路径量出来**；若加门会伤 `#01`，**照实写"测了之后决定不做"**——那也是交付。

### 验收（R1）

1. 分数/产量的扫描结果（逐 N、逐岗位、**给展布不给均值**）。
2. `#40` 上限臂 + **负对照证明它会红**。
3. `看摊` 的判定 + 证据；若加门，**`#01` 前后对照必须给**（阿丽的驻店率、社交满足路径）。
4. N=12 逐字节不变，或走**完整 R12**（三锚重烘 + `rebake_history` + held-out 13-30 两侧）。
5. §2.5 三行包络；`bash tools/ci.sh` 全绿（**读输出，不是读退出码**）。

## 二、R2 · 室内是"数据驱动"的，但它**看起来**是什么样？（art / visual）

**owns**：`game/scripts/WorldView.gd`、`game/assets/**`、`game/data/interiors.json`、
`tools/ci.sh`（**本波只有你能改它**）、`tools/` 下的美术相关脚本
**不得触碰**：`game/scripts/Sim.gd`、`game/data/production.json` / `jobs.json`（R1 的行）、
`game/bench/Invariants.gd`、`docs/README.md` / `README.md`（R3 的行）
**你的编号文档**：`docs/69-*.md`（**已替你占好**）

README 现在这么写："**多层室内**：七栋楼都有数据驱动的内部，居民真的会进屋、上楼、回家睡觉，**不是贴图**。"

**这句话说的是行为，读者会当成画面读。** 本波要回答的是画面那一半：

1. **先量**：进到七栋楼内部各截一张图（真引擎，不是 headless 猜）。
   逐栋列出：地板/墙/家具各用了哪些贴图、有几种、**有多少栋在视觉上是彼此的复制品**。
   ⚠️ H1 的教训在这里直接适用：**它量的是"一张 sheet 服务 5 个对象"的别名率，不是"缺贴图"**——
   **先确认你量的是哪个对象。**
2. **再动**：把最差的那几栋提上来。**红线 R2（docs/43）：素材必须 CC0 或自己画的，
   生成图不得作为出货素材。** GPT 图生成**只能当参考/情绪板，绝不能进 `game/assets/`。**
3. **加门**：本波允许你动 `tools/ci.sh`（加一道室内美术门），
   但**任何新门都要按 §2.5 报 `detects` / `does_not_detect` / `confidence: N=?`**，
   且 `does_not_detect` **必须是跑出来的**。**别在 CI 正跑的时候改 `ci.sh`**（§1.5②，bash 按字节偏移读文件）。
4. **眼验 + 存证**：真引擎截图放 `docs/media/`，命名带 `r2_` 前缀。
   若能录一段室内的短片，一并出（`tools/record-godot.sh` → `tools/make_gif.sh`；
   **只走整数倍缩放，且实测唯一可读的档位是 1×**——README 抬头那段是这条流水线自己量出来的）。

### 验收（R2）

1. 七栋楼的逐栋清点表（**改前**），含复制品对数。
2. 改动 + **前后对照截图**（同一栋、同一机位）。
3. 新门的 §2.5 三行包络 + 负对照（**故意抽掉一张贴图，门必须红**）。
4. **仿真侧逐字节不变**——你只改了表现层，**请用 digest 证明它，别只是声称**。
5. `bash tools/ci.sh` 全绿（读输出）。

## 三、R3 · 索引停在 48，而树已经到 66（docs / 阶段产出）

**owns**：`docs/README.md`、`README.md`、`README_EN.md`、`docs/56-阶段报告-给非技术读者.md`、
`docs/13-实验札记-experiment-journey.md`、`docs/05-路线图与里程碑.md`
**不得触碰**：任何 `game/**`、`tools/**`
**你的编号文档**：`docs/70-*.md`（**已替你占好**）

### 三件事

1. **`docs/README.md` 的抬头写着"本树上有编号文档 00-25、28-48"——树上已经到 66。**
   把 49-66 补进索引与分类表。
   ⚠️ **不要引用 67 之后的任何编号**（68/69 是 R1/R2 的，**它们此刻还不存在**）。
   **本 session 已经两次因为写死未来文档名把 `lint_links` 弄红了，别成为第三次。**
2. **`docs/56`（给非技术读者的阶段报告）要出新的一版。** 它写于 K 波之前，而此后落地了：
   双尺度产出池（K1）、`work_pull`（L2）、`SURVIVAL_GATE` 32→36（M2）、
   谣言通道打通（O1：旅人的消息从 0 个人到 15 个人）、多镇提案的外部评审裁决（docs/62）、
   知识局部性实测（P1/docs/63）、模型路的知识泄漏判定（Q2/docs/65）、派系接触门（Q1/docs/66）。
   **口径照旧：不含一行代码，"还不行的"那节继续直白，不替它打圆场。**
   **每一条都要能追到一份编号文档或一个 commit**——你写的是给人看的报告，**不是营销文案**。
3. **`docs/13` 实验札记**补上 K-Q 这一段。**照旧记被推翻的结论**：本段里至少有
   O1 推翻三个假说、P1 的推断被 Q1 证伪、我把 `#5` 的软硬档次说错、
   我把 N=20 当成未决——**这些都要在里面。**

⚠️ **README 的"经 Codex 打磨"这一环本波拿不到**：Codex desktop 的 computer-use 授权被用户拒绝过，
**不再重复弹窗**。⇒ **README 由你自己打磨，并在回执里明写这一环没走。**

### 验收（R3）

1. `docs/README.md` 覆盖到 66 且 **`lint_links` 绿**（`bash tools/ci.sh` 里那一步，或单跑）。
2. 新版 `docs/56` + 每条断言的出处（编号文档或 commit）。
3. `docs/13` 的 K-Q 段，**含至少四条被推翻的结论**。
4. **不得声称任何你没跑过的门是绿的。**

## 四、共同约束（照抄，不重述理由）

- docs/41 红线四条 + §0.5 规模矩阵；**计划不得给契约加码**。
- **`does_not_detect` 必须是跑出来的**；**"测了之后决定不做"必须写进回执**。
- **看到"零引用/零调用"先跑 `git log -S`**；**别在 CI 跑的时候改 `ci.sh`**。
- **加不齐先重新数**；**报极值连并列一起报**；**收紧判据前先量余量**。
- **文档编号已在上面替你们占好**——别自己另取，本 session 撞过号。
- 隔离副本里做负对照；越界要声明。
