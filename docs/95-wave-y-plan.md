# 95 · Wave Y 计划——**把 X1 那条已经量出效果的臂做完；把 X3 的菜单买下来；故事层第二轮**

> 前置：docs/41 全部、docs/47 **R12**、**docs/92（X1：#01 的族判定与两条不出货的臂）**、
> **docs/94（X3：夹具有效性普查与它的价签菜单）**、docs/90（W3 故事层）。
> **回执必须带 §2.5 三行包络，`does_not_detect` 必须是跑出来的。**

## 〇、这一波从哪来——三条都是上一波自己交出来的

1. **X1 的臂 B（`obj_survival_pull`）明显有效但刻意没出货**：
   17/5 更优（p≈0.0085）、社交地板中位 16.90→19.25、最差地板 **0.00→14.10**、最长锁死 380→158。
   **它欠的是 R12 + N=12/16 回归 + 剂量曲线顶点 + `need_id=="social"` 的隔离。**
   ⇒ **本波把它做完，或者证明它做不完。**
2. **X3 给了一张带价签的菜单**，其中一条已经验证过：
   `CI_BG_DAYS` 8→30 把后端门的空转率从 **46% 降到 27%**，约 **+140 s**，**实测仍然绿**。
3. 故事层覆盖率 W3 做到 **56.0%**，而它明写还有两支够不着。

## 一、Y1 · 把臂 B 做完（**必然走 R12**）

**owns**：`game/scripts/Sim.gd`、`game/data/**`、`game/bench/**`
**不得触碰**：`tools/**`（Y2 的行）、`game/scripts/Story.gd` / `Director.gd`（Y3 的行）、`WorldView.gd` / `Main.gd`、
`docs/` 下除你自己那份外的任何文件
**你的编号文档**：编号 **96**

### X1 已经做完的（**别重跑**）

- **族判定**：`#01` 在 N=40 上红**不是口粮的下游**——`_social_candidates` 在 `_min_need < SURVIVAL_GATE` 时返回 `[]`，
  **而当 argmin 就是 social 时，这道门关掉的正是唯一能补它的通道**。
  触底 tick 成因 `GATED` 27/27 与 125/125，而 **`ALONE` 是 0/27 与 0/125**。
- **补丁在 `analysis/x1/arms/patch_B_obj_survival_pull.py`**，臂的原始数据在 `analysis/x1/arms/`。
- ⚠️ **判据不能用红数**：X1 实测 `obj_dist_penalty` 0.400→0.401（**与社交语义无关**）**同样让两个硬红都消失**
  ⇒ 在 2/60 的基准率上"红没了"对任何足够大的扰动都成立。**一律用连续余量对着那条零假设臂判。**

### 本波要补齐的四件

1. **N=12 / N=16 回归**：两格都不得回归（这是 CI 真正跑的两格）。
2. **剂量曲线**：找顶点，**别停在第一个能用的值上**——M2 当年就是这么做对的。
3. **`need_id=="social"` 的隔离**：证明这条修法**只**作用在 social 上，没有顺手改别的需求。
4. **完整 R12**：三锚重烘 + `rebake_history` + held-out 13-30 两侧。
   ⚠️ U1、V1 已各重烘一次，**你的基线是它们之后**；且 **W2 证明跨树拼接会出事**（144 个可比 seed 零个摘要相同）
   ⇒ **自己的基线自己跑。**

### 验收（Y1）

1. 剂量曲线（**给展布不给均值**）+ 顶点的选择理由。
2. `N=40 seeds 1-12` 与 `49-60` 的逐 seed 前后对照，**对着零假设臂判、不看红数**。
3. N=12/N=16 不回归；`#41`/`#29`/`#16`/`#20`/`#5`/放逐一族仍绿。
4. 完整 R12；§2.5 三行包络；`bash tools/ci.sh` 全绿（**读输出**）。
5. **做不完就写"做不完 + 卡在哪 + 下一个该测什么"**——X1 与 T1 都这么交过，而那两份回执各自成了下一波的立项依据。

## 二、Y2 · 把菜单买下来（**`tools/`**）

**owns**：`tools/**`、你的编号文档
**不得触碰**：`game/**`
**你的编号文档**：编号 **97**

X3 量了但没买的三条，**价签都在编号 94 里**：

1. **`CI_BG_DAYS` 8→30**：后端门空转 46%→27%，约 +140 s，**X3 已验证仍然绿**。⇒ **买。**
2. **`player_agency_test` 打印冻结的"实测地板 101"，而今天的现算值是 85**
   ——**而 S2 当初把它更正成的 81 本身也已经过期**。⇒ **这是同一个字面量的第三代**。
   改成**现算现打印**（S2 的统一结论：每次重算并打印的臂至今全准，打印冻结字面量的全过期）。
3. **`CI_GOALS_DAYS` 14→30**：能让 11 个目标全可达，**但它的判据只查"≥1"**
   ⇒ **先判断"把天数抬上去"值不值**，若判据本身太弱，**说出来并给建议，别顺手改判据**。

### 还有一件（**X3 自己点出来的那条脆弱性**）

X3 说：全 CI 夹具空转率合看是 **0**，**但那只因为 4c 的 betray 轨与 4a 的 N=16 恰好盖住了 step 4 的洞
——而没有任何东西在维护这份互补。**

⇒ **给它加一道守卫**：让"某条不变量在**所有**门控它的夹具上都空转"这件事**可机检**。
⚠️ **别把 CI 弄红**：若今天就有这样的不变量，**那是发现不是 bug**——照实报。
X3 的普查工具已在树上（`tools/gate_fixture_audit.py`），**复用它，别重写。**

### 验收（Y2）

1. 三条菜单各自的处置 + 实测代价（**墙钟给区间，X3 说机器负载的影响比改动还大**）。
2. 互补性守卫：判据 + §2.5 三行包络 + **负对照**（造一个"处处空转"的情形，它必须响）。
3. `bash tools/ci.sh` 全绿（读输出）。⚠️ **造了门就在它真要跑的那个环境里跑一次。**

## 三、Y3 · 故事层第二轮（**只动叙述层**）

**owns**：`game/scripts/Story.gd`、`game/scripts/Director.gd`
**不得触碰**：`game/scripts/Sim.gd` / `game/data/**` / `game/bench/**`（Y1 的行）、`tools/**`（Y2 的行）、`WorldView.gd` / `Main.gd`
**你的编号文档**：编号 **98**

W3 把覆盖率做到 **56.0%（14/25 种事件）/ 21.3%（按条数）**，并留下三件：

1. **`slipped`（说漏嘴）结构上够不着**：`leak` 事件里没有秘密主人（只在 `beliefs[...]["owner"]`）。
   ⚠️ 修它要动 `Sim.gd`（Y1 的行）⇒ **本波别修，但请复核这个判断是否仍然成立**（`git log -S` + 现读代码）。
2. **手艺弧只可能关于同一个居民**：`production.craft_credit` 只覆盖 8 个产出岗位里的 1 个。
   ⇒ **那是数据问题（Y1 的行），不是你的**；但**叙述层要不要为此做点什么**，请判断并说明。
3. 覆盖率还有 11 种事件在外面。**先量哪几种最值钱**（按条数 × 可读性），再动。

⚠️ **W3 用负对照钉死了一条边界，本波必须继承并且不得削弱**：
把两条弧的文案对调 ⇒ 屏幕上胡说八道而**审计全绿** ⇒ **这道审计守的是"有依据"，不是"说得对"。**
⇒ **若你能让它多守一点"说得对"，那是真进展；做不到就照实说做不到。**

### 验收（Y3）

1. 改前清点 + 你选了哪几种事件与为什么（**给数字**）。
2. 覆盖率前后 + 可追溯性**机制**（不是保证）。
3. **仿真侧逐字节不变**：三条独立证据（自造 A/B + 开工前的金标 + 留出 seed）。
4. §2.5 三行包络；`bash tools/ci.sh` 全绿（读输出）。
   ⚠️ **`story_test` 现在跑 40 天了**（X3 把它从 14 天改上去，因为 14 天时它不可能变红）——
   **你的改动会真的被它判**。

## 四、共同约束（照抄，不重述理由）

- docs/41 红线四条 + §0.5；**计划不得给契约加码**；**改契约是用户的决定**。
- **`does_not_detect` 必须是跑出来的**；**"测了之后决定不做"必须写进回执**。
- **红数不是判据**（X1：一个语义无关的 0.25% 扰动同样能消掉两个硬红）⇒ **用连续余量对着零假设臂判。**
- **跨树拼接会出事**（W2：144 个可比 seed 零个摘要相同）⇒ **自己的基线自己跑。**
- **单 seed 的 PASS 不含信息**；**`tee | tail` 会吃掉退出码** ⇒ 写文件再读。
- **别在 CI 跑的时候改它正在读的任何文件**；**整篇改写会静默删掉限制条款**（X2 自陈，且没有门会响）。
- **先确认你量的是哪个对象**——H1/S3/V3/W3 四根棒的第一版度量都量错了对象，**其中三次是读过这条纪律之后踩的**。
- **报极值连并列一起报**；**收紧判据前先量余量**；**看到"零引用"先跑 `git log -S`**。
- **编号已占好，但【不要】在正文写 `docs/` + 数字的纯文本形式**；
  **别把 `lint_links` 自己的失败输出粘进回执**。
- **worktree 的 checkout 可能落后 250 个 commit**（上一波三根棒都是 249）：**先确认基线，是祖先才 ff。**
- 隔离副本里做负对照；越界要声明。
