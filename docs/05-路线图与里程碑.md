# 05 · 路线图与里程碑

> 原则（取自 22nd 成功经验）：**先做无悔的确定性地基（无模型也好玩），再叠 LLM 灵魂；一次只动一个旋钮；
> 用复用的 bench/自走子做可复现评测。** 每个里程碑都要「可玩 + 可 headless 验证」。
>
> **本文档结构**：§A 是**当前路线图**（now / next / later），§B 起是 M0-M6 里程碑的**历史记录**。
> M0-M4 已交付；M5/M6 的原始描述早于规模 / LOD / 端上 SLM 这一整段，只作为出发点的存档，不再是计划。

---

# §A · 当前路线图（更新于 2026-07-25；**权威计划指针更新于 2026-08-01**）

> ⚠️⚠️ **先读这一段，再读下面的表。**
> **2026-07-26 起，工作按「波」组织，权威计划是当波的编号文档，不是本 §A 的 NOW/NEXT 表。**
> 到 2026-08-01 为止已经走完 **Wave C → R** 十几波，本 §A 下面那两张表**基本已被逐条兑现或取代**：
>
> | 波 | 计划文档 | 一句话 |
> |---|---|---|
> | C | [43](43-wave-c-plan.md) | 从「研究仓库」转向「像个游戏」 |
> | D | [46](46-wave-d-plan.md) | 从「看得清」到「有人想玩」 |
> | E | [47](47-wave-e-plan.md) | 让劳动真的产出东西；立 **R12**（会移动金标的改动怎么验收） |
> | F | [48](48-wave-f-plan.md) | 分工要有地方发生；拆三道长绿门 |
> | G/H/I/J | [49](49-wave-g-plan.md) / [50](50-wave-h-plan.md) / [53](53-wave-i-plan.md) / [55](55-wave-j-plan.md) | 美术从「一道门都没有」到 46 张里 45 张有门；**先看，再上门** |
> | K/L/M | [57](57-wave-k-plan.md) / [58](58-wave-l-plan.md) | 双尺度落地；CI 第一次跑池倍率 ≠ 1；`SURVIVAL_GATE` 32→36 |
> | N/O | [60](60-multi-town-feasibility.md) / [61](61-wave-o-plan.md) | 多镇主诉先被证伪、再被救活（旅人的消息 0 → 15 人） |
> | P/Q | [63](63-epistemic-locality.md) / [64](64-wave-q-plan.md) / [65](65-model-path-epistemic-read.md) / [66](66-faction-contact-gate.md) | 知识局部性实测；派系接触门；模型路的知识泄漏 |
> | **R（当前）** | **[67](67-wave-r-plan.md)** | 还 L2 的两笔账；给 `#40` 补上限臂；把索引与阶段产出追回来 |
>
> 外部评审裁决（多镇提案，**不批准整体开工**）见 [62](62-multi-town-review-verdict.md)；
> 共同约束与规模能力矩阵见 [41](41-baton-contract.md)（§0.5 / §0.8 / §2.5）。
>
> ⚠️ 下面这段是 2026-07-26 写的、**只针对 Wave C 的**过期声明，保留原文不改写：
> 本 §A 的 NOW/NEXT 里有 **5 行已经过期**，逐条更正见 [docs/43 §1.1](43-wave-c-plan.md)：
> 戏剧事件**已经**全部 emit（五处 `Sim.gd` 行号在那里）；`buildings.json` **已填**（7 栋 / 12 间房）；
> 灯笼节对象**已移进广场**（到场 9.94 人/节日日）；"ZOOM_MIN 0.6 只见 14%"**混了两个 bug**；
> "把确定性钉在单一二进制之外"**已做完**（金标 + 逐 tick 前缀链 + 自有 FNV-1a + 稳定候选身份加盐）。
> **不要照着下面这两张表排期，先读 docs/43。**
> 另：docs/43 §1.2 记录了**两条已经写完但没合进 `integration/batons` 的工作**（模型路径不变量门、节日 CI 门）——
> 只读 docs/34-42 是看不出来的。

## 红线（每条候选工作都要过这五关）

1. **确定性 + 逐字节回放**：决策逻辑里没有 RNG（`randi/randf/randomize`）也没有墙钟（`Time.*`）；CI 跑 `Sim.backend = null`。
2. **无模型可玩**：逻辑地板独立成立，模型是可 ablate 的皮肤。
3. **消费级硬件，含手机**。
4. **无版权风险**：美术 CC0，模型权重不随仓库分发。
5. **复用优先**：先扩已有的 bench，别造新基建。

外加一条本项目特有的架构红线：**相机可以决定画什么，绝不能决定仿真什么**。
它由 `game/bench/lod_verify.gd` 机器化守着（5 个 `lod_focus` 值 → 同一 digest），在 `tools/ci.sh` 第 4b 关。

## NOW —— 桌面即可做，无阻塞

| 方向 | 为什么现在做 |
|---|---|
| **把确定性钉在单一二进制之外** | 红线 #1 是逐字节回放，但仓库里**没有任何 golden digest**——CI 今天只证明"同一个二进制跑两遍答案一样"。而 `_aid()` 用引擎内部的 `String.hash()` 派生每条 RNG 流、平局抖动按候选**数组下标**加盐，所以一次 Godot 升级或一次无害的枚举重构就能改写全部历史而 CI 全绿。配套：把软门的绝对容差换成**比率**（现在是 `seeds.size() - 1`，加 seed 反而更严）、给 scenario 建确定性门、加"被 git 跟踪的模型权重 / 缺数据文件"守卫。 |
| **让已仿真的戏剧变得可读** | 30 天 headless 跑出 ~1100 事件、37/37 全绿，然后表现层把最戏剧的五类事件（betray / conflict-born / election / pact formed / pact dissolved）整个丢掉——它们只 `_log_event` 从不 `emit_signal("social_event")`，玩家**根本无从得知**。剩下的还有约 28% 以原始英文 id 打进日志。修法早已躺在引擎里没接线（`Sim._verb`、没人监听的 `log_line` 信号）。这是**表现问题不是仿真问题**，且在没有键盘的手机上同样生效。 |
| **让小镇看起来像个镇子，让地图够得着** | 只有广场有真地板，其余七个区只是 0.10 alpha 的色块 → 建筑读起来像"里面长草的无顶围栏"；`ZOOM_MIN 0.6` 对 64×48 的地图意味着"回到全镇"只显示约 14% 的镇子；关系连线无上限无剔除，第 48 天变成横跨全图的毛线团（真机实测占 166.7ms 一帧里的 59.5ms——修可读性顺便买回帧时间）。全是 View 代码，碰不到红线。 |
| **公开面说实话**（本轮） | README 里有且只有一处**可证伪**的过度声称（Node 端口并不跑同一组不变量），以及大量**声称不足**（一整月最难的工程在 README 里一个字没有）。详见 §A 末尾"本轮已做"。 |
| **把方便的分母换成诚实的分母** | 端上 SLM 的 67% 是 landed/**fired**，不是 landed/总决策——worker 严格串行，**镇级吞吐与 N 无关**。另一处：LOD 是否默认开，至今只对**硬**不变量评估过（`LodAblation.gd` 明写"软不变量按设计可漂"），而软不变量恰恰就是产品本身。抓自己的方便分母是本项目自己立的规矩，该用在这两处。 |

## NEXT —— 排在上面之后，或需要把手机接回来

- **给帧埋永久计时器**。全仓库 `grep` 不到一个 `get_ticks_usec`——那次最有价值的性能测量（sim-tick 63.9ms + 社交/agent 绘制 59.5ms / 166.7ms 一帧）只活在一次 docs commit 里。同时把**原生堆**报进 perf overlay：`OS.get_static_memory_usage` 只数 GDScript 那半边，正是这个盲区藏住了 16GB 原生泄漏。再加一个 `[sim] lod_agg` 写进 `user://settings.cfg`（照抄已验证好用的 `[slm] use_gpu` 那套），让 LOD 的 A/B 变成改文件而不是重打 APK。
- **那次从没做过的真机跑**。两个旗舰声称各自有证据、合起来没有证据：所有真机数据点要么是 N≈12 + SLM，要么是 N=200 + logic，**从没有出货配置的交集**（最多 60 居民 **且** 端上推理）。两笔开销压在同一颗 CPU 上：N=60 的仿真开销约是 N=12 的 5 倍，而推理开销不变，且单是 CPU 推理就已经把 FPS 从 88 压到 34（N=12 时）。一并卡在这里的还有：N=60 的 LOD 开/关 A/B、N=60 长时段的渲染开销（桌面软渲染明确**不是**有效替身）、以及 M1 跨平台确定性门的 ARM 那一半。
- **需要重新烘 golden 的基线改动**（必须单独做，不能并行）：把灯笼节的对象从 [12,7]（住宅区里、离广场约 30 格，实测四次节日**到场人数为 0**）挪进广场矩形；把 `game/data/buildings.json` 填上（现在 `buildings: []`）以点亮已经写好的约 135 行剖面建筑渲染器，并恢复"房间隐私门"——否则秘密可以在人挤人的广场上耳语。
- **给社交的"暖"那一半重新配平**。跑一个模拟月的默认存档产出：2 次约会、**0 次结盟、0 次互助、0 次吐露/泄密/背叛**。出货的是一台声誉机器，不是宣传语里那座会自愈的小镇。
- **Node 端口：补齐或退役**。它已经跟数据漂开、部分 seed 跑红、且不在 CI 里（见 [docs/08 §1](08-测试与验证.md)）。要么补 #34-#37 并接进 CI，要么明确标成历史存档。

## LATER

- **声音**：全仓库 `grep` 不到任何 `AudioStream`——一个 19.2 秒走完完整昼夜的镇子是**全哑的**。
- **手机上能玩而不只是能看**：7 个玩家动词全是键盘绑定、player 模式只有 CLI；需要触屏动作条 + 一次宽高比/HUD 锚点整理（现在约 25% 是黑边）。
- **目标与上手**：没有目标、没有引导、没有单局形状——第 3 分钟没有任何东西把玩家叫回来。
- **新美术**：约 20 类社交事件里有 10 类没有表情图标，也没有立绘。两者都是"落在既有回退链后面的附加文件"，符合本项目已有的署名先例。
- **模型输出质量**：端上瓶颈已从延迟转到输出质量——但**"bad_parse 29-36%"这个数是错的**（2026-07-26 修正，见 [docs/36](36-model-influence-delta-over-c.md)）：它把"模型回 prose"与"回答有效但等待期世界变了(候选过期)"混为一谈。拆开后**真实格式不遵从 7.5%-19.4%**，且随镇子变大而下降（N=60 时三分之二的表观失败是陈旧）。**更根本的问题不是格式而是方向**：shadow 对照实测模型的选择与**均匀随机不可区分**，在引擎效用轴上还略差于随机（Δ/L 与零假设相差 ≤1.4pt、分数序位 0.41-0.54 vs 随机 0.50）。所以"提升输出质量"的目标要重新定义：不是把 7.5% 的格式错压到 0，而是让选择**带上方向**。**约束 JSON-schema 解码是死路**——试过，只吐一个 `{` 就挂（`AIBackend.gd` 里有记录）。剩下的路是蒸馏更小的学生模型；仓库有 pipeline（`bench/bakeoff/`），但**得先设计一个独立评分器**——现有 bake-off 自己诚实报告过 LLM 自评法官不可靠。
- **`event_log` 压缩**：它无界增长且被完整序列化进每个存档，是移动端红线的慢性泄漏；但**六条不变量依赖账本完整**，所以这需要一个设计，不是一个上限。
- **把 Theory Engine 接进游戏循环**：[docs/24](24-theory-engine-design.md) / [25](25-theory-engine-four-opportunity-synthesis.md) 谈的下一个架构阶段，目前是 `bench/bakeoff/theory_engine.py` 这个**离线 Python 原型，在 `game/` 里 grep 不到任何存在**。任何"玩家今天能看到 theory-engine 行为"的路线图都是错的 scope。

> ### ⚠️ 上面 NOW / NEXT / LATER 三张表的过期情况（复核于 2026-08-01）
>
> **不逐条重写，只标注**——按 K2 立的规矩：*给过期判词标日期，比删掉它更诚实*。
>
> **已交付**（写在表里时是待办）：
> - 「把确定性钉在单一二进制之外」——**已做完**：金标 + 逐 tick 前缀链 + 自有 FNV-1a + 稳定候选身份加盐。
> - 「让已仿真的戏剧变得可读」——小镇纪事已出货（引擎事件讲成中文而非枚举名）。
> - 「Node 端口：补齐或退役」——**已退役**（[39](39-node-port-disposition.md)）。
> - 「把方便的分母换成诚实的分母」——已做两处（[35](35-slm-decision-share-and-lod-soft-gate.md) / [36](36-model-influence-delta-over-c.md)），
>   并一路做到 [38](38-does-the-decision-path-earn-it.md) / [40](40-device-n60-slm-the-shipping-intersection.md) / [42](42-prompt-pathology-or-capability-ceiling.md)。
> - LATER 的「声音」——**音频代码已出货**（但**至今没有人真的听过**，见下）。
> - LATER 的「目标与上手」的**目标那一半**——`Goals.gd` + `goals.json` **11 条**目标已出货，回放等价由 CI 机检。
> - LATER 的「手机上能玩」的**触屏那一半**——触屏动作条在 CI 里有断言（`player_touch_test`）。
> - 「灯笼节对象挪进广场」「`buildings.json` 填上」——均已做。
>
> **仍然为真、且是今天的头号阻塞**（不要因为上面一串✅就以为它们没了）：
> - **没有引导、没有第 3 分钟把玩家叫回来的东西；本项目至今没有任何能测 playability 的仪器。**
> - **音频没有人戴耳机听过；真机触屏动作条没有人用手指按过。** 这两条**只能由人做，不假装完成**。
> - **`event_log` 压缩**仍未做（六条不变量依赖账本完整 ⇒ 这需要一个设计，不是一个上限）。
> - **把 Theory Engine 接进游戏循环**仍未做（`bench/bakeoff/theory_engine.py` 仍是离线 Python 原型，
>   在 `game/` 里 grep 不到任何存在）。
> - **模型输出质量**那条的最新状态在 [42](42-prompt-pathology-or-capability-ceiling.md)：
>   下一步**不是蒸馏**，是先切到代码本就默认的 3B + 修那两条 prompt 字符串，然后把 36/38/40 的测量**全部重跑**。
>   ⚠️ **这三次重跑至今没有发生。**

## 本轮（repo truth pass）已做

- 杀掉唯一那处可证伪的过度声称（"两个运行时通过同一组不变量"），换成实测核对表（见 [docs/08 §1](08-测试与验证.md)）。
- [docs/08](08-测试与验证.md) 从 13 条修到 37 条 + 硬/软划分 + #15 只报不门。
- 两个 README 补上移动端 / 端上 SLM 一节，并补上一批**已经有门守着、却在"当前状态"里没写**的系统。
- 本文档从"M0 进行中"改成 now / next / later。
- 新建 [docs/README.md](README.md) 索引；`tools/lint_links.py` 增加"纯文本 `docs/NN` 引用必须落地"的检查。

---

# §B · 里程碑历史记录

> 以下是 M0-M6 的原始描述，按当时写下的样子保留。**M0-M4 已交付**；
> M5/M6 早于规模 / LOD / 端上 SLM 这整段工作，其"下一步"已被 §A 取代。

### M0 · 骨架 ✅ 已交付（2026-06-27）
- [x] 现有项目与流水线分析（doc 00）+ 新项目设计（doc 01–04）
- [x] Godot 工程骨架：autoload `Sim/AIBackend/Art`、`Memory/Director`、`WorldView`、数据 JSON、`Main.gd`
- [x] 在 Godot 4.6.2 里打开能跑（占位渲染 + 时钟推进）
- [x] `sim_soak.gd` headless 跑 N 天不报错

### M1 · 确定性生活模拟（肌肉，零 LLM）✅ 已交付
**目标：一座 6–10 NPC 的小镇照常生活、好看、可加速/暂停，完全不依赖任何模型。**
- 需求向量 + 每 tick 衰减；世界对象「广告」交互；效用 AI 从 `agent_candidates()` 选 option。
- option-action 分层：option 持续、低层动作脚本执行。
- tick(60Hz)/step(~1s) 调度；像素地图 + Agent 精灵 + 寻路 + 头顶需求/动作气泡。
- `sim_soak.gd`：跑 30 天，断言无饿死/卡死、需求在健康带、社交发生过 → 平衡纪律就位。
- **验收**：浏览器/桌面可玩的「会自己过日子的小镇」；headless soak 通过。

#### M1-social · 确定性社交底座【垂直切片，已落地】✅（2026-06-27，见 docs/06）
> 两轮评审（内部 7 维 + GPT-5.5 Pro）一致判定：原骨架最致命缺陷是「引擎无任何 Agent↔Agent 交互」。
> 据此先把社交做成**一等的确定性能力**（先于 M2 LLM）。本切片已实现。
> ⚠️ **原文此处写"经 `tools/sim_social_port.mjs` 跨 5 seed + 60 天验证"——该说法 2026-07-26 起不再成立**：
> 该 Node 端口已正式退役（见 [docs/39](39-node-port-disposition.md)）。它逻辑冻结在 2026-07-03、两天后即因阵容 6→12 而失效，
> 且**它从来不是 differential**（两边各跑各的轨迹、从不比对状态），今天与它自己那个年代分别有 7/12 与 4/12 的 seed 是红的。
> 现在真正为该切片背书的是 `tools/ci.sh` step 4：**金标跨进程锚 + 逐 tick 前缀链 + 12 seed × 60 天硬不变量**。
- `agent_candidates()` 除物件外，枚举【同区可感知】其他 agent 的社交动作 **greet / give / gossip**。
- **SocialTransaction** 协议：发起 → `_acceptance_rule` 评估(接受/拒绝) → 提交 → 双方+旁观者各写**视角不同**的记忆。
- **关系账本** `relationships{affinity,familiarity,last_pos,last_neg}`（每次变化溯源 event id）。
- **belief / 知识边界**：claim 只能经 gossip 事务或观察获知（种子谣言 R1 靠 gossip 扩散）；无来源 belief = 0。
- **不可变 `event_log`**（replay/debug/bench 根）；`option` 带 `partner` + 多 tick 对话相；`_rng_at` 接入平局抖动（修死代码）。
- `sim_soak.gd` 升级为**社交不变量门**（7 条断言 + nonzero 退出）：无饿穿 / 社交发生 / 无永久孤立 / 关系分化 / 谣言传播 / 知识边界 / 账本可溯源。
- **已在真 Godot 4.6.2 跑通**（复用 22nd 已构建的 `gamecraft-runner:4.6.2` 镜像、独立一次性容器、`tools/soak-godot.ps1`，不碰 22nd 的 pipeline/容器）：determinism（同 seed 双跑一致）+ 多 seed 不变量全过、退出码 0。真引擎逼出并修掉三个 headless 专有问题：`max()` 返回 Variant 的 `:=` 类型推断、`class_name` 全局类未导入（改 `preload`）、Sim 前向引用晚注册的 AIBackend（**已解耦**：`Sim.backend` 可注入，Sim 不再引用任何 autoload 全局名，引擎可独立 headless 实例化）。

#### M1-commit · 承诺系统【已落地】✅（2026-06-27，GPT-5.5 §7.2 点名的「戏剧来源」）
- 新社交动作 **`invite`**（约对方稍后在【当下同区】再聚）→ 创建 **`meet` 承诺** `{id,a,b,area,created,deadline,status}`；按熟悉度加权（约熟人、非陌生人）。
- 引擎给 **`attend`（赴约）** 行为加权：临近 deadline 主动前往约定地；但**需求危机**（任一 need < `NEED_CRISIS`）会让人**放弃赴约 → 爽约**（确定性的「危机即背叛」之源）。
- 每 tick `_resolve_commitments()`：双方按时到场 → **fulfilled**（trust↑/affinity↑）；到点缺席 → **broken**（按谁缺席**归责**：trust↓/resentment↑/affinity↓ + meet 违约事件 + 双方视角记忆）。关系账本加 `trust`/`resentment`。
- soak 增 3 条承诺不变量（共 **10 条**）：承诺生命周期(创建>0 且兑现>0)、无悬挂承诺(已过点必结算)、违约可溯源(每个 broken 有违约事件 + resentment 出现)。
- **真 Godot 验证**：45 天多 seed → 创建 81–95 / 兑现 79–93 / **爽约 1–3** / 待办若干，10/10 全过、determinism 一致。先经 Node 端口跨 5 seed+60 天验证。
#### M1-conflict · 冲突生命周期【已落地】✅（2026-06-27，GPT-5.5 §9.3）
- 怨气来源：**爽约**(+5)与**被婉拒**(+3) 经 `_bump_resentment` 累积；过阈值(`CONFLICT_TRIGGER`)→触发一段冲突 `{a 委屈方, b 冒犯方, status, severity, escalations, confronted, repaired}`，状态机：**simmering→escalated→confronted→repaired / lingering**。
- 新社交动作 **`confront`（当面对质）**：B 接茬→`confronted`（通往和解）；B 否认/回避（寡言者更易）→`escalated`(severity↑/resentment↑)。**`apologize`（道歉）**：**仅在被对质后**才会发起（知识边界——冒犯方被挑明才知错）；A 视 `severity` 决定原谅→`repaired`(清账、trust/affinity 恢复) 或拒绝（留待下次）。久未对质→`lingering`(冷战)。
- soak 增 3 条冲突不变量（共 **13 条**）：冲突生命周期(触发>0 且 对质/修复>0)、先对质后和解(repaired 必先 confronted)、修复可溯源(每个 repaired 有被接受的 apologize 事件)。
- **真 Godot 验证**：45 天多 seed → 触发 6–26 / 对质≈触发 / 修复多数、含"未被原谅"分支(seed42=6/6/3)与 lingering(60天偶发)；13/13 全过、determinism 一致。先经 Node 端口跨 5 seed+60 天验证。
- 现在小镇能长出完整因果链：约见→爽约→积怨→（成冲突）→当面对质→道歉→和解（或拒绝/冷战），全部确定性、可 soak、零模型。

- **下一步（按 GPT-5.5 §7.2/§9）**：SocialTransaction 字段化(preconditions/effects)、Topic 对象、类型化反思、还礼/秘密类承诺；扩到 15 个社交动作；接 WorldView 渲染 `social_event`（气泡/关系连线/冲突标记）；写 living-town 的 bench task 接进 22nd 评审；然后才进 M2 接 LLM 对话。

### M2 · 接 LLM 对话（声音，玩家↔NPC）✅ 已交付（真模型已联调：桌面 `llm`/`slm` 均实跑，端上 SLM 见 [docs/34](34-slm-device-hang-leak.md)）
- 复用 22nd 三档后端：`logic`(默认) / `llm`(LM Studio) / `slm`(godot-llm 内置) / + `mock`(验证)。✅ 已实现异步生命周期(`_wait`/兜底/`thinking`/在飞上限/deadline) + `parse_decision` 健壮 pick glue + json_schema；`scenes/m2_test.tscn` 自测过。
- 待办：玩家自由文本输入框 UI、流式回应、真模型联调（NPC↔NPC 已可由 mock/真模型自动驱动）。
- 玩家走近 NPC → 文本输入 → 模板 prompt(PERSONA/WORLD/NOW) → 流式回应 + 「思考中」气泡。
- 结构化输出：`{speech,emotion,action,target,affinity_delta}`，enum + 语法约束 + 代码校验 + 引擎兜底。
- 异步 `HTTPRequest`，`thinking` 标志，~400ms deadline 降级到罐头台词。
- **验收**：装/不装模型都能玩；装了能和 NPC 自由对话且行为合法；无卡帧。

### M3 · 记忆与反思（活人感）✅ 已交付（`game/scripts/Memory.gd`）
- `Memory.gd`：append-only 记忆流 + recency/importance/relevance 算术检索（先不用 embedding）。
- 对话/重要事件写入记忆；NPC 回应注入 top 3–5 条记忆 → 记得你/记得彼此。
- 夜间**反思**：把当天记忆 LLM 提炼成洞察写回；summarize-and-forget 控上下文。
- **验收**：NPC 隔天记得昨天的事；记忆面板可看；上下文长度有界（与时长无关）。

### M4 · 涌现社会（灵魂）✅ 已交付（声誉/派系/盟约/秘密/选举，由不变量 #14-#37 守着）
- NPC↔NPC 相遇生成简短对话、互写记忆；好感/关系图演化。
- 轻量 `Director`：偶发设定镇上意图（八卦、心情、点名接近玩家）。
- 玩家介入：送礼/撮合/办活动 → 经由对话与记忆**自行扩散**（致敬 Smallville 派对涌现）。
- PIANO 式并发：快反射(脚本)/慢决策(LLM)/反思(LLM) 不同节奏，CC 保「说/做」一致。
- **验收**：出现非脚本的社会小戏剧；每周目不同；成本受控（导演-Agent 分层 + 缓存）。

### M5 · 评测 / 平衡 / 打磨（复用 22nd 流水线）⚠️ 部分交付，其余已被 §A 取代
> 实际走法与原计划不同：评测没有走 22nd 的 LLM-as-judge，而是长成了**不变量门 + 反事实探针**
> （`game/bench/` 的 37 条不变量、shadow 探针、预注册 metric card）——因为 LLM 自评被实测证明不可靠
> （见 [`bench/bakeoff/README.md`](../bench/bakeoff/README.md)）。平衡（社交"暖"那一半）与安全过滤层仍未做，已进 §A。
- 把工程接入 22nd `pipeline`：写一个 bench task（instruction + rubric + scenario demo），用 **LLM-as-judge** 自动评「小镇看起来活不活、需求/社交对不对」。
- soak 仿真扩展到经济/关系；一次调一个旋钮看健康带。
- 美术三级回退：先占位像素图（svg/gen png），后零代码换正式像素美术（Aseprite）。
- 安全过滤层（玩家可见文本上线前）。

### M6 · 发布 ⚠️ 原计划已过时
> 实际发行方向变成了 **Android APK + 端上 SLM**（[docs/18](18-android-apk-build.md)、[docs/34](34-slm-device-hang-leak.md)），
> 不是 HTML5/wasm。原文保留作出发点存档。
- HTML5/wasm（`logic` + 可选远程）+ 桌面（内置 SLM）双轨，复用 `serve.js` 与 exe 导出。
- 可选打磨：语音 STT/TTS（Mantella 路）、建造/经济沙盒、更多 NPC/地图（S/M/L 内容分档）。
- 逃生舱：若 Agent 数走向上千 → 评估把热循环移 C#，极端再看 Bevy。

## 成本/性能护栏（贯穿全程）
- 每-NPC 每游戏小时**硬性 LLM 调用预算**；并发 1–2；优先队列；`max_tokens≈80`。
- 先把脚本 AI 做好做平衡，LLM 永远是渐进增强。
- bench 红线：scenario/回放/soak 永远确定性、零模型。

## 立即下一步（M0→M1）· 已全部完成，存档
1. ~~本机/容器装 Godot 4.6.2，打开 `game/` 跑通占位渲染 + 时钟。~~
2. ~~跑 `godot --headless --path game --script res://scripts/sim_soak.gd -- --days 30` 验 soak。~~
3. ~~把需求/效用 AI 与寻路做扎实（M1 验收），再进 M2 接模型。~~
4. ~~确定最终游戏设定/题材与 6–10 个 NPC 人设。~~ 当前是 12 名可命名居民（`game/data/agents.json`），
   出货 cast 可在 6-60 之间调（`Main.gd` 的 `clampi(..., 6, 60)`）。

**当前的下一步请看本文 §A。**
