# 93 · Wave X · X2 回执——阶段产出：**头条是一次撤回，而撤回的是我们自己上一波发布出去的一个数**

> 派单：[docs/91 §二](91-wave-x-plan.md)（X2）。契约 [docs/41](41-baton-contract.md) 全文。
> **owns**：`docs/README.md`、`README.md`、`README_EN.md`、[docs/56](56-阶段报告-给非技术读者.md)、
> [docs/13](13-实验札记-experiment-journey.md)、[docs/05](05-路线图与里程碑.md)、
> 给 [docs/85](85-wave-v-v2-n24-nonmonotonic.md) **加一条过期声明**（只加声明），以及本文。
> **`game/**` 与 `tools/**` 一个字节没碰**（`git status` 佐证见 §六）。
> **基线**：worktree checkout 落后 **249 个 commit**（`38ba4a7`），
> `git merge-base --is-ancestor` 确认是祖先后 `--ff-only` 到 `integration/batons` = **`0667018`**。
> **本棒只亲自跑过一样东西：`python tools/lint_links.py`**（§五）。**本文不对任何其它门做断言。**

---

## ★ 交付状态

| 派单要求 | 状态 |
|---|---|
| [docs/56](56-阶段报告-给非技术读者.md) 出新版（它停在 Wave T，见 §〇②） | **做了**，改写为覆盖 **U / V / W 三波九棒**；⭐ 那条 −42% 的撤回**含"明知机制不明仍然合并"**，是第三节第①条 |
| `N=40` 上硬不变量是红的、而红线 #3 承诺 60 人 | **写进 56 §三②与 §四**、`README` / `README_EN` 的"还不到位的"第二条、[docs/05](05-路线图与里程碑.md) 的头号阻塞 |
| W3 的叙述审计守的是"有依据"不是"说得对" | **写进 56 §三③与 §四**、两个 README 的「当前状态」与「还不到位的」，**逐字带上它自己那条负对照** |
| [docs/13](13-实验札记-experiment-journey.md) 补段，照旧记被推翻的结论 | **做了**：新增 `2026-08-02 · Wave U→W` 一节（九小节），**并逐条重数了"我错了几次"**——见 §〇① |
| 「H1/S3/V3/W3 四根棒第一版都量错了对象」这条形状 | **做了**，在 13 的新段 §八 末尾做成一张表，**并标出其中三次是在读过这条纪律【之后】踩的** |
| 给 [docs/85](85-wave-v-v2-n24-nonmonotonic.md) 加过期声明，**只加声明不改正文** | **做了**（§三）。`git diff --stat` 显示该文件**只有新增行、零删除行** |
| README：口径照旧、"还不行的"继续直白、不打圆场 | **做了**；⚠️ **且不声称任何我没跑过的门是绿的**（§五） |
| `lint_links` 实际输出 | **§五**，含一次**跑出来的**负对照 |

**动过的文件（8 个，全部在 owns 之内）**

| 文件 | 改了什么 |
|---|---|
| [docs/56](56-阶段报告-给非技术读者.md) | **整篇改写**（U/V/W 三波九棒）。§三 按分量排九条，头两条都是撤回/翻案 |
| [docs/13](13-实验札记-experiment-journey.md) | **追加**一节（2312 → 2570 行）。**只追加、未改动任何既有行** |
| [docs/05](05-路线图与里程碑.md) | 权威计划指针 U → X；波次表补 U/V/W/X 四行；**补两条会改变全文"已交付"读法的更正**；头号阻塞表加一行 |
| [docs/README.md](README.md) | 编号范围 `53-79、81` → `53-91、93`；波次表补 **80、82-91、93** 共 12 行；不变量条数 40 → **41**；札记行数复核 |
| [docs/85](85-wave-v-v2-n24-nonmonotonic.md) | **只在抬头加了一个过期声明块**，正文零改动 |
| `README.md` | 见 §四 |
| `README_EN.md` | 同上，中英同口径 |
| `docs/93-*.md` | 本文 |

---

## 〇、这份 brief 哪里是错的（[docs/41 §4](41-baton-contract.md)）

### ① ★「本段里协调者至少错了五次（把 seed 36 的臂说反、把相关系数安错对象、把两个吧台并成一个、说室内分色能在录屏里看到、把 `CR:<职位>` 说成事件类型），逐条在编号 88/89/90 里」——**五条里有四条不属于本段，而且早就记录在案了**

**这是本 brief 最要紧的一处错，因为它正好是这一波在别处反复指控的那个形状。**

我逐条去编号 88 / 89 / 90 里找，**只找到第五条**：

```
grep "seed 36|吧台|0.618|室内分色|录屏里看到"  docs/88 docs/89 docs/90
  → 唯一命中：docs/89:269，而它是一次【正当引用】（"S1 在 N=12 上量到 ρ=0.618"），不是一条更正
```

前四条**属于上一段（R / S / T）**，并且**已经逐字写在 [docs/13](13-实验札记-experiment-journey.md) 那一节的 §七 里**
（第 1/2/3/4 条，出处分别是 [docs/76 §〇①](76-wave-t-t1-stock-pull.md)、[docs/76 §〇②](76-wave-t-t1-stock-pull.md)、
[docs/68 §〇①](68-wave-r-r1-economy-ceiling.md) + [docs/69 §四·3](69-wave-r-r2-interior-art-receipt.md)、
[docs/74 §五·2](74-wave-s-s3-furniture-role-receipt.md) + [docs/78 §二·1](78-wave-t-t3-interior-cam-and-hud.md)）。
**照 brief 写会把同一批错误在同一份文档里记两遍**，而第二遍还会挂错出处。

**处置**：**我回到 80 / 82 / 84 / 85 / 88 / 89 / 90 重新数了一遍**，得到本段真正的五条 + 一条形状不同的：

| # | 本段真正的那一条 | 出处 |
|---|---|---|
| 1 | 把一句"机制没找到"的观察当成需要解释的现象、并据此排下一整波议程；**支撑它的零假设也选错了对象**（不同事件类基数差三个数量级，`greet` 5887 vs `aid` 118；换成正确零假设后 11/12 的单尾 p ≈ 0.054，**过不了 0.05**） | [docs/88 §〇②③](88-wave-w-w1-the-42-percent.md) |
| 2 | 把 `CR:<职位>` 这条**信念 id** 说成一种**事件类型** ⇒ 照它去改会扑空 | [docs/90 §十一.1](90-wave-w-w3-story-layer.md) |
| 3 | 照抄「N=40 硬不变量 12/12 全绿、破的是软门」，**而今天这棵树上是硬红 + 软破** | [docs/89 §〇①](89-wave-w-w2-n40-red.md) |
| 4 | 把一条在 **N=60** 上量到的结论搬到 **N=16** 那一格当成"第一个要证伪或坐实的东西" | [docs/80 §〇①](80-wave-u-u1-open-the-key.md) |
| 5 | 把"N=24 到底特殊在哪"写成一个**预设它是一个点**的问句 | [docs/85 §〇①](85-wave-v-v2-n24-nonmonotonic.md) |
| + | 把「树上还有第二份拷贝」与「今天还能重算」**用一个"或"连起来** | [docs/82 §〇②](82-wave-u-u3-recomputable.md) |

⚠️ **这两份名单只有【第 2 条】重叠。** brief 的名单不是"记错了一条"，是**整份名单来自上一段**。

> **⇒ 为什么这条值得放在第一位**：这一波抓到的病，[docs/13](13-实验札记-experiment-journey.md) 上一段自己下过定义
> ——**"先有一句读起来顺的话，再把事实塞进它"**。
> "本段我错了五次"这句话读起来完全顺，而**填进去的五个例子是从上一段搬来的**。
> **我没有照抄它。** 但它值得记，因为**如果我照抄了，`lint_links` 不会红、任何一道门都不会红**
> ——这与本波头条那条 −42% 的形状**逐字相同**：*所有机器都是绿的，出问题的是那句话。*

### ② 「`docs/56` 停在 Wave U」——**更准确的说法是"它的最后一次编辑【由】Wave U 的文档棒做，而它讲的内容停在 Wave T"**

现有那一版 §三 的第一句逐字写着「上一版之后又跑了三波、九根并行的活（**代号 R / S / T**）」，
全文 `grep` 不到 `Wave U` / `U1` / `U2` / `U3`。同理 [docs/13](13-实验札记-experiment-journey.md) 最后一节的标题是
「2026-08-01 · Wave **R→T**」。

⇒ **要补的缺口是 U / V / W 三波九棒，不是"U 之后"。**
这个差别不是措辞：按 brief 的读法我会只写 V/W 六根棒，**而 Wave U 那三根里有两条是这一波最实用的结论**
（钥匙开了 + 那颗"复现不出来的红"被定性；以及 `xref` 判据会单调变松）。
**同理 brief 说"`docs/13` 补 R→W 段"——R→T 已经在树上了，实际要补的是 U→W。**

### ③ 「那四行就在 V1 引用的那几行【上面】一两行」——**方向是 commit 标题说的，回执本身没说方向**

[docs/88 §十.2](88-wave-w-w1-the-42-percent.md) 的原话是：

> 这一次证伪整条结论的两行，就在 V1 自己跑的留出日志里，**与它引用的行只隔一两行**。

而 `1046fe0` 的 commit 标题写的是"**上面**一两行"。§3.1 那四行的行号是 `67/68` 与 `79/80`（off 在 on 之上），
**但回执没有写 V1 引用的是哪一行** ⇒ **"上面"这个方向在回执里没有依据。**
⇒ **本轮所有对外文案一律用回执的措辞「只隔一两行」**，不写方向。
（这是一条小得几乎不值一提的更正，写下来是因为**这一波的头条正是"一个方向词决定了修法"**。）

### ④ 「⚠️ 绝对不要引用 92 或 94」——**成立，而且我实测了它会怎么红**

见 §五的负对照：**它确实是 `lint_links` 检查 (2) 的命中**，而且是**逐行点名**的。
⚠️ 按 brief 的要求，**本文没有粘贴那次失败的输出**——因为那份输出里含有它自己判红的那些字符串，
**粘进来会把一条负对照变成四条真死引用**（[docs/91 §四](91-wave-x-plan.md) 点名 R2/R3/U2 都踩过）。

### ⑤ brief 里**对**的、而且救命的两条

- 「checkout 可能落后两百多个 commit」——**落后 249 个**（上一波是 231/241/241，这一波更多）。
  不 ff 的话我改的是 `38ba4a7`（= `master`），那棵树上 **80-91 十二份文档一份都不存在**，
  而我这一棒的全部工作就是把它们讲出来 ⇒ **本文会变成一份关于空气的索引。**
- 「不要在正文写 `docs/` + 未落地编号的纯文本形式」——**§五的负对照证明它是对的**，且成本是一次 `rc=1`。

---

## 一、[docs/56](56-阶段报告-给非技术读者.md) 新版要点（**给非技术读者，每条都能追到编号文档或 commit**）

**结构没变**（六节），**内容整篇重写**。§三 按**分量**排九条，而**头两条都不是"多了什么"**：

| § | 一句话 | 出处 |
|---|---|---|
| 三① | ⭐ **撤回**：那个 −42% 不存在；**而我明知机制不明仍然合了**，并把这句话写进了 `2d314c7` | [88](88-wave-w-w1-the-42-percent.md)、`1046fe0`、[87 §〇](87-wave-w-plan.md) |
| 三② | ⭐ **40 人的镇子上一条【硬】规则是红的**，而红线承诺 60 人；`seeds 49-60` 那一窗**供给全绿、门照样红** | [89](89-wave-w-w2-n40-red.md)、`b8116db` |
| 三③ | ⭐ 新装的叙述审计守的是"**有依据**"不是"**说得对**"——文案对调，屏幕上胡说八道而整门全绿 | [90 §七](90-wave-w-w3-story-layer.md) |
| 三④ | 那把锁着的钥匙开了；**而 T1 那颗"复现不出来的红"被原地复现并定性：不是随机，是数据文件被改了** | [80](80-wave-u-u1-open-the-key.md)、`188f4c8` |
| 三⑤ | 「树上还有第二份拷贝」不能当重算路径用——**反例是那份说它不可复算的报告本身** | [82](82-wave-u-u3-recomputable.md) |
| 三⑥ | 「24 人是红的」这句话的**主语**错了 | [85](85-wave-v-v2-n24-nonmonotonic.md) |
| 三⑦ | 跨版本拼表：**144 个可比开局，零个摘要相同** | [89 §〇之前](89-wave-w-w2-n40-red.md) |
| 三⑧ | 分工第一次有了**正向**的社会痕迹——**而拆开之后只有一条通道真的改变世界** | [84](84-wave-v-v1-craft-social-trace.md) |
| 三⑨ | 三处"绿着却不成立"（窗口表数字错、硬红印在第 224 行没人读、新门红在打印 ✅ 的编码上） | [89 §〇③④](89-wave-w-w2-n40-red.md)、`98954e1` |

**三条刻意的写法，写下来供下一棒沿用或推翻**：

1. **抬头第三条警告写的是"本版作者只亲自跑过一项检查"**，并要求正文里凡提到某道门通过，
   **一律注明是哪一根棒在它自己那棵树上跑的**。⇒ **这份报告不替任何人担保。**
   （理由：本 session 出过一次假 "CI PASS"，见 `ef332fe`。）
2. **§四「还不到位的」新增五条、更新三条，而【上一版列过的一条都没有被丢掉】**；
   其中**新增的第一条就是那次撤回**——**把撤回放进"还不到位的"，而不是放进"这一轮的成果"**。
   ⚠️ **这条我差点没做到，写下来**：初稿里我把上一版的三条（闲话的那笔代价、派系门只修一半、
   "钥匙一旦打开 CI 里没东西守着它"）**顺手漏掉了**——不是判断它们不成立，是**改写整篇时它们没被抄过来**。
   逐条对完上一版之后补了回去，其中第三条明写"**钥匙开了之后这句话还成不成立，本轮没有人量**"。
   ⇒ **"整篇改写"这个动作本身就是一台会静默删掉限制条款的机器**，而它不会让任何门变红。
3. **§四 里刻意留了一条"没有被推翻，也没有被复核"**：上一版那条「12 个开局上没出问题 ≠ 这套规则成立」，
   **本轮既没有重跑也没有推翻，而它仍在被引用**。⇒ **不复核而继续引用，正是这一轮反复抓到的病，所以它自己也要被标出来。**

---

## 二、[docs/13](13-实验札记-experiment-journey.md) 记了哪几条被推翻的结论

新增一节 `2026-08-02 · Wave U→W`（九小节）。**只追加，未改动任何既有行。**
被推翻的清单如下（**每一条都是执行的棒用数据推翻的**）：

| # | 被推翻的 | 推翻它的 |
|---|---|---|
| 1 | ⭐ **「某机制让 `aid` 掉 42%」**（**我们自己上一波发布出去的**） | 三条独立证伪：留出 seed **+5.5%**、剂量-响应不单调、**零社会含量的假扰动掉 45%** |
| 2 | 「N=40 破的是软门，硬不变量 12/12 全绿」 | **硬 `#01` 红**（seed 8 / 56）；`seeds 49-60` **软门 12/12 全绿而门照样红** |
| 3 | 「N=24 特殊」 | 主语是 **(N=24, seeds 1-12)**；672 局铺开后 N=40/48 一样红 |
| 4 | 「N≥16 时两个货位被豆子/话本**永久**占着」 | **不需要新跑一格就能证伪**：豆子在 N=16 上只占 7/12；那句话是在 N=60 上量的 |
| 5 | 「那次同 seed 两跑不一致复现不出来、没有解释」 | **原地复现 + 定性**：`game/data/**` 在两跑之间被改，**不是红线 #1 破了** |
| 6 | 「树上还有第二份拷贝 ⇒ 这个数抓得住」 | 反例：那第二份拷贝**就是那份说它不可复算的回执**；`xref` **随时间单调变松** |
| 7 | 「叙述审计守住了故事的正确性」 | **文案对调 ⇒ 0 违规、rc=0 全绿**（写门的人自己跑的负对照） |
| 8 | 「上一波那份逐 seed 的表可以直接引用」 | **144 个可比 seed 零个 digest 相同**，而**判据一个字节没变** |
| 9 | 「上限臂只可能在 N ≤ 32 响」（[docs/85 §七.2](85-wave-v-v2-n24-nonmonotonic.md) 给下一棒的话） | 今天这棵树上 **N=40 响了 3 次** |
| 10 | 「`story_test` 那一格是有牙的」 | CI 默认档 14 天 ⇒ promise/secret/pact **12/12 个 seed 全是 0/0** |

**外加两条只在札记里、不进 56 的**（太技术，但下一棒会用到）：

- **「N=40 上 `面点师+渔夫` 的左尾 ⇒ 下限臂红」**——**它是主导变量不是判据**：
  前 12 名里 **7 个不是下限臂红**，且它们的 `口粮` 全落在 **0.510–0.541**（地板正上方那条缝）；
  而排名第 31 的 seed 48 红了。**"左尾 ⇒ 红"在这 12 行里会误报 7 次。**
- **V3 为打散点阵引入的"随机档"，自己的低位又是一张点阵**（相邻同值率 **0.0%**、隔 2 格 **100%**）
  ⇒ **"先量再动"对【自己的改动】同样成立。**

**并按派单另记了一条形状**（做成表，在新段 §八 末尾）：
**H1 / S3 / V3 / W3 四根棒的第一版度量，量错的都是【对象】**——不是算错。
⚠️ **我给它加了一条派单没要求、但数据支持的补充**：**四次里有三次是在读过这条纪律【之后】踩的**
（[docs/86 §一·3](86-wave-v-v3-town-exterior-receipt.md) 逐字写着"我在读过之后又踩了一次"）
⇒ **它不是一条"记住就能避开"的纪律，需要的是一个动作**：动手前先写下"我量的是哪个对象"，并让下一个人能核。

---

## 三、[docs/85](85-wave-v-v2-n24-nonmonotonic.md) 的过期声明怎么写的

**加在抬头（标题之后、`★ 一句话交付`之前）的一个引用块，正文一个字未动。** 它由五段构成：

1. **一句判决**：**本文里每一个【逐 seed】的数字都已经过期，一个都不能直接抄。**
2. **出处与机制**：[docs/89 §〇之前](89-wave-w-w2-n40-red.md)；V2 的基线是 `5d2214f`，之后 V1 合了进来。
3. **证据表**（照抄 W2 逐位对过的那张）：三格 `ship@16/24/40` 的 **digest 逐位相同 = 0/60、0/60、0/24**，
   并把**本文记的红 seed** 与 **W2 今天量到的红 seed** 并排 ⇒ **144 个可比 seed，零个相同。**
4. **明确分开"仍然可用"与"不可用"**：
   - **可用**：结构性结论（主语是 (N=24, seeds 1-12)、下限臂只有 `口粮` 这一族、`面点师+渔夫` 是主导变量、两条臂不是一个旋钮的两头）——**它们已被 W2 在今天这棵树上独立复现**。
   - **不可用**：**哪个 seed 红、红多深、逐 seed 展布**——**一个都不能抄，要用就自己重跑。**
   - 另点出 W2 就地更正的两处（§2.2 那格「1 红」其实是 0 红；§七.2 那句「N ≤ 32」被证伪）。
5. **为什么只加声明不改正文**（这一段是重点）：
   > 本仓库的口径是**结论被推翻就照实记、原文留着**；而这份回执的价值有一大半在**它当时是怎么量的**
   > ——**删掉数字就把方法一起删了**。
   > ⚠️ 更要紧的一条：**判据一个字节都没变，变的是世界**（W2 逐行 grep 确认了这一点）
   > ⇒ **这不是"V2 量错了"，是"引用别人的原始数据时，除了对基线还要对【它当时那棵树】"。**

**机检**：`git diff --stat docs/85-*` 显示该文件**只有新增、零删除**。

---

## 四、README 改了什么（中英同口径）

**四处新增、四处更新，没有删任何一条"还不行的"。**

**新增（"当前状态"）**
- **「干得好也会被人看见」**：正向 497/0 vs 反向 65% 的清点、`craft_credit` 一个 JSON 键、新硬不变量 `#41`。
  **同一条里贴着三条边界**：只有 `standing` 真的改变世界；只装在 8 个岗位里的 1 个；**它当初报的那条 −42% 已被证明不存在**。
- **「镇子会讲自己的故事，而每一行叙述都带出处」**：12/25 → 14/25、392 弧 / 1234 行 / 0 违规、变异体红 62 条。
  **紧跟着就是那条负对照**（文案对调 ⇒ 全绿），以及"**别拿条数覆盖率当成绩单**"。

**新增（"还不到位的"，全部排在这一节最前面）**
- ⭐ **那次撤回**，含三条证伪的具体数字、**"我明知机制不明仍然合了"的原文**，以及"**跑了 ≠ 读了**"。
- ⭐ **N=40 的硬红**，含四条边界（擦零不是饥荒 / 成因没查 / N=48-60 没跑 / 地板配置不代表手机）。
- ⭐ **叙述审计守不住"说得对"**；⭐ **`story_test` 跑在它不可能变红的配置上**。
- ⭐ **跨版本引用逐 seed 数字会出事**（144/0），并指向 85 抬头那条声明。

**更新**
- 那条"钥匙还锁着"**改成"钥匙开了"**，并补上：**卡住它的不是我让它先测的那件事**、
  **那颗 `DetGate` 红被定性了**（不是随机、不是红线破，是**实验卫生**）、
  **而 T1 自己的验收条件仍未达成**（seed 44 豆子 0.495）。
- 不变量 **40 → 41 条（26 硬 + 14 软 + 1 诊断）**，并附上那条"**一条'怎么数'的说明把自己数了进去**"的 off-by-one 更正。
- CI **17 → 18 道关**（补 `2e` 可重算门、`4c` 的数据指纹、`6` 的三道新视觉判据），
  并给 `2e` 配一段"它在设计阶段就推翻了一条被引用过的判据"的说明。
- 索引表补 **80 / 82 / 84 / 85 / 88 / 89 / 90** 七行。

**刻意没做的三件**
1. **没有更新任何演示图/录屏**——这一波三根棒（U1/W1/W2）里**没有一根动过表现层**，
   V3 动了外景但**它的前后对照图不在我的 owns 里**，而我**没有出图眼验的手段**（[docs/41 §6](41-baton-contract.md)：视觉改动必须出图眼验）。
   ⇒ **不换图，而不是换一张我没看过的。**
2. **没有把任何一道门写成"绿的"**——凡提到就注明是哪一根棒跑的。
3. **没有替"还不行的"打圆场**，一条都没有软化；那一节今天比上一版**更长**。

---

## 五、`lint_links` 的**实际输出**（本棒唯一亲自跑过的东西）

```
$ PYTHONIOENCODING=utf-8 python tools/lint_links.py ; echo "rc=$?"
lint_links: OK — 109 markdown files, all relative links resolve and all docs/NN citations exist (90 numbered docs)
rc=0
```

⚠️ **`PYTHONIOENCODING=utf-8` 不是装饰**：不带它，Windows 控制台把那个破折号打成乱码。
这与 V3 的门第一次接进 `visual_gate.sh` 时**红在打印 ✅ 那一行的编码上**（`98954e1`）是同一个坑的轻量版
——**判据全对，炸在输出上。**

⚠️ **这个 90 里有我一份**：本文把编号文档从 89 推到 **90**、markdown 文件数从 108 推到 **109**。
[docs/89 §7.1](89-wave-w-w2-n40-red.md) 与 [docs/90 §十](90-wave-w-w3-story-layer.md) 各自也做过同样的自陈
（那条 `lint-links-md-count` 注册项**刻意不上门**，理由是"谁加一份文档它都会变"）。
**写出来，免得下一个人以为这个差额里有别的东西。**

### 5.1 探测包络（[docs/41 §2.5](41-baton-contract.md)）

⚠️ **先说清楚：本棒【没有新增也没有收紧任何一道门】。** 下面这三行讲的是
**"我用来验自己这次改动的那一样东西，它能抓什么、抓不到什么"**，不是"我守住了系统的什么性质"。

```
detects:（**跑出来的**，不是想出来的）
  ① 纯文本引用一个【此刻不在树上的编号】⇒ rc=1，并【逐行点名】文件与行号。
     实测方法：在本文里临时插入一条指向本波另一根棒编号的纯文本引用（那份文档此刻不在树上），
     跑 lint_links ⇒ rc=1；删掉之后重跑 ⇒ rc=0（就是上面那一行）。
     ⚠️ 按 docs/91 §四 的要求，**那次失败的输出没有粘进本文**——它自己含有被判红的那些字符串，
        粘进来会把一条负对照变成四条真死引用。
  ② 相对链接指向不存在的文件 ⇒ 同样 rc=1。本轮我新写了 40+ 条指向 80-91 的 markdown 链接，
     它们全部通过 ⇒ **这一栏在本次改动上是【有输入】的，不是空跑。**

does_not_detect:（**跑出来的**）
  ① **链接指对了文件，但指错了小节。** 检查 (1) 只 `split("#")[0]`，**锚点整段被丢掉**
     （`lint_links.py:43`）⇒ 本文里所有 `§〇①` / `§十一.1` 之类的定位**一条都没有被验过**。
  ② **`docs/NN@分支` 这条逃生门零验证**：它不查分支存不存在、不查那份文档在不在
     ——**这正是 52 号那个洞活下来的通道**（docs/README.md 自己记着）。
  ③ **文字内容对不对，它一个字都不管。** 本棒改的绝大部分是【断言】而不是【链接】：
     "N=40 硬红"、"144/0"、"+5.5%" 全部是**我从别人的回执里抄过来的**，
     **lint_links 对抄错一个数字完全失明。** ⇒ 见 §六的诚实边界。
  ④ **它不是那 18 道关。** 本棒**没有跑** `tools/ci.sh`（理由见 §六·2）。

confidence: **N=2 个变异体实测**（①一条死编号引用 ⇒ 红；②本轮新增的 40+ 条真链接 ⇒ 全绿，零假红）。
            **正确性侧**：全树 109 份 markdown、90 个编号文档，rc=0。
            ⚠️ **这个 N 很小，而且两个变异体都在【链接】这一维**——
            对"数字抄错"那一维，**本棒的置信度是 0，因为根本没有仪器**。
```

---

## 六、诚实边界与"我没能测到什么"（[docs/41 §4](41-baton-contract.md)，不用推断填空）

1. **本文里几乎每一个数字都是【二手的】。** 我没有跑过一局仿真、没有出过一张图、没有跑过任何一道门。
   **56 / 13 / README 里的每一条断言都注了出处**，而**出处的正确性靠的是我读得仔细，不是靠任何机器**
   ——`lint_links` 只验"这份文档在不在"，**不验"这句话是不是它说的"**（§5.1 `does_not_detect` ③）。
   ⇒ **这一棒的产出，结构上就是 [docs/41 §4](41-baton-contract.md) 那句"转述会把观察伪造成机制"的高危区。**
   **我能做的只有两件，都做了**：①**逐字引原句，不压缩重写**（[docs/41 §0.8](41-baton-contract.md) 的要求）；
   ②凡是"某某是绿的"一律注明是谁跑的。
2. **没有跑 `tools/ci.sh`。** 理由是取舍不是遗忘：本棒**只碰 `.md`**，而 18 道关里唯一会读 `.md` 的是第 2 关
   （`lint_links`）与第 2e 关（可重算门读的是注册表里的命令，不是文档正文）。
   ⚠️ **而这个理由有一个我没消掉的洞**：**2e 的注册表里有一条 `lint-links-md-count`**
   （[docs/89 §7.1](89-wave-w-w2-n40-red.md) 与 [docs/90 §十](90-wave-w-w3-story-layer.md) 都记过），
   **它会随文档数移动**——我加了一份文档 ⇒ 那条**只记录、不上门**的差异**又移动了一格**。
   它**结构上不该上门**（注册表原话：给它上门 = 造一道会因无关改动变红的门），
   **所以它不是红**，但**我把它推动了一格这件事要写出来**。**我没有跑 2e 去确认这一点**，
   这是本棒最实的一处未验证声明。（这与 R3 那次的取舍相同：写出来而不是省略。）
3. **没有眼验任何画面。** V3 那一波改了外景，README 的主图与前后对照图**一张都没换**（§四）。
4. **没有核实 56 §一/§二 里的"存量"数字**（12 个居民、九个职业、十张脸、表情图标）——
   **沿用上一版口径，并在原地标注"沿用不等于复核"**。这是**第三版连续沿用**同一批数字。
5. **中英两版我保证的是【口径】一致，不是逐句对译。** README_EN 的新增段落是**照中文重写的**，
   若有出入以中文为准。**我没有找第二个人复核英文。**
6. **[docs/05](05-路线图与里程碑.md) 的 NOW/NEXT/LATER 三张表本轮没有逐条重跑复核**——
   只在头部与"头号阻塞"表加了三条更正。那三张表上一次逐条复核是 2026-08-01。
7. **`git status` 自陈**：收工的树上 **`game/**` 与 `tools/**` 零改动**，`analysis/` 未新建。
   越界：**零**。

---

## 七、给下一棒的三句话

1. **那个 −42% 请从任何引用里删掉**，包括口头转述。它不是"变小了"，是**不存在**。
   而**救了这件事的不是任何一道门**（当时全绿）——**是合并者写下的那句"机制没找到"**。
   ⇒ **值得抄走的规矩只有一条：把"我知道我不知道什么"写进 commit，它比门便宜、而且这一次比门管用。**
2. **`N=40` 的硬 `#01` 成因还没查**（[docs/89 §5.2](89-wave-w-w2-n40-red.md) 明写"顺手扫出来的"），
   而 **N=48/60 今天这棵树上没有人跑过**。凡是要写"我们支持 N 人"的地方，
   今天能说的上界是 **"在跑过的 {16,24,40} 里，40 是红的"**。
3. **要引用本仓库任何一个逐 seed 的数字之前，先跑一句 `git diff` 对一下【它当时那棵树】**
   ——判据一个字节没变而 **144 个可比 seed 零个 digest 相同**。
   [docs/85](85-wave-v-v2-n24-nonmonotonic.md) 抬头那条声明就是这条纪律的第一个实例，**而它一定不是最后一个**。
