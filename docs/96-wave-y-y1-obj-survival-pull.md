# 96 · Wave Y · Y1 回执——**臂 B 做完了，而做完它的过程把它自己改小了两次**

> **★ 一句话交付**（全部数字在**我自己这棵树**上重跑，`c9e6ee6`）：
>
> **出货：`utility.obj_survival_pull = 1.0`，作用面比 X1 那条臂窄两格 —— 只对 `social`，且只在 `social` 就是 argmin 时。
> 两格都是量出来的，不是设计洁癖。**
>
> - **窄第一格（`need_id == "social"`）**：X1 的臂 B 对**五条 need 一视同仁**。把它拆成两半分开跑
>   （N=40 × 24 局，同一批 seed）：**「除 social 外的每一条 need」那一半对未改动的树是 8 好 / 9 坏 / 7 不变，
>   `p = 0.50` —— 与什么都不做分不开**；而「全部 need」减掉它（= 只剩 social 那一项）是 **16 好 / 3 坏，`p = 0.0022`**。
>   ⇒ **效果整个住在 social 这一项里。** 摘掉另外四条，代价是 **0**（`bsoc` vs `ball` 8/10/6，`p = 0.41`）。
> - **窄第二格（只在 social 是 argmin 时）**：这一道在出货剂量上是**免费**的
>   —— k=0.5 与 k=1.0 各 24 局，**带门与不带门的 `Inv.digest` 逐位相同 24/24**；
>   而 **k=2.0 起只剩 21/24、k=3.0 只剩 15/24** ⇒ **大剂量的"额外收益"有一部分正是靠让 social
>   压过一条【更急】的需求换来的**，这一道门把那条路按构造堵死。
>   **它买到的是一条构造保证，不是一次行为改变。**
>
> **★ 剂量曲线：量了八档（0.5 / 0.75 / 1.0 / 1.25 / 1.5 / 2.0 / 3.0 / 10.0），结论是【收益侧没有内点顶点】。**
> 对着零假设臂逐 seed 配对，"最长锁段变短"的比分是 17/7 · 18/6 · **18/6** · 18/6 · 19/5 · 20/4 · 18/6 · 22/2
> —— **单调、且相邻两档的差小于零假设臂自己制造的抖动**（`null` 一手把中位地板挪了 −2.62，而 0.75↔1.5 只差 0.10）。
> ⇒ **"找顶点"在收益侧【没有答案】，而这本身是结果。** 顶点最后由三条**彼此独立、量的东西完全不同**
> 而**落在同一个 k=2.0** 的线定住（§四末）：①这一项的上限 `36k` 在那里首次超过它所修饰的分数本身；
> ②N=12 的金标在那里首次移动；③argmin 那道门在那里首次咬。**出货取在它下面一档半。**
>
> **★ R12 走了完整一套，而三份锚里【恰好动了一份】**：`Harness` 金标 12/12 没动、
> `ModelPathGate` 失败 0 没动、**`DetGate` 的 `betray` seed 1 动了**（硬 16/16、两跑一致 16/16、
> 只有摘要移动）⇒ 三份锚同 commit 重烘 + `rebake_history` 补一条 + 留出种子 13-30 两侧都跑都报（§六）。
>
> **★ 而本棒最该被记住的一条，是我自己的一个结论【被我自己的留出种子推翻】**：
> 我原本要用「**取最大的、能让 N=12 逐字节不动的剂量**」来选出货值，证据是金标 **12/12** 一个 seed 没动。
> **留出种子把它证伪了——同样 N=12、同样 k=1.0，seeds 13-30 里动了 5 个（18/25/27/29/30）。**
> ⇒ **「金标 12/12 不动」只等于「金标抽到的那 12 个 seed 不动」**，
> 而拿它当剂量判据 = **把剂量拟合到一道门的边界上**，正是契约反复点名的那个病。
> 出货值的理由因此换成了三条与"哪道门保持绿"无关的（§二.4），其中承重的那条是结构性的：
> **这一项的上限 `36k` 不该超过它所修饰的那个分数本身（长椅满 urgency 的 `benefit` = 66.67）
> —— `k=1.0` 是 0.54 倍，而 `k=2.0` 就是 1.08 倍、加分项反客为主。**
>
> **★ 规模激活，而金标那 12 个 seed 是一个 ~1% 的巧合**：同一个 k=1.0，
> **N=12 金标 seeds 1-12 动 0/12，而同为 N=12 的另外 48 个 seed 动了 15 个**
> （13-30 动 5、31-60 动 10 ⇒ 31%）。**在 31% 的移动率下，抽到"12 个全不动"的概率约 1.1%。**
> N=16 动 6/12、N=40 动 21/24。
>
> **卡在哪、下一个该测什么，写在 §八。**

> 派单：[docs/95 §一](95-wave-y-plan.md)（Y1）。契约 [docs/41](41-baton-contract.md) 全文（尤其 §2.5 / §3 R12 / §5）、
> [docs/47](47-wave-e-plan.md) 的 **R12**。直接前置 [docs/92](92-wave-x-x1-n40-hard-red.md)（X1：族判定与臂 B）。
> 分支 `worktree-agent-a69d11c522a1efb5b`，worktree 开工时落后 **257 个 commit**
> （`38ba4a7`，`git merge-base --is-ancestor` 确认是祖先后 ff 到 **`c9e6ee6`**）。
> 全部数字：`backend=null` · 无 LOD · 60 天 · 逐 seed，**不给均值**。

## 〇、这份 brief 哪里是错的（[docs/41 §4](41-baton-contract.md)）

### ① **"证明它只作用在 social 上"——这句话对臂 B 按构造不可能成立，它要的是别的东西**

派单第 3 件写的是「`need_id=="social"` 的隔离 —— 证明它**只**作用在 social 上，没顺手改别的需求」。
**臂 B 的补丁（`analysis/x1/arms/patch_B_obj_survival_pull.py`）加的是 `score + _survival_pull(cur)`，
而 `cur` 是【这条广告自己那条 need】的当前值** ⇒ 它对 `hunger`/`energy`/`hygiene`/`fun` **一律生效**。
X1 自己在 [docs/92 §5.5](92-wave-x-x1-n40-hard-red.md) 最后一条缺口里写清楚了这一点。

⇒ **"证明"这个词用不上**：能做的不是证明一个已知为假的命题，而是**把效果拆开、看它住在哪一项里**。
本棒因此把它拆成三条臂（§三），结论是效果整个住在 social 里，另外四条 need **可以摘掉且代价为 0**
——**所以出货的那份代码与臂 B 不是同一个东西**，我把它写在 §三 而不是含糊地叫"臂 B 落地"。

### ② **"跨树拼接会出事"被转述成了一条比实测更宽的规则**

派单写：「W2 实测 V1 合入后 **144 个可比 seed 零个摘要相同** ⇒ **跨树拼接会出事**」。
**前半句是实测，后半句是从它外推出来的，而外推错了。**

本棒实测：`git diff 0667018 HEAD -- game/scripts/ game/data/` **输出为空**
——X1 那棵树与我这棵树隔着 **257 个 commit**，而**仿真的输入逐字节相同**。
于是我自己重跑的基线臂 `base` 的 seed 1-12 摘要与 X1 的 `k00` **逐位相同**
（`2131479952 · 2002675861 · 1862416786 · 3053337682 · 2571643439 · 4188238627 · 1829711643 ·
1966159113 · 3204388903 · 3553219940 · 2903565299 · 4175233281`），两颗硬红也落在同样的 seed 8 / seed 56。

> **正确的规则窄一格、也更有用**：**摘要跨树可比 ⟺ `game/scripts/**` 与 `game/data/**` 逐字节相同，
> 而这一条是一次 `git diff` 就能查的。** W2 那 144 个之所以全不同，是因为 **V1 真的改了仿真**，
> 不是因为"树不同"。
> ⇒ **"自己的基线自己跑"这条纪律我照做了**（本文所有基线数都是我自跑的），
> **但它的理由不是"跨树不可比"，而是"你得先查一次才知道可不可比"。**

### ③ 派单转述的臂 B 数字对得上，但**它们是"全部 need"那一版的**

派单写「17/5 更优（p≈0.0085）、社交地板中位 16.90→19.25、最差地板 0.00→14.10、最长锁死 380→158」。
这些是 X1 `o10` 臂（全部 need、k=1.0）的数。**本棒自跑的同一条臂**（`ball_10`，24 局）是
地板中位 **16.52 → 19.00**、最差地板 **0.00 → 14.10**、最长锁段 **380 → 158**、对基线 17/5/2
——**逐条复现**。但**出货的不是它**（§三），所以本文正文里凡是"出货臂"的数都用 `bsoc/bmin` 那一列。

### ④ 一条**门自己的**措辞问题（本棒没有改它，只报告）

`Harness` 的判决行在**没有传 `--golden`** 时照样打印 **`金标 过`**：

```
— 金标（跨进程锚）—
  (未启用 --golden)
=== S0 GATE: PASS ✅  (硬不变量 seed 12/12 全绿, 软通过率门 … 过, 活性 过, 金标 过, det 1/1) ===
```

`ci.sh` 第 4 步传了 `--golden`，**第 4a 步（N=16）没传** ⇒ 4a 的判决行里那个绿色的"金标 过"
**对应的比较一次都没发生**。这不是假绿（4a 的金标本来就只烘在 N=12 上、不该比），
但它与 [docs/94](94-wave-x-x3-fixture-validity.md)（X3）查的那一族是同一个形状：
**一行读起来是判决的输出，其实什么都没判。** 改它要动 `Harness.gd`（本棒的 owns），
但那会改 CI 的输出文本、且不是派单要的东西 ⇒ **本棒不改，写在这里给下一棒。**

## 一、基线：**我自己跑的，两颗硬红都复现**

`game/bench/x1_margin.gd`（X1 的量具，**复用、未改一字**），N=40 × 60 天 × `backend=null`，
两块各 12 个 seed。原始逐 seed 记录在 `analysis/y1/arms/*.txt`（每个文件断言只有一段汇总）。

| | `1-12` | `49-60` |
|---|---|---|
| 硬 `#01` 红 | **seed 8**（`social×27`，`npc_21`） | **seed 56**（`social×125`，`ben`） |
| `social` 全局地板 | 0.00（s8）… 22.50（s5） | 0.00（s56）… 24.35（s57） |

24 局合看：**`social` 地板 min 0.00 · max 24.35 · 中位 16.52**；
**最长「`social < 36`」连续段 min 83 · max 380 · 中位 151.0**；锁面积 min 884 · max 2849 · 中位 1519.5。

## 二、剂量曲线：**收益侧没有内点顶点，顶点是代价侧给的**

### 2.1 判据：一律对着【零假设臂】判，不看红绿

零假设臂逐字抄 X1：**只把 `utility.obj_dist_penalty` 从 0.400 改成 0.401**，与社交语义毫无关系。
本棒自跑的它（24 局）：

| | 基线 | **零假设臂 `null`** |
|---|---|---|
| 硬 `#01` 红 | **8 · 56** | **无** |
| `social` 地板 min..max（中位） | 0.00..24.35（**16.52**） | 7.55..22.35（**13.90**） |
| 最长锁段 min..max（中位） | 83..380（**151.0**） | 99..204（**159.5**） |
| 锁面积 中位 | 1519.5 | 1743.5 |
| 逐 seed 对基线 好/坏 | — | **10 / 14** |

⇒ **X1 那条方法学负结果在我这棵树上完整复现**：一个语义为零的扰动**同样**把两颗硬红都拿掉了，
**同时把分布弄坏了**。所以下面每一档都报「对零假设臂的逐 seed 配对符号检验」，不报红绿。

### 2.2 八档（N=40，24 局，同一批 seed，只换 `utility.json` 的一个数）

| `obj_survival_pull` | `social` 地板 min..max（**中位**） | 最长锁段 min..max（**中位**） | 锁面积（中位） | **对 `null`：地板 好/坏（p）** | **对 `null`：锁段 好/坏（p）** |
|---|---|---|---|---|---|
| **0（缺键=基线）** | 0.00..24.35（**16.52**） | 83..380（**151.0**） | 1519.5 | 14/10（0.271） | 15/9（0.154） |
| 0.5 | 11.30..22.50（17.00） | 104..177（138.5） | 1477.5 | 16/8（**0.076**） | 17/7（0.032） |
| 0.75 | 14.10..26.15（18.30） | 70..158（127.0） | 1492.0 | 17/7（0.032） | 18/6（0.011） |
| **★ 1.0（出货）** | 11.40..25.75（**17.92**） | 73..177（**131.0**） | 1478.5 | **17/7（0.032）** | **18/6（0.011）** |
| 1.25 | 11.00..25.75（18.00） | 73..190（129.5） | 1437.0 | 18/6（0.011） | 18/6（0.011） |
| 1.5 | 11.00..25.75（18.40） | 73..190（126.0） | 1466.5 | 18/6（0.011） | 19/5（**0.0033**） |
| 2.0 | 14.10..26.50（19.12） | 68..182（121.0） | 1358.5 | 20/4（**0.0008**） | 20/4（**0.0008**） |
| 3.0 | 14.05..25.95（20.07） | 72..158（115.0） | 1282.5 | 18/6（0.011） | 18/6（0.011） |
| 10.0 | 15.70..27.55（21.90） | 61..146（106.5） | 1111.5 | 22/2（**0.00002**） | 22/2（**0.00002**） |

**三件必须一起读的：**

1. **没有内点顶点。** 中位地板 17.00 → 18.30 → 17.92 → 18.00 → 18.40 → 19.12 → 20.07 → **21.90**，
   锁面积中位 1477 → 1492 → 1478 → 1437 → 1466 → 1358 → 1282 → **1111** ——**一直在变好，到 k=10 都没转弯。**
   ⇒ **"找顶点"这件事在收益侧【没有答案】**，而这本身是结果，不是没测出来。
2. **相邻两档的差小于零假设臂自己制造的抖动。** `null` 把中位地板从 16.52 挪到 13.90（**−2.62**），
   而 0.75 与 1.5 之间只差 0.10。⇒ **0.75..3.0 这一段彼此不可分辨**，把其中任何一档说成"最优"都是过读。
3. **0.5 是唯一在地板那一列跨不过 0.05 的档**（16/8，p=0.076），10.0 是唯一明显最强的。
   **收益单调 ⇒ 若只看收益，正确答案是"越大越好"，而那显然不对。**

**顺带一条方法学的正面结果**：基线自己对 `null` 是 14/10（p=0.271）、`null` 对基线是 10/14
⇒ **这套配对检验在"两条都没有真效应"的情形下确实给不出显著** —— 它不是一把见谁都说"显著"的尺子。

### 2.3 代价侧：**恰好有一处不连续，而它落在 1.5 与 2.0 之间**

同一批剂量，跑 CI 真正跑的那两格（`Harness`，seeds 1-12 × 60 天）：

| k | **N=12 与未改动的树三元组（digest / event_digest / 逐 tick 链）全同** | N=16 全同 | N=12 活性变动 >5% 的事件类 |
|---|---|---|---|
| 0.5 | **12/12** | 8/12 | **0 种** |
| **1.0** | **12/12** | 6/12 | **0 种**（25 类**全部** ±0.0%） |
| 1.25 | **12/12** | 6/12 | **0 种** |
| 1.5 | **12/12** | 4/12 | **0 种** |
| **2.0** | **11/12（seed 12 移动）** | 3/12 | **1 种：`aid` 68 → 79（+16.2%）** |
| 3.0 | **11/12（seed 12 移动）** | 2/12 | **1 种：`aid` 68 → 79（+16.2%）** |

**N=16 那一列是单调的**（8 → 6 → 6 → 4 → 3 → 2 个 seed 不动），⇒ **剂量在 N=16 上是连续起效的。**

**硬不变量在上面每一格都是 12/12 全绿、软通过率门全过、事件类 seed 覆盖一类没掉**（判决行见 §五）。

⇒ **代价侧的不连续点在 `1.5 → 2.0` 之间。**

> ⚠️⚠️ **但 N=12 那一列的"12/12 恒定到 1.5"【不是】"这一格上有个阈值"——
> 它是【这 12 个 seed】上的巧合，而我先把它读成了机制。**
> §六.2 把同为 N=12 的另外 48 个 seed 也跑了：**移动率 31%**，而金标这 12 个是 0%
> ——**在 31% 下抽到 0/12 的概率约 1.1%**。
> ⇒ **这张表的 N=12 那一列，正确读法是"金标那 12 个 seed 的采样噪声"，不是剂量的性质。**
> 它仍然有用（它决定 CI 第 4 步会不会红、要不要重烘），**但它不能当选剂量的物理依据**（§二.4）。

### 2.4 ⚠️ **我第一版的选取理由是错的，而【杀掉它的是我自己的留出种子】**

我先写下的判据是：**「取 `[0.5, 1.5]` 这段平台里最大的、能让 N=12 逐字节不动的剂量」**，
证据就是上面那张表里 N=12 那一列的 **12/12**。

**然后留出种子把它证伪了**（§六）：**同样是 N=12、同样是 k=1.0，seeds 13-30 里有 5 个（18/25/27/29/30）
的三元组移动了。**

> ### ★ 本棒最该被记住的一条：**「金标 12/12 不动」不等于「N=12 不动」。**
> 它等于「**金标那 12 个 seed** 不动」。这正是 [docs/41 §2](41-baton-contract.md) 第三个盲区
> （一道门跑在一个它不可能变红的配置上）的**自我版本**——而它差一点骗过的人是我：
> 我本来要把「让那 12 个 seed 保持逐字节不动」当成选剂量的判据，
> **那等于拿金标的抽样当物理性质，也等于把剂量拟合到一道门的边界上**，
> 正是契约反复点名的那个病（"给出货配置预埋一条永不变红的门"）。
> ⇒ **R12 第 4 条「必须跑留出种子」在这里不是形式**：它是唯一发现这件事的东西。

**所以出货值的理由换成了下面三条，一条都不依赖"哪道门保持绿"：**

1. **收益侧与零假设臂清楚分开，两个指标都过 0.05**：k=1.0 是 `18/6`（锁段，p=0.011）与 `17/7`（地板，p=0.032）。
   **k=0.5 在地板那一列跨不过（16/8，p=0.076）** ⇒ 它是被数据排掉的，不是被偏好排掉的。
2. **★ 这一项不应该压过它所修饰的那个分数本身**（结构判据，与网格无关）：
   长椅在 `social=0` 时的 `benefit = 100 × 40/60 = 66.67`，而这一项的上限是 `36k`。
   ⇒ `k=0.75 → 0.40 倍`、**`k=1.0 → 0.54 倍`**、`k=1.5 → 0.81 倍`、**`k=2.0 → 1.08 倍（这一项开始比正主还大）**。
   **k=2.0 正是"加分项反客为主"的那一档**，而它与下面第 3 条、与 §二.3 的代价拐点**三条线落在同一处**。
3. **在 k=1.0 上，argmin 那道门是免费的**（§四：**24/24** 逐位相同）
   ⇒ 实测在 24 局 × 60 天 × 40 人里，这一项**一次都没有**压过一条比 social 更急的需求；
   而 k=2.0 起它压过了（21/24）、k=3.0 更多（15/24）。
   ⇒ **上面第 2 条与第 3 条给出的是【同一个】临界点 k=2.0，而它们用的量完全不同**（§四末尾）。

⚠️ **诚实边界，三条**：
- 2.0 与 10.0 在 N=40 上**确实更好**（p=0.0008 / 0.00002）。我用上面第 2 条把它们换掉，
  **而那是一条结构论证，不是一次实测的代价**——`aid +16.2%` 的下游后果**我没查**。
- **0.75 与 1.0 在本网格上【完全】分不开**：24 局的两个符号检验**逐字相同**
  （地板 17/7 p=0.032、锁段 18/6 p=0.011），中位地板 18.30 vs 17.92（0.75 还略高）。
  取 1.0 而不取 0.75，靠的是第 2 条的"约一半"这个整数感与 §四 那三条线的余量，**不是数据**。
  **这一条我不装作是量出来的。**
- 平台的上边缘在 1.5 与 2.0 之间，**而那个边界本身是在 N=12 的 12 个金标 seed 上量的**
  ⇒ 按本节开头那条更正，**它的可信度比表面看上去低**。真正承重的是第 2 条与第 3 条。

## 三、隔离：**效果整个住在 `social` 这一项里**

三条臂，同一个 k=1.0、同一批 24 个 seed，只差 `_survival_pull` 里那一个 `if`：

| 臂 | 生效范围 | `social` 地板中位 | 最长锁段中位 | **对基线 好/坏/不变（p）** |
|---|---|---|---|---|
| 基线 | — | 16.52 | 151.0 | — |
| `ball`（**X1 的臂 B 原样**） | **五条 need 全部** | 19.00 | 125.5 | 17/5/2（0.0085） |
| **`bsoc`（出货）** | **只 `social`** | 17.92 | 131.0 | 14/5/5（0.032） |
| `bnon`（**负对照**） | **除 `social` 外的四条** | 15.35 | 147.5 | **8/9/7（p = 0.50）** |

**三条配对检验（同一批 seed，逐 seed 配对）：**

```
bnon  vs 基线   地板  8/ 9/ 7  p = 0.5000   ← 与什么都不做分不开
ball  vs bnon   地板 16/ 3/ 5  p = 0.0022   ← 两者之差 = 只剩 social 那一项 ⇒ 效果在这里
bsoc  vs ball   地板  8/10/ 6  p = 0.4073   ← 摘掉另外四条，代价为 0
```

⇒ **把 `ball` 拆成 `bsoc ⊕ bnon`：`bnon` 那一半的效应量与零无异，两者之差显著。**
**这是分解，不是"证明"**（§〇①）——它回答的是"效果住在哪一项里"，而这正是派单真正要的东西。

⚠️ **`bnon` 也把两颗硬红都拿掉了**（24 局 0 红）。**又一次**：红绿在这一族上不含信息。

## 四、第二道门：**只在 `social` 是 argmin 时** —— 在出货剂量上免费，在大剂量上不免费

出货的那份代码比 §三 的 `bsoc` 还窄一格：`cur > min_need` ⇒ 不加分。
语义是 X1 那条诊断的**镜像**：生存门因为"有需求跌破 36"关掉社交，而锁死发生在**那个 argmin 就是 social 自己**时
⇒ 就在这一种情形下打开物件那条路，别的情形一律不动。

| k | 带 argmin 门（`bmin`） vs 不带（`bsoc`）：`Inv.digest` 逐位相同（24 局） |
|---|---|
| 0.5 | **24 / 24 —— 完全相同** |
| **1.0（出货）** | **24 / 24 —— 完全相同** |
| 2.0 | 21 / 24 —— 开始咬 |
| 3.0 | **15 / 24** —— 咬 9 局 |

⇒ **在 k ≤ 1.0 上这道门一次都没有触发过它会改变结果的那种情形**（24 局 × 60 天 × 40 人）
⇒ 它**不是**一次行为改变，**是一条构造保证**：这一项永远不可能压过一条比 `social` 更急的需求
（硬不变量 `#01` 的方向）。而 **k=2.0 起它开始咬**
⇒ **大剂量的额外收益里有一部分正是"让 social 压过更急的需求"换来的**。

> **★ 三条彼此独立的线落在同一处（k = 2.0），这是本棒选剂量时最硬的一块证据**：
> ① 这一项的上限 `36k` 在 k=2.0 处**首次超过**它所修饰的分数本身（66.67）；
> ② N=12 的金标在 k=2.0 处**首次移动**（seed 12）；
> ③ argmin 那道门在 k=2.0 处**首次咬**（21/24）。
> **三条线用的是三种完全不同的量**（代数、摘要、反事实臂），而它们指向同一个数
> —— 出货值取在它下面一档半的位置。
> ⚠️ **三条里第 ② 条要打折**：它只有 12 个 seed，而 §六.2 证明这 12 个 seed 的静默是 ~1% 的巧合。
> **承重的是 ① 和 ③**，它们一个是代数、一个是 24 局的反事实。

**零扰动对照（这条代码形状自己的）**：`bmin_00` = 代码在、`obj_survival_pull` 键不在
⇒ 与未改动的树 **12/12 个 seed 的 `Inv.digest` 逐位相同**，**且硬红仍然恰好落在 seed 8**
（`maxf(0, GATE−cur) * 0.0` 加进 `sum` 是 IEEE 恒等；`min_need` 那次提取也是逐位恒等）。

## 四之二、逐 seed 前后对照（N=40，**24 局全列，对着零假设臂判**）

`social` 全局地板 / 最长「`social < 36`」连续段。**"对 null" 一列才是判决**（不是"比基线好"）。

| seed | 基线 | 零假设臂 `null` | **出货 k=1.0** | 对 null |
|---|---|---|---|---|
| 1 | 12.00 / 173 | 18.45 / 127 | **25.10 / 78** | 好 |
| 2 | 17.65 / 172 | 8.95 / 194 | **23.00 / 102** | 好 |
| 3 | 19.80 / 117 | 17.95 / 130 | **11.55 / 175** | **坏** |
| 4 | 16.90 / 137 | 20.00 / 127 | **17.55 / 133** | **坏** |
| 5 | 22.50 / 104 | 11.35 / 174 | **22.50 / 104** | 好 |
| 6 | 14.10 / 158 | 19.50 / 120 | **14.10 / 158** | **坏** |
| 7 | 20.45 / 111 | 18.15 / 129 | **22.65 / 96** | 好 |
| **8** | **0.00 / 367（硬红）** | 12.40 / 170 | **19.20 / 155** | 好 |
| 9 | 17.90 / 130 | 17.10 / 144 | **17.90 / 130** | 好 |
| 10 | 12.60 / 169 | 10.30 / 184 | **17.95 / 130** | 好 |
| 11 | 17.10 / 154 | 22.35 / 99 | **16.90 / 137** | **坏** |
| 12 | 19.80 / 116 | 10.30 / 184 | **11.40 / 177** | 好 |
| 49 | 10.25 / 185 | 16.70 / 138 | **19.90 / 116** | 好 |
| 50 | 17.80 / 131 | 12.55 / 169 | **18.65 / 124** | 好 |
| 51 | 11.30 / 177 | 17.80 / 131 | **14.20 / 156** | **坏** |
| 52 | 18.60 / 125 | 15.05 / 150 | **15.80 / 143** | 好 |
| 53 | 14.90 / 151 | 20.80 / 152 | **15.95 / 144** | **坏** |
| 54 | 11.35 / 175 | 11.25 / 178 | **20.10 / 114** | 好 |
| 55 | 15.60 / 159 | 10.80 / 181 | **25.75 / 73** | 好 |
| **56** | **0.00 / 380（硬红）** | 11.30 / 176 | **20.65 / 110** | 好 |
| 57 | 24.35 / 83 | 12.75 / 167 | **15.05 / 150** | 好 |
| 58 | 17.60 / 132 | 9.80 / 187 | **17.60 / 132** | 好 |
| 59 | 14.90 / 151 | 7.55 / 204 | **18.70 / 124** | 好 |
| 60 | 16.15 / 140 | 19.30 / 120 | **16.15 / 140** | **坏** |

**合计 17 好 / 7 坏（p = 0.032）；最长锁段那一列是 18 好 / 6 坏（p = 0.011）。**

**三条必须一起读的（都是这张表自己说的）：**

1. **两颗硬红都被拉回来了**：seed 8 `0.00/367 → 19.20/155`、seed 56 `0.00/380 → 20.65/110`。
   ⚠️ **而零假设臂也把它们拉回来了**（12.40/170 与 11.30/176）——**所以这两行不是判决，判决是那 24 行的比分。**
2. **它有 7 个 seed 变坏，而且不小**：seed 3 从 19.80 掉到 11.55、seed 12 从 19.80 掉到 11.40。
   **这条修法不是每局都更好，它是【分布向好】。** 报极值时这两个坏的也一并报。
3. **有 5 个 seed（5 / 6 / 9 / 58 / 60）在这两个指标上与基线一模一样，而其中只有 3 个是 `digest` 逐位相同。**
   ⇒ **指标相同 ≠ 轨迹相同**：另外 2 局世界真的走了不同的路，只是"最深的那个坑"恰好一样深。
   凡是拿指标当"没变化"的证据，都要先看一眼摘要。

## 五、N=12 / N=16 回归：**两格都不回归**

`Harness`，seeds 1-12 × 60 天，**门自己的判决行**（改后 = 出货树，k=1.0）：

```
N=12  改前  === S0 GATE: PASS ✅  (硬不变量 seed 12/12 全绿, 软通过率门 ≥11/12(90%) 过, 活性 过, 金标 过, det 3/3) ===
N=12  改后  === S0 GATE: PASS ✅  (硬不变量 seed 12/12 全绿, 软通过率门 ≥11/12(90%) 过, 活性 过, 金标 过, det 3/3) ===
N=16  改前  === S0 GATE: PASS ✅  (硬不变量 seed 12/12 全绿, 软通过率门 ≥11/12(90%) 过, 活性 过, 金标 过, det 1/1) ===
N=16  改后  === S0 GATE: PASS ✅  (硬不变量 seed 12/12 全绿, 软通过率门 ≥11/12(90%) 过, 活性 过, 金标 过, det 1/1) ===
```

- **N=12 seeds 1-12**：三元组（`digest` / `event_digest` / 逐 tick `chain`）**12/12 逐位相同**，
  活性 25 类**全部 ±0.0%**。⚠️ **但别把这句读成"N=12 不动"**——同样 N=12 的 seeds 13-30 动了 5 个（§六.1）。
- **N=16 seeds 1-12**：**6/12 个 seed 的轨迹移动**（真的在动，不是空转），硬不变量 12/12 全绿、软失败 0、
  **没有任何事件类的 seed 覆盖下降**、基数 ≥20 的事件类**没有一类变动超过 5%**。
- **顺带把 N=12 的网格推宽到 seeds 31-60**（U1 做过的那一格）：改后
  `=== S0 GATE: PASS ✅ (硬不变量 seed 30/30 全绿, 软通过率门 ≥27/30(90%) 过, 活性 过, 金标 过, det 1/1) ===`。
- `#41` / `#29` / `#16` / `#20` / `#5` / 放逐一族：上面每一格的每一侧**均无失败**
  （逐 seed 的 `hard_fails` 与 `soft_fails` 全空）。

## 六、R12：**三份锚逐个跑过，动的恰好是一份（而不是零份，也不是三份）**

[docs/47](47-wave-e-plan.md) 的 R12 是给**会移动 digest 的棒**写的。本棒**先跑后判**，
三份锚都在**出货那棵树**上真跑了一遍（不是在隔离副本上推的）：

| 锚 | 命令 | 结果 |
|---|---|---|
| `Harness` seeds 段（N=12 · seeds 1-12 · 60 天） | `--golden game/bench/golden_digests.json` | **金标一致 12/12（含逐 tick 前缀链）—— 没动** |
| **`DetGate` scenarios 段**（4 轨 × seeds 1-4 · 20 天） | `--seeds 1-4 --days 20` | **❌ 金标 15/16 —— `betray` seed 1 移动** |
| `ModelPathGate` 锚（seeds 1-4 · 8 天 · N=12） | `res://bench/ModelPathGate.tscn` | **PASS（失败 0）—— 没动** |

`betray` seed 1：`2895846081 / 326780007998127894 / 1117411208` → `4067550143 / 979380727193785299 / 1133450791`，
**硬不变量 16/16、两跑一致 16/16、数据指纹一致 16/16 —— 只有摘要动了**，正是 R12 说的那种受控移动。

⇒ **重烘按 R12 走完整套**（同一个 commit）：`Harness --bake-golden` + `DetGate --bake-golden`
+ `ModelPathGate --bake-anchor`，并在 `_meta.rebake_history` 补一条。
⚠️ **前后两份 `golden_digests.json` 的 `git diff` 里，`seeds` 段 12 个 seed 的值一个字节都没变**
——把三份都烘是照 R12 的字面要求（E1 实测只烘两份会让 `ci.sh` 变红），
**不是因为那两份的内容需要变**。这一点写在这里，免得下一个人从"烘了三份"推出"三份都动了"。

### 6.1 ★ 留出种子 13-30 × 60 天（N=12）：**两侧都跑、两侧都报，而它推翻了我自己的一条结论**

```
改前  === S0 GATE: PASS ✅  (硬不变量 seed 18/18 全绿, 软通过率门 ≥17/18(90%) 过, 活性 过, 金标 过, det 1/1) ===
改后  === S0 GATE: PASS ✅  (硬不变量 seed 18/18 全绿, 软通过率门 ≥17/18(90%) 过, 活性 过, 金标 过, det 1/1) ===
```

**硬不变量两侧各 18/18 全绿、软失败两侧各 0、逐 seed 饿穿两侧恒 0。**
而三元组（`digest` / `event_digest` / 逐 tick `chain`）**18 个里动了 5 个：seed 18 · 25 · 27 · 29 · 30。**

⇒ **这五个 seed 就是 §二.4 那条更正的证据**：同样是 N=12、同样是 k=1.0，
**金标那 12 个 seed 一个不动，而留出的 18 个里动了 5 个** ⇒
「N=12 上这条修法是 no-op」是**假的**，真的说法是「**金标抽到的那 12 个 seed 上它是 no-op**」。

### 6.2 把 N=12 的网格再推宽到 seeds 31-60 —— **把"巧合"量成了一个数**

```
改前  === S0 GATE: PASS ✅  (硬不变量 seed 30/30 全绿, 软通过率门 ≥27/30(90%) 过, 活性 过, 金标 过, det 1/1) ===
改后  === S0 GATE: PASS ✅  (硬不变量 seed 30/30 全绿, 软通过率门 ≥27/30(90%) 过, 活性 过, 金标 过, det 1/1) ===
```

三元组 **30 个里动了 10 个**（34/36/38/39/40/47/49/51/59/60）。合起来：

| N=12 的 seed 段 | 可比 | **三元组移动** |
|---|---|---|
| **1-12（金标抽的那批）** | 12 | **0（0%）** |
| 13-30（留出） | 18 | 5（28%） |
| 31-60 | 30 | 10（33%） |
| **13-60 合计（非金标）** | **48** | **15（31%）** |

⇒ **在 31% 的移动率下，"金标那 12 个恰好一个都不动"的概率是 `0.6875^12 ≈ 1.1%`。**
**它不是"这条修法在 N=12 上是 no-op"的证据，它是一次 ~1% 的抽样巧合。**

⇒ 契约 §3 第 4 条「必须跑留出种子证明不是对基准过拟合」在本棒身上以**字面**的方式生效了：
**我差一点把"对基准过拟合"当成选剂量的理由。**

## 七、§2.5 探测包络

⚠️ **本棒【没有新增也没有收紧任何一道门】**：判据（`Invariants.gd`）、`SUPPLY_FLOOR` / `SURVIVAL_GATE` /
`SOFT_RATE` / 软通过率门**一个字节没改**，量具不进 `tools/ci.sh`、不做判决。
所以下面这三行写的是**这次改动本身**的包络：什么样的失效模式会被现有的门抓到、什么样的不会。

```
Y1 · utility.obj_survival_pull = 1.0（Sim._survival_pull，三道门：缺键 / 只 social / 只 argmin）

detects:（**四条都是跑出来的；而我先写下的那一版有两条是【想】出来的，被下面这次实测打掉了**）
  ① **`DetGate` 的 `betray` seed 1 现在钉住了这条行为的【存在】**（§六）：重烘之后那一格是
     `4067550143 / 979380727193785299 / 1133450791`。四条变异体各跑一次
     （隔离副本，各自装的是【重烘后】的金标）：
       · **`obj_survival_pull` 删掉**            ⇒ 金标=❌ 实得 `2895846081/…/1117411208`（退回改前值）**红**
       · **把 `social` 那道门【反过来】**（只对其余四条 need 生效） ⇒ 金标=❌ 同上 **红**
     ⇒ 被机检住的**恰好是"social 那一项还在"**——**注意它不等于"只有 social 那一项"**，
     后者是 `does_not_detect` ⑦（把别的 need 加回去，这一格照样绿）。
  ② 剂量走过头**会被 CI 第 4 步（金标）抓到**：k ≥ 2.0 ⇒ N=12 seed 12 的三元组移动 ⇒ 金标红。
     实测 11/12（k=2.0 与 3.0 各一次）。⚠️ 但见 `does_not_detect` ⑦——这一条的可信度只有 12 个 seed。
  ③ 缺键即零扰动是**实测**不是声明：代码在、键不在（`bmin_00` 臂）⇒ N=40 的 12 个 `Inv.digest`
     与未改动的树逐位相同、**硬红仍落在 seed 8**；`base` 臂 12/12 同理。
  ④ 量具自身与门一致：`x1_margin` 的 `base` 臂 12 个 `Inv.digest` 与 `Harness` 基线逐位相同，
     两颗硬红落在同样的 seed（8 与 56），`#01` 的 detail 文字逐字相同
     ⇒ 它不是"又量了一遍得到别的数"。

does_not_detect:（**跑出来的，不是想出来的**）
  ① ★ **金标（N=12 seeds 1-12）看不见这条改动。** 它 12/12 逐位不动 —— 而同为 N=12 的
     seeds 13-30 动了 5 个、31-60 也在动。⇒ **CI 第 4 步对本次改动的正确性零覆盖**，
     守住它的是第 4c 步（`DetGate` betray）那一格，**而那是一格**。
     ⚠️ 这一条不是推的：我原本要拿"金标 12/12 不动"当剂量判据，是留出种子把它打掉的（§二.4）。
  ② **红绿计数对这一族不含信息，而本次改动没有改善这一点。** 零假设臂（`obj_dist_penalty`
     0.400→0.401）与负对照臂 `bnon`（明确无效，p=0.50）**都**把两颗硬红拿掉了。
     ⇒ **`#01` 在 N=40 上仍然是一条【没有统计功效】的门**，下一棒仍然只能用连续余量判。
  ③ **它守不住"社交发生的质量"。** 它把人推去坐长椅，而长椅是**一个人也能坐**的物件候选
     —— `social` 涨了 40 分，**没有任何第二个人参与**。本棒**没有量**「被推去坐长椅的那些 tick 里
     旁边有没有人」，所以**不能**声称它修好的是"孤独"，只能声称它修好的是**那条 need 曲线**。
     ⚠️ 这是本次交付最容易被读过头的一条。
  ④ **`hunger`/`energy`/`hygiene`/`fun` 的地板它一概不守。** 出货臂在 24 局上这四条的最小值是
     6.88 / 23.86 / 24.43 / 12.92（基线 1.74 / 18.08 / 24.54 / 12.40）—— 没有变坏，
     但**这是观察不是保证**：argmin 门只保证不压过【更急】的需求，不保证不压过**同样急**的。
  ⑤ **backend != null / 有 LOD / 有玩家 / N∈{24,48,60} / days≠60 / seed>60：一格没跑。**
     [docs/41 §2](41-baton-contract.md) 点名的五个温和配置里，本棒只换掉了 **N**（12/16/40）与 **seed**。
  ⑥ **它不查因果链**：没有做"被推去坐长椅 ⇒ 少被拒 ⇒ 棘轮变慢"这条链的隔离干预。
  ⑦ ★ **`DetGate` 那颗牙【只咬"在不在"，不咬"多宽"也不咬"多大"】** —— 这两条是我先写成
     `detects` 然后被实测打掉的（同一批四个变异体，同一条命令）：
       · **把 `need_id != "social"` 那道门【去掉】**（= X1 臂 B 原样，对五条 need 全生效，k=1.0）
         ⇒ `=== DetGate: PASS ✅ (… 金标 16/16 可比) ===` —— **绿的。**
       · **剂量翻倍到 k=2.0** ⇒ `=== DetGate: PASS ✅ (… 金标 16/16 可比) ===` —— **也是绿的。**
     ⇒ **§三 那条"只对 social"的窄化，今天【没有任何门在维护】**；守着它的只有
     `Sim._survival_pull` 抬头的注释与本文。同理，剂量只被 CI 第 4 步的 12 个金标 seed 拦着，
     而 §六.2 已经说明那 12 个 seed 的静默是 ~1% 的巧合 ⇒ **k=2.0 会不会被 CI 拦住，
     取决于金标抽的是哪 12 个 seed。这是一条脆弱性，不是一条保护。**
  ⑧ **本棒没有为 ⑦ 加门。** 加门要动 `Invariants.gd` 或 `tools/`（后者是别人的行），
     且 [docs/95 §四](95-wave-y-plan.md) 明写"判据不许放宽"也隐含"别顺手改判据"。
     ⇒ **照实报，留给下一棒**（§八.2）。

confidence: **N=8 个剂量档**（0.5/0.75/1.0/1.25/1.5/2.0/3.0/10.0）× **24 局**（N=40，两块 12-seed）
  + **3 条隔离臂**（social / 非-social / 全部，各 24 局）+ **2 条零扰动对照**（`base`、`bmin_00`）
  + **1 条零假设臂**（24 局）+ **12 格回归**（N=12/16 × k=0.5/1.0/1.25/1.5/2.0/3.0，各 12 seed）
  + **N=12 seeds 13-30（18）与 31-60（30）改前改后各一次**
  + **三份提交锚各跑 1 次 + `DetGate` 的 4 个变异体各跑 1 次（2 红 2 绿）**。
  **全部在 backend=null · 无 LOD · 60 天（DetGate 20 天）上量过，别的配置一格没有。**
```

## 八、卡在哪，下一个该测什么

### 8.1 三个卡点

1. **收益侧没有顶点 ⇒ 剂量只能靠代价侧定，而代价侧我只量了一个维度。**
   我用的是「N=12 逐字节动不动」+「N=12 活性变动」。**没量**：N=16/N=40 上的活性代价、
   帧时、以及 `aid +16.2%` 那一格的下游后果。⇒ **2.0 是有数据支持的下一档，而否掉它的理由是【我没量】而不是【它更差】。**
2. **`#01` 在 N=40 上仍然没有统计功效**（60 个 seed 2 颗红），而本棒**没有改善这一点**。
   X1 已经给过一条便宜推论：**加 seed 比加 N 更快找到下一颗红**（N=40 相对 N=24，中位锁段只涨 11%、最长那一段涨 84%）。
   ⇒ **下一棒要判"还有没有红"，应该跑 seeds 61-180 × N=40，而不是跑 N=48/60。**
3. **"孤独"与"social 这条数"被本棒当成了一回事，而它们不是。** 见 §七 `does_not_detect` ③。

### 8.2 下一个该测什么（按手上的证据排序）

1. **量「被推去坐长椅的那些 tick 里旁边有没有人」。** 挂已有的只读钩子 `Sim.decision_sink`，
   数 `option == 长椅/吧台` 且 `need == social` 的那些决策点上的 `_nearby_agents` 大小。
   **这一格决定本次交付该被叫做"修好了孤独"还是"修好了一条 need 曲线"**，而今天只能叫后者。
2. **`Sim.gd` 里 `_journey_candidates` 的 `if nid == "social" ... continue`**
   —— X1 点名的一个 token 的改动，**本棒同样没测**。X1 实测 seed 56 的 1182 个锁 tick 里
   **329 个（28%）是结构性零**：他在屋里，而跨平面行程按构造不为社交发起。
   ⚠️ 它会移动全部 digest ⇒ 要付整套 R12（本次没付，是因为本次没动）。
3. **k=2.0 的完整代价核算。** 若 N=12 那 `aid 68→79` 被判为可接受，2.0 在 N=40 上是
   20/4（p=0.0008），比出货的 1.0 好一档。**这是一次明确的、有数字的取舍，不是遗留问题。**
4. **社交家具随人口缩放**（X1 的第 3 条，本棒没测）：全镇「社交」/「闲聊」广告各只有一条、
   收益是常数 ⇒ 物件那一侧不随人口涨。本次的 `+ (36−social)·k` 是**给同一条广告加分**，
   **不是加广告** —— 两条修法正交，可以叠。
5. **`gossip_rep` 的枚举压制**（X1 的第 4 条，本棒没测）。
6. ★ **给 §七 ⑦ 那条脆弱性加一道门。** 今天"这一项只对 `social` 生效"完全靠注释维护
   —— 把它去掉，`DetGate` 与 CI 全绿（实测）。**这与 X3 查的那一族是同一个形状**
   （几道门恰好互补，而没有任何东西在维护那份互补）。
   便宜的做法：给 `Invariants.gd` 加一条**只观测**的计数（这一项施加了几次、施加在哪条 need 上），
   再让某道门断言"非 social 的施加次数恒为 0"。⚠️ **加门要先量余量、要写包络、要有负对照**，
   而且它跨到判据文件，**应该单独派，不该顺手加。**

### 8.3 测了之后决定不做的（[docs/95 §四](95-wave-y-plan.md) 要求写进回执）

- **不出货 k=2.0 / 3.0 / 10.0**，理由在 §二.4 —— **它们在 N=40 上更好，我是拿 N=12 的标定风险换掉的。**
- **不出货"全部 need"那一版（X1 的臂 B 原样）**：另外四条 need 的效应量与零无异（p=0.50），
  而它们要付 N=12/N=16 上额外的轨迹移动。
- **没有动 `SURVIVAL_GATE`**（36 原样）、**没有动 `SUPPLY_FLOOR` / `SOFT_RATE` / 软通过率门**、
  **没有动 `Invariants.gd` 一个字节**。
- **没有改 `Harness` 那句"没传 `--golden` 也打印【金标 过】"**（§〇④）——
  它在本棒的 owns 里、改起来是一行，但那会改 CI 的输出文本、且不是派单要的东西。**留给下一棒。**
- **没有测 `_journey_candidates` 里 `if nid == "social" ... continue`**（X1 点名的一个 token 的改动）
  —— 它与本次修法是**同一条出路的另一半**（本次让长椅赢下 argmax，那一条让"进了屋的人还能想到出门"），
  但它会移动全部 digest、要付第二套 R12，**一波塞不下**。

## 九、`bash tools/ci.sh` 的实际输出

命令：`GODOT=… bash tools/ci.sh > analysis/y1/ci_final.txt 2>&1`
（**没有用 `tee | tail`**——它会吃掉退出码），`rc = 0`。然后**把整份文件读一遍**：
**全文 645 行**，`✅` 命中 **227** 条，`❌ / FAIL / ✗` 命中 **2 条，而两条都不是失败**（§9.1）。

```
### 0.  ✅ no tracked weights/binaries                            ⏱ 10s
### 1/1b/2.  ✅ lint_data · ✅ audit_map · ✅ lint_links           ⏱ 0s / 0s / 1s
### 2b/2c/2d. === ART GATE PASS ✅ === · === TERRAIN GATE PASS ✅ === · === ASSET GATE PASS ✅ ===
### 2e. === recalc · 注册表 7 条（上门 5 条 / 只记录 2 条）===
        === RECALC GATE: PASS ✅（5 条数字现读现算现比）===              ⏱ 2s
### 3.  ✅ import/parse clean                                     ⏱ 11s
### 4.  === S0 GATE: PASS ✅  (硬不变量 seed 12/12 全绿, 软通过率门 ≥11/12(90%) 过, 活性 过, 金标 过, det 3/3) ===   ⏱ 249s
### 4a. ✅ 4a 预检：池倍率 ×16/12 ≠ 1 · days=60 ≥ SUPPLY_MIN_DAYS=60
        === S0 GATE: PASS ✅  (硬不变量 seed 12/12 全绿, 软通过率门 ≥11/12(90%) 过, 活性 过, 金标 过, det 1/1) ===   ⏱ 323s
### 4b. === LOD-VERIFY GATE: ✅ PASS  (V2+V3abc 全绿) ===                                          ⏱ 21s
### 4c. === DetGate: PASS ✅  (硬 16/16, 两跑一致 16/16, 数据指纹一致 16/16[2382182544], 金标 16/16 可比) ===  ⏱ 153s
### 4d. === BackendGate: PASS ✅  (硬 8/8, 两跑一致 8/8, 闭集封闭 8/8, 自检臂必红 4/4, 伪造落地 4/4) ===        ⏱ 39s
### 4e. === ModelPathGate: PASS ✅  (失败 0)      ### 4f. === VoiceGate: PASS ✅ ===              ⏱ 8s / 35s
### 5.  ✅ 9 个 unit / integration 场景（goals_test 124s · story_test 331s）                       ⏱ 490s
### 6.  === DAYNIGHT GATE: PASS === · === ROUNDTRIP GATE: PASS === · === POND GATE PASS ✅ ===
        ✅ 视觉门（昼夜 / 界外层重画 / 空间往返 / 岸线 / 室内外壳 / 家具语义 / 树丛点阵）          ⏱ 199s

  ⏱  全程 1544s
=== CI PASS ✅ ===
```

**★ 第 4c 步就是本次改动的那颗牙**：`金标 16/16 可比` —— 其中 `betray` seed 1 用的是**重烘后**的值。
重烘之前同一条命令在同一棵树上是 `FAIL ❌ 金标 15/16`（§六），**两次都跑过、两个输出都在树上**
（`analysis/y1/grids/after_anchor_detgate.txt` 是红的那次，`analysis/y1/ci_final.txt` 是绿的那次）。

### 9.1 那两条 `❌` 是什么（**先说清楚再说 PASS**）

两条都在 **2e 可重算门**的「**只记录、不上门**」那两行里（注册表 7 条：上门 5 条 / 只记录 2 条），
`RECALC GATE` 自己是 `PASS ✅`：

```
palette-gpl-de00-6p3   否   文档 6.3   现跑 8.0659   ❌
lint-links-md-count    否   文档 93    现跑 113      ❌
```

⚠️ **第二条里的 113 有我一份**：本棒新增了本文档，把全仓 `.md` 数推到 **113**。
[docs/92 §9.1](92-wave-x-x1-n40-hard-red.md) 记的是 109 ⇒ **X1 之后到我之前又多了 3 份**（不是我加的）。
那条条目**刻意不上门**（注册表原话：给它上门 = 造一道会因为无关改动变红的门），所以它不是红。

### 9.2 三条诚实边界

1. **这一跑跑完之后，我又编辑了本文件**（把这段 CI 输出、§七 的四个变异体结果贴进来）。
   ⇒ **提交的树与 CI 跑过的树，差别只有 `docs/96-*` 的正文与 `analysis/y1/**` 的几个文件**，
   **没有任何代码 / 数据 / 锚 / 量具的改动**。贴完之后我**单独重跑了 `tools/lint_links.py`**
   （唯一会读 `.md` 的那道检查），结果见下方。
2. **CI 跑的是本棒的树，不是 Wave Y 三根棒的 union。** 本棒只碰 `game/scripts/Sim.gd` +
   `game/data/utility.json` + `game/bench/{golden_digests,modelpath_anchor}.json` + 本文档 + `analysis/y1/**`，
   与 Y2（`tools/**`）、Y3（`Story.gd`/`Director.gd`）**不重叠**；
   但 **union 上的那一次 CI 本棒没有跑过**，合流的人要自己再跑一遍。
   ⚠️ **而这一次的合流比平常危险一格**：**Y3 动的正是 `story_test` 判的那一层，
   而本棒动了仿真** ⇒ 两边的改动会在同一个 fixture 上相遇。
3. **`godot` 的退出码在本机不可信**（X1 §〇④ 记过，本棒复核仍然成立：它是个 `.cmd` 包装）。
   本文全部红绿**按判决行判**，`ci.sh` 自己的 `rc=0` 只作旁证。

## 十、没能测到什么（**"没测"明写，不用推断填空**）

- **N=24 / N=48 / N=60 一格没跑。** 本文凡是"随 N 激活"的话，真实范围是 **N ∈ {12, 16, 40}**。
- **seed > 60 没跑**；**days ≠ 60 没跑**。
- **`backend != null` 没跑、LOD 没跑、真机没跑、有玩家没跑。**
  ⚠️ 这一条对本文结论有**方向明确**的影响：`#01` 自己的包络写着「零模型地板上 100% 是 social；
  **模型路上 `hunger` 反而是主项**」⇒ **本文"效果整个住在 social 里"是【零模型地板】的性质。**
- **帧时 / 内存没量。**
- **"被推去坐长椅时旁边有没有人"没量**（§七 ③）——本次交付**不能**被读成"修好了孤独"。
- **`aid +16.2%`（k=2.0，N=12）的下游后果没查。**
- **`#01` 自己那条"不区分'一个人 61 tick'与'61 个人 1 tick'"的盲区本棒没有修。**
