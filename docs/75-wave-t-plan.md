# 75 · Wave T 计划——**先修根因：让加宽后的 seed 网格【真的】能绿**

> 前置：docs/41 全部（**§0.5**、**§0.8**、**§2.5 探测包络**、**§1.5**）、docs/47 **R12**、
> **docs/72（S1 的基准率实测，本波的立项依据）**、docs/73（S2 的门牙审计）、docs/74（S3 的家具门）。
> **回执必须带 §2.5 三行包络，`does_not_detect` 必须是跑出来的。**

## 〇、用户 2026-08-01 定的方向

S1 量到：**S0 软门的绿有相当一部分是【选种子】选出来的。**

| 网格 | 破 `SUPPLY_FLOOR=0.50` |
|---|---|
| seeds 1-12（CI 跑的，**也是当初挑阈值用的那一格**） | **0/12** |
| seeds 13-30（held-out） | 1/18（seed 18 = 0.419） |
| seeds 31-60（**此前没有任何人跑过**） | **4/30**（36 / 40 / 50 / 58） |

没参与挑阈值的 seed 上 **5/48 = 10.4%**，Wilson95% **[4.5%, 22.2%]**。
**而这不是算术推论**：`--seeds 49-60` 在**未改动的树**上直接打印 `=== S0 GATE: FAIL ❌ ===`。

**用户的选择：先修根因，让加宽后的网格【真的】能绿；CI 暂维持 seeds 1-12，修完再加宽。**
⇒ **本波不许用"改判据 / 换阈值 / 挑一格好看的网格"通过验收。**

## 一、T1 · 口粮为什么会两个生产者一起塌（**很可能移动金标，走 R12**）

**owns**：`game/scripts/Sim.gd`、`game/data/**`、`game/bench/Invariants.gd`
**不得触碰**：`game/scripts/Main.gd`、`game/scripts/WorldView.gd`、`game/assets/**`（T3 的行）、`tools/**`（T2 的行）
**你的编号文档**：编号 **76**

### S1 已经把根因量到了，你从这里接着往下

- **5 个红里 4 个**是**面点师 + 渔夫【一起】塌**的 seed（两人产量和 5/6/7/8，而正常展布是 5..30；n=60，ρ=0.618）。
- **第 5 个**（不同族）是**咖啡师 60 天里只上工了一次**。
  ⇒ **这两条别混成一件事修**；第 5 个很可能是排班/在班判定那一族，不是产量那一族。

### 硬要求

1. **判据不许动。** `SUPPLY_FLOOR` / `SUPPLY_MIN_DEMAND` / `SUPPLY_MIN_DAYS` 以及软通过率门
   **本波一律不许放宽**——那是用改判据冒充修根因。**若你认为判据本身错了，写进回执当结论，但不要在本波改它。**
2. **验收网格是 seeds 31-60**（S1 跑出 4 红的那一格）**加 13-30**。
   ⚠️ **修完之后 1-12 也必须还绿**——别用"修好了远端、弄坏了近端"换分。
3. **别只看均值。** S1 的红是**两个生产者一起塌**，那是**联合分布**的尾部；
   把两人产量的联合展布画出来，**给展布不给均值**。
4. **先量再动**，且**先证明你的修法作用在你以为的那条路上**——
   S1 自己预测"fun-32 的广告约 0 次选中"就当场被数据推翻了（它把 `喝咖啡` 翻了三倍）。
5. 移动金标就走**完整 R12**（三锚重烘 + `rebake_history` + held-out 13-30 两侧）。

### 验收（T1）

1. 联合分布的展布 + 根因判定（两族分开报）。
2. **seeds 31-60 与 13-30 的前后对照**，逐 seed 报，**不合并**。
3. **seeds 1-12 不得回归**；金标要么逐字节不变，要么走完 R12。
4. **负对照**：把你的修法关掉（开关或 revert），那 5 个红必须回来。**回不来说明你修的不是它。**
5. §2.5 三行包络；`bash tools/ci.sh` 全绿（**读输出，不是读退出码**）。

## 二、T2 · 把分母的另一半跑出来（**前瞻注入**）

**owns**：`tools/**`、你的编号文档
**不得触碰**：`game/**`
**你的编号文档**：编号 **77**

S2 交付了 `tools/brief_mutate.py` 与**回溯**那一半：
catch rate **62/82 = [0.653, 0.836]**，**而整个区间都是上界**（未被核的假断言不可观测）。
**但它明写前瞻那一半没做 ⇒ 探测率本身 N=0。**

⇒ 本棒做前瞻：**往真实 brief 里注入已知的假断言，派棒，看抓不抓得住。**

### 硬要求

1. **分层，不许合并。** S2 已经证明五种注入的证伪成本差**两个数量级**——
   合并成一个数就是 docs/50 §八 那个 30/30 犯过的错。
2. **恒给区间。** 30/30 的 Wilson 是 [0.886, 1.000]，那个上界毫无信息量。
3. **保留原始数据。** docs/50 §八 那次没保留 ⇒ 它引的那个 `52@<分支>` 形式的路径
   指向一个**在任何分支上都不存在的文件**。
   （⚠️ 本行最初写成了带 `docs/` 前缀的形式，`lint_links` **在提交前当场抓住了它**——
   而这一行的内容恰好就是在讲那个号不存在。这道门是有牙的。）
   **你的原始数据必须落盘进仓库。**
4. **注入的 brief 不许真的派去改代码**——用只读任务或隔离副本，**别让实验污染出货树**。
5. ⚠️ **S2 自己的负对照发现它的"数字"分层会 false-positive 1/2**。**先复现这一条，再往上加东西。**

### 验收（T2）

1. 前瞻实验的设计 + **逐层**结果 + 每层的区间。
2. 与 S2 回溯那一半的对照：**两者一致还是矛盾？矛盾也是结果。**
3. 原始数据落盘。
4. **明写这次实验自己的局限**（样本量、注入是否代表真实错误的分布、评判者是不是同一方）。

## 三、T3 · 录屏进不了室内，而 HUD 只有 1× 能读

**owns**：`game/scripts/Main.gd`、`game/scripts/WorldView.gd`、`game/assets/**`、UI 场景
**不得触碰**：`game/scripts/Sim.gd`、`game/data/**`、`game/bench/**`（T1 的行）、`tools/**`（T2 的行）
**你的编号文档**：编号 **78**

### 两件，都是上一波定位到但够不着的

1. **录屏进不去室内。** S3 定位到行：`--shot-fit` 的相机代码嵌在 `Main.gd` 的
   `if _shot_path != ""` 分支里 ⇒ **只有出图模式走得到**；`--demo-cam` 全程停在小镇平面
   （实测 45 帧中 **0 帧**是室内取景）。`Main.gd` 当时不在它的 owns 里，**现在在你的**。
   ⇒ 让演示镜头能进屋，**录一段真的能看见室内的**。
2. **HUD 只有 1× 能读。** `tools/make_gif.sh` 抬头那张五行对照表是实测的：
   **唯一拿得到可读性的档位是 1×**。那不是缩放的问题，是**字号/密度**的问题
   ——1280×768 原生下 HUD 文字本来就贴着可读下限。
   ⇒ 这是用户点名要的 UI/UX 那一项。**先量再动**：给出改前的字号/行高/对比度实测，再动。

⚠️ **红线 R2（docs/43）**：素材必须 CC0 或自己画，**生成图不得作为出货素材**。
GPT 图生成**只能当参考/情绪板**。

### 验收（T3）

1. **仿真侧逐字节不变**——**用 digest 证明**（S3 给了三条独立证据的先例：自造 A/B + 开工前的金标 + 留出 seed）。
2. 相机改动**不得让仿真依赖相机**——`lod_verify` 的 V2 臂就是守这条的，**跑它，读输出**。
3. HUD 改动的前后对照截图 + 可读性的**实测**判据（不是"我觉得清楚了"）。
4. 新录屏（**这次要有室内**）；录不出来照实说。
5. `bash tools/ci.sh` 全绿（读输出）。

## 四、共同约束（照抄，不重述理由）

- docs/41 红线四条 + §0.5；**计划不得给契约加码**。
- **`does_not_detect` 必须是跑出来的**；**"测了之后决定不做"必须写进回执**。
- **看到"零引用/零调用"先跑 `git log -S`**；**别在 CI 跑的时候改 `ci.sh`**。
- **加不齐先重新数**；**报极值连并列一起报**；**收紧判据前先量余量**。
- **单 seed 标定出来的容差是冻结字面量**——S2 证明这一族全会过期，S3 当场撞上（2.08 vs 5.65）。
  **任何新阈值都要报它在多个 seed 上的展布。**
- **编号已占好，但【不要】在正文写 `docs/` + 数字的纯文本形式**（`lint_links` 检查 (2) 认它）。
- **worktree 的 checkout 可能落后两百个 commit**（上一波三根棒分别落后 199 / 199 / 199）：
  **开工前先确认基线，是祖先才 ff。**
- 隔离副本里做负对照；越界要声明。
