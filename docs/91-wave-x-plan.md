# 91 · Wave X 计划——**硬不变量在 N=40 上是红的；以及"这道门跑在它不可能变红的配置上"**

> 前置：docs/41 全部、docs/47 **R12**、**docs/89（W2：N=40 的硬红与三 N 决策表）**、
> docs/88（W1：那个 −42% 不存在）、docs/90（W3：故事层与它自己钉死的边界）。
> **回执必须带 §2.5 三行包络，`does_not_detect` 必须是跑出来的。**

## 〇、这一波的排序理由

1. **红线 #3 写的是"单镇最多 60 居民"，而 W2 今天实测：N=40 上硬不变量 `#01` 是红的**
   （seed 8，两次 Harness 逐字节复现）；`seeds 49-60` 更尖锐——`#40` 软门 **12/12 全绿**，
   **门纯靠硬不变量判红**（seed 56）。**有史以来记录到的三个硬红全部在 N=40。**
   ⇒ **这是能力承诺与实测之间的一道缺口，本波第一优先。**
2. **W3 顺手发现 `story_test` 跑在一个它不可能变红的配置上**（默认 14 天，
   promise/secret/pact 在 12/12 个 seed 上全是 0/0）。
   ⇒ **那么还有几道门是这样？** 这是 S2 "门有没有牙"那条追问的**另一半**：
   S2 查的是**阈值**，这次查的是**夹具**。
3. 用户每一轮都要阶段产出，而 `docs/56` 停在 Wave U，V/W 两波之后**至少有一条是撤回**（那个 −42%）。

## 一、X1 · N=40 的硬红（**很可能走 R12**）

**owns**：`game/scripts/Sim.gd`、`game/data/**`、`game/bench/**`
**不得触碰**：`tools/**`（X3 的行）、`game/scripts/Story.gd` / `Director.gd` / `WorldView.gd` / `Main.gd`、
`docs/` 下除你自己那份外的任何文件（X2 的行）
**你的编号文档**：编号 **92**

### W2 已经量到的（**从这里接着往下，别重跑**）

- N=40 的两个 `#40` 红：**`口粮`、下限臂、两个都是**（满足率约 0.47、约 35/60 天断供）。
- 今天这棵树上 180 局里，**12 个下限臂红 seed 全部是口粮跌破地板**；**没有任何 seed 在口粮不破时破。**
- 机制：口粮由**面点师 + 渔夫**的在班完成数驱动（ρ 0.66-0.87，**随 N 增强**；八个岗位里只有这两个在下滑）。
  ⚠️ **但 W2 明说它是主导变量、不是谓词**——N=40 排名最低的 12 个 seed 里有 **7 个没红**，
  全部落在 **0.510-0.541**，刚好在 0.50 地板之上。
- **硬红 `#01`（任一需求触底）在 N=40 seed 8 上；seeds 49-60 的红在 seed 56。**

### 要回答的

1. **`#01` 在 N=40 上红的机制是什么？** 是口粮断供的下游（吃不上饭 ⇒ 饥饿触底），
   还是另一族（social 吸收态那一类，M2 修过的）？**先判族，再修。**
2. **它与 `#40` 的口粮红是同一件事吗？** 若是，修一个可能修两个；若不是，**别把它们绑在一起报。**
3. **红线 #3 承诺的是 60 人，而我们连 40 人都没绿。** 这一条要么修到绿，
   要么**照实写"到 N=? 为止是绿的"并建议契约怎么改**——⚠️ **改契约是用户的决定，你只能建议。**

### 硬要求

- **判据不许放宽**（`SUPPLY_FLOOR` / `SURVIVAL_GATE` / 软通过率门等一律不动）。
  ⚠️ M2 当年抬过 `SURVIVAL_GATE` 32→36 并且是对的，**但那次是剂量-响应扫出来的**；
  **这次不许"红了就抬一格"。**
- **验收网格**：`N=40 seeds 1-12`、`49-60`，且 **N=12 / N=16 两格不得回归**。
- **负对照**：把你的修法关掉，那些红必须回来。
- ⚠️ **W2 刚证明跨树拼接会出事**（144 个可比 seed，零个摘要相同）⇒ **你自己的基线自己跑，别引用编号 89 的逐 seed 数。**

### 验收（X1）

1. `#01` 的族判定 + 机制证据链。
2. 修法 + 逐 seed 前后对照（`N=40`、`49-60`，**给展布不给均值**）；或"修不了 + 卡在哪 + 下一个该测什么"。
3. N=12/N=16 不得回归；金标逐字节不变或走完整 R12（⚠️ U1、V1 已各重烘一次）。
4. §2.5 三行包络；`bash tools/ci.sh` 全绿（**读输出**）。

## 二、X2 · 阶段产出（**只碰文档**）

**owns**：`docs/README.md`、`README.md`、`README_EN.md`、`docs/56-*`、`docs/13-*`、`docs/05-*`、
以及**给 docs/85 加一条过期声明**（只加声明，不改它的正文）
**不得触碰**：任何 `game/**`、`tools/**`
**你的编号文档**：编号 **93**

`docs/56` 停在 Wave U。此后 V/W 两波六根棒，其中**至少三条是对本仓库自我叙述的修正**：

- **V1 报的 `aid` −42% 回归【不存在】**（W1）。留出 seed 上是 **+5.5%**；
  一次**抹黑**同样让它掉 33%；一个零社会内容的假扰动（0.400→0.401）代价更大。
  ⇒ **这一条必须写进去，包括"我当时明知机制不明仍然合并了"。**
- **`N=40` 上硬不变量是红的**，而红线 #3 承诺 60 人。
- **W3 的叙述审计守的是"有依据"不是"说得对"**——把两条弧的文案对调，屏幕上胡说八道而门全绿。

**另三件**：
1. `docs/13` 补 R→W 段，**照旧记被推翻的结论**（本段我至少错了五次，逐条在编号 88/89/90 里）。
2. **给 docs/85 加一条过期声明**：W2 实测 V1 合入后 **144 个可比 seed 零个摘要相同**
   ⇒ **那份回执里每一个逐 seed 数字都已过期**。⚠️ **只加声明，别改它的正文**（本仓库不事后删改过程记录）。
3. README：⚠️ **不要声称任何你没亲自跑过的门是绿的**（本 session 出过一次假 "CI PASS"）。

## 三、X3 · 还有几道门跑在它不可能变红的配置上？（**`tools/`**）

**owns**：`tools/**`、你的编号文档
**不得触碰**：`game/**`、`docs/` 下除你自己那份外的任何文件
**你的编号文档**：编号 **94**

W3 顺手发现：**`story_test` 的默认档是 14 天，而 promise/secret/pact 在 12/12 个 seed 上全是 0/0**
⇒ **一道已经在 CI 里、已经是绿的门，跑在它不可能变红的配置上。**

⇒ **这是 S2 那条追问的另一半。S2 查的是【阈值】是不是调到让今天绿的；本棒查的是【夹具】：
   这道门要判的东西，在它实际跑的那个配置上到底发生过没有？**

### 要做的

1. **逐门普查**：`ci.sh` 每一步的夹具（seeds / days / N / 后端 / 时段）下，
   **它要判的那件事发生过几次？** 零次的就是"不可能变红"。**给表，给展布。**
2. **先量再动**：⚠️ **别假设很多道门都这样**——S2 的 11 条对账里 9 条是准的，
   U3 的扫描也证明"问题集中在少数几份"。**给出比例。**
3. **修 `story_test` 那一格**（`CI_STORY_DAYS` 或等价手段），并**给出代价**（墙钟）。
4. 其余发现的，**能便宜修的就修；贵的照实列出来并给代价，留给用户决定。**

⚠️ **别把 CI 弄红**：若某道门一旦跑在有效夹具上就会红，**那是一个发现，不是一个要当场修的 bug**
——**照实报，并说明它红是因为门有牙还是因为世界真有问题。**

### 验收（X3）

1. 逐门夹具有效性表（**零次发生的点名**）。
2. `story_test` 修好 + 代价；其余的处置与理由。
3. §2.5 三行包络；`bash tools/ci.sh` 全绿（读输出）。
   ⚠️ **造了门就在它真要跑的那个环境里跑一次**——V3 的门第一次接进 `visual_gate.sh` 就红了，
   而红在打印 ✅ 的编码上，不在判据。

## 四、共同约束（照抄，不重述理由）

- docs/41 红线四条 + §0.5；**计划不得给契约加码**；**改契约是用户的决定**。
- **`does_not_detect` 必须是跑出来的**；**"测了之后决定不做"必须写进回执**。
- **跨树拼接会出事**（W2：144 个可比 seed 零个摘要相同）⇒ **自己的基线自己跑。**
- **单 seed 的 PASS 不含信息**；**`tee | tail` 会吃掉退出码** ⇒ 写文件再读。
- **别在 CI 跑的时候改它正在读的任何文件**（U1、V3 各自陈过一次）。
- **先确认你量的是哪个对象**——H1、S3、V3、W3 四根棒的第一版度量都量错了对象。
- **报极值连并列一起报**；**收紧判据前先量余量**；**看到"零引用"先跑 `git log -S`**。
- **编号已占好，但【不要】在正文写 `docs/` + 数字的纯文本形式**；
  **别把 `lint_links` 自己的失败输出粘进回执**（R2、R3、U2 都踩过）。
- **worktree 的 checkout 可能落后两百多个 commit**（上一波三根棒分别 231/241/241）：**先确认基线，是祖先才 ff。**
- 隔离副本里做负对照；越界要声明。
